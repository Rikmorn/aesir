---
issue: 14
kind: tech-debt
summary: 59 human-verification items across the ten shipped milestones plus v2.9's executed-but-unarchived phases have not been checked off.
---

# Open human-verification items from the v1 to v2.9 verification reports

## Context
Lead item: Phase 73's verification status is `human_needed` — 11 of 11 automated must-haves passed, but six checks (running the triangular workflow validation among them) still need a person to look. A second concrete example: Phase 87's "GIN Index Applied at Query Time" needs a running PostgreSQL instance with migration 0021 applied and real data, since index usage depends on planner decisions no static read can verify — it can't be discharged by code reading at all. `docs/history/phases.md` carries a "Human verification still open" list per milestone, generated by `extract-phases.ts` on 2026-09-15; across the ten shipped milestones plus v2.9's executed-but-unarchived phases it totals 59 items, none checked off. Eighteen of those sit in phases 67, 72, 73 and 87 — recovered as the extractor's heading-depth handling was corrected: 67/72/73 when the v2.7 phases were restored, 87 when a second heading-depth bug (`##` sections with `###` items, instead of the usual `###`/`####` nesting) was fixed. The issue was originally titled around phases 77 to 87, before those phase-index corrections surfaced phases 67, 72 and 73; it has since been retitled to the full v1–v2.9 scope this note covers. The full inventory is long enough that restating it here would just be a second copy; the corrected list is a comment on the issue itself.

## Trigger to revisit
Pick individual items up whenever the corresponding feature is touched again — each either passes on inspection or spawns its own issue. Phase 73's `human_needed` status is the one actively awaiting sign-off.

## Reference
- Rikmorn/aesir#14 (status lives there; the corrected inventory is a comment on the issue)
- `docs/history/phases.md` ("Human verification still open" lists)

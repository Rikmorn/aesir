---
issue: 14
kind: tech-debt
summary: 86 human-verification items, per the 2026-09-15 extract-phases.ts run, have not been checked off — see docs/history/phases.md for the live count.
---

# Open human-verification items from the v1 to v2.9 verification reports

## Context
Lead item: Phase 73's verification status is `human_needed` — 11 of 11 automated must-haves passed, but six checks (running the triangular workflow validation among them) still need a person to look. A second concrete example: Phase 87's "GIN Index Applied at Query Time" needs a running PostgreSQL instance with migration 0021 applied and real data, since index usage depends on planner decisions no static read can verify — it can't be discharged by code reading at all. `docs/history/phases.md` is the live source for the full list, generated by `extract-phases.ts`; as of the 2026-09-15 run it totals 86 items, none checked off, up from an original 57 across three rounds of extractor fixes. The first round restored v2.7's phases 67, 72 and 73; the second recovered Phase 87 from a heading-depth bug; the third recognized further item formats and located previously-unfound report files, recovering several more phases. Rather than copy that phase list here — a hand-listed set that has already gone stale twice — this note points at `docs/history/phases.md` as the authoritative enumeration. The issue was originally titled around phases 77 to 87, before these corrections surfaced items well outside that range; it has since been retitled to the full v1–v2.9 scope this note covers.

## Trigger to revisit
Pick individual items up whenever the corresponding feature is touched again — each either passes on inspection or spawns its own issue. Phase 73's `human_needed` status is the one actively awaiting sign-off.

## Reference
- Rikmorn/aesir#14 (status lives there; the corrected inventory is a comment on the issue)
- `docs/history/phases.md` ("Human verification still open" lists)

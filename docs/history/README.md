# history/ — the frozen record, v1 to v2.9

What was built between 2026-01-15 and 2026-02-23, as it was recorded at the time. Nothing here is edited after landing; corrections go in a new file that names what it corrects.

| File | What it is | How it was made |
|---|---|---|
| `milestones.md` | The eleven milestones: dates, scope, what each proved, what debt it left | Authored during the 2026-09 reset from MILESTONES.md, STATE.md, ROADMAP.md and PROJECT.md (the four inputs its own Sources line names) |
| `phases.md` | One row per phase with goal, plan count, verification status and open human checks | Generated by `tools/extract-phases.ts` |
| `decisions-log.md` | Every recorded decision: per-plan (from 378 plan summaries), project-level, and design-vision | Generated by `tools/extract-decisions.ts` |
| `requirements.md` | Each milestone's requirements as planned | Concatenated from `milestones/*-REQUIREMENTS.md` and the v2.9 REQUIREMENTS.md (`git show 39c7015c:.planning/REQUIREMENTS.md`) |
| `qa-plan-v2.8-v2.9.md` | The end-to-end QA plan written 2026-02-23, never run (Rikmorn/aesir#2) | Moved verbatim |
| `specs/` | The milestone specs v2.1–v2.9 and the design-vision master document, as written | Moved verbatim |
| `milestones/` | Per-milestone ROADMAP, REQUIREMENTS and MILESTONE-AUDIT records, v1–v2.8 | Moved verbatim |
| `tools/` | The two extractors (`extract-phases.ts`, `extract-decisions.ts`) | Kept for provenance -- they read `.planning/` and `docs/history/milestones/` together, which no single commit holds: restore the first with `git checkout v2.9 -- .planning` on this branch, run them from the repo root, then remove it again. A bare `v2.9` checkout has no `docs/history/`, and `509a9f4f^` (the commit before `.planning/` was deleted) predates the extractors' v2.8 fix |

## Retrieving what was not kept

The GSD planning tree (`.planning/`, ~1,090 files: per-plan PLAN/SUMMARY/VERIFICATION records, per-phase research and context) was deleted from the working tree in the 2026-09 reset. It is intact in git history:

```bash
git show 39c7015c:.planning/ROADMAP.md                       # any file at the last v2.9 commit
git show 39c7015c:.planning/phases/84-scheduled-execution/84-01-SUMMARY.md
git ls-tree -r --name-only v2.9 -- .planning | head      # list what existed
git log --all --oneline -- .planning/phases/86-*         # history of a phase
```

`39c7015c` is the last v2.9 commit (2026-02-23, tip of `master` before the reset). The GSD-era tags that marked each milestone existed only locally and were deleted on 2026-09-16 — nothing was ever released; whether the project tags releases is decided once the setup work is done. The commits they marked:

| Tag | Commit | Date |
|---|---|---|
| v1 | `6ab5d8e0` | 2026-01-19 |
| v2.0 | `73a80b6c` | 2026-01-25 |
| v2.1 | `f6c3b3b9` | 2026-01-28 |
| v2.2 | `0b66065c` | 2026-02-01 |
| v2.3 | `c482fa38` | 2026-02-04 |
| v2.4 | `247a5387` | 2026-02-05 |
| v2.5 | `5a42f709` | 2026-02-08 |
| v2.6 | `6b48425e` | 2026-02-09 |
| v2.7 | `a491b4e4` | 2026-02-13 |
| v2.8 | `ce507e22` | 2026-02-18 |
| v2.9 | `39c7015c` | 2026-02-23 |

The generated files above are the index into it.

Files under `specs/` and `milestones/` are as written and still cite `.planning/...` paths in prose; those resolve to siblings in this folder or to `git show 39c7015c:<path>`.

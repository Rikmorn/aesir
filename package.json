{
  "name": "aesir",
  "version": "0.1.0",
  "description": "Agentic development platform for automated software workflows",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:watch": "vitest --project '!integration' --project '!sandbox'",
    "test:fast": "vitest run --project '!integration' --project '!sandbox'",
    "test:integration": "vitest run --project integration",
    "test:sandbox": "vitest run --project sandbox",
    "test:coverage": "vitest run --coverage --project '!integration' --project '!sandbox'",
    "test:coverage:open": "pnpm test:coverage && open coverage/index.html",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "knip": "knip",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "infra:up": "docker compose up -d postgresql",
    "infra:logs": "docker compose logs -f postgresql",
    "db:generate": "pnpm -r db:generate",
    "db:migrate": "pnpm -r run --sequential db:migrate",
    "prepare": "husky"
  },
  "keywords": [
    "agent",
    "ai",
    "automation",
    "conversation-executor"
  ],
  "author": "",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.13",
    "@types/node": "^22.19.7",
    "@vitest/coverage-v8": "^5.0.1",
    "husky": "^9.1.7",
    "knip": "^6.35.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  },
  "packageManager": "pnpm@12.4.2"
}

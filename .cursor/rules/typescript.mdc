---
description: "TypeScript coding standards for Aesir project"
globs: ["src/**/*.ts"]
alwaysApply: true
---

# TypeScript Standards

- Use explicit return types for public functions
- Prefer `unknown` over `any` - Biome enforces noExplicitAny
- Use Zod for runtime validation at all external boundaries
- No console.log in production code (use logger from src/logging/)

# Error Handling

- Wrap external API calls in try/catch
- Use typed error classes for domain errors (AppError base class in v2.0)
- Log errors with context using pino logger
- Include relevant IDs (taskId, prNumber, etc.) in error logs

# Formatting

- Biome handles formatting - don't manually adjust
- 2 spaces indent, 80 char line width
- Run `npm run lint:fix` to auto-fix issues
- Pre-commit hook runs Biome automatically

# Naming Conventions

- camelCase for variables and functions
- PascalCase for classes, types, interfaces, and Zod schemas
- CONSTANT_CASE for module-level constants
- snake_case allowed for object properties (env vars, API shapes)

# Imports

- Biome auto-organizes imports alphabetically
- Use `.js` extension for local imports (ESM requirement)
- Type imports: `import type { Foo } from "./foo.js"`
- Side-effect imports first: `import "./config/env.js"`

# Async Patterns

- Always await promises or handle them explicitly
- Use async/await over .then() chains
- Handle both success and error cases
- Consider timeout for external API calls

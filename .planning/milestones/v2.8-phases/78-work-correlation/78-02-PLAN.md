---
phase: 78-work-correlation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agents/src/adapters/linear.ts
  - packages/agents/src/adapters/github.ts
  - packages/agents/src/adapters/slack.ts
autonomous: true
requirements:
  - CORR-01

must_haves:
  truths:
    - "Linear adapter sets entityRef with entityType 'linear_issue' and entityId from issueId on all issue-related events"
    - "GitHub adapter sets entityRef with entityType 'github_pr' and entityId as 'owner/repo#number' on all PR events"
    - "Slack adapter sets entityRef with entityType 'slack_thread' and entityId as 'channelId:threadTs' on thread-based events"
    - "Events without a clear entity (workspace-level, missing IDs) omit entityRef entirely"
  artifacts:
    - path: "packages/agents/src/adapters/linear.ts"
      provides: "entityRef extraction for Linear events"
      contains: "entityRef"
    - path: "packages/agents/src/adapters/github.ts"
      provides: "entityRef extraction for GitHub events"
      contains: "entityRef"
    - path: "packages/agents/src/adapters/slack.ts"
      provides: "entityRef extraction for Slack events"
      contains: "entityRef"
  key_links:
    - from: "packages/agents/src/adapters/linear.ts"
      to: "packages/agents/src/adapters/types.ts"
      via: "Uses EntityRef type from IncomingEvent"
      pattern: "entityRef.*linear_issue"
    - from: "packages/agents/src/adapters/github.ts"
      to: "packages/agents/src/adapters/types.ts"
      via: "Uses EntityRef type from IncomingEvent"
      pattern: "entityRef.*github_pr"
    - from: "packages/agents/src/adapters/slack.ts"
      to: "packages/agents/src/adapters/types.ts"
      via: "Uses EntityRef type from IncomingEvent"
      pattern: "entityRef.*slack_thread"
---

<objective>
Add entity reference extraction to all three adapters (Linear, GitHub, Slack) so incoming events carry typed entityRef for correlation routing.

Purpose: The router needs entityRef to look up work correlations. Adapters are the only place that understands event payload structure well enough to extract entity IDs.
Output: All three adapters produce entityRef on entity-related events.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/78-work-correlation/78-CONTEXT.md
@.planning/phases/78-work-correlation/78-RESEARCH.md
@packages/agents/src/adapters/types.ts
@packages/agents/src/adapters/linear.ts
@packages/agents/src/adapters/github.ts
@packages/agents/src/adapters/slack.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add entityRef extraction to Linear adapter</name>
  <files>packages/agents/src/adapters/linear.ts</files>
  <action>
    For every event case that has an `issueId` in the payload, add `entityRef` to the returned IncomingEvent:
    ```typescript
    entityRef: { entityType: 'linear_issue' as const, entityId: issueId },
    ```

    Specific cases:
    - `linear.agent_session.created`: has `issueId` -- add entityRef
    - `linear.issue.created`: has issue data -- extract issueId from payload, add entityRef
    - `linear.issue.updated`: has issue data -- extract issueId from payload, add entityRef
    - `linear.comment.created` / `issue_comment`: has `issueId` -- add entityRef
    - `linear.agent_session.prompted` / `agent_prompt`: has `issueId` -- add entityRef

    Read the full file to identify all cases. Only add entityRef when issueId is structurally available. If an event type does not have an issueId in its payload, omit entityRef.
  </action>
  <verify>
    - `pnpm run typecheck` passes
    - `grep -c "entityRef" packages/agents/src/adapters/linear.ts` shows entityRef added to all issue-related cases
  </verify>
  <done>Linear adapter produces entityRef with entityType 'linear_issue' on all issue-related events.</done>
</task>

<task type="auto">
  <name>Task 2: Add entityRef extraction to GitHub and Slack adapters</name>
  <files>
    packages/agents/src/adapters/github.ts
    packages/agents/src/adapters/slack.ts
  </files>
  <action>
    **GitHub adapter:**
    For all PR-related events, extract the entity reference. The PR number and repo info should be available in the payload. Format the entityId as `owner/repo#number` per CONTEXT.md.

    Read the full github.ts adapter to identify all switch cases. For each case that has PR data:
    ```typescript
    entityRef: {
      entityType: 'github_pr' as const,
      entityId: `${owner}/${repo}#${prNumber}`,
    },
    ```
    Extract `owner`, `repo`, `prNumber` from the payload. The GitHub config has `githubOwner` and `githubRepo`; check if these are available via `config` import or if they need to come from the payload's `repository.full_name` and `pull_request.number` fields. Use whatever the adapter already has access to.

    **Slack adapter:**
    For thread-based events (message.created with threadTs, app_mention.created), add entityRef:
    ```typescript
    entityRef: {
      entityType: 'slack_thread' as const,
      entityId: `${channelId}:${threadTs}`,
    },
    ```
    - `app_mention.created`: has `channelId` and `threadTs` (the thread root) -- add entityRef
    - `message.created` (with threadTs): has `channelId` and `threadTs` -- add entityRef
    - Block action events (approval, escalation): these are interactions, not entity events. Only add entityRef if they have thread context; otherwise omit.

    Only add entityRef when the required fields are structurally present in the payload. Omit for events where channel/thread IDs aren't available.
  </action>
  <verify>
    - `pnpm run typecheck` passes
    - `grep -c "entityRef" packages/agents/src/adapters/github.ts` shows entityRef on PR events
    - `grep -c "entityRef" packages/agents/src/adapters/slack.ts` shows entityRef on thread events
  </verify>
  <done>GitHub adapter produces entityRef with 'github_pr' on all PR events. Slack adapter produces entityRef with 'slack_thread' on thread-based events.</done>
</task>

</tasks>

<verification>
- `pnpm run typecheck` passes for all adapter changes
- Each adapter produces entityRef only when the entity is structurally unambiguous
- Entity ID formats match CONTEXT.md: Linear=issueId, GitHub=owner/repo#number, Slack=channelId:threadTs
</verification>

<success_criteria>
All three adapters produce typed entityRef on entity-related events. Events without clear entity references omit entityRef. The router can use entityRef for correlation lookup.
</success_criteria>

<output>
After completion, create `.planning/phases/78-work-correlation/78-02-SUMMARY.md`
</output>

---
phase: 63-outbound-denormalizer
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/agents/src/shared/communication/types.ts
  - packages/agents/src/shared/communication/index.ts
  - packages/agents/src/shared/communication/denormalizer.ts
  - packages/agents/src/shared/communication/denormalizer.test.ts
autonomous: true

must_haves:
  truths:
    - "denormalize() with Slack replyContext + threadTs calls slack:reply_to_thread via callMcpTool"
    - "denormalize() with Slack replyContext without threadTs calls slack:send_message via callMcpTool"
    - "denormalize() with Linear replyContext calls linear:create_comment with { issueId, body }"
    - "denormalize() with GitHub replyContext calls github:create_pr_comment with { owner, repo, prNumber, body }"
    - "denormalize() returns callMcpTool's result directly (no wrapping)"
    - "CommunicationToolDeps has logger field and no conversationId field"
  artifacts:
    - path: "packages/agents/src/shared/communication/denormalizer.ts"
      provides: "Pure dispatch function: replyContext + text -> MCP tool call"
      exports: ["denormalize", "DenormalizeParams"]
    - path: "packages/agents/src/shared/communication/denormalizer.test.ts"
      provides: "Unit tests covering all dispatch paths and error propagation"
      min_lines: 80
  key_links:
    - from: "packages/agents/src/shared/communication/denormalizer.ts"
      to: "packages/agents/src/shared/mcp/client.ts"
      via: "callMcpTool import"
      pattern: "callMcpTool"
    - from: "packages/agents/src/shared/communication/denormalizer.ts"
      to: "packages/agents/src/shared/communication/types.ts"
      via: "ReplyContext and CommunicationToolDeps types"
      pattern: "CommunicationToolDeps"
---

<objective>
Implement the outbound denormalizer: a pure dispatch function that routes domain-language text to the correct integration MCP tool based on replyContext channel type. Also update the CommunicationToolDeps type to match the locked design (add logger, remove conversationId).

Purpose: The denormalizer is the core routing logic of the outbound communication layer -- it bridges the agent's channel-agnostic "send text to replyContext" intent to concrete integration MCP calls.
Output: denormalizer.ts with denormalize() function, updated types.ts, barrel export updates, comprehensive TDD test coverage.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-outbound-denormalizer/63-CONTEXT.md
@.planning/phases/63-outbound-denormalizer/63-RESEARCH.md

@packages/agents/src/shared/communication/types.ts
@packages/agents/src/shared/communication/index.ts
@packages/agents/src/shared/mcp/client.ts
@packages/agents/src/shared/mcp/errors.ts
@packages/agents/src/shared/mcp/types.ts
@packages/agents/src/shared/tools/integration/mcp-wrapper.ts
@packages/agents/src/shared/tools/integration/slack-tools.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CommunicationToolDeps and write denormalizer RED tests</name>
  <files>
    packages/agents/src/shared/communication/types.ts
    packages/agents/src/shared/communication/index.ts
    packages/agents/src/shared/communication/denormalizer.test.ts
  </files>
  <action>
**Step 1: Update CommunicationToolDeps in types.ts.**

Per locked decision, change CommunicationToolDeps to:
```typescript
import type { PinoLogger } from "@aesir/platform";

export interface CommunicationToolDeps {
  agentId: string;
  correlationId: string;
  taskId?: string;
  logger: PinoLogger;
}
```

- Add `logger: PinoLogger` (required, not optional)
- Remove `conversationId?: string` and its JSDoc comment
- Add `import type { PinoLogger } from "@aesir/platform";` at the top of the file

**Step 2: Update barrel exports in index.ts.**

Add exports for the new denormalizer module (these will fail until Task 2 creates the file):
```typescript
export { denormalize, type DenormalizeParams } from "./denormalizer.js";
```

**Step 3: Create denormalizer.test.ts with RED tests.**

Mock `callMcpTool` using vi.mock pattern from integration-tools.test.ts:
```typescript
const mockCallMcpTool = vi.fn();
vi.mock("../../mcp/client.js", () => ({
  callMcpTool: (...args: unknown[]) => mockCallMcpTool(...args),
}));
```

Create a mock logger using the same pattern from tool-factories.test.ts (createMockLogger).

Write these test cases for `denormalize()`:

1. **Slack with threadTs** -- calls callMcpTool with `{ integration: "slack", tool: "reply_to_thread", params: { channel: channelId, threadTs, text } }`. Assert agentId and correlationId are passed.
2. **Slack without threadTs** -- calls callMcpTool with `{ integration: "slack", tool: "send_message", params: { channel: channelId, text } }`. Assert NO threadTs in params (conditional spread compliance).
3. **Linear** -- calls callMcpTool with `{ integration: "linear", tool: "create_comment", params: { issueId, body: text } }`.
4. **GitHub** -- calls callMcpTool with `{ integration: "github", tool: "create_pr_comment", params: { owner, repo, prNumber, body: text } }`.
5. **GitHub with commentId** -- same as #4 but replyContext includes `commentId: 42`, assert `inReplyTo: 42` is in params (conditional spread for exactOptionalPropertyTypes).
6. **Return value passthrough** -- mock callMcpTool to return `{ success: true, data: "test" }`, assert denormalize returns the same value.
7. **MCP error propagation** -- mock callMcpTool to throw McpError, assert denormalize throws McpError (not wrapped).
8. **Info-level logging** -- assert `deps.logger.info` is called with an object containing `{ channel, tool, agentId }` (the tool name should be the MCP tool selected, e.g., "reply_to_thread" or "create_comment").

Import McpError from `../../mcp/errors.js` for the error propagation test.

All tests should FAIL (RED) because denormalizer.ts does not exist yet.
  </action>
  <verify>Run `npx vitest run packages/agents/src/shared/communication/denormalizer.test.ts` -- all tests should FAIL (RED phase). The test file should parse and import correctly, with failures coming from the missing denormalize function.</verify>
  <done>CommunicationToolDeps updated to match locked decision (has logger, no conversationId). 8+ RED tests covering all dispatch paths, error propagation, logging, and return passthrough.</done>
</task>

<task type="auto">
  <name>Task 2: Implement denormalize() function (GREEN)</name>
  <files>
    packages/agents/src/shared/communication/denormalizer.ts
  </files>
  <action>
Create `packages/agents/src/shared/communication/denormalizer.ts`.

**Interface:**
```typescript
export interface DenormalizeParams {
  replyContext: ReplyContext;
  text: string;
}
```

**Implementation of denormalize():**

```typescript
export async function denormalize(
  params: DenormalizeParams,
  deps: CommunicationToolDeps,
): Promise<unknown> {
```

- Import `callMcpTool` from `../../mcp/client.js`
- Import `ReplyContext` and `CommunicationToolDeps` from `./types.js`
- Destructure `{ replyContext, text }` from params

**Log first** -- single info-level log with `{ channel, tool, agentId }` where `tool` is the MCP tool name about to be called. Compute the tool name before logging (the switch determines it).

**Dispatch via exhaustive switch on `replyContext.channel`:**

Case `"slack"`:
- If `replyContext.threadTs` is present: call `callMcpTool({ integration: "slack", tool: "reply_to_thread", params: { channel: replyContext.channelId, threadTs: replyContext.threadTs, text }, agentId: deps.agentId, correlationId: deps.correlationId, ...(deps.taskId && { taskId: deps.taskId }) })`
- If `replyContext.threadTs` is absent: call `callMcpTool({ integration: "slack", tool: "send_message", params: { channel: replyContext.channelId, text }, agentId: deps.agentId, correlationId: deps.correlationId, ...(deps.taskId && { taskId: deps.taskId }) })`

Case `"linear"`:
- Call `callMcpTool({ integration: "linear", tool: "create_comment", params: { issueId: replyContext.issueId, body: text }, agentId: deps.agentId, correlationId: deps.correlationId, ...(deps.taskId && { taskId: deps.taskId }) })`

Case `"github"`:
- Call `callMcpTool({ integration: "github", tool: "create_pr_comment", params: { owner: replyContext.owner, repo: replyContext.repo, prNumber: replyContext.prNumber, body: text, ...(replyContext.commentId && { inReplyTo: replyContext.commentId }) }, agentId: deps.agentId, correlationId: deps.correlationId, ...(deps.taskId && { taskId: deps.taskId }) })`

**No default case.** TypeScript exhaustive checking on the discriminated union ensures compile-time safety. If a runtime guard is desired, use `const _exhaustive: never = replyContext;` after the switch, but this is optional since the Zod union prevents unknown channels.

**Return** the callMcpTool result directly. No wrapping, no error catching -- errors propagate as-is per locked decision.

Use conditional spread `...(deps.taskId && { taskId: deps.taskId })` for exactOptionalPropertyTypes compliance on the taskId field passed to callMcpTool.
  </action>
  <verify>Run `npx vitest run packages/agents/src/shared/communication/denormalizer.test.ts` -- all 8+ tests should PASS (GREEN phase). Then run `pnpm run typecheck` from monorepo root to verify no TypeScript errors.</verify>
  <done>denormalize() function passes all tests. Dispatches correctly to Slack (reply_to_thread/send_message), Linear (create_comment), GitHub (create_pr_comment). Returns MCP result directly. Logs once per dispatch. No error wrapping.</done>
</task>

</tasks>

<verification>
1. `npx vitest run packages/agents/src/shared/communication/denormalizer.test.ts` -- all tests pass
2. `pnpm run typecheck` -- no TypeScript errors in the agents package
3. CommunicationToolDeps in types.ts has `logger: PinoLogger` and no `conversationId`
4. denormalize() has no default case in switch (TypeScript exhaustive check)
5. denormalize() returns callMcpTool result directly (no wrapping)
6. denormalize() does not catch errors (MCP errors propagate as-is)
</verification>

<success_criteria>
- denormalize() dispatches to correct MCP tool for each channel type
- Slack threadTs presence/absence determines reply_to_thread vs send_message
- Linear always uses create_comment with issueId + body
- GitHub always uses create_pr_comment with owner/repo/prNumber/body
- GitHub commentId conditionally maps to inReplyTo param
- Single info-level log per dispatch with { channel, tool, agentId }
- All tests pass, typecheck clean
</success_criteria>

<output>
After completion, create `.planning/phases/63-outbound-denormalizer/63-01-SUMMARY.md`
</output>

---
phase: 47-cleanup-documentation
plan: 04
type: execute
wave: 3
depends_on: ["47-02"]
files_modified:
  - .claude/CLAUDE.md
  - README.md
autonomous: true

must_haves:
  truths:
    - "CLAUDE.md describes v2.3 architecture as current reality (not migration target)"
    - "CLAUDE.md contains zero references to Temporal, LangGraph, or per-agent services"
    - "CLAUDE.md Agent-First Decision Checklist references v2.3 paths (definitions/, framework/)"
    - "CLAUDE.md includes v2.3-specific gotchas (agent definitions, conversation executor, event log, history compaction)"
    - "CLAUDE.md mentions .planning/ folder holds historical context (no milestone history sections)"
    - "README.md references agent-service (not dev-agent/product-agent)"
    - "README.md uses aesir credentials in all examples"
    - "README.md does not mention Temporal anywhere"
  artifacts:
    - path: ".claude/CLAUDE.md"
      provides: "Complete v2.3 architecture documentation for Claude"
      contains: "ConversationExecutor"
    - path: "README.md"
      provides: "Updated project README reflecting v2.3"
      contains: "agent-service"
  key_links:
    - from: ".claude/CLAUDE.md"
      to: "packages/agents/src/framework/"
      via: "directory structure documentation"
      pattern: "framework"
    - from: ".claude/CLAUDE.md"
      to: "packages/agents/definitions/"
      via: "agent definition documentation"
      pattern: "definitions"
---

<objective>
Rewrite CLAUDE.md and README.md to reflect v2.3 architecture as current reality.

Purpose: Documentation is the primary interface for anyone (human or AI) working on this codebase. After Phase B deleted all legacy code, the documentation must describe what actually exists -- not what used to exist. CLAUDE.md is especially critical because it's the system prompt for AI assistants working on the project.
Output: Fully rewritten CLAUDE.md (~800-900 lines) and updated README.md (~400-500 lines) with zero legacy references.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-cleanup-documentation/47-CONTEXT.md
@.planning/phases/47-cleanup-documentation/47-RESEARCH.md
@.planning/phases/47-cleanup-documentation/47-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md for v2.3 architecture</name>
  <files>.claude/CLAUDE.md</files>
  <action>
    Read the current CLAUDE.md (~903 lines). Rewrite it to describe v2.3 as the current reality. This is a FULL REWRITE, not a patch.

    **Sections to KEEP (updated for v2.3):**
    - Project description at the top (update to reflect v2.3: "Postgres-backed conversation executor with declarative agent definitions")
    - MANDATORY: Agent-First Decision Checklist -- update file paths from `packages/agents/src/shared/temporal/` and `*/orchestrator/` to `packages/agents/definitions/` and `packages/agents/src/framework/`
    - 3-Layer Architecture -- update to show v2.3 layers: Agents (definitions/ + framework/) -> Integrations -> Platform
    - Integration packages section (Linear, GitHub, Slack) -- these are unchanged
    - MCP Layer documentation -- unchanged
    - Agent Usage of MCP -- unchanged (agents still use callMcpTool)
    - Environment Configuration pattern -- unchanged
    - Zod Validation pattern -- unchanged
    - Logging pattern -- unchanged
    - Error Handling pattern -- unchanged
    - Dependency Injection pattern -- unchanged
    - Testing section -- update to remove legacy test references
    - Docker Networking gotchas -- update for single service
    - OAuth Tokens gotchas -- unchanged
    - Package Imports gotchas -- update to remove dead package refs

    **Sections to REMOVE entirely:**
    - v2.0 Foundation Work (entire section with completed phases list and key achievements)
    - v2.1 Codebase Cleanup (entire section)
    - v2.2 Design Principles -- KEEP the agent-first principles but move them to the main architecture section; remove the v2.2 framing
    - All references to Temporal (workflows, activities, workers, signals, proxyActivities)
    - All references to LangGraph (state machines, checkpoints, nodes)
    - Per-agent service references (dev-agent:3004, product-agent:3005)
    - `Key Frameworks` sub-section mentioning LangGraph and Temporal
    - `State & Persistence` sub-section mentioning LangGraph checkpointer and Temporal workflows

    **Sections to ADD:**
    - v2.3 Architecture overview: ConversationExecutor, WorkerLoop, EventLog, declarative agent definitions
    - Directory Structure updated for v2.3:
      ```
      packages/agents/
        definitions/           # Agent YAML + prompt.md files
          dev-agent/
          product-agent/
          coder/
          researcher/
          tester/
        src/
          adapters/            # Event normalization (linear, github, slack, pass-through)
          framework/           # Core: ConversationExecutor, WorkerLoop, EventLog, HistoryManager, AgentRegistry, ToolRegistry
          router/              # Event routing (router, slow-path, system-prompt, types, tools/)
          service/             # Unified HTTP entry point (main.ts)
          shared/              # Shared utilities
            agent-loop/        # Core agent loop runtime
            config/            # Agent configuration
            db/                # Database client, schema, migrations
            env/               # Environment validation
            mcp/               # MCP client
            tools/             # Reusable tool factories (codebase/, coordination/, integration/)
      ```
    - Agent Definitions pattern: YAML + prompt.md, how to add a new agent
    - ConversationExecutor pattern: start(), signal(), get(), cancel(), worker loop
    - Event Log and Session Projection pattern
    - History Manager pattern (compaction, summarization)
    - Updated Common Commands: remove per-agent commands, add `pnpm --filter @aesir/agents start` for the unified service
    - v2.3-specific Gotchas (from research finding 6):
      1. Agent definitions in YAML + prompt.md, not TypeScript
      2. ConversationExecutor.start() is idempotent (same correlationKey returns same conversation)
      3. EventLog.append() is synchronous/buffered -- call flush() at lifecycle boundaries
      4. HistoryManager compacts messages (old tool results summarized by Haiku)
      5. WaitForState enables pause/resume -- wait_for tool creates pending wait
      6. Router tools vs agent tools are separate sets (router/tools/ vs shared/tools/)
      7. Agent-First: fix agent behavior via prompts/tools, not workflow code

    **Historical context note:** Add a brief note: "Historical project context (milestones, decisions, architecture evolution) is maintained in the `.planning/` directory."

    **Key principle from user decisions:** Frame v2.3 as "the logical conclusion of v2.2's agent-first principles" -- removing Temporal/LangGraph was the natural result of trusting agents to manage their own state.

    **Credential references:** Update any DATABASE_URL examples from temporal to aesir credentials.

    **Docker commands:** Update all Docker examples:
    - Remove `docker compose up -d dev-agent`, `docker compose up -d product-agent`
    - Remove `temporal temporal-ui` from infra commands
    - Keep `docker compose up` as the primary command (starts postgresql + integrations + agent-service)
  </action>
  <verify>
    Check that CLAUDE.md contains zero occurrences of these strings (case-insensitive search):
    - "temporal" (except in the context of "temporal ordering" or similar non-Temporal-the-product usage -- verify manually)
    - "langgraph" or "langraph"
    - "dev-agent:3004" or "product-agent:3005"
    - "v2.0 Foundation" or "v2.1 Codebase" or "v2.2 Design"
    - "worker.ts" (as an entry point reference)
    - "orchestrator/"

    Check that CLAUDE.md DOES contain:
    - "ConversationExecutor"
    - "definitions/"
    - "framework/"
    - "service/main.ts"
    - "agent-first" (decision checklist)
    - ".planning/"
  </verify>
  <done>CLAUDE.md fully rewritten for v2.3. Zero legacy references. Agent-First Decision Checklist updated with v2.3 paths. v2.3 gotchas documented. Historical context delegated to .planning/ folder.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md for v2.3</name>
  <files>README.md</files>
  <action>
    Read the current README.md (~503 lines). Update to reflect v2.3 architecture. This is a comprehensive update but not a full rewrite -- the structure can stay similar, but content must be updated.

    **Features section:**
    - Update "Human-in-the-Loop" from "Temporal-orchestrated approval gates" to "Conversation-based approval gates via wait_for tool"
    - Keep Product Agent and Dev Agent descriptions (they still exist as capabilities, just implemented differently)

    **Quick Start section:**
    - Update database commands: remove temporal/temporal-ui references
    - Infrastructure command: `docker compose up -d postgresql` (not `postgresql temporal temporal-ui`)
    - Update `pnpm db:migrate` description (no change to command)
    - Update Docker Compose examples to use aesir credentials
    - Remove any `npm run infra:up` that references temporal

    **Architecture section:**
    - Update to describe: unified agent-service, ConversationExecutor, declarative definitions
    - Remove Temporal workflow descriptions
    - Remove LangGraph state machine descriptions
    - Update directory structure to match v2.3

    **Docker Compose Services:**
    - Remove Temporal Server and Temporal UI from service list
    - Remove dev-agent and product-agent as separate services
    - Add agent-service as the unified service
    - Update ports: just postgresql (5432), integrations (3001-3003), agent-service (3004)
    - Remove Temporal UI port (8080)

    **Database section:**
    - Remove references to old tables (tasks, context_snapshots, execution_traces)
    - Update schema table: agents schema has conversations, agent_events, agent_sessions
    - Update credential examples from temporal to aesir

    **Development commands:**
    - Remove per-agent commands (`pnpm --filter @aesir/agents dev-agent`, `pnpm --filter @aesir/agents product-agent`)
    - Add unified command if one exists
    - Remove `npm run infra:*` commands that reference temporal

    **Credential references:**
    - Update all DATABASE_URL from `temporal:temporal` to `aesir:aesir`
    - Update volume references if mentioned

    **Access URLs:**
    - Remove Temporal UI (http://localhost:8080)
    - Remove Temporal gRPC (localhost:7233)
    - Keep PostgreSQL, integrations, agent service
  </action>
  <verify>
    Check that README.md contains zero occurrences of:
    - "temporal" (case-insensitive, except "temporal ordering" if present)
    - "langgraph" (case-insensitive)
    - "localhost:8080" (Temporal UI)
    - "localhost:7233" (Temporal gRPC)
    - "dev-agent:3004" or "product-agent:3005"

    Check that README.md DOES contain:
    - "agent-service" or "unified service"
    - "aesir" (in credential examples)
    - "ConversationExecutor" or "conversation executor"
    - "pnpm db:migrate"
  </verify>
  <done>README.md updated for v2.3. No Temporal/LangGraph references. Credentials use aesir. Services list shows unified agent-service. Quick Start works for fresh clone.</done>
</task>

</tasks>

<verification>
Documentation accuracy check:
- `grep -i "temporal" .claude/CLAUDE.md README.md` -- should return ZERO results (or only false positives like "temporal ordering")
- `grep -i "langgraph\|langraph" .claude/CLAUDE.md README.md` -- should return ZERO results
- `grep -i "worker.ts" .claude/CLAUDE.md` -- should return ZERO results (entry point references)
- `grep "definitions/" .claude/CLAUDE.md` -- should have multiple matches
- `grep "ConversationExecutor" .claude/CLAUDE.md` -- should have multiple matches
- `grep "aesir" .env.example docker-compose.yml` -- should have multiple matches (from plan 03, verify consistency)
</verification>

<success_criteria>
1. CLAUDE.md is a complete v2.3 reference with zero Temporal/LangGraph/per-agent-service references
2. CLAUDE.md Agent-First Decision Checklist references definitions/ and framework/ paths
3. CLAUDE.md includes v2.3 gotchas (agent definitions, executor, event log, history, wait_for, tool separation)
4. CLAUDE.md mentions .planning/ for historical context
5. README.md updated with v2.3 architecture, aesir credentials, unified service
6. README.md Quick Start works for a fresh clone
7. Both files pass content verification (grep checks above)
</success_criteria>

<output>
After completion, create `.planning/phases/47-cleanup-documentation/47-04-SUMMARY.md`
</output>

---
phase: 47-cleanup-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agents/src/router/router.ts
  - packages/agents/src/router/types.ts
  - packages/agents/src/router/index.ts
  - packages/agents/src/router/slow-path.ts
  - packages/agents/src/router/system-prompt.ts
  - packages/agents/src/router/tools/send-message.ts
  - packages/agents/src/shared/index.ts
  - packages/agents/src/shared/db/index.ts
  - packages/agents/src/shared/tools/index.ts
  - packages/agents/src/shared/tools/coordination/index.ts
  - packages/agents/src/dev-agent/index.ts
  - packages/agents/src/index.ts
  - packages/platform/src/index.ts
  - packages/platform/src/logging/index.ts
  - packages/types/src/index.ts
autonomous: true

must_haves:
  truths:
    - "All barrel exports to dead code are severed -- no live file imports any dead file"
    - "pnpm typecheck passes after refactoring (dead code is isolated)"
    - "pnpm lint passes after refactoring"
    - "ROUTER_SYSTEM_PROMPT_V2 renamed to ROUTER_SYSTEM_PROMPT (legacy prompt deleted)"
    - "send-message.ts uses EventRouterDeps instead of RouterDeps"
  artifacts:
    - path: "packages/agents/src/router/router.ts"
      provides: "Only v2.3 routeEvent function remains"
      contains: "routeEvent"
    - path: "packages/agents/src/router/types.ts"
      provides: "Only v2.3 types remain (RouteResult, EventRouterDeps, RouteEventDeps, RouteEventResult)"
    - path: "packages/agents/src/router/index.ts"
      provides: "Clean barrel with only v2.3 exports"
    - path: "packages/agents/src/shared/index.ts"
      provides: "No temporal re-export"
    - path: "packages/agents/src/shared/tools/index.ts"
      provides: "No toolkits.ts re-export"
    - path: "packages/agents/src/shared/tools/coordination/index.ts"
      provides: "No spawn-agent.ts re-export"
  key_links:
    - from: "packages/agents/src/router/tools/send-message.ts"
      to: "packages/agents/src/router/types.ts"
      via: "import type { EventRouterDeps }"
      pattern: "EventRouterDeps"
    - from: "packages/agents/src/router/slow-path.ts"
      to: "packages/agents/src/router/system-prompt.ts"
      via: "import ROUTER_SYSTEM_PROMPT"
      pattern: "ROUTER_SYSTEM_PROMPT"
---

<objective>
Phase A: Refactor all live-to-dead references to isolate dead code before deletion.

Purpose: After this plan, every dead file (Temporal workflows, legacy orchestrators, old DB stores, legacy router tools) is completely unreferenced by any barrel export or live import. This creates a safe boundary for Phase B mass deletion.
Output: All barrel exports and imports cleaned. Dead code isolated. Typecheck and lint pass.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-pre-cleanup-verification/46-DELETION-MANIFEST.md
@.planning/phases/47-cleanup-documentation/47-RESEARCH.md
@.planning/phases/47-cleanup-documentation/47-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean router module (A1-A5 + send-message.ts fix + rename ROUTER_SYSTEM_PROMPT_V2)</name>
  <files>
    packages/agents/src/router/router.ts
    packages/agents/src/router/types.ts
    packages/agents/src/router/index.ts
    packages/agents/src/router/slow-path.ts
    packages/agents/src/router/system-prompt.ts
    packages/agents/src/router/tools/send-message.ts
  </files>
  <action>
    Execute manifest steps A1 through A5 plus the research-identified send-message.ts fix and system prompt rename:

    **router/router.ts (A1):**
    - Remove imports: `executeFastPath`, `matchFastPath` from `./fast-path.js`; `routeViaAgentLoop` from `./slow-path.js` (keep `routeViaAgentLoopV2`); `RouterDeps` from `./types.js` (keep `RouteEventDeps`, `RouteEventResult`, `RouteResult`)
    - Delete functions: `routeEventLegacy` (lines ~241-291), `handleRoutingFailureLegacy` (lines ~296-320), `sendRoutingAlertLegacy` (lines ~325-363)
    - Keep: `routeEvent` (v2.3), `handleRoutingError`, `sendRoutingAlertV2`

    **router/types.ts (A2):**
    - Remove: `import type { Client } from "@temporalio/client"`, `RouterDeps` interface, `SignalAction` interface, `StartAction` interface, `IgnoreAction` interface, `FastPathAction` type alias, `RoutingRule` interface
    - Keep: `RouteResult`, `EventRouterDeps`, `RouteEventDeps`, `RouteEventResult`

    **router/index.ts (A3):**
    - Remove all legacy exports: `DETERMINISTIC_RULES`, `executeFastPath`, `matchFastPath` from fast-path; `routeEventLegacy` from router; `routeViaAgentLoop` from slow-path; `ROUTER_SYSTEM_PROMPT` from system-prompt; `FastPathAction`, `RouterDeps`, `RoutingRule` from types
    - After: export `routeEvent` from router; `formatEventForLLM`, `routeViaAgentLoopV2` from slow-path; `ROUTER_SYSTEM_PROMPT` from system-prompt (this is the renamed V2); `EventRouterDeps`, `RouteEventDeps`, `RouteEventResult`, `RouteResult` from types
    - Update module JSDoc comment to remove legacy references

    **router/slow-path.ts (A4):**
    - Remove: `ROUTER_SYSTEM_PROMPT` import (keep renamed `ROUTER_SYSTEM_PROMPT` which was `ROUTER_SYSTEM_PROMPT_V2`); `createQueryWorkflowsTool` import; `createSignalWorkflowTool` import; `createStartWorkflowTool` import; `RouterDeps` from type import (keep `EventRouterDeps`, `RouteResult`)
    - Delete entire `routeViaAgentLoop` function (legacy slow-path)
    - Keep: `formatEventForLLM` and `routeViaAgentLoopV2`
    - Update `routeViaAgentLoopV2` to use `ROUTER_SYSTEM_PROMPT` (the renamed constant)

    **router/system-prompt.ts (A5 + rename):**
    - Delete `ROUTER_SYSTEM_PROMPT` constant (the legacy prompt referencing Temporal workflow tools)
    - Rename `ROUTER_SYSTEM_PROMPT_V2` to `ROUTER_SYSTEM_PROMPT`
    - This is cosmetic but eliminates the V2 suffix since it is now the only version

    **router/tools/send-message.ts (research finding 3 -- NOT in manifest):**
    - Change line 16: `import type { RouterDeps } from "../types.js"` to `import type { EventRouterDeps } from "../types.js"`
    - Change line 40: `_deps: RouterDeps` to `_deps: EventRouterDeps`
    - This is CRITICAL: without this fix, deleting `RouterDeps` from types.ts will break send-message.ts
  </action>
  <verify>Run `pnpm --filter @aesir/agents typecheck` -- should pass (dead code files still exist but are no longer referenced from barrels; however tsconfig includes pattern may still pick them up which is fine at this stage)</verify>
  <done>All 6 router files refactored: only v2.3 code remains. Legacy functions, types, imports, and constants deleted. send-message.ts uses EventRouterDeps. ROUTER_SYSTEM_PROMPT_V2 renamed to ROUTER_SYSTEM_PROMPT.</done>
</task>

<task type="auto">
  <name>Task 2: Clean all barrel exports across packages (A6-A11 + tools barrels + agents index)</name>
  <files>
    packages/agents/src/shared/index.ts
    packages/agents/src/shared/db/index.ts
    packages/agents/src/shared/tools/index.ts
    packages/agents/src/shared/tools/coordination/index.ts
    packages/agents/src/dev-agent/index.ts
    packages/agents/src/index.ts
    packages/platform/src/index.ts
    packages/platform/src/logging/index.ts
    packages/types/src/index.ts
  </files>
  <action>
    Execute manifest steps A6 through A11 plus the research-identified barrel cleanups:

    **packages/agents/src/shared/index.ts (A6):**
    - Remove line 19: `export * from "./temporal/index.js";`
    - Update JSDoc to remove "Temporal" from description

    **packages/agents/src/shared/db/index.ts (A7):**
    - Remove: `export * from "./context-manager.js"`, `export { getTaskTokenUsage, type TaskTokenUsage } from "./cost-tracking.js"`, `export * from "./task-store.js"`, `export * from "./trace-recorder.js"`
    - Keep: `export { closeDatabase, db } from "./client.js"`, `export * from "./schema.js"`
    - Update JSDoc

    **packages/agents/src/shared/tools/index.ts (research finding 6 -- NOT in manifest):**
    - Remove line 16: `export * from "./toolkits.js";`
    - Update JSDoc to remove "toolkits" reference

    **packages/agents/src/shared/tools/coordination/index.ts (research finding 4 -- NOT in manifest):**
    - Remove: `export { type AgentTypeConfig, createSpawnAgentTool, type SpawnAgentDeps } from "./spawn-agent.js";`
    - Keep: `export { createRequestHumanInputTool, HUMAN_INPUT_MARKER } from "./request-human-input.js";`
    - Update JSDoc to remove "spawning sub-agents" reference

    **packages/agents/src/dev-agent/index.ts (A8):**
    - Remove ALL exports (worker exports AND orchestrator exports) -- the entire dev-agent directory will be deleted in Phase B. Clear the file to just an empty module: `// Deprecated: dev-agent module removed in v2.3. See definitions/dev-agent/ and framework/.`

    **packages/agents/src/index.ts (research finding 13):**
    - Remove: `export * from "./dev-agent/index.js"` and `export * from "./product-agent/index.js"`
    - Keep: `export * from "./shared/index.js"`
    - Update JSDoc to reflect v2.3 (remove "dev-agent and product-agent" references)

    **packages/platform/src/index.ts (A9):**
    - Remove line 12: `export * from "./temporal/index.js";`
    - Update comment to remove "temporal" reference

    **packages/platform/src/logging/index.ts (A10):**
    - Remove lines 29-34: the `createTemporalLogger`, `TemporalLoggerInterface`, `TemporalLogLevel` exports from `./temporal-logger.js`
    - Update JSDoc

    **packages/types/src/index.ts (A11):**
    - Remove line 12: `export * from "./temporal/index.js";`
    - Update comment to remove "Temporal types" reference
  </action>
  <verify>Run `pnpm typecheck && pnpm lint` from the repo root. Both must pass. This is GATE A from the manifest.</verify>
  <done>All 9 barrel files cleaned. No live file imports any dead code. GATE A passes: `pnpm typecheck && pnpm lint` both green. Dead code is fully isolated and ready for Phase B deletion.</done>
</task>

</tasks>

<verification>
GATE A: Run `pnpm typecheck && pnpm lint` from repo root. Both must pass with zero errors.
- If typecheck fails on dead files (files still exist but unreferenced from barrels), this indicates tsconfig includes pattern is pulling them in. This is expected and will be resolved by Phase B deletion. However, verify the failure is ONLY in dead files, not in live code.
- If lint fails, fix any formatting issues introduced by the refactoring.
</verification>

<success_criteria>
1. All 6 router files contain only v2.3 code (no legacy functions, types, or constants)
2. send-message.ts uses EventRouterDeps (not RouterDeps)
3. ROUTER_SYSTEM_PROMPT_V2 renamed to ROUTER_SYSTEM_PROMPT
4. All 9 barrel files cleaned of dead code exports
5. No live file imports from: shared/temporal/, dev-agent/orchestrator/, product-agent/orchestrator/, shared/db/{task-store,context-manager,trace-recorder,cost-tracking}, shared/tools/toolkits, shared/tools/coordination/spawn-agent, router/fast-path, router/tools/{start-workflow,query-workflows,signal-workflow}
6. `pnpm typecheck && pnpm lint` pass (GATE A)
</success_criteria>

<output>
After completion, create `.planning/phases/47-cleanup-documentation/47-01-SUMMARY.md`
</output>

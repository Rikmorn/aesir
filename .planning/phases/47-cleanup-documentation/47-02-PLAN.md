---
phase: 47-cleanup-documentation
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - packages/agents/src/shared/temporal/ (entire directory deleted)
  - packages/agents/src/dev-agent/ (entire directory deleted)
  - packages/agents/src/product-agent/ (entire directory deleted)
  - packages/agents/src/shared/db/task-store.ts (deleted)
  - packages/agents/src/shared/db/task-store.test.ts (deleted)
  - packages/agents/src/shared/db/context-manager.ts (deleted)
  - packages/agents/src/shared/db/context-manager.test.ts (deleted)
  - packages/agents/src/shared/db/trace-recorder.ts (deleted)
  - packages/agents/src/shared/db/trace-recorder.test.ts (deleted)
  - packages/agents/src/shared/db/cost-tracking.ts (deleted)
  - packages/agents/src/shared/db/cost-tracking.test.ts (deleted)
  - packages/agents/src/shared/db/schema.ts
  - packages/agents/src/shared/tools/toolkits.ts (deleted)
  - packages/agents/src/shared/tools/toolkits.test.ts (deleted)
  - packages/agents/src/shared/tools/coordination/spawn-agent.ts (deleted)
  - packages/agents/src/shared/tools/coordination/coordination-tools.test.ts
  - packages/agents/src/router/fast-path.ts (deleted)
  - packages/agents/src/router/fast-path.test.ts (deleted)
  - packages/agents/src/router/tools/start-workflow.ts (deleted)
  - packages/agents/src/router/tools/query-workflows.ts (deleted)
  - packages/agents/src/router/tools/signal-workflow.ts (deleted)
  - packages/agents/src/router/main.ts (deleted)
  - packages/platform/src/temporal/ (entire directory deleted)
  - packages/platform/src/logging/temporal-logger.ts (deleted)
  - packages/types/src/temporal/ (entire directory deleted)
autonomous: true

must_haves:
  truths:
    - "No Temporal-related source files remain in the codebase (except node_modules)"
    - "No legacy orchestrator directories remain"
    - "No dead DB store files remain"
    - "No legacy router tools or fast-path files remain"
    - "schema.ts only contains v2.3 table definitions (conversations, agent_events, agent_sessions)"
    - "coordination-tools.test.ts retains request-human-input tests only"
    - "pnpm typecheck and pnpm lint pass after all deletions"
  artifacts:
    - path: "packages/agents/src/shared/db/schema.ts"
      provides: "Only v2.3 table definitions"
      contains: "conversations"
    - path: "packages/agents/src/shared/tools/coordination/coordination-tools.test.ts"
      provides: "Tests for request-human-input only (spawn-agent tests removed)"
      contains: "createRequestHumanInputTool"
  key_links:
    - from: "packages/agents/src/framework/tool-factories.ts"
      to: "packages/agents/src/shared/tools/coordination/index.ts"
      via: "import createRequestHumanInputTool"
      pattern: "createRequestHumanInputTool"
---

<objective>
Phase B: Delete all dead files and directories isolated by Plan 47-01.

Purpose: Remove ~80+ files of dead code -- Temporal workflows/activities, legacy orchestrators, old DB stores, legacy router tools, dead entry points. Also clean schema.ts to remove old table definitions and surgically edit coordination-tools.test.ts to keep only live tests.
Output: Only v2.3 code remains in the agents, platform, and types packages. ~300+ dead tests removed.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-pre-cleanup-verification/46-DELETION-MANIFEST.md
@.planning/phases/47-cleanup-documentation/47-RESEARCH.md
@.planning/phases/47-cleanup-documentation/47-CONTEXT.md
@.planning/phases/47-cleanup-documentation/47-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all dead directories and files (B1-B10, B12 + extended scope)</name>
  <files>
    packages/agents/src/shared/temporal/ (entire directory)
    packages/agents/src/dev-agent/ (entire directory)
    packages/agents/src/product-agent/ (entire directory)
    packages/agents/src/shared/db/task-store.ts
    packages/agents/src/shared/db/task-store.test.ts
    packages/agents/src/shared/db/context-manager.ts
    packages/agents/src/shared/db/context-manager.test.ts
    packages/agents/src/shared/db/trace-recorder.ts
    packages/agents/src/shared/db/trace-recorder.test.ts
    packages/agents/src/shared/db/cost-tracking.ts
    packages/agents/src/shared/db/cost-tracking.test.ts
    packages/agents/src/shared/tools/toolkits.ts
    packages/agents/src/shared/tools/toolkits.test.ts
    packages/agents/src/shared/tools/coordination/spawn-agent.ts
    packages/agents/src/router/fast-path.ts
    packages/agents/src/router/fast-path.test.ts
    packages/agents/src/router/tools/start-workflow.ts
    packages/agents/src/router/tools/query-workflows.ts
    packages/agents/src/router/tools/signal-workflow.ts
    packages/agents/src/router/main.ts
    packages/platform/src/temporal/ (entire directory)
    packages/platform/src/logging/temporal-logger.ts
    packages/types/src/temporal/ (entire directory)
  </files>
  <action>
    Delete all dead files and directories. Execute manifest steps B1-B10, B12 plus the extended scope from CONTEXT.md decisions.

    **IMPORTANT: Verify before deleting.** For each directory deletion, first confirm that nothing inside is imported by live code. Plan 47-01 already severed all barrel exports, but a quick grep for key imports is prudent.

    **Directory deletions (use `rm -rf`):**
    1. `packages/agents/src/shared/temporal/` -- entire directory (B1). ~19 files. Temporal activities, workflows, signals, types.
    2. `packages/agents/src/dev-agent/` -- entire directory (B2-B5 + extended). Contains api/, classification/, orchestrator/, main.ts, worker.ts, utils/, index.ts, README.md. ALL confirmed dead by research finding 3.
    3. `packages/agents/src/product-agent/` -- entire directory (B3-B5 + extended). Contains api/, orchestrator/, main.ts, worker.ts, index.ts, README.md. ALL confirmed dead by research finding 3.
    4. `packages/platform/src/temporal/` -- entire directory (B7). ~12 files. Platform Temporal client, worker, signals, types, approval workflow.
    5. `packages/types/src/temporal/` -- entire directory (B9). 1 file: index.ts.

    **Individual file deletions:**
    6. `packages/platform/src/logging/temporal-logger.ts` (B8)
    7. `packages/agents/src/shared/db/task-store.ts` + `.test.ts` (B10)
    8. `packages/agents/src/shared/db/context-manager.ts` + `.test.ts` (B10)
    9. `packages/agents/src/shared/db/trace-recorder.ts` + `.test.ts` (B10)
    10. `packages/agents/src/shared/db/cost-tracking.ts` + `.test.ts` (B10)
    11. `packages/agents/src/shared/tools/toolkits.ts` + `.test.ts` (B12)
    12. `packages/agents/src/shared/tools/coordination/spawn-agent.ts` (extended scope)
    13. `packages/agents/src/router/fast-path.ts` (B6). Also delete fast-path.test.ts if it exists.
    14. `packages/agents/src/router/tools/query-workflows.ts` (B6)
    15. `packages/agents/src/router/tools/signal-workflow.ts` (B6)
    16. `packages/agents/src/router/tools/start-workflow.ts` (B6)
    17. `packages/agents/src/router/main.ts` (B5)

    **After all deletions, verify directory structure:**
    - `packages/agents/src/` should contain: `adapters/`, `framework/`, `router/`, `service/`, `shared/`, `index.ts`
    - `packages/agents/src/shared/` should contain: `agent-loop/`, `config/`, `db/`, `env/`, `mcp/`, `tools/`, `index.ts`
    - `packages/agents/src/router/` should contain: `router.ts`, `slow-path.ts`, `system-prompt.ts`, `types.ts`, `index.ts`, `tools/` (with 4 files: query-conversations.ts, start-conversation.ts, signal-conversation.ts, send-message.ts)
    - `packages/platform/src/` should NOT contain `temporal/`
    - `packages/types/src/` should NOT contain `temporal/`
  </action>
  <verify>Run `ls` on key directories to confirm structure is correct. Then run a quick grep: `grep -r "@temporalio" packages/agents/src/ packages/platform/src/ packages/types/src/ --include="*.ts" | grep -v node_modules | grep -v ".test.ts"` -- should return zero results from non-test source files.</verify>
  <done>All dead directories and files deleted. Only v2.3 source code remains.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor schema.ts and coordination test file (B11 + research finding 7)</name>
  <files>
    packages/agents/src/shared/db/schema.ts
    packages/agents/src/shared/tools/coordination/coordination-tools.test.ts
  </files>
  <action>
    **schema.ts (B11) -- Remove old table definitions:**
    Read the file first. Remove ALL legacy table definitions and their associated types/enums:
    - Remove `contextSnapshots` table definition and its CREATE TABLE section
    - Remove `taskStatusValues`, `TaskStatus`, `approvalStatusValues`, `ApprovalStatus` enum definitions
    - Remove `tasks` table definition and its CREATE TABLE section
    - Remove `traceTypeValues`, `TraceType` enum definitions
    - Remove `executionTraces` table definition and its CREATE TABLE section
    - Remove type exports for old tables: `ContextSnapshot`, `NewContextSnapshot`, `AgentTask`, `NewAgentTask`, `ExecutionTrace`, `NewExecutionTrace`

    Keep:
    - `agentsSchema` definition
    - `conversations` table definition + its types (`Conversation`, `NewConversation`)
    - `agentEvents` table definition + its types (`AgentEvent`, `NewAgentEvent`)
    - `agentSessions` table definition + its types (`AgentSession`, `NewAgentSession`)
    - The `createId` import (still needed by `agentEvents`)
    - Any shared utility types/functions used by the kept tables
    - Update file JSDoc comment to reflect v2.3 (remove references to context snapshots, tasks, execution traces)

    **coordination-tools.test.ts (research finding 7) -- Surgical test removal:**
    Read the file first. This file tests BOTH `createSpawnAgentTool` (dead) and `createRequestHumanInputTool` (live).
    - Remove the `describe("createSpawnAgentTool", ...)` block and all its tests entirely
    - Remove any imports related to spawn-agent (`createSpawnAgentTool`, `AgentTypeConfig`, `SpawnAgentDeps`)
    - Keep the `describe("createRequestHumanInputTool", ...)` block and all its tests intact
    - Keep all imports needed by the remaining tests
    - Update file-level JSDoc/comments to reflect it only tests request-human-input
  </action>
  <verify>Run `pnpm typecheck && pnpm lint` from the repo root. This is GATE B. Both must pass with zero errors. Then run `pnpm test:fast -- --run packages/agents/src/shared/tools/coordination/coordination-tools.test.ts` to verify the surviving tests still pass.</verify>
  <done>schema.ts contains only v2.3 table definitions (conversations, agent_events, agent_sessions). coordination-tools.test.ts contains only request-human-input tests. GATE B passes.</done>
</task>

</tasks>

<verification>
GATE B: Run `pnpm typecheck && pnpm lint` from repo root. Both must pass with zero errors.

Additional checks:
- `grep -r "@temporalio" packages/agents/src/ packages/platform/src/ packages/types/src/ --include="*.ts" | grep -v node_modules` -- should return ZERO results
- `ls packages/agents/src/shared/temporal/` -- should fail (directory does not exist)
- `ls packages/agents/src/dev-agent/` -- should fail (directory does not exist)
- `ls packages/agents/src/product-agent/` -- should fail (directory does not exist)
- Coordination tests pass: `pnpm test:fast -- --run packages/agents/src/shared/tools/coordination/coordination-tools.test.ts`
</verification>

<success_criteria>
1. ~80+ dead files deleted across agents, platform, and types packages
2. No @temporalio import references remain in source files
3. No shared/temporal/, dev-agent/, product-agent/ directories exist
4. schema.ts contains only conversations, agent_events, agent_sessions table definitions
5. coordination-tools.test.ts only tests createRequestHumanInputTool
6. `pnpm typecheck && pnpm lint` pass (GATE B)
7. Surviving coordination tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/47-cleanup-documentation/47-02-SUMMARY.md`
</output>

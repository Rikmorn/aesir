---
phase: 47-cleanup-documentation
plan: 03
type: execute
wave: 3
depends_on: ["47-02"]
files_modified:
  - packages/agents/package.json
  - packages/platform/package.json
  - package.json
  - docker-compose.yml
  - .env.example
  - packages/agents/drizzle.config.ts
  - packages/platform/drizzle.config.ts
  - packages/observability/drizzle.config.ts
  - packages/integrations/linear/drizzle.config.ts
  - packages/integrations/github/drizzle.config.ts
  - packages/integrations/slack/drizzle.config.ts
  - packages/agents/src/shared/db/migrations/0000_create_agents_schema.sql
  - packages/agents/src/shared/db/migrations/0001_add_v23_tables.sql (deleted)
  - packages/agents/src/shared/db/migrations/meta/_journal.json
  - packages/types/src/utils/ids.ts
  - pnpm-lock.yaml
autonomous: true

must_haves:
  truths:
    - "No @temporalio dependencies remain in any package.json"
    - "Docker Compose uses aesir credentials (not temporal) for PostgreSQL"
    - "All 6 drizzle configs default to aesir credentials"
    - ".env.example uses aesir credentials throughout"
    - "Fresh clone: pnpm db:migrate creates only v2.3 tables on a clean database"
    - "No legacy ID generators remain (agentTask, contextSnapshot, executionTrace)"
    - "Root package.json has no stale scripts or keywords (langchain, langgraph removed)"
    - "pnpm typecheck, pnpm lint, and pnpm test:fast all pass"
  artifacts:
    - path: "packages/agents/package.json"
      provides: "No @temporalio dependencies, no legacy scripts"
    - path: "packages/platform/package.json"
      provides: "No @temporalio dependencies"
    - path: "docker-compose.yml"
      provides: "PostgreSQL with aesir credentials, no temporal references"
      contains: "POSTGRES_USER: aesir"
    - path: "packages/agents/src/shared/db/migrations/0000_create_agents_schema.sql"
      provides: "Consolidated v2.3 schema (conversations, agent_events, agent_sessions)"
      contains: "conversations"
    - path: "packages/types/src/utils/ids.ts"
      provides: "No legacy ID generators"
  key_links:
    - from: "docker-compose.yml"
      to: ".env.example"
      via: "DB credential defaults must match"
      pattern: "aesir"
    - from: "packages/agents/drizzle.config.ts"
      to: "docker-compose.yml"
      via: "DB credential defaults must match"
      pattern: "aesir"
---

<objective>
Phase C: Remove dependencies, update config artifacts, consolidate migrations, and rename credentials.

Purpose: Remove all @temporalio package dependencies, rename DB credentials from temporal to aesir, consolidate migration files to clean-slate v2.3, update all 6 drizzle configs, clean root package.json, and remove legacy ID generators. After this plan, `docker compose down -v && docker compose up -d && pnpm db:migrate` creates a working system from scratch.
Output: Clean dependency tree, consistent credentials, consolidated migrations, all gates pass.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-pre-cleanup-verification/46-DELETION-MANIFEST.md
@.planning/phases/47-cleanup-documentation/47-RESEARCH.md
@.planning/phases/47-cleanup-documentation/47-CONTEXT.md
@.planning/phases/47-cleanup-documentation/47-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove @temporalio dependencies, clean package.json files, remove legacy ID generators (C1-C3, C7)</name>
  <files>
    packages/agents/package.json
    packages/platform/package.json
    package.json
    packages/types/src/utils/ids.ts
  </files>
  <action>
    **packages/agents/package.json (C1):**
    Read the file first. Remove from `dependencies`:
    - `"@temporalio/activity": "^1.14.1"`
    - `"@temporalio/client": "^1.14.1"`
    - `"@temporalio/worker": "^1.14.1"`
    - `"@temporalio/workflow": "^1.14.1"`

    Remove from `scripts`:
    - `"dev-agent": "tsx src/dev-agent/main.ts"`
    - `"product-agent": "tsx src/product-agent/main.ts"`

    **packages/platform/package.json (C2):**
    Read the file first. Remove from `dependencies`:
    - `"@temporalio/client": "^1.14.1"`
    - `"@temporalio/worker": "^1.14.1"`
    - `"@temporalio/workflow": "^1.14.1"`

    **package.json (root) (C3):**
    Read the file first.
    Remove from `scripts`:
    - `"agent:dev": "pnpm --filter @aesir/agents dev-agent"`
    - `"agent:product": "pnpm --filter @aesir/agents product-agent"`
    - `"docker:agent:dev": "docker compose up -d dev-agent && docker compose logs -f dev-agent"`
    - `"docker:agent:product": "docker compose up -d product-agent && docker compose logs -f product-agent"`

    Update `scripts`:
    - `"infra:up"` from `"docker compose up -d postgresql temporal temporal-ui"` to `"docker compose up -d postgresql"`
    - `"infra:logs"` from `"docker compose logs -f postgresql temporal temporal-ui"` to `"docker compose logs -f postgresql"`

    Update `keywords`:
    - Remove `"langchain"` and `"langgraph"`
    - Add `"conversation-executor"` (or just keep `"agent"`, `"ai"`, `"automation"`)

    **packages/types/src/utils/ids.ts (C7):**
    Read the file first. Remove legacy ID generators:
    - `agentTask: () => \`atask_${nanoid()}\``
    - `contextSnapshot: () => \`ctx_${nanoid()}\``
    - `executionTrace: () => \`trace_${nanoid()}\``
    Keep all other ID generators that are used by v2.3 code (conversation, agentEvent, agentSession, etc.).
  </action>
  <verify>Run `pnpm typecheck` to confirm no code references the deleted ID generators.</verify>
  <done>All @temporalio dependencies removed from agents and platform package.json. Root package.json scripts cleaned of legacy references. Keywords updated. Legacy ID generators removed.</done>
</task>

<task type="auto">
  <name>Task 2: Credential rename, drizzle configs, migration consolidation, pnpm install (C4-C6, C8 + extended)</name>
  <files>
    docker-compose.yml
    .env.example
    packages/agents/drizzle.config.ts
    packages/platform/drizzle.config.ts
    packages/observability/drizzle.config.ts
    packages/integrations/linear/drizzle.config.ts
    packages/integrations/github/drizzle.config.ts
    packages/integrations/slack/drizzle.config.ts
    packages/agents/src/shared/db/migrations/0000_create_agents_schema.sql
    packages/agents/src/shared/db/migrations/0001_add_v23_tables.sql (deleted)
    packages/agents/src/shared/db/migrations/meta/_journal.json
  </files>
  <action>
    **docker-compose.yml (C4):**
    Read the file first. Rename all DB credentials from `temporal` to `aesir`:
    - PostgreSQL service: `POSTGRES_USER: temporal` -> `POSTGRES_USER: aesir`, `POSTGRES_PASSWORD: temporal` -> `POSTGRES_PASSWORD: aesir`, `POSTGRES_DB: temporal` -> `POSTGRES_DB: aesir`
    - PostgreSQL healthcheck: `pg_isready -U temporal` -> `pg_isready -U aesir`
    - All 4 service environments (linear-integration, github-integration, slack-integration, agent-service): `DB_USER=temporal` -> `DB_USER=aesir`, `DB_PASSWORD=temporal` -> `DB_PASSWORD=aesir`, `DB_NAME=temporal` -> `DB_NAME=aesir`
    - Also update any DATABASE_URL connection strings from `temporal:temporal` to `aesir:aesir` and database name from `/temporal` to `/aesir`
    - Rename volume: `temporal-postgresql` -> `aesir-postgresql` and update the external name from `aesir-temporal-postgresql` to `aesir-postgresql`

    **IMPORTANT:** Do NOT remove the volume definition -- just rename it. The volume name change means existing devs must run `docker compose down -v` to recreate.

    **.env.example (C5):**
    Read the file first. Update all credential references:
    - `DATABASE_URL=postgresql://temporal:temporal@localhost:5432/temporal` -> `DATABASE_URL=postgresql://aesir:aesir@localhost:5432/aesir`
    - `DB_USER=temporal` -> `DB_USER=aesir`
    - `DB_PASSWORD=temporal` -> `DB_PASSWORD=aesir`
    - `DB_NAME=temporal` -> `DB_NAME=aesir`
    - Update the Docker Compose internal comment: `postgresql://temporal:temporal@postgresql:5432/temporal` -> `postgresql://aesir:aesir@postgresql:5432/aesir`
    - Update the local against Docker comment similarly

    **All 6 drizzle configs (research finding 11 -- NOT in manifest):**
    Read each file first. In each of the 6 files, update the credential defaults:
    - `user: process.env.DB_USER ?? "temporal"` -> `user: process.env.DB_USER ?? "aesir"`
    - `password: process.env.DB_PASSWORD ?? "temporal"` -> `password: process.env.DB_PASSWORD ?? "aesir"`
    - `database: process.env.DB_NAME ?? "temporal"` -> `database: process.env.DB_NAME ?? "aesir"`

    Files:
    1. `packages/agents/drizzle.config.ts`
    2. `packages/platform/drizzle.config.ts`
    3. `packages/observability/drizzle.config.ts`
    4. `packages/integrations/linear/drizzle.config.ts`
    5. `packages/integrations/github/drizzle.config.ts`
    6. `packages/integrations/slack/drizzle.config.ts`

    **Migration clean-slate (C6 replacement -- user decision: delete old migrations instead of DROP TABLE):**
    Replace migration 0000 content with consolidated v2.3 schema. Delete migration 0001 (its content is merged into 0000). Keep migration 0002 as-is.

    1. **Replace 0000_create_agents_schema.sql content** (keep same filename and tag):
       - New content: Create agents schema, create updated_at trigger function, create conversations table, create agent_events table, create agent_sessions table, create triggers for conversations and agent_sessions updated_at
       - Use the EXACT table definitions from the current 0001_add_v23_tables.sql combined with the schema creation and trigger function from current 0000
       - Use `CREATE TABLE IF NOT EXISTS` and `CREATE SCHEMA IF NOT EXISTS` for idempotency
       - Include all indexes from 0001
       - Update the file comments to say "v2.3 Agents Schema" (not "Context snapshots, tasks, execution traces")

    2. **Delete 0001_add_v23_tables.sql** -- its content is now in the consolidated 0000

    3. **Update meta/_journal.json:**
       - Remove the entry with `"tag": "0001_add_v23_tables"` (idx 1)
       - Update the remaining entry for 0002 to have `"idx": 1` (was idx 2)
       - Result: 2 entries -- 0000_create_agents_schema (idx 0) and 0002_add_executor_columns (idx 1)

    4. **Delete meta/0000_snapshot.json** if it exists (drizzle will regenerate it)

    **pnpm install (C8):**
    Run `pnpm install` to remove @temporalio packages from node_modules and update the lockfile. Use `--legacy-peer-deps` if needed (as noted in project gotchas), but since pnpm is used (not npm), this flag is not applicable -- pnpm handles peer deps differently. Just run `pnpm install`.
  </action>
  <verify>
    GATE C: Run `pnpm typecheck && pnpm lint && pnpm test:fast` from repo root. All three must pass.

    Additional verification:
    - `grep -r "temporal" docker-compose.yml` -- should return ZERO results (no temporal references in compose)
    - `grep -r '"temporal"' packages/*/drizzle.config.ts packages/integrations/*/drizzle.config.ts` -- should return ZERO results
    - `grep "temporal" .env.example` -- should return ZERO results
  </verify>
  <done>All credentials renamed from temporal to aesir. All 6 drizzle configs updated. Migrations consolidated to clean-slate. pnpm lockfile updated. GATE C passes: typecheck + lint + test:fast all green.</done>
</task>

</tasks>

<verification>
GATE C: Run `pnpm typecheck && pnpm lint && pnpm test:fast` from repo root. All three must pass.

Credential verification:
- `grep -ri "temporal" docker-compose.yml .env.example packages/*/drizzle.config.ts packages/integrations/*/drizzle.config.ts` -- should return ZERO results
- Migration file listing: `ls packages/agents/src/shared/db/migrations/` should show `0000_create_agents_schema.sql`, `0002_add_executor_columns.sql`, and `meta/` only (no 0001)

Test expectations:
- ~300+ dead tests removed (from Phase B deletions)
- All remaining v2.3 tests should pass
- No test should reference @temporalio, Temporal workflows, or legacy stores
</verification>

<success_criteria>
1. No @temporalio dependencies in any package.json across the monorepo
2. Docker Compose uses aesir credentials everywhere, no temporal references
3. .env.example uses aesir credentials everywhere
4. All 6 drizzle configs default to aesir credentials
5. Migration 0000 contains consolidated v2.3 schema, migration 0001 deleted, 0002 unchanged
6. meta/_journal.json has 2 entries (0000 and 0002)
7. Legacy ID generators removed from ids.ts
8. Root package.json cleaned (no legacy scripts, no stale keywords)
9. pnpm lockfile updated (no @temporalio in node_modules)
10. GATE C passes: `pnpm typecheck && pnpm lint && pnpm test:fast`
</success_criteria>

<output>
After completion, create `.planning/phases/47-cleanup-documentation/47-03-SUMMARY.md`
</output>

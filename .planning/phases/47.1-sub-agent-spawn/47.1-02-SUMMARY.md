---
phase: 47.1-sub-agent-spawn
plan: 02
subsystem: agents
tags: [spawn-agent, worker-loop, tool-factories, token-budget]

# Dependency graph
requires:
  - phase: 47.1-01
    provides: SpawnAgentDeps, createSpawnAgentTool, ToolContext.spawnDeps
provides:
  - Real spawn_agent wired into tool-factories.ts (replaces placeholder)
  - Worker loop spawnDeps population with shared token budget
  - Sub-agent definition validation at startup (fail fast)
  - Token budget passed to parent runAgentLoop when agent can spawn
affects:
  - End-to-end spawn path now operational for dev-agent and sub-agents

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Conditional spawnDeps population based on tool list membership"
    - "Fail-fast sub-agent definition validation before event log init"
    - "Shared token budget creation gated on coordination:spawn_agent tool presence"

key-files:
  created: []
  modified:
    - packages/agents/src/framework/tool-factories.ts
    - packages/agents/src/framework/tool-factories.test.ts
    - packages/agents/src/framework/worker-loop.ts
    - packages/agents/src/framework/worker-loop.test.ts

key-decisions:
  - "tokenBudget conditionally narrowed via && instead of non-null assertion -- satisfies Biome noNonNullAssertion lint rule"
  - "spawnDeps gated on both hasSpawnAgent AND tokenBudget truthiness for type safety"

patterns-established:
  - "Worker loop populates ToolContext.spawnDeps only when agent has coordination:spawn_agent -- prevents unintended spawn access"
  - "Sub-agent definitions validated at conversation start, before event log init -- fail fast on misconfigured agent definitions"

# Metrics
duration: 3m
completed: 2026-02-03
---

# Phase 47.1 Plan 02: Worker Loop Spawn Wiring Summary

**Real createSpawnAgentTool replacing placeholder in tool-factories, worker loop wiring spawnDeps with token budget and fail-fast sub-agent validation**

## Performance

- **Duration:** 3 min
- **Started:** 2026-02-03T23:53:05Z
- **Completed:** 2026-02-03T23:56:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Replaced Phase 40 spawn_agent placeholder with real createSpawnAgentTool implementation
- Worker loop now populates spawnDeps in ToolContext when agent has coordination:spawn_agent
- Sub-agent definition validation at startup prevents runtime failures from missing definitions
- Shared token budget created from definition.tokenBudget and passed to runAgentLoop
- Removed unused zod import from tool-factories.ts (was only used by placeholder)

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace spawn_agent placeholder with real createSpawnAgentTool** - `b254fee` (feat)
2. **Task 2: Wire spawnDeps, token budget, and definition validation into worker loop** - `fb579d8` (feat)

## Files Created/Modified
- `packages/agents/src/framework/tool-factories.ts` - Replaced placeholder spawn_agent with createSpawnAgentTool import and registration
- `packages/agents/src/framework/tool-factories.test.ts` - Updated test to expect "not available in this context" error
- `packages/agents/src/framework/worker-loop.ts` - Added createTokenBudget import, sub-agent definition validation, spawnDeps population, tokenBudget passthrough
- `packages/agents/src/framework/worker-loop.test.ts` - Added 5 tests for spawn wiring: spawnDeps presence/absence, fail-fast validation, token budget creation/absence

## Decisions Made
- Used `hasSpawnAgent && tokenBudget &&` double guard instead of non-null assertion for spawnDeps population -- satisfies Biome's noNonNullAssertion lint rule while maintaining type safety
- Sub-agent definition validation placed after step 1 (definition load) but before step 2 (event log init) -- fail fast without side effects

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed Biome noNonNullAssertion lint error**
- **Found during:** Task 2 (worker-loop.ts spawnDeps wiring)
- **Issue:** Plan specified `tokenBudget!` non-null assertion which Biome rejects
- **Fix:** Changed to `hasSpawnAgent && tokenBudget &&` double truthiness check
- **Files modified:** packages/agents/src/framework/worker-loop.ts
- **Verification:** `pnpm run lint` shows no new errors from modified files
- **Committed in:** fb579d8 (Task 2 commit)

**2. [Rule 1 - Bug] Fixed Biome formatting on inline type cast**
- **Found during:** Task 2 (worker-loop.test.ts)
- **Issue:** Inline type cast `{ total: number; remaining: number }` needed multi-line formatting
- **Fix:** Split to multi-line type cast format
- **Files modified:** packages/agents/src/framework/worker-loop.test.ts
- **Verification:** `pnpm run lint` shows no formatting errors in modified file
- **Committed in:** fb579d8 (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (2 bug fixes for lint compliance)
**Impact on plan:** Both fixes are mechanical lint compliance adjustments. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- End-to-end spawn path is now operational: tool-factories registers real createSpawnAgentTool, worker loop populates spawnDeps with token budget, and sub-agent definitions are validated at startup
- Phase 47.1 complete: both plans (01: SpawnAgentDeps + createSpawnAgentTool, 02: worker loop wiring) are done
- The spawn_agent tool will work when an agent definition includes coordination:spawn_agent in its tool list and has a subAgents mapping

---
*Phase: 47.1-sub-agent-spawn*
*Completed: 2026-02-03*

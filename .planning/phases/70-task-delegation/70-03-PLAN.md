---
phase: 70-task-delegation
plan: 03
type: execute
wave: 2
depends_on: ["70-01"]
files_modified:
  - packages/agents/definitions/dev-agent/definition.yaml
  - packages/agents/definitions/dev-agent/prompt.md
  - packages/agents/definitions/product-agent/definition.yaml
  - packages/agents/definitions/product-agent/prompt.md
autonomous: true

must_haves:
  truths:
    - "dev-agent and product-agent definitions include task:delegate and task:respond in their tools list"
    - "dev-agent prompt includes delegation judgment guidance distinguishing spawn vs delegate with role-specific criteria"
    - "product-agent prompt includes delegation judgment guidance with role-specific criteria"
    - "Prompts describe when to delegate, when NOT to delegate, and how to handle rejection -- goal-oriented, not procedural"
  artifacts:
    - path: "packages/agents/definitions/dev-agent/definition.yaml"
      provides: "task:delegate and task:respond tool references"
      contains: "task:delegate"
    - path: "packages/agents/definitions/dev-agent/prompt.md"
      provides: "Delegation judgment section in dev-agent prompt"
      contains: "delegation"
    - path: "packages/agents/definitions/product-agent/definition.yaml"
      provides: "task:delegate and task:respond tool references"
      contains: "task:delegate"
    - path: "packages/agents/definitions/product-agent/prompt.md"
      provides: "Delegation judgment section in product-agent prompt"
      contains: "delegation"
  key_links:
    - from: "packages/agents/definitions/dev-agent/definition.yaml"
      to: "packages/agents/src/framework/tool-factories.ts"
      via: "tool reference strings"
      pattern: "task:delegate"
    - from: "packages/agents/definitions/product-agent/definition.yaml"
      to: "packages/agents/src/framework/tool-factories.ts"
      via: "tool reference strings"
      pattern: "task:respond"
---

<objective>
Add delegation tools to orchestrator agent definitions and write delegation judgment guidance in their prompts.

Purpose: Without tool references in the definition YAML, agents cannot access task:delegate and task:respond. Without prompt guidance, agents won't know when to delegate vs spawn, how to handle rejection, or when delegation overhead isn't justified. The prompts are the primary control surface for delegation behavior (agent-first principle).

Output: Updated definition.yaml files with new tool references, updated prompt.md files with delegation judgment sections.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/agents/definitions/dev-agent/definition.yaml
@packages/agents/definitions/dev-agent/prompt.md
@packages/agents/definitions/product-agent/definition.yaml
@packages/agents/definitions/product-agent/prompt.md
@packages/agents/definitions/PROMPT_GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add task:delegate and task:respond to orchestrator definitions</name>
  <files>
    packages/agents/definitions/dev-agent/definition.yaml
    packages/agents/definitions/product-agent/definition.yaml
  </files>
  <action>
  1. **dev-agent/definition.yaml**: Add two tool references to the `tools` list:
     - `task:delegate` -- add after the existing task tools (after `task:get_task_context`)
     - `task:respond` -- add right after `task:delegate`
     This keeps the task tools grouped together.

  2. **product-agent/definition.yaml**: Add the same two tool references:
     - `task:delegate` -- add after the existing task tools (after `task:get_task_context`)
     - `task:respond` -- add right after `task:delegate`
  </action>
  <verify>
  - `grep "task:delegate" packages/agents/definitions/dev-agent/definition.yaml` returns a match
  - `grep "task:respond" packages/agents/definitions/dev-agent/definition.yaml` returns a match
  - `grep "task:delegate" packages/agents/definitions/product-agent/definition.yaml` returns a match
  - `grep "task:respond" packages/agents/definitions/product-agent/definition.yaml` returns a match
  </verify>
  <done>Both orchestrator agent definitions reference task:delegate and task:respond tools.</done>
</task>

<task type="auto">
  <name>Task 2: Write delegation judgment guidance in orchestrator prompts</name>
  <files>
    packages/agents/definitions/dev-agent/prompt.md
    packages/agents/definitions/product-agent/prompt.md
  </files>
  <action>
  1. **dev-agent/prompt.md**: Add a `## Task Delegation` section inside the existing `<domain_knowledge>` block, after the "Sub-Agent Delegation" section. The content should follow the PROMPT_GUIDE.md principles: goal-oriented (not procedural), judgment criteria over rigid rules, minimal directive density.

     Content per locked decisions:
     ```markdown
     ## Task Delegation

     You can delegate work to other agents when a task requires capabilities outside your domain. Delegation creates a new conversation for a different agent, with its own budget and context.

     **Spawn vs Delegate:**
     - **Spawn** a sub-agent (researcher, coder, tester) when the work is part of YOUR job but needs a specialist tool. Sub-agents share your budget, sandbox, and conversation. If the role is in your subAgents list, spawn.
     - **Delegate** (via directory:find + task:delegate) when the work belongs to a DIFFERENT agent's domain. Delegation creates an independent conversation with separate budget and tracking. If you need to discover who can help, delegate.

     **When to delegate:**
     - Genuine capability gap -- the work requires tools or expertise you lack
     - Distinct unit of work with a clear deliverable -- ask yourself: "would I create a separate Linear issue for this?"
     - The work justifies independent budget and tracking overhead

     **When NOT to delegate:**
     - The work is within your capabilities, even if imperfect -- don't delegate code writing, you have a coder sub-agent
     - The task is small relative to delegation overhead (handshake, new conversation, signal routing)
     - You already have the context from research -- delegating forces context re-discovery

     **Delegation flow:**
     1. Find candidates: `directory:find` with a capability description
     2. Delegate: `task:delegate` with targetEntityId and a thorough description (the brief IS the context -- the target agent cannot see your conversation)
     3. Wait: `wait_for` with type "task_handshake" and timeout "30s" to receive accept/reject

     **Handling rejection:**
     If a delegation is rejected, consider the reason. Try the next candidate from your existing directory:find results. If the rejection suggests you need a different capability ("you need a DBA, not a backend dev"), re-query the directory. If all candidates are exhausted, report failure to your delegator or handle the work yourself.

     **Receiving delegations:**
     When you receive a `<delegation>` block, evaluate whether you can fulfill it. Respond via `task:respond` -- accept with an estimate if you can handle it, reject with a reason if you cannot. If you accept, the work is yours. Query shared knowledge if you need additional context beyond what the brief provides.
     ```

  2. **product-agent/prompt.md**: Add a similar `## Task Delegation` section inside `<domain_knowledge>`, after the "Task Lifecycle" section. Tailor to product-agent's role:

     ```markdown
     ## Task Delegation

     You can delegate work to other agents when a task requires capabilities outside your domain (e.g., implementation, testing, code review).

     **When to delegate:**
     - Implementation work -- you create well-structured issues, developers implement them
     - Technical investigation that requires codebase access or test execution
     - Work that belongs to another agent's capability set

     **When NOT to delegate:**
     - User conversation and clarification -- never delegate your direct user interaction
     - Issue creation and prioritization -- this is your core competency
     - Tasks small enough that the delegation overhead exceeds the work itself

     **Delegation flow:**
     1. Find candidates: `directory:find` with a capability description
     2. Delegate: `task:delegate` with targetEntityId and a thorough description (include requirements, acceptance criteria, context -- the target agent cannot see your conversation)
     3. Wait: `wait_for` with type "task_handshake" and timeout "30s" to receive accept/reject

     **Handling rejection:**
     If a delegation is rejected, consider the reason. Try the next candidate from your existing directory:find results. If the rejection suggests you need a different capability, re-query the directory. If all candidates are exhausted, inform the user and suggest alternatives.

     **Receiving delegations:**
     When you receive a `<delegation>` block, evaluate whether it falls within your capabilities (product work, issue creation, user communication). Respond via `task:respond` -- accept if the work is in your domain, reject with a reason if it requires capabilities you lack.
     ```

  **Prompt writing principles (from PROMPT_GUIDE.md and locked decisions):**
  - Goal-oriented, not procedural -- the 3-step delegation flow is the one exception because it's a tool sequence, not a branching state machine
  - No MUST/ALWAYS/NEVER except for genuine safety boundaries
  - Trust the model to judge context -- provide criteria, not rules
  - Agent-specific criteria: dev-agent's "don't delegate code writing" differs from product-agent's "never delegate user conversation"
  - The `<delegation>` block handling section prepares agents for Phase 70+ where they receive delegated work
  </action>
  <verify>
  - `grep -c "Task Delegation" packages/agents/definitions/dev-agent/prompt.md` returns 1
  - `grep -c "Task Delegation" packages/agents/definitions/product-agent/prompt.md` returns 1
  - `grep "directory:find" packages/agents/definitions/dev-agent/prompt.md` returns a match
  - `grep "task:respond" packages/agents/definitions/product-agent/prompt.md` returns a match
  </verify>
  <done>Both orchestrator prompts contain delegation judgment guidance with role-specific criteria. Prompts describe spawn vs delegate heuristic, when to delegate, when not to, delegation flow, rejection handling, and receiving delegations. Content follows PROMPT_GUIDE.md principles: goal-oriented, judgment-based, minimal directives.</done>
</task>

</tasks>

<verification>
- Both orchestrator definitions include task:delegate and task:respond in tools list
- Both orchestrator prompts have delegation judgment sections
- Prompt content distinguishes spawn (sub-agent) from delegate (cross-conversation)
- Prompts are goal-oriented, not procedural state machines
- Each orchestrator has role-specific criteria (dev-agent: code; product-agent: user conversation)
</verification>

<success_criteria>
- Agent definitions reference the new delegation tools
- Prompts guide agents on when to delegate, when to spawn, and how to handle the handshake
- Content follows established prompt writing principles
- No procedural state machines or excessive directives in prompts
</success_criteria>

<output>
After completion, create `.planning/phases/70-task-delegation/70-03-SUMMARY.md`
</output>

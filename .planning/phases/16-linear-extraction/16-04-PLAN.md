---
phase: 16-linear-extraction
plan: 04
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - packages/integrations/linear/src/client/factory.ts
  - packages/integrations/linear/src/client/issues.ts
  - packages/integrations/linear/src/client/activities.ts
  - packages/integrations/linear/src/client/types.ts
  - packages/integrations/linear/src/client/index.ts
  - packages/integrations/linear/src/index.ts
autonomous: true

must_haves:
  truths:
    - "LinearClient can be created with OAuth tokens"
    - "Issue operations (read, update status, create) work via SDK"
    - "Agent activities can be emitted to Linear sessions"
  artifacts:
    - path: "packages/integrations/linear/src/client/factory.ts"
      provides: "LinearClient factory with token management"
      contains: "createLinearClient"
    - path: "packages/integrations/linear/src/client/issues.ts"
      provides: "Issue CRUD operations"
      contains: "readIssue"
    - path: "packages/integrations/linear/src/client/activities.ts"
      provides: "Agent activity emitters"
      contains: "emitThought"
  key_links:
    - from: "packages/integrations/linear/src/client/factory.ts"
      to: "@linear/sdk"
      via: "SDK import"
      pattern: "LinearClient"
    - from: "packages/integrations/linear/src/client/issues.ts"
      to: "@linear/sdk"
      via: "SDK types"
      pattern: "Issue"
---

<objective>
Move Linear SDK client code to the Linear package including client factory, issue operations, and agent activity emitters.

Purpose: Linear package provides complete API access - client creation, issue management, and agent activities.

Output: Fully functional Linear API layer that agents can use.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-linear-extraction/16-CONTEXT.md
@.planning/phases/16-linear-extraction/16-RESEARCH.md
@packages/integrations/src/linear/client.ts
@packages/integrations/src/linear/issues.ts
@packages/integrations/src/linear/activities.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Linear client factory and types</name>
  <files>
    packages/integrations/linear/src/client/types.ts
    packages/integrations/linear/src/client/factory.ts
  </files>
  <action>
    1. Create packages/integrations/linear/src/client/types.ts:
       - Define LinearConfig interface:
         - accessToken: string
         - refreshToken?: string
         - expiresAt: number
       - Export type

    2. Create packages/integrations/linear/src/client/factory.ts:
       - Copy and adapt from packages/integrations/src/linear/client.ts
       - Import createPinoLogger from @aesir/common
       - Import LinearClient from @linear/sdk
       - Import LinearConfig from ./types.js

       - Copy TokenRefreshResponse interface (internal)

       - Move refreshOAuthToken function:
         - Reads LINEAR_CLIENT_ID and LINEAR_CLIENT_SECRET from process.env
         - POSTs to https://api.linear.app/oauth/token
         - Returns { accessToken, refreshToken, expiresIn }

       - Move stripBearerPrefix helper function (internal)

       - Move createLinearClient function:
         - Takes config: LinearConfig and optional onTokenRefresh callback
         - Checks if token needs refresh (within 60s buffer)
         - Handles token refresh and callback
         - Returns Promise<LinearClient>

       - Move getLinearClient function:
         - Takes token string
         - Detects token type (lin_api_ prefix = API key, otherwise OAuth)
         - Returns LinearClient immediately (no refresh logic)

       - Export: refreshOAuthToken, createLinearClient, getLinearClient
  </action>
  <verify>
    pnpm --filter @aesir/integration-linear typecheck
    ls packages/integrations/linear/src/client/factory.ts
  </verify>
  <done>Linear client factory exists with token management</done>
</task>

<task type="auto">
  <name>Task 2: Create issue operations and agent activities</name>
  <files>
    packages/integrations/linear/src/client/issues.ts
    packages/integrations/linear/src/client/activities.ts
    packages/integrations/linear/src/client/index.ts
    packages/integrations/linear/src/index.ts
  </files>
  <action>
    1. Create packages/integrations/linear/src/client/issues.ts:
       - Copy from packages/integrations/src/linear/issues.ts
       - Import createPinoLogger, IssueStatus from @aesir/common
       - Import Issue, IssueLabel, LinearClient, Team, WorkflowState from @linear/sdk

       - Copy interfaces: CreateIssueParams, CreateIssueResult, TeamInfo, LabelInfo

       - Copy functions:
         - createIssue(client, params): Promise<CreateIssueResult>
         - listTeams(client): Promise<TeamInfo[]>
         - listLabels(client, teamId): Promise<LabelInfo[]>

       - Copy readIssue and updateIssueStatus from client.ts:
         - readIssue(client, issueId): Promise<Issue>
         - updateIssueStatus(client, issueId, statusName): Promise<void>

    2. Create packages/integrations/linear/src/client/activities.ts:
       - Copy from packages/integrations/src/linear/activities.ts
       - Import LinearClient from @linear/sdk
       - Import AgentPlanItem from ../webhooks/types.js

       - Copy all activity functions:
         - emitThought(client, sessionId, body)
         - emitAction(client, sessionId, action, parameter)
         - emitResponse(client, sessionId, body)
         - emitError(client, sessionId, body)
         - emitElicitation(client, sessionId, body)
         - updateSessionPlan(client, sessionId, plan)

    3. Create packages/integrations/linear/src/client/index.ts:
       - // === TYPES ===
       - export type { LinearConfig } from "./types.js"
       - export type { CreateIssueParams, CreateIssueResult, TeamInfo, LabelInfo } from "./issues.js"
       - // === CLIENT FACTORY ===
       - export { createLinearClient, getLinearClient, refreshOAuthToken } from "./factory.js"
       - // === ISSUES ===
       - export { createIssue, listTeams, listLabels, readIssue, updateIssueStatus } from "./issues.js"
       - // === ACTIVITIES ===
       - export { emitThought, emitAction, emitResponse, emitError, emitElicitation, updateSessionPlan } from "./activities.js"

    4. Update packages/integrations/linear/src/index.ts:
       - Add: // === CLIENT ===
       - Add: export * from "./client/index.js"
  </action>
  <verify>
    pnpm --filter @aesir/integration-linear build
    pnpm --filter @aesir/integration-linear typecheck
  </verify>
  <done>Issue operations and agent activities exist in Linear package</done>
</task>

</tasks>

<verification>
- `pnpm --filter @aesir/integration-linear build` succeeds
- createLinearClient factory handles token refresh
- Issue operations (read, create, update) are exported
- Agent activity emitters (thought, action, response, error, elicitation) are exported
</verification>

<success_criteria>
- LinearClient factory with OAuth token management
- Issue operations: readIssue, createIssue, updateIssueStatus, listTeams, listLabels
- Agent activities: emitThought, emitAction, emitResponse, emitError, emitElicitation, updateSessionPlan
- All functions exported from Linear package
</success_criteria>

<output>
After completion, create `.planning/phases/16-linear-extraction/16-04-SUMMARY.md`
</output>

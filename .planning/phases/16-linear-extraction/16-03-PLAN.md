---
phase: 16-linear-extraction
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - packages/integrations/linear/src/webhooks/signature.ts
  - packages/integrations/linear/src/webhooks/parser.ts
  - packages/integrations/linear/src/webhooks/types.ts
  - packages/integrations/linear/src/webhooks/index.ts
  - packages/integrations/linear/src/index.ts
autonomous: true

must_haves:
  truths:
    - "Webhook signature verification uses timing-safe comparison"
    - "Webhook payload parsing has Zod validation"
    - "Type guards correctly identify AgentSession events"
  artifacts:
    - path: "packages/integrations/linear/src/webhooks/signature.ts"
      provides: "HMAC-SHA256 signature verification"
      contains: "verifyWebhookSignature"
    - path: "packages/integrations/linear/src/webhooks/parser.ts"
      provides: "Zod schema parsing for webhook payloads"
      contains: "parseAgentSessionPayload"
    - path: "packages/integrations/linear/src/webhooks/types.ts"
      provides: "Webhook payload type definitions"
      contains: "AgentSessionPayload"
  key_links:
    - from: "packages/integrations/linear/src/webhooks/signature.ts"
      to: "node:crypto"
      via: "HMAC import"
      pattern: "createHmac"
    - from: "packages/integrations/linear/src/webhooks/parser.ts"
      to: "zod"
      via: "schema validation"
      pattern: "z\\.object"
---

<objective>
Move webhook handling code to the Linear package including signature verification, payload parsing with Zod validation, and type guards.

Purpose: Linear owns its webhook processing completely - signature verification, payload validation, and event type detection.

Output: Complete webhook handling module that can verify and parse Linear webhooks.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-linear-extraction/16-CONTEXT.md
@.planning/phases/16-linear-extraction/16-RESEARCH.md
@packages/integrations/src/linear/webhooks.ts
@packages/integrations/src/linear/types.ts
@packages/agents/src/api/webhooks/schemas/linear-webhook.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create webhook types and signature verification</name>
  <files>
    packages/integrations/linear/src/webhooks/types.ts
    packages/integrations/linear/src/webhooks/signature.ts
  </files>
  <action>
    1. Create packages/integrations/linear/src/webhooks/types.ts:
       - Move webhook types from packages/integrations/src/linear/types.ts:
         - WebhookPayloadBase interface (type, data, webhookTimestamp, webhookId)
         - WebhookPayload interface (extends base, adds action)
         - AgentSessionPayload interface (action: created/prompted, agentSession object)
         - AgentActivityType union
         - ThoughtActivityContent, ActionActivityContent, ResponseActivityContent,
           ErrorActivityContent, ElicitationActivityContent interfaces
         - AgentActivityContent union type
         - AgentPlanItem interface
       - DO NOT move LinearConfig (stays in types/config.ts)
       - Export all types

    2. Create packages/integrations/linear/src/webhooks/signature.ts:
       - Copy verifyWebhookSignature from packages/integrations/src/linear/webhooks.ts
       - Uses createHmac, timingSafeEqual from node:crypto
       - HMAC-SHA256 with hex-encoded signature
       - Timing-safe comparison to prevent timing attacks
       - Copy validateWebhookTimestamp function (tolerance-based timestamp validation)
       - Export both functions
  </action>
  <verify>
    pnpm --filter @aesir/integration-linear typecheck
    ls packages/integrations/linear/src/webhooks/signature.ts
    ls packages/integrations/linear/src/webhooks/types.ts
  </verify>
  <done>Webhook types and signature verification exist in Linear package</done>
</task>

<task type="auto">
  <name>Task 2: Create webhook parser with Zod validation</name>
  <files>
    packages/integrations/linear/src/webhooks/parser.ts
    packages/integrations/linear/src/webhooks/index.ts
    packages/integrations/linear/src/index.ts
  </files>
  <action>
    1. Create packages/integrations/linear/src/webhooks/parser.ts:
       - Import z from zod
       - Import AgentSessionPayload, WebhookPayloadBase from ./types.js

       - Create AgentSessionSchema (Zod schema):
         - action: z.enum(["created", "prompted"])
         - type: z.literal("AgentSessionEvent")
         - webhookTimestamp: z.number()
         - webhookId: z.string()
         - agentSession: z.object({
             id: z.string(),
             issueId: z.string(),
             status: z.enum(["pending", "active", "completed"]),
             url: z.string(),
             creator: z.object({ id: z.string() }).optional()
           })

       - Create parseAgentSessionPayload function:
         - Takes rawBody string
         - Returns { success: true, data: AgentSessionPayload } | { success: false, error: ZodError }
         - Uses safeParse for controlled validation

       - Create type guards:
         - isAgentSessionEvent(payload: WebhookPayloadBase): payload is AgentSessionPayload
         - isIssueEvent(payload: WebhookPayloadBase): boolean

       - Create parseWebhookPayload<T>(rawBody: string): T for generic parsing

    2. Create packages/integrations/linear/src/webhooks/index.ts:
       - // === TYPES ===
       - export type { WebhookPayloadBase, WebhookPayload, AgentSessionPayload, AgentActivityType, ThoughtActivityContent, ActionActivityContent, ResponseActivityContent, ErrorActivityContent, ElicitationActivityContent, AgentActivityContent, AgentPlanItem } from "./types.js"
       - // === SIGNATURE ===
       - export { verifyWebhookSignature, validateWebhookTimestamp } from "./signature.js"
       - // === PARSER ===
       - export { parseAgentSessionPayload, parseWebhookPayload, isAgentSessionEvent, isIssueEvent, AgentSessionSchema } from "./parser.js"

    3. Update packages/integrations/linear/src/index.ts:
       - Add: // === WEBHOOKS ===
       - Add: export * from "./webhooks/index.js"
  </action>
  <verify>
    pnpm --filter @aesir/integration-linear build
    pnpm --filter @aesir/integration-linear typecheck
  </verify>
  <done>Webhook parser with Zod validation exists in Linear package</done>
</task>

</tasks>

<verification>
- `pnpm --filter @aesir/integration-linear build` succeeds
- verifyWebhookSignature function uses timing-safe comparison
- parseAgentSessionPayload returns Zod SafeParseResult
- Type guards correctly narrow payload types
</verification>

<success_criteria>
- Webhook signature verification with HMAC-SHA256
- Zod schema for AgentSession payload validation
- Type guards for event type detection
- All webhook types exported from Linear package
</success_criteria>

<output>
After completion, create `.planning/phases/16-linear-extraction/16-03-SUMMARY.md`
</output>

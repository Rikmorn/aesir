---
phase: 16-linear-extraction
plan: 11
type: execute
wave: 7
depends_on: ["16-10"]
files_modified:
  - .claude/CLAUDE.md
  - .cursor/rules/architecture.mdc
autonomous: false

must_haves:
  truths:
    - "CLAUDE.md documents the new Linear package structure"
    - "Architecture reflects integrations as independent packages"
    - "Import paths updated in documentation"
  artifacts:
    - path: ".claude/CLAUDE.md"
      provides: "Updated architecture documentation"
      contains: "@aesir/integration-linear"
    - path: ".cursor/rules/architecture.mdc"
      provides: "Updated cursor rules"
      contains: "packages/integrations/linear"
  key_links:
    - from: ".claude/CLAUDE.md"
      to: "packages/integrations/linear"
      via: "directory structure"
      pattern: "integrations/linear"
---

<objective>
Update AI context files (CLAUDE.md and cursor rules) to reflect the new Linear package structure.

Purpose: AI tools understand the new architecture and guide future development correctly.

Output: Updated documentation reflecting Linear as an independent package.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-linear-extraction/16-CONTEXT.md
@.planning/phases/16-linear-extraction/16-RESEARCH.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md</name>
  <files>
    .claude/CLAUDE.md
  </files>
  <action>
    1. Update the Architecture section:
       - Update 3-Layer Structure diagram to show:
         ```
         Agents (dev-agent, product-agent)
            ↓ uses
         Integrations (Linear, GitHub, Slack)
            ├── @aesir/integration-linear (independent package)
            ├── @aesir/integrations (GitHub, Slack, shared)
            ↓ uses
         Platform (config, logging, state, temporal)
         ```

       - Add note: "Each integration (starting with Linear) is extracted to its own package
         for independent deployment, versioning, and lifecycle management."

    2. Update Directory Structure:
       - Add packages/integrations/linear/ structure:
         ```
         packages/
         ├── integrations/
         │   ├── linear/           # @aesir/integration-linear (independent)
         │   │   ├── src/
         │   │   │   ├── api/      # HTTP routes (webhooks, OAuth)
         │   │   │   ├── client/   # Linear SDK wrapper
         │   │   │   ├── db/       # linear.* schema, credential store
         │   │   │   ├── oauth/    # Token management
         │   │   │   ├── webhooks/ # Signature verification, parsing
         │   │   │   ├── types/    # Config, errors
         │   │   │   ├── index.ts  # Barrel export
         │   │   │   └── main.ts   # HTTP server entry
         │   │   ├── Dockerfile
         │   │   └── package.json
         │   └── src/              # Shared integrations (GitHub, Slack)
         ```

    3. Update Code Patterns section:
       - Add Linear import example:
         ```typescript
         // For Linear functionality, import from the dedicated package
         import {
           createLinearClientFromDatabase,
           verifyWebhookSignature,
           emitThought,
         } from "@aesir/integration-linear";
         ```

    4. Update Gotchas section:
       - Add: "Linear uses its own PostgreSQL schema (linear.*), separate from integrations.* schema"
       - Add: "Import Linear from @aesir/integration-linear, not from @aesir/integrations"

    5. Update v2.0 Foundation Work section:
       - Mark Phase 16 as complete
       - Note extraction pattern for future GitHub/Slack phases
  </action>
  <verify>
    ls .claude/CLAUDE.md
    grep -c "@aesir/integration-linear" .claude/CLAUDE.md
  </verify>
  <done>CLAUDE.md updated with Linear package documentation</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Linear package extracted as @aesir/integration-linear with:
    - Independent package structure
    - Own database schema (linear.*)
    - HTTP server entry point
    - Updated AI documentation
  </what-built>
  <how-to-verify>
    1. Run the build to verify everything compiles:
       ```bash
       pnpm build
       ```

    2. Run tests to verify functionality:
       ```bash
       pnpm test
       ```

    3. (Optional) If database is running, test the migration:
       ```bash
       pnpm exec tsx scripts/migrate-linear-credentials.ts
       ```

    4. Verify Linear service can start:
       ```bash
       pnpm --filter @aesir/integration-linear build
       # Check the main.ts compiles correctly
       ls packages/integrations/linear/dist/main.js
       ```

    5. Review CLAUDE.md updates for accuracy
  </how-to-verify>
  <resume-signal>Type "approved" to complete phase, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- CLAUDE.md contains @aesir/integration-linear references
- Architecture diagram shows independent Linear package
- Import examples use correct package name
- Gotchas section documents Linear-specific details
</verification>

<success_criteria>
- CLAUDE.md accurately documents new architecture
- Cursor rules updated for Linear package
- Human verification confirms extraction works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/16-linear-extraction/16-11-SUMMARY.md`
</output>

---
phase: 16-linear-extraction
plan: 10
type: execute
wave: 6
depends_on: ["16-06", "16-07", "16-08", "16-09"]
files_modified:
  - packages/integrations/src/linear/index.ts
  - packages/integrations/src/index.ts
  - packages/agents/src/api/webhooks/linear-agent-session.ts
  - packages/agents/src/scripts/linear-oauth.ts
  - packages/agents/src/temporal/activities/linear-activities.ts
  - packages/agents/package.json
autonomous: true

must_haves:
  truths:
    - "Agents package imports from @aesir/integration-linear instead of @aesir/integrations linear/"
    - "Old integrations linear/ module re-exports from new package for backward compatibility"
    - "All existing functionality continues to work"
  artifacts:
    - path: "packages/integrations/src/linear/index.ts"
      provides: "Re-exports from @aesir/integration-linear"
      contains: "@aesir/integration-linear"
    - path: "packages/agents/package.json"
      provides: "Dependency on @aesir/integration-linear"
      contains: "@aesir/integration-linear"
  key_links:
    - from: "packages/agents/src/api/webhooks/linear-agent-session.ts"
      to: "@aesir/integration-linear"
      via: "import statement"
      pattern: "@aesir/integration-linear"
    - from: "packages/integrations/src/linear/index.ts"
      to: "@aesir/integration-linear"
      via: "re-export"
      pattern: "export.*from.*@aesir/integration-linear"
---

<objective>
Update consumers to use the new Linear package and create backward-compatible re-exports in the old location.

Purpose: Clean migration path - agents import from new package, old location re-exports for any remaining consumers.

Output: All Linear imports use the new package, with backward compatibility layer.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-linear-extraction/16-CONTEXT.md
@.planning/phases/16-linear-extraction/16-RESEARCH.md
@packages/agents/src/api/webhooks/linear-agent-session.ts
@packages/agents/src/scripts/linear-oauth.ts
@packages/agents/src/temporal/activities/linear-activities.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update agents package to use new Linear package</name>
  <files>
    packages/agents/package.json
    packages/agents/src/api/webhooks/linear-agent-session.ts
    packages/agents/src/scripts/linear-oauth.ts
    packages/agents/src/temporal/activities/linear-activities.ts
  </files>
  <action>
    1. Update packages/agents/package.json:
       - Add dependency: "@aesir/integration-linear": "workspace:*"

    2. Update packages/agents/src/api/webhooks/linear-agent-session.ts:
       - Change imports from @aesir/integrations to @aesir/integration-linear:
         - verifyWebhookSignature
         - validateWebhookTimestamp
         - startApprovalWorkflow (keep from @aesir/integrations - Temporal)
       - Update import paths

    3. Update packages/agents/src/scripts/linear-oauth.ts:
       - Change import of saveLinearTokens from @aesir/integrations to @aesir/integration-linear

    4. Update packages/agents/src/temporal/activities/linear-activities.ts:
       - Change imports from @aesir/integrations to @aesir/integration-linear:
         - Linear client functions
         - Linear types
       - Keep Temporal-related imports from @aesir/integrations

    5. Search for any other Linear imports in agents package and update:
       - Use grep to find: from "@aesir/integrations".*linear
       - Update each import to use @aesir/integration-linear
  </action>
  <verify>
    pnpm install
    pnpm --filter @aesir/agents typecheck
  </verify>
  <done>Agents package uses @aesir/integration-linear directly</done>
</task>

<task type="auto">
  <name>Task 2: Create backward-compatible re-exports</name>
  <files>
    packages/integrations/src/linear/index.ts
    packages/integrations/src/index.ts
    packages/integrations/package.json
  </files>
  <action>
    1. Update packages/integrations/package.json:
       - Add dependency: "@aesir/integration-linear": "workspace:*"

    2. Update packages/integrations/src/linear/index.ts:
       - Replace all exports with re-exports from @aesir/integration-linear:
         ```typescript
         /**
          * Linear Integration Module
          *
          * @deprecated Import from @aesir/integration-linear directly.
          * This module re-exports for backward compatibility.
          */

         // Re-export everything from the new package
         export * from "@aesir/integration-linear";
         ```

    3. Update packages/integrations/src/index.ts:
       - Linear section now re-exports from the new package
       - Add @deprecated JSDoc to Linear section
       - Keep all other integrations (GitHub, Slack) as-is

    4. Keep old Linear source files for reference:
       - Rename packages/integrations/src/linear/ to packages/integrations/src/_legacy/linear/
       - This provides reference during migration, can be deleted later
       - Keep the new index.ts at packages/integrations/src/linear/index.ts (just re-exports)
  </action>
  <verify>
    pnpm install
    pnpm --filter @aesir/integrations build
    pnpm --filter @aesir/agents build
  </verify>
  <done>Backward-compatible re-exports exist in old location</done>
</task>

</tasks>

<verification>
- `pnpm install` succeeds with new workspace dependency
- `pnpm --filter @aesir/agents build` succeeds
- `pnpm --filter @aesir/integrations build` succeeds
- Imports from @aesir/integrations still work (re-exports)
</verification>

<success_criteria>
- Agents package depends on @aesir/integration-linear
- All Linear imports in agents updated to new package
- Old location (integrations/src/linear) re-exports from new package
- All builds pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-linear-extraction/16-10-SUMMARY.md`
</output>

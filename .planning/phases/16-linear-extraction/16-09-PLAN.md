---
phase: 16-linear-extraction
plan: 09
type: execute
wave: 5
depends_on: ["16-03", "16-04", "16-05"]
files_modified:
  - packages/integrations/linear/src/client/factory.test.ts
  - packages/integrations/linear/src/client/issues.test.ts
  - packages/integrations/linear/src/webhooks/signature.test.ts
  - packages/integrations/linear/src/webhooks/parser.test.ts
  - packages/integrations/linear/src/db/credential-store.test.ts
autonomous: true

must_haves:
  truths:
    - "Webhook signature verification has unit tests"
    - "Client factory has unit tests"
    - "Credential store has type contract tests"
  artifacts:
    - path: "packages/integrations/linear/src/webhooks/signature.test.ts"
      provides: "Signature verification tests"
      contains: "verifyWebhookSignature"
    - path: "packages/integrations/linear/src/client/factory.test.ts"
      provides: "Client factory tests"
      contains: "createLinearClient"
    - path: "packages/integrations/linear/src/db/credential-store.test.ts"
      provides: "Credential store tests"
      contains: "createLinearCredentialStore"
  key_links:
    - from: "packages/integrations/linear/src/webhooks/signature.test.ts"
      to: "packages/integrations/linear/src/webhooks/signature.ts"
      via: "test import"
      pattern: "from.*signature"
---

<objective>
Move and adapt existing tests to the Linear package, ensuring core functionality has coverage.

Purpose: Tests move with the code - the Linear package is testable independently.

Output: Unit tests for webhooks, client, and credential store.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-linear-extraction/16-CONTEXT.md
@.planning/phases/16-linear-extraction/16-RESEARCH.md
@packages/integrations/src/linear/webhooks.test.ts
@packages/integrations/src/linear/client.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move webhook tests</name>
  <files>
    packages/integrations/linear/src/webhooks/signature.test.ts
    packages/integrations/linear/src/webhooks/parser.test.ts
  </files>
  <action>
    1. Create packages/integrations/linear/src/webhooks/signature.test.ts:
       - Copy and adapt from packages/integrations/src/linear/webhooks.test.ts
       - Import describe, it, expect from vitest
       - Import verifyWebhookSignature, validateWebhookTimestamp from ./signature.js

       - Test suite for verifyWebhookSignature:
         - "returns true for valid signature"
         - "returns false for invalid signature"
         - "returns false for tampered body"
         - "returns false for invalid hex signature"

       - Test suite for validateWebhookTimestamp:
         - "returns true for recent timestamp"
         - "returns false for old timestamp"
         - "handles custom tolerance"

    2. Create packages/integrations/linear/src/webhooks/parser.test.ts:
       - Import describe, it, expect from vitest
       - Import parseAgentSessionPayload, isAgentSessionEvent, isIssueEvent from ./parser.js

       - Test suite for parseAgentSessionPayload:
         - "successfully parses valid AgentSession payload"
         - "returns error for invalid payload"
         - "returns error for missing required fields"

       - Test suite for type guards:
         - "isAgentSessionEvent returns true for AgentSessionEvent type"
         - "isAgentSessionEvent returns false for other types"
         - "isIssueEvent returns true for Issue type"
  </action>
  <verify>
    pnpm --filter @aesir/integration-linear test -- src/webhooks/
  </verify>
  <done>Webhook tests exist and pass</done>
</task>

<task type="auto">
  <name>Task 2: Move client and credential store tests</name>
  <files>
    packages/integrations/linear/src/client/factory.test.ts
    packages/integrations/linear/src/client/issues.test.ts
    packages/integrations/linear/src/db/credential-store.test.ts
  </files>
  <action>
    1. Create packages/integrations/linear/src/client/factory.test.ts:
       - Copy and adapt from packages/integrations/src/linear/client.test.ts
       - Import describe, it, expect, vi from vitest
       - Import getLinearClient from ./factory.js

       - Test suite for getLinearClient:
         - "creates client with API key when token starts with lin_api_"
         - "creates client with OAuth token for other tokens"
         - "strips Bearer prefix from token"

       - Note: createLinearClient requires mocking fetch, keep tests simple

    2. Create packages/integrations/linear/src/client/issues.test.ts:
       - Copy and adapt from packages/integrations/src/linear/issues.test.ts (if exists)
       - Test interfaces and types rather than SDK calls (SDK is mocked)
       - Test parameter building logic for createIssue

    3. Create packages/integrations/linear/src/db/credential-store.test.ts:
       - Type contract tests (following 13-04 decision)
       - Import describe, it, expect, vi from vitest
       - Import createLinearCredentialStore, type LinearCredentialStore from ./credential-store.js

       - Test suite for createLinearCredentialStore:
         - "throws if db is not provided"
         - "throws if logger is not provided"
         - "returns object with required methods"

       - Test interface compliance:
         - "store method returns ResultAsync"
         - "get method returns ResultAsync"
         - "getByWorkspace method returns ResultAsync"
         - "updateTokens method returns ResultAsync"
         - "delete method returns ResultAsync"
         - "health method returns Promise"
         - "close method returns Promise"

       - Note: Full integration tests require database (defer to Phase 20)
  </action>
  <verify>
    pnpm --filter @aesir/integration-linear test
  </verify>
  <done>Client and credential store tests exist and pass</done>
</task>

</tasks>

<verification>
- `pnpm --filter @aesir/integration-linear test` passes
- Webhook signature tests cover valid/invalid cases
- Client factory tests cover token type detection
- Credential store has type contract tests
</verification>

<success_criteria>
- All tests pass in Linear package
- Webhook signature verification fully tested
- Zod parser validation tested
- Type guards tested
- Client factory token detection tested
</success_criteria>

<output>
After completion, create `.planning/phases/16-linear-extraction/16-09-SUMMARY.md`
</output>

---
phase: 36-end-to-end-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/36-end-to-end-validation/36-01-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "Smart router handles all v2.1 event types via 9 deterministic rules + LLM slow path"
    - "Context snapshots are written at activity end and read at next activity start"
    - "Sub-agents receive focused tool sets: researcher 4 tools, coder 4 tools, tester 3 tools"
    - "execution_traces table has parent/child correlation via agent_instance_id and parent_agent_instance_id"
    - "All guardrails verified: sandbox enforcement, merge protection, budget enforcement, cost tracking"
    - "Zero @langchain/* dependencies in packages/agents"
  artifacts:
    - path: ".planning/phases/36-end-to-end-validation/36-01-SUMMARY.md"
      provides: "Static verification evidence for E2EV-06 through E2EV-11"
  key_links:
    - from: "router/fast-path.ts"
      to: "shared/temporal/signals.ts"
      via: "signal definitions for planApproval, prCompletion, escalationResolved"
      pattern: "planApprovalSignal|prCompletionSignal|escalationResolvedSignal"
    - from: "shared/temporal/activities/orchestrator-activities.ts"
      to: "shared/db/context-manager.ts"
      via: "contextManager.writeSnapshot and readLatestSnapshot"
      pattern: "contextManager\\.(writeSnapshot|readLatestSnapshot)"
    - from: "shared/db/trace-recorder.ts"
      to: "shared/db/schema.ts"
      via: "execution_traces table insert"
      pattern: "executionTraces"
---

<objective>
Verify E2EV-06 through E2EV-11 via code inspection, existing test execution, and static analysis.

Purpose: These 6 requirements describe structural properties (tool availability, schema fields, dependency absence, event routing) that can be verified without new tests. This produces documented evidence that the implemented architecture meets its validation criteria.

Output: Verification evidence documented in the plan summary with PASS/FAIL for each requirement.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-end-to-end-validation/36-RESEARCH.md
@.planning/phases/35-guardrails-cleanup/35-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build base packages and run existing test suite</name>
  <files>No files modified -- verification only</files>
  <action>
Build the base packages required for test resolution, then run the agents test suite to establish the clean baseline.

Steps:
1. Run `pnpm --filter @aesir/types build && pnpm --filter @aesir/platform build` to build base packages
2. Run `npx vitest run packages/agents` (or `pnpm test:fast` if scoped to agents) to get the full test results
3. Document the test output: total passed, total failed, total skipped
4. Separately note any failures in `platform/temporal/client.test.ts` as PRE-EXISTING (not counted against Phase 36)
5. Confirm all router tests pass: `fast-path.test.ts`, `slow-path.test.ts`, `router.test.ts`
6. Confirm trace-recorder tests pass: `trace-recorder.test.ts`
7. Confirm context-manager tests pass: `context-manager.test.ts`
8. Confirm toolkits tests pass: `toolkits.test.ts`
9. Confirm token-budget tests pass: `token-budget.test.ts`
10. Confirm agent-config tests pass: `agent-config.test.ts`

This establishes the test baseline that E2EV requirements build upon.
  </action>
  <verify>All agents package tests pass (313+ tests pass, 0 failures in packages/agents). Pre-existing failures in platform/temporal/client.test.ts are documented but not blocking.</verify>
  <done>Test baseline established with documented counts: X passed, Y skipped, Z pre-existing failures outside agents package.</done>
</task>

<task type="auto">
  <name>Task 2: Static verification of E2EV-06 through E2EV-11</name>
  <files>No source files modified -- inspection only</files>
  <action>
Inspect source code to verify each requirement. Document evidence in the plan summary.

**E2EV-06: Smart router handles all current v2.1 event types**
1. Read `packages/agents/src/router/fast-path.ts` -- enumerate all 9 DETERMINISTIC_RULES (7 actionable + 2 ignore)
2. Verify fast-path covers: `slack.block_actions.approved`, `slack.block_actions.rejected`, `slack.block_actions.escalation_retry`, `slack.block_actions.escalation_abort`, `github.pull_request.merged`, `github.pull_request.closed`, `linear.agent_session.created`, `linear.issue.created` (ignore), `linear.issue.updated` (ignore)
3. Read `packages/agents/src/router/slow-path.ts` -- verify it handles unmatched events via LLM reasoning (slack.app_mention.created, linear.comment.created, slack.message.created in threads)
4. Cross-reference against v2.1 event types from requirements (ROUT-05)
5. Confirm all router tests passed in Task 1

**E2EV-07: Context survives Temporal boundaries**
1. Read `packages/agents/src/shared/temporal/activities/orchestrator-activities.ts` -- find `contextManager.writeSnapshot()` calls at end of each activity (pre-approval and post-approval)
2. Read `packages/agents/src/shared/temporal/activities/orchestrator-activities.ts` -- find `contextManager.readLatestSnapshot()` call at start of post-approval activity
3. Read `packages/agents/src/shared/db/context-manager.ts` -- verify writeSnapshot stores: summary, completed_actions, pending_intent, known_issues, key_files, research_findings, plan
4. Read `packages/agents/src/shared/db/schema.ts` -- verify context_snapshots table has all semantic fields
5. Confirm context-manager tests passed in Task 1

**E2EV-08: Sub-agents get focused context**
1. Read `packages/agents/src/shared/tools/toolkits.ts` -- verify per-role tool counts: researcher (4), coder (4), tester (3), orchestrator (13+)
2. Read `packages/agents/src/dev-agent/orchestrator/system-prompts.ts` -- verify 4 separate prompts: ORCHESTRATOR_SYSTEM_PROMPT, RESEARCHER_SYSTEM_PROMPT, CODER_SYSTEM_PROMPT, TESTER_SYSTEM_PROMPT
3. Read `packages/agents/src/shared/tools/coordination/spawn-agent.ts` -- verify spawn_agent creates a fresh runAgentLoop call with only the task brief as initialMessage (not full orchestrator history)
4. Confirm toolkits tests passed in Task 1

**E2EV-09: All tool calls queryable in execution_traces**
1. Read `packages/agents/src/shared/db/schema.ts` -- verify execution_traces table has: agent_instance_id, parent_agent_instance_id columns with indexes
2. Read `packages/agents/src/shared/db/trace-recorder.ts` -- verify onToolCall and onResponse callbacks record to execution_traces
3. Read `packages/agents/src/dev-agent/orchestrator/orchestrator.ts` -- verify trace recorder is wired with onToolCall and onResponse
4. Verify sub-agent traces get their own agent_instance_id but inherit taskId for correlation
5. Confirm trace-recorder tests passed in Task 1

**E2EV-10: Guardrails enforced**
1. Cross-reference Phase 35 VERIFICATION.md -- all 9 GUAR requirements already verified as PASS
2. Confirm token-budget tests passed in Task 1
3. Confirm agent-config tests passed in Task 1
4. Verify sandbox enforcement in run-command.ts (containerManager.execute), merge protection in toolkits.ts (filter excludes merge_pull_request)

**E2EV-11: No @langchain/* dependencies**
1. Run `grep -r "@langchain" packages/agents/` -- expect zero results
2. Read `packages/agents/package.json` -- confirm no @langchain/* entries in dependencies or devDependencies
3. Cross-reference Phase 35 VERIFICATION.md GUAR-07 evidence

For each requirement, document:
- Status: PASS or FAIL
- Evidence: specific file paths, line references, code snippets, test results
- Follow the evidence format from Phase 35 VERIFICATION.md
  </action>
  <verify>All 6 requirements (E2EV-06 through E2EV-11) have documented PASS/FAIL status with supporting evidence.</verify>
  <done>E2EV-06 through E2EV-11 all verified as PASS with code inspection evidence, test execution results, and cross-references to Phase 35 verification.</done>
</task>

</tasks>

<verification>
1. All existing agents tests pass (no regressions)
2. Each of E2EV-06 through E2EV-11 has documented evidence with PASS status
3. Pre-existing failures are documented separately and not counted against Phase 36
</verification>

<success_criteria>
- E2EV-06: All 9 fast-path rules documented + slow-path LLM coverage confirmed + all router tests pass
- E2EV-07: writeSnapshot/readLatestSnapshot calls found in activity code + context-manager tests pass
- E2EV-08: Per-role tool counts match spec (researcher 4, coder 4, tester 3) + spawn creates fresh context
- E2EV-09: execution_traces schema has parent/child columns + trace recorder wired in orchestrator
- E2EV-10: Phase 35 VERIFICATION.md cross-referenced + guardrail tests pass
- E2EV-11: Zero @langchain grep matches + clean package.json
</success_criteria>

<output>
After completion, create `.planning/phases/36-end-to-end-validation/36-01-SUMMARY.md`
</output>

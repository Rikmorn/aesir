---
phase: 36-end-to-end-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agents/src/dev-agent/orchestrator/e2e-validation.test.ts
  - packages/agents/src/product-agent/orchestrator/e2e-validation.test.ts
  - .planning/phases/36-end-to-end-validation/36-02-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "E2EV-02 test verifies README edit completes in under 10 tool calls with no test execution and no research spawning"
    - "E2EV-04 test verifies failure recovery diagnoses the error and tries a different approach, not identical retry"
    - "E2EV-05 test verifies product agent creates issue path in minimal tool calls for clear request, and asks questions for vague request"
  artifacts:
    - path: "packages/agents/src/dev-agent/orchestrator/e2e-validation.test.ts"
      provides: "Behavioral tests for E2EV-02 (README edit efficiency) and E2EV-04 (test failure recovery)"
      min_lines: 120
    - path: "packages/agents/src/product-agent/orchestrator/e2e-validation.test.ts"
      provides: "Behavioral tests for E2EV-05 (product agent adaptiveness)"
      min_lines: 80
  key_links:
    - from: "e2e-validation.test.ts (dev-agent)"
      to: "dev-agent/orchestrator/orchestrator.ts"
      via: "imports and calls real runDevAgentOrchestrator() with mocked SDK"
      pattern: "import.*runDevAgentOrchestrator.*from.*orchestrator"
    - from: "e2e-validation.test.ts (product-agent)"
      to: "product-agent/orchestrator/orchestrator.ts"
      via: "imports and calls real runProductAgent() with mocked SDK"
      pattern: "import.*runProductAgent.*from.*orchestrator"
---

<objective>
Write behavioral tests for E2EV-02, E2EV-04, and E2EV-05 using SDK mocking to prove agent reasoning behavior.

Purpose: Three E2EV requirements describe runtime behavioral properties (task efficiency, error recovery intelligence, conversation adaptiveness) that cannot be verified by code inspection alone. Behavioral tests mock the Anthropic SDK at the boundary and script LLM responses to exercise the agent logic.

Output: Two new test files with passing behavioral tests that prove adaptive agent behavior.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-end-to-end-validation/36-RESEARCH.md
@packages/agents/src/dev-agent/orchestrator/orchestrator.test.ts
@packages/agents/src/dev-agent/orchestrator/orchestrator.ts
@packages/agents/src/product-agent/orchestrator/orchestrator.ts
@packages/agents/src/shared/tools/toolkits.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2EV-02 and E2EV-04 behavioral tests for dev agent</name>
  <files>packages/agents/src/dev-agent/orchestrator/e2e-validation.test.ts</files>
  <action>
Create a behavioral test file for the dev agent orchestrator that validates E2EV-02 and E2EV-04.

Follow the EXACT mock pattern from `orchestrator.test.ts`:
- Module-level vi.mock for `@anthropic-ai/sdk` with mockCreate
- Module-level vi.mock for `@anthropic-ai/sdk/helpers/beta/zod` with betaZodTool stub
- Module-level vi.mock for `../../shared/db/trace-recorder.js` with mock callbacks
- Module-level vi.mock for `../../shared/mcp/client.js`
- Module-level vi.mock for `@aesir/types` with createId stub
- Dynamic `await import("./orchestrator.js")` AFTER mocks
- Helper functions: mockTextResponse, mockToolUseResponse, createTestOptions, extractToolCallSequence

CRITICAL: The test MUST import and call the REAL `runDevAgentOrchestrator` function from `./orchestrator.js` via dynamic import after mocks are set up. This is NOT a unit test of a mock -- it exercises the real orchestrator logic with a mocked LLM boundary. Verify after writing that the test file contains:
1. `await import("./orchestrator.js")` -- dynamic import of the real module
2. A call to `runDevAgentOrchestrator(options)` -- invocation of the real function
3. The mock is ONLY on `@anthropic-ai/sdk` (the LLM boundary), not on the orchestrator itself

**E2EV-02: README edit completes in under 10 tool calls, no tests, no research**

Test: "README edit completes efficiently without research or test execution"
Script the mock SDK to simulate:
1. LLM call 1: tool_use for linear_get_issue (read the issue -- always first)
2. LLM call 2: tool_use for read_file (read current README)
3. LLM call 3: tool_use for spawn_agent with agentType "coder" (directly spawn coder, NO researcher)
4. Inner LLM call (coder sub-agent): text response "Updated README.md"
5. LLM call 4: tool_use for github_create_branch
6. LLM call 5: tool_use for github_create_commit
7. LLM call 6: tool_use for github_create_pull_request
8. LLM call 7: text response "PR created. README edit complete."

Assertions:
- result.status === "completed"
- Total tool calls (via mockOnToolCall count) < 10
- No spawn_agent calls with agentType "researcher" (extract from mockOnToolCall)
- No spawn_agent calls with agentType "tester" (extract from mockOnToolCall)
- No tool calls named "run_command" at the orchestrator level

**E2EV-04: Test failure recovery diagnoses error and tries different approach**

Test: "agent diagnoses test failure and retries with a different approach"
Script the mock SDK to simulate:
1. LLM call 1: tool_use for linear_get_issue
2. LLM call 2: tool_use for spawn_agent agentType "researcher"
3. Inner LLM (researcher): text with findings
4. LLM call 3: tool_use for spawn_agent agentType "coder" with task "Approach A: implement using X"
5. Inner LLM (coder): text "Implemented changes"
6. LLM call 4: tool_use for spawn_agent agentType "tester" with task "Run tests"
7. Inner LLM (tester): text "FAILED: TypeError - missing import for Y"
8. LLM call 5: tool_use for spawn_agent agentType "coder" with DIFFERENT task "Approach B: fix by adding import for Y"
9. Inner LLM (coder): text "Fixed the import"
10. LLM call 6: tool_use for spawn_agent agentType "tester"
11. Inner LLM (tester): text "All tests pass"
12. LLM call 7: text response "Implementation complete after fixing test failure"

Assertions:
- result.status === "completed"
- Extract spawn_agent inputs from mockOnToolCall -- filter by name "spawn_agent"
- Find two "coder" spawn calls with DIFFERENT task briefs (approach A vs approach B)
- The second coder brief references the error diagnosis (contains "import" or "fix")
- A tester was spawned AFTER the second coder (not just blind retry of same code)

IMPORTANT: Test behavioral PROPERTIES, not exact sequences. Do NOT assert exact tool call order beyond what is required by the requirement.

Do NOT create a `describe("E2EV-02")` etc. -- instead use descriptive test names:
- `describe("end-to-end validation")` with nested describe blocks for each scenario
  </action>
  <verify>Run `npx vitest run packages/agents/src/dev-agent/orchestrator/e2e-validation.test.ts` -- all tests pass.</verify>
  <done>E2EV-02 test proves README edit < 10 tool calls with no researcher/tester; E2EV-04 test proves different approach after failure diagnosis.</done>
</task>

<task type="auto">
  <name>Task 2: E2EV-05 behavioral test for product agent</name>
  <files>packages/agents/src/product-agent/orchestrator/e2e-validation.test.ts</files>
  <action>
Create a behavioral test file for the product agent that validates E2EV-05.

Follow the mock pattern from existing tests, adapted for product agent:
- Module-level vi.mock for `@anthropic-ai/sdk` with mockCreate
- Module-level vi.mock for `@anthropic-ai/sdk/helpers/beta/zod` with betaZodTool stub
- Module-level vi.mock for `../../shared/db/trace-recorder.js` with mock callbacks
- Module-level vi.mock for `../../shared/mcp/client.js`
- Module-level vi.mock for `@aesir/types` with createId stub
- Dynamic `await import("./orchestrator.js")` AFTER mocks
- Helper functions: mockTextResponse, mockToolUseResponse

CRITICAL: Same wiring requirement as Task 1 -- the test MUST import and call the REAL `runProductAgent` function from `./orchestrator.js` via dynamic import. The mock is ONLY on `@anthropic-ai/sdk`. Verify the test file contains `await import("./orchestrator.js")` and calls the real `runProductAgent(options)`.

Read `packages/agents/src/product-agent/orchestrator/orchestrator.ts` to understand ProductAgentOptions interface and how to construct test options. Key fields: threadTs, channelId, message, linearTeamId, agentId, correlationId, db, logger. Optional: conversationHistory, maxIterations, maxTokenBudget.

**E2EV-05a: Clear request creates issue quickly (1-2 tool calls before issue creation)**

Test: "clear request creates issue in minimal tool calls"
Input message: "Add a health check endpoint at /health that returns 200 OK with uptime info"
Script mock SDK:
1. LLM call 1: tool_use for linear_search_issues (duplicate check -- part of clear request flow per system prompt)
2. LLM call 2: tool_use for slack_send_message (send summary for confirmation, since prompt says to confirm before creating)
3. LLM call 3: text response with `<phase>clarifying</phase>` (waiting for user confirmation per behavior section)

Assertions:
- result.status === "completed"
- Tool calls include linear_search_issues (duplicate check)
- Tool calls include slack_send_message (user communication)
- Tool calls do NOT include any codebase tools (read_file, write_file, etc. -- product agent has no codebase tools)
- Total tool calls <= 4 (search + message, with possible label lookup)

**E2EV-05b: Vague request triggers clarifying questions**

Test: "vague request triggers clarifying questions via Slack"
Input message: "we need better error handling"
Script mock SDK:
1. LLM call 1: tool_use for slack_send_message with text containing a question (e.g., "Could you tell me more about which errors..." or similar)
2. LLM call 2: text response with `<phase>clarifying</phase>`

Assertions:
- result.status === "completed"
- Tool calls include slack_send_message
- NO linear_create_issue tool call (agent asks questions instead of creating)
- Total tool calls <= 3 (sends a question, does not try to create an issue)

IMPORTANT: The product agent communicates with users ONLY through slack_send_message tool. Text output is internal reasoning only. Test that the agent uses the tool for communication, not that it produces specific text.
  </action>
  <verify>Run `npx vitest run packages/agents/src/product-agent/orchestrator/e2e-validation.test.ts` -- all tests pass.</verify>
  <done>E2EV-05 proven: clear request leads to search + message (minimal calls), vague request leads to question via Slack (no issue creation).</done>
</task>

</tasks>

<verification>
1. `npx vitest run packages/agents/src/dev-agent/orchestrator/e2e-validation.test.ts` -- all tests pass
2. `npx vitest run packages/agents/src/product-agent/orchestrator/e2e-validation.test.ts` -- all tests pass
3. E2EV-02: README edit test proves < 10 tool calls, no researcher, no tester
4. E2EV-04: Recovery test proves different approach after failure diagnosis
5. E2EV-05: Clear request test proves minimal calls, vague request test proves clarifying question
</verification>

<success_criteria>
- 2 new test files created and passing
- Both test files import and call real orchestrator functions (not mocks) via dynamic import
- E2EV-02: Test asserts tool_call_count < 10 AND no "researcher" spawn AND no "tester" spawn
- E2EV-04: Test asserts two different coder briefs (distinct task strings) AND second brief references the error
- E2EV-05a: Test asserts clear request uses <= 4 tool calls AND includes search_issues AND does NOT create issue prematurely
- E2EV-05b: Test asserts vague request calls slack_send_message AND does NOT call linear_create_issue
</success_criteria>

<output>
After completion, create `.planning/phases/36-end-to-end-validation/36-02-SUMMARY.md`
</output>

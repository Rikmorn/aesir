---
phase: 11-monorepo-setup
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - packages/common/package.json
  - packages/common/tsconfig.json
  - packages/common/vitest.config.ts
  - packages/common/src/index.ts
  - packages/platform/package.json
  - packages/platform/tsconfig.json
  - packages/platform/vitest.config.ts
  - packages/platform/src/index.ts
  - packages/integrations/package.json
  - packages/integrations/tsconfig.json
  - packages/integrations/vitest.config.ts
  - packages/integrations/src/index.ts
  - packages/agents/package.json
  - packages/agents/tsconfig.json
  - packages/agents/vitest.config.ts
  - packages/agents/src/index.ts
autonomous: true

must_haves:
  truths:
    - "Four package directories exist under packages/"
    - "Each package has package.json with @aesir scope"
    - "Each package has tsconfig.json extending base with correct references"
    - "Layer dependencies are encoded: common has none, platform refs common, integrations refs common+platform, agents refs common+integrations"
  artifacts:
    - path: "packages/common/package.json"
      provides: "@aesir/common package definition"
      contains: "@aesir/common"
    - path: "packages/platform/package.json"
      provides: "@aesir/platform package definition"
      contains: "workspace:*"
    - path: "packages/integrations/package.json"
      provides: "@aesir/integrations package definition"
      contains: "workspace:*"
    - path: "packages/agents/package.json"
      provides: "@aesir/agents package definition"
      contains: "workspace:*"
  key_links:
    - from: "packages/agents/tsconfig.json"
      to: "packages/integrations"
      via: "TypeScript project reference"
      pattern: "references.*integrations"
    - from: "packages/integrations/tsconfig.json"
      to: "packages/platform"
      via: "TypeScript project reference"
      pattern: "references.*platform"
---

<objective>
Create the four package directories with their configuration files.

Purpose: Scaffold all packages with correct package.json dependencies, TypeScript configs with project references encoding the layer rules, and Vitest configs. No source code is moved yet - just the structure.

Output: packages/{common,platform,integrations,agents}/ directories with config files ready for code migration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-monorepo-setup/11-CONTEXT.md
@.planning/phases/11-monorepo-setup/11-RESEARCH.md
@.planning/phases/11-monorepo-setup/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create @aesir/common package</name>
  <files>packages/common/package.json, packages/common/tsconfig.json, packages/common/vitest.config.ts, packages/common/src/index.ts</files>
  <action>
  1. Create directory structure:
  ```
  packages/common/
  ├── src/
  │   └── index.ts
  ├── package.json
  ├── tsconfig.json
  └── vitest.config.ts
  ```

  2. Create packages/common/package.json:
  ```json
  {
    "name": "@aesir/common",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
      ".": {
        "import": "./dist/index.js",
        "types": "./dist/index.d.ts"
      },
      "./*": {
        "import": "./dist/*.js",
        "types": "./dist/*.d.ts"
      }
    },
    "scripts": {
      "build": "tsc -b",
      "typecheck": "tsc --noEmit",
      "test": "vitest run",
      "test:watch": "vitest"
    },
    "dependencies": {
      "zod": "3.25.67",
      "dotenv-flow": "^4.1.0"
    },
    "devDependencies": {
      "typescript": "^5.7.0",
      "vitest": "^3.0.0"
    }
  }
  ```

  Note: common has NO internal @aesir/* dependencies - it's the leaf of the dependency tree.

  3. Create packages/common/tsconfig.json:
  ```json
  {
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
      "outDir": "./dist",
      "rootDir": "./src"
    },
    "include": ["src/**/*"]
  }
  ```

  Note: No references array - common depends on nothing internal.

  4. Create packages/common/vitest.config.ts:
  ```typescript
  import { defineProject } from "vitest/config";

  export default defineProject({
    test: {
      name: "common",
      environment: "node",
    },
  });
  ```

  5. Create packages/common/src/index.ts (placeholder):
  ```typescript
  // @aesir/common - shared contracts, config loading, utilities
  // Code will be moved here in plan 03

  export {};
  ```
  </action>
  <verify>
  - `ls packages/common/` shows package.json, tsconfig.json, vitest.config.ts, src/
  - package.json has name "@aesir/common" and no @aesir/* dependencies
  - tsconfig.json extends ../../tsconfig.base.json with no references
  </verify>
  <done>@aesir/common package scaffolded with correct configuration</done>
</task>

<task type="auto">
  <name>Task 2: Create @aesir/platform package</name>
  <files>packages/platform/package.json, packages/platform/tsconfig.json, packages/platform/vitest.config.ts, packages/platform/src/index.ts</files>
  <action>
  1. Create directory structure:
  ```
  packages/platform/
  ├── src/
  │   └── index.ts
  ├── package.json
  ├── tsconfig.json
  └── vitest.config.ts
  ```

  2. Create packages/platform/package.json:
  ```json
  {
    "name": "@aesir/platform",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
      ".": {
        "import": "./dist/index.js",
        "types": "./dist/index.d.ts"
      },
      "./*": {
        "import": "./dist/*.js",
        "types": "./dist/*.d.ts"
      }
    },
    "scripts": {
      "build": "tsc -b",
      "typecheck": "tsc --noEmit",
      "test": "vitest run",
      "test:watch": "vitest"
    },
    "dependencies": {
      "@aesir/common": "workspace:*",
      "@langchain/langgraph-checkpoint-postgres": "^0.1.0",
      "@temporalio/activity": "^1.14.1",
      "@temporalio/client": "^1.14.1",
      "@temporalio/common": "^1.14.1",
      "@temporalio/worker": "^1.14.1",
      "@temporalio/workflow": "^1.14.1",
      "dockerode": "^4.0.9"
    },
    "devDependencies": {
      "@types/dockerode": "^4.0.0",
      "typescript": "^5.7.0",
      "vitest": "^3.0.0"
    }
  }
  ```

  Note: platform depends only on @aesir/common (workspace:*).

  3. Create packages/platform/tsconfig.json:
  ```json
  {
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
      "outDir": "./dist",
      "rootDir": "./src"
    },
    "include": ["src/**/*"],
    "references": [
      { "path": "../common" }
    ]
  }
  ```

  Note: References only common - platform cannot import from integrations or agents.

  4. Create packages/platform/vitest.config.ts:
  ```typescript
  import { defineProject } from "vitest/config";

  export default defineProject({
    test: {
      name: "platform",
      environment: "node",
    },
  });
  ```

  5. Create packages/platform/src/index.ts (placeholder):
  ```typescript
  // @aesir/platform - state, temporal, sandbox
  // Code will be moved here in plan 03

  export {};
  ```
  </action>
  <verify>
  - `ls packages/platform/` shows package.json, tsconfig.json, vitest.config.ts, src/
  - package.json has "@aesir/common": "workspace:*" dependency
  - tsconfig.json has references to only common
  </verify>
  <done>@aesir/platform package scaffolded with correct dependencies</done>
</task>

<task type="auto">
  <name>Task 3: Create @aesir/integrations and @aesir/agents packages</name>
  <files>packages/integrations/package.json, packages/integrations/tsconfig.json, packages/integrations/vitest.config.ts, packages/integrations/src/index.ts, packages/agents/package.json, packages/agents/tsconfig.json, packages/agents/vitest.config.ts, packages/agents/src/index.ts</files>
  <action>
  **@aesir/integrations:**

  1. Create directory structure: packages/integrations/src/

  2. Create packages/integrations/package.json:
  ```json
  {
    "name": "@aesir/integrations",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
      ".": {
        "import": "./dist/index.js",
        "types": "./dist/index.d.ts"
      },
      "./*": {
        "import": "./dist/*.js",
        "types": "./dist/*.d.ts"
      }
    },
    "scripts": {
      "build": "tsc -b",
      "typecheck": "tsc --noEmit",
      "test": "vitest run",
      "test:watch": "vitest"
    },
    "dependencies": {
      "@aesir/common": "workspace:*",
      "@aesir/platform": "workspace:*",
      "@linear/sdk": "^70.0.0",
      "@octokit/rest": "^22.0.1",
      "@slack/bolt": "^4.6.0",
      "@slack/web-api": "^7.13.0"
    },
    "devDependencies": {
      "typescript": "^5.7.0",
      "vitest": "^3.0.0"
    }
  }
  ```

  3. Create packages/integrations/tsconfig.json:
  ```json
  {
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
      "outDir": "./dist",
      "rootDir": "./src"
    },
    "include": ["src/**/*"],
    "references": [
      { "path": "../common" },
      { "path": "../platform" }
    ]
  }
  ```

  4. Create packages/integrations/vitest.config.ts:
  ```typescript
  import { defineProject } from "vitest/config";

  export default defineProject({
    test: {
      name: "integrations",
      environment: "node",
    },
  });
  ```

  5. Create packages/integrations/src/index.ts (placeholder):
  ```typescript
  // @aesir/integrations - linear, github, slack
  // Code will be moved here in plan 03

  export {};
  ```

  **@aesir/agents:**

  1. Create directory structure: packages/agents/src/

  2. Create packages/agents/package.json:
  ```json
  {
    "name": "@aesir/agents",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
      ".": {
        "import": "./dist/index.js",
        "types": "./dist/index.d.ts"
      },
      "./*": {
        "import": "./dist/*.js",
        "types": "./dist/*.d.ts"
      }
    },
    "scripts": {
      "build": "tsc -b",
      "typecheck": "tsc --noEmit",
      "test": "vitest run",
      "test:watch": "vitest",
      "dev-agent": "tsx src/scripts/start-dev-agent.ts",
      "product-agent": "tsx src/scripts/start-product-agent.ts"
    },
    "dependencies": {
      "@aesir/common": "workspace:*",
      "@aesir/integrations": "workspace:*",
      "@langchain/anthropic": "^0.3.0",
      "@langchain/core": "^0.3.0",
      "@langchain/langgraph": "^0.2.0",
      "langsmith": "^0.3.0"
    },
    "devDependencies": {
      "@types/node": "^22.0.0",
      "tsx": "^4.0.0",
      "typescript": "^5.7.0",
      "vitest": "^3.0.0"
    }
  }
  ```

  Note: agents depends on common and integrations (NOT platform directly - goes through common for shared contracts).

  3. Create packages/agents/tsconfig.json:
  ```json
  {
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
      "outDir": "./dist",
      "rootDir": "./src"
    },
    "include": ["src/**/*"],
    "references": [
      { "path": "../common" },
      { "path": "../integrations" }
    ]
  }
  ```

  Note: References common and integrations only - NOT platform. Agents access platform services through the integrations layer.

  4. Create packages/agents/vitest.config.ts:
  ```typescript
  import { defineProject } from "vitest/config";

  export default defineProject({
    test: {
      name: "agents",
      environment: "node",
    },
  });
  ```

  5. Create packages/agents/src/index.ts (placeholder):
  ```typescript
  // @aesir/agents - dev-agent, product-agent
  // Code will be moved here in plan 03

  export {};
  ```
  </action>
  <verify>
  - `ls packages/integrations/` shows package.json, tsconfig.json, vitest.config.ts, src/
  - `ls packages/agents/` shows same structure
  - integrations/package.json has @aesir/common and @aesir/platform as workspace deps
  - agents/package.json has @aesir/common and @aesir/integrations as workspace deps
  - integrations/tsconfig.json references common and platform
  - agents/tsconfig.json references common and integrations (NOT platform)
  </verify>
  <done>@aesir/integrations and @aesir/agents packages scaffolded with layer-correct dependencies</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. `ls packages/` shows common, platform, integrations, agents
2. Each package directory has: package.json, tsconfig.json, vitest.config.ts, src/index.ts
3. Layer dependency check:
   - common: no @aesir/* deps, no tsconfig references
   - platform: @aesir/common only, references common only
   - integrations: @aesir/common + @aesir/platform, references common + platform
   - agents: @aesir/common + @aesir/integrations, references common + integrations
4. All packages use workspace:* protocol for internal dependencies
</verification>

<success_criteria>
- Four packages exist: packages/{common,platform,integrations,agents}/
- Each has package.json with @aesir/* scope and correct internal dependencies
- Each has tsconfig.json extending tsconfig.base.json with layer-appropriate references
- Each has vitest.config.ts using defineProject
- Each has src/index.ts placeholder
- Dependency graph enforces: common <- platform <- integrations <- agents
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo-setup/11-02-SUMMARY.md`
</output>

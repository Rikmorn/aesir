---
phase: 11-monorepo-setup
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - packages/common/src/**/*
  - packages/platform/src/**/*
  - packages/integrations/src/**/*
  - packages/agents/src/**/*
  - src_old/**/*
  - biome.json
autonomous: true

must_haves:
  truths:
    - "All source files moved from src/ to appropriate packages"
    - "src/ renamed to src_old/ for reference"
    - "Config code is in common (shared across layers)"
    - "State, temporal, sandbox code is in platform"
    - "Linear, GitHub, Slack code is in integrations"
    - "Agent code is in agents"
  artifacts:
    - path: "packages/common/src/config/"
      provides: "Config loading and env validation"
      min_lines: 50
    - path: "packages/platform/src/state/"
      provides: "LangGraph state definitions"
      min_lines: 50
    - path: "packages/integrations/src/linear/"
      provides: "Linear integration"
      min_lines: 100
    - path: "packages/agents/src/dev-agent/"
      provides: "Dev agent implementation"
      min_lines: 100
  key_links:
    - from: "packages/agents/src/dev-agent/"
      to: "packages/integrations/src/linear/"
      via: "Import statement"
      pattern: "@aesir/integrations"
---

<objective>
Move all source files from src/ to the appropriate package directories.

Purpose: This is the big migration step. Every file in src/ moves to its new home in packages/. The goal is to preserve all code while reorganizing into the 4-layer structure. Import paths will be broken after this - fixing them happens in plan 04.

Output: All code migrated to packages/, src/ renamed to src_old/ for reference.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-monorepo-setup/11-CONTEXT.md
@.planning/phases/11-monorepo-setup/11-RESEARCH.md
@.planning/phases/11-monorepo-setup/11-01-SUMMARY.md
@.planning/phases/11-monorepo-setup/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move config and logging to @aesir/common</name>
  <files>packages/common/src/config/*, packages/common/src/logging/*, packages/common/src/index.ts</files>
  <action>
  The common package gets truly shared code that ALL layers might need:

  1. Move src/config/ -> packages/common/src/config/
     - agent-config.ts, agent-config.test.ts
     - index.ts
     - env.ts (the Zod validation)

  2. Move src/logging/ -> packages/common/src/logging/
     - logger.ts, logger.test.ts
     - trace-store.ts, trace-store.test.ts
     - index.ts

  3. Update packages/common/src/index.ts to export the public API:
  ```typescript
  // Config
  export * from "./config/index.js";

  // Logging
  export * from "./logging/index.js";
  ```

  Note: Keep internal file structure intact. We're moving directories wholesale.
  Note: Imports within these files will be broken (relative paths). That's fixed in plan 04.
  </action>
  <verify>
  - `ls packages/common/src/config/` shows env.ts, index.ts, agent-config.ts
  - `ls packages/common/src/logging/` shows logger.ts, trace-store.ts, index.ts
  - packages/common/src/index.ts exports config and logging
  </verify>
  <done>Config and logging code moved to @aesir/common</done>
</task>

<task type="auto">
  <name>Task 2: Move state, temporal, sandbox, testing to @aesir/platform</name>
  <files>packages/platform/src/state/*, packages/platform/src/temporal/*, packages/platform/src/sandbox/*, packages/platform/src/testing/*, packages/platform/src/index.ts</files>
  <action>
  The platform package gets infrastructure services:

  1. Move src/state/ -> packages/platform/src/state/
     - All state definition files (agent-state.ts, dev-workflow-state.ts, etc.)
     - Tests

  2. Move src/temporal/ -> packages/platform/src/temporal/
     - All temporal workflow and activity files
     - Types
     - Tests

  3. Move src/sandbox/ -> packages/platform/src/sandbox/
     - docker-sandbox.ts
     - types.ts
     - index.ts
     - Tests

  4. Move src/testing/ -> packages/platform/src/testing/
     - Test utilities that can be shared

  5. Update packages/platform/src/index.ts to export the public API:
  ```typescript
  // State
  export * from "./state/index.js";

  // Temporal
  export * from "./temporal/index.js";

  // Sandbox
  export * from "./sandbox/index.js";

  // Testing utilities
  export * from "./testing/index.js";
  ```

  Note: Temporal code has special bundling requirements - keep it together, we'll verify it works after import fixes.
  </action>
  <verify>
  - `ls packages/platform/src/` shows state/, temporal/, sandbox/, testing/
  - Each subdirectory contains moved files with tests
  - packages/platform/src/index.ts exports all modules
  </verify>
  <done>Platform infrastructure code moved to @aesir/platform</done>
</task>

<task type="auto">
  <name>Task 3: Move integrations and agents, rename src to src_old</name>
  <files>packages/integrations/src/*, packages/agents/src/*, src_old/*, biome.json</files>
  <action>
  **Move integrations:**

  1. Move src/integrations/linear/ -> packages/integrations/src/linear/
  2. Move src/integrations/github/ -> packages/integrations/src/github/
  3. Move src/integrations/slack/ -> packages/integrations/src/slack/

  4. Update packages/integrations/src/index.ts:
  ```typescript
  // Linear
  export * from "./linear/index.js";

  // GitHub
  export * from "./github/index.js";

  // Slack
  export * from "./slack/index.js";
  ```

  **Move agents:**

  1. Move src/agents/ -> packages/agents/src/
     This includes:
     - dev-agent.ts, dev-workflow.ts, dev-workflow-runner.ts
     - guards.ts, run-agent.ts
     - nodes/ directory
     - product-agent/ directory
     - tracing/ directory
     - index.ts

  2. Move src/scripts/ -> packages/agents/src/scripts/
     - start-dev-agent.ts
     - start-product-agent.ts
     - linear-oauth.ts (this may need to move to integrations later, but keep with agents for now since it's a CLI script)

  3. Move src/api/ -> packages/agents/src/api/
     - Webhook handlers (these interact with agents)

  4. Update packages/agents/src/index.ts:
  ```typescript
  // Dev Agent
  export * from "./dev-agent.js";
  export * from "./dev-workflow.js";

  // Product Agent
  export * from "./product-agent/index.js";

  // Shared
  export * from "./guards.js";
  export * from "./tracing/index.js";
  ```

  **Handle orphans:**

  1. Check src/integration/ (note: singular, different from integrations/) - this appears to be test files
     - Move to _legacy/ if unclear purpose
     - Or move to packages/agents/tests/integration/ if they're integration tests

  2. src/index.ts - this is the old entry point
     - Move to _legacy/ for reference

  3. src/tools/ - if exists, evaluate:
     - If agent tools -> packages/agents/src/tools/
     - If platform tools -> packages/platform/src/tools/

  **Rename src to src_old:**

  After all moves are complete:
  ```bash
  mv src src_old
  ```

  **Update Biome config:**

  Update biome.json to include packages:
  ```json
  {
    "files": {
      "includes": ["packages/**/*.ts", "packages/**/*.tsx", "*.json"]
    }
  }
  ```

  Remove the old src/**/*.ts pattern.
  </action>
  <verify>
  - `ls packages/integrations/src/` shows linear/, github/, slack/
  - `ls packages/agents/src/` shows nodes/, product-agent/, scripts/, api/, tracing/
  - `ls src_old/` exists with original structure for reference
  - `ls src/` fails (directory renamed)
  - biome.json includes pattern is packages/**/*.ts
  </verify>
  <done>All code migrated to packages, src renamed to src_old</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. `ls packages/common/src/` shows config/, logging/
2. `ls packages/platform/src/` shows state/, temporal/, sandbox/, testing/
3. `ls packages/integrations/src/` shows linear/, github/, slack/
4. `ls packages/agents/src/` shows agents, nodes, scripts, api
5. `ls src_old/` shows original structure
6. `ls src/` fails (renamed to src_old)
7. File count check: `find packages -name "*.ts" | wc -l` should be ~120+ (most of original 124 files)
</verification>

<success_criteria>
- All TypeScript files from src/ are now in packages/*/src/
- src/ has been renamed to src_old/
- biome.json updated to lint packages/**/*.ts
- Package structure matches architecture:
  - common: config, logging
  - platform: state, temporal, sandbox, testing
  - integrations: linear, github, slack
  - agents: agents, nodes, scripts, api, tracing
- No files lost (can diff against src_old if needed)
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo-setup/11-03-SUMMARY.md`
</output>

---
phase: 57-conversation-reopening
plan: 02
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - packages/dashboard/src/lib/schema.ts
  - packages/dashboard/src/services/conversations.ts
  - packages/dashboard/src/app/api/conversations/[id]/reopen/route.ts
  - packages/dashboard/src/components/conversation-detail/reopen-dialog.tsx
  - packages/dashboard/src/components/conversation-detail/live-detail-panels.tsx
  - packages/dashboard/src/components/conversation-detail/metadata-sidebar.tsx
  - packages/dashboard/src/components/conversation-detail/event-icon.tsx
  - packages/dashboard/src/components/conversations/columns.tsx
  - packages/dashboard/src/app/conversations/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Clicking Reopen on a completed conversation opens a modal dialog requesting a reason"
    - "Clicking Retry on a failed conversation opens a modal dialog requesting a reason"
    - "Submitting the dialog with a reason calls POST /api/conversations/:id/reopen which proxies to agent-service"
    - "After successful reopen, the detail page refreshes to show the new queued status"
    - "Reopen count badge is visible on conversation detail metadata sidebar"
    - "Reopen count badge column appears in the conversation list table"
    - "agent.reopened events render with a distinct icon in the event timeline"
  artifacts:
    - path: "packages/dashboard/src/app/api/conversations/[id]/reopen/route.ts"
      provides: "Next.js API route proxying reopen to agent-service"
      min_lines: 15
    - path: "packages/dashboard/src/components/conversation-detail/reopen-dialog.tsx"
      provides: "Modal dialog with reason textarea for reopening conversations"
      min_lines: 40
    - path: "packages/dashboard/src/lib/schema.ts"
      provides: "Dashboard schema mirror with reopen_count and agent.reopened"
      contains: "reopen_count"
  key_links:
    - from: "packages/dashboard/src/components/conversation-detail/reopen-dialog.tsx"
      to: "packages/dashboard/src/app/api/conversations/[id]/reopen/route.ts"
      via: "fetch POST to /dashboard/api/conversations/:id/reopen"
      pattern: "fetch.*api/conversations"
    - from: "packages/dashboard/src/app/api/conversations/[id]/reopen/route.ts"
      to: "agent-service POST /conversations/:id/reopen"
      via: "HTTP proxy fetch to AGENT_SERVICE_URL"
      pattern: "AGENT_SERVICE_URL"
    - from: "packages/dashboard/src/components/conversation-detail/live-detail-panels.tsx"
      to: "packages/dashboard/src/components/conversation-detail/reopen-dialog.tsx"
      via: "ReopenDialog rendered in action bar area"
      pattern: "ReopenDialog"
---

<objective>
Implement the dashboard UI for conversation reopening: API proxy route, reopen dialog modal, action bar integration, metadata sidebar reopen count, event icon for agent.reopened, and list table reopen count column.

Purpose: Give operators a way to manually reopen/retry conversations from the dashboard, with contextual labels (Reopen vs Retry) and reason capture.
Output: Working dashboard reopen flow from button click through to agent-service API call.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-conversation-reopening/57-CONTEXT.md
@.planning/phases/57-conversation-reopening/57-RESEARCH.md
@.planning/phases/57-conversation-reopening/57-01-SUMMARY.md
@packages/dashboard/src/lib/schema.ts
@packages/dashboard/src/services/conversations.ts
@packages/dashboard/src/app/conversations/[id]/page.tsx
@packages/dashboard/src/components/conversation-detail/live-detail-panels.tsx
@packages/dashboard/src/components/conversation-detail/metadata-sidebar.tsx
@packages/dashboard/src/components/conversation-detail/event-icon.tsx
@packages/dashboard/src/components/conversations/columns.tsx
@packages/dashboard/src/app/api/events/[id]/content/route.ts
@packages/dashboard/src/components/ui/dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard schema mirror, conversations service, and API proxy route</name>
  <files>
    packages/dashboard/src/lib/schema.ts
    packages/dashboard/src/services/conversations.ts
    packages/dashboard/src/app/api/conversations/[id]/reopen/route.ts
  </files>
  <action>
    **Dashboard schema mirror (must stay in sync with Plan 01 changes):**

    1. In `packages/dashboard/src/lib/schema.ts`:
       - Add `"agent.reopened"` to the `agentEventTypeValues` array (after `"agent.resumed"`, before `"signal.received"`)
       - Add `reopen_count` column to the `conversations` table definition: `reopen_count: integer("reopen_count").notNull().default(0)` (place after `error_message`, before `parent_conversation_id`)

    **Conversations service updates:**

    2. In `packages/dashboard/src/services/conversations.ts`:
       - Add `reopenCount: number` to the `ConversationDetail` interface (after `retryCount`)
       - Add `reopenCount: number` to the `ConversationListItem` interface (after `errorMessage`)
       - In `getConversationById()`: add `reopen_count: conversations.reopen_count` to the select fields, and map it as `reopenCount: row.reopen_count` in the return object
       - In `listConversations()`: add `reopen_count: conversations.reopen_count` to the select fields, and map it as `reopenCount: row.reopen_count` in the items map

    **API proxy route:**

    3. Create `packages/dashboard/src/app/api/conversations/[id]/reopen/route.ts`:
       - Follow the existing proxy pattern from `api/events/[id]/content/route.ts`
       - Export a `POST` handler
       - Extract `id` from route params (use `Promise<{ id: string }>` pattern)
       - Read the request body as JSON, extract `reason: string`
       - Validate that reason is a non-empty string (return 400 if not)
       - Read `AGENT_SERVICE_URL` from `process.env.AGENT_SERVICE_URL` (same env var used by `lib/agent-service.ts`). Default to `"http://localhost:3004"` if not set.
       - POST to `${AGENT_SERVICE_URL}/conversations/${id}/reopen` with `{ reason }` body and `Content-Type: application/json` header
       - If the upstream response is not ok, forward the status code and error body
       - If successful, return `NextResponse.json({ reopened: true })`
       - Wrap in try/catch, return 500 on unexpected errors (use `console.error` with `biome-ignore` comment for dashboard convention)
  </action>
  <verify>
    - `pnpm run typecheck` passes
    - The API route file exists at `packages/dashboard/src/app/api/conversations/[id]/reopen/route.ts`
    - Dashboard schema has `reopen_count` and `agent.reopened`
    - ConversationDetail and ConversationListItem types include `reopenCount`
  </verify>
  <done>
    Dashboard schema mirror is updated with reopen_count and agent.reopened. Conversations service returns reopenCount in detail and list queries. API proxy route forwards reopen requests to agent-service.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reopen dialog, action bar integration, metadata sidebar, event icon, and list columns</name>
  <files>
    packages/dashboard/src/components/conversation-detail/reopen-dialog.tsx
    packages/dashboard/src/components/conversation-detail/live-detail-panels.tsx
    packages/dashboard/src/components/conversation-detail/metadata-sidebar.tsx
    packages/dashboard/src/components/conversation-detail/event-icon.tsx
    packages/dashboard/src/components/conversations/columns.tsx
    packages/dashboard/src/app/conversations/[id]/page.tsx
  </files>
  <action>
    **Reopen dialog component (NEW file):**

    1. Create `packages/dashboard/src/components/conversation-detail/reopen-dialog.tsx`:
       - "use client" component
       - Props: `conversationId: string`, `status: string` (completed or failed), `onReopened: () => void`
       - Uses Radix Dialog from `@/components/ui/dialog` (Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle, DialogTrigger)
       - Uses Button from `@/components/ui/button`
       - Import `RotateCcw` from lucide-react for the button icon
       - State: `open`, `reason` (string), `submitting` (boolean), `error` (string | null)
       - Label logic: show "Retry" when `status === "failed"`, "Reopen" otherwise (per locked decision)
       - Placeholder logic: "What was fixed that should allow this to succeed now?" for failed, "What changed since this conversation completed?" for completed
       - DialogTrigger wraps a `Button variant="outline" size="sm"` with the RotateCcw icon and label text
       - DialogContent contains:
         - DialogHeader with DialogTitle "{label} Conversation" and DialogDescription "The agent will resume with awareness of its previous run. Provide context about what changed."
         - A `<textarea>` with Tailwind classes: `w-full min-h-[100px] rounded-md border border-input bg-background px-3 py-2 text-sm placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring`
         - Error display: if error state is set, show `<p className="text-sm text-destructive">{error}</p>`
         - DialogFooter with cancel Button (variant="ghost", onClick closes dialog) and submit Button (disabled when `!reason.trim() || submitting`)
       - Submit handler: POST to `/dashboard/api/conversations/${conversationId}/reopen` with `{ reason: reason.trim() }`. On success: close dialog, clear state, call `onReopened()`. On error: set error state from response body.
       - Use Claude's discretion: modal dialog (not inline text field) per research recommendation. Post-reopen: stay on detail view (onReopened triggers router.refresh).

    **Live detail panels integration:**

    2. In `packages/dashboard/src/components/conversation-detail/live-detail-panels.tsx`:
       - Import `ReopenDialog` from `./reopen-dialog`
       - In the `SerializedConversationDetail` interface, add `reopenCount: number`
       - In the `deserializeConversation` function, pass through `reopenCount` (it's a number, no deserialization needed)
       - Add `reopenCount` to the `ConversationDetail` type used by `conversationMeta` state
       - Add a reopen action bar area. In the render, above the `<DetailLayout>`, after the connection status indicator block, add a conditional block:
         ```tsx
         {(conversationMeta.status === "completed" || conversationMeta.status === "failed") && (
           <div className="flex items-center justify-end">
             <ReopenDialog
               conversationId={serverConversation.id}
               status={conversationMeta.status}
               onReopened={() => router.refresh()}
             />
           </div>
         )}
         ```
       - Also handle the `agent.reopened` SSE event type in the lifecycle metadata update effect: when `sse.type === "agent.reopened"`, set status to "queued" (optimistically -- the worker will pick it up)
       - The SSE `isActive` check already covers "running" and "waiting". After a reopen, the status goes to "queued" then "running". The `router.refresh()` in `onReopened` will re-fetch server data, and SSE will activate once status reaches "running". Add `conversationMeta.status === "queued"` to the `isActive` check so SSE starts early.

    **Metadata sidebar:**

    3. In `packages/dashboard/src/components/conversation-detail/metadata-sidebar.tsx`:
       - The component receives `ConversationDetail` which now has `reopenCount`
       - After the "Retries" MetadataField, add a "Reopens" MetadataField that shows `conversation.reopenCount` (always visible, shows 0 if never reopened)
       - If `reopenCount > 0`, display the count with slightly emphasized styling (e.g., `text-sm font-medium` instead of just `text-sm`)

    **Event icon:**

    4. In `packages/dashboard/src/components/conversation-detail/event-icon.tsx`:
       - Import `RotateCcw` from lucide-react
       - Add to `iconMap`: `"agent.reopened": RotateCcw`
       - Add to `colorMap`: `"agent.reopened": "text-cyan-500"` (distinct from other colors -- cyan suggests refresh/restart)

    **Columns (conversation list):**

    5. In `packages/dashboard/src/components/conversations/columns.tsx`:
       - Import `RotateCcw` from lucide-react
       - Add a new column after the "error" column (or before it) for reopen count badge:
         ```typescript
         {
           id: "reopenCount",
           header: "",
           size: 40,
           cell: ({ row }) => {
             if (row.original.reopenCount > 0) {
               return (
                 <span className="inline-flex items-center gap-1 text-xs text-muted-foreground" title={`Reopened ${row.original.reopenCount} time${row.original.reopenCount > 1 ? "s" : ""}`}>
                   <RotateCcw className="h-3 w-3" />
                   {row.original.reopenCount}
                 </span>
               );
             }
             return null;
           },
         },
         ```

    **Detail page data passing:**

    6. In `packages/dashboard/src/app/conversations/[id]/page.tsx`:
       - The `conversation` object from `getConversationById` now includes `reopenCount`
       - Ensure it's passed through in the spread: the existing `{...conversation, createdAt: ..., updatedAt: ...}` spread already passes all fields. Verify `reopenCount` is included (it will be since it's a number, not a Date -- no serialization needed). No changes may be needed here, but verify.
  </action>
  <verify>
    - `pnpm run typecheck` passes
    - `pnpm run build` succeeds (Next.js build validates all components)
    - Reopen dialog component exists at the expected path
    - Event icon includes `agent.reopened` mapping
    - Columns file includes reopenCount column
    - Live detail panels imports and renders ReopenDialog
    - Metadata sidebar shows reopenCount field
  </verify>
  <done>
    Dashboard has a working reopen/retry flow: modal dialog captures reason, posts to API proxy, which forwards to agent-service. Completed/failed conversations show the contextual Reopen/Retry button. Reopen count is visible in both the detail metadata sidebar and list table. agent.reopened events have a distinct cyan RotateCcw icon in the timeline.
  </done>
</task>

</tasks>

<verification>
1. `pnpm run typecheck` passes for the entire monorepo
2. `pnpm run build` succeeds (Next.js build validates RSC/client boundary, component imports)
3. Dashboard schema mirrors agent schema for reopen_count and agent.reopened
4. API proxy route exists and follows established proxy pattern
5. ReopenDialog renders "Retry" for failed status, "Reopen" for completed status
6. Metadata sidebar shows reopenCount
7. Event timeline shows distinct icon for agent.reopened events
8. Conversation list table shows reopen count badge
</verification>

<success_criteria>
- A completed conversation's detail page shows a "Reopen" button in the action area
- A failed conversation's detail page shows a "Retry" button in the action area
- Clicking the button opens a modal dialog with a textarea for providing a reason
- Submitting the dialog with a reason calls the proxy API and triggers router.refresh()
- The reopen count appears in the metadata sidebar
- The reopen count badge appears in the conversation list table for reopened conversations
- agent.reopened events have a distinct RotateCcw icon with cyan color in the timeline
- No deferred ideas are implemented (no threshold warnings, no cancelled status, no auto-reopening)
</success_criteria>

<output>
After completion, create `.planning/phases/57-conversation-reopening/57-02-SUMMARY.md`
</output>

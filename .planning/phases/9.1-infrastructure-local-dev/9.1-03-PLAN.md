# Plan 9.1-03: README Documentation & E2E Verification

## Metadata

| Field | Value |
|-------|-------|
| Phase | 9.1 - Infrastructure & Local Dev |
| Plan | 03 of 03 |
| Wave | 3 (depends on 9.1-01 and 9.1-02) |
| Estimated Scope | Medium (~30 min) |
| Prerequisites | 9.1-01 (OAuth), 9.1-02 (Docker Compose, Dev Agent) |

## Must-Haves

### Truths (invariants)
- README serves dual purpose: human developers AND LLMs reading project
- Clone-to-running experience is non-negotiable for v1.0
- Documentation must include Docker Compose setup (new)
- Documentation must include Linear OAuth flow (new)
- E2E verification confirms all components work together

### Artifacts
- `README.md` — comprehensive setup documentation
- E2E test results documented

### Key Links
- Context: `9.1-CONTEXT.md` → User's vision for README dual purpose
- Depends: 9.1-01 → OAuth script and token utilities
- Depends: 9.1-02 → Docker Compose and Dev Agent entry point
- Current: `README.md` → existing documentation to enhance

---

<objective>
Update README with complete local development setup including Docker Compose, Linear OAuth flow, and Dev Agent instructions. Verify the full E2E workflow works from clone to running Product Agent.
</objective>

<execution_context>
@9.1-CONTEXT.md
@README.md
@9.1-01-PLAN.md (OAuth)
@9.1-02-PLAN.md (Docker Compose)
</execution_context>

<context>
Current README documents:
- Quick Start basics
- Slack App setup (detailed)
- Environment variables
- Project structure
- Troubleshooting

Missing:
- Docker Compose / Temporal setup
- Linear OAuth flow instructions
- Dev Agent instructions
- Clear "clone to running" quickstart
</context>

---

## Tasks

<task id="1" name="Add Docker Compose section to README">
<description>
Document how to start Temporal infrastructure with Docker Compose.
</description>

<files>
- MODIFY: README.md
</files>

<acceptance_criteria>
- Section titled "## Infrastructure Setup" or similar
- Clear steps: `npm run infra:up` to start
- Port information: 7233 (Temporal), 8080 (Temporal UI)
- Link to Temporal UI: http://localhost:8080
- How to check services are healthy
- How to stop: `npm run infra:down`
</acceptance_criteria>

<implementation_hints>
Place this section BEFORE "Running the Product Agent" since infrastructure must be running first.
</implementation_hints>
</task>

<task id="2" name="Add Linear OAuth section to README">
<description>
Document the Linear OAuth authorization flow for app identity.
</description>

<files>
- MODIFY: README.md
</files>

<acceptance_criteria>
- Section titled "## Linear OAuth Setup" under Linear Configuration
- Explains why OAuth (app identity vs personal API key)
- Steps: create OAuth app in Linear, get client ID/secret, run `npm run linear-oauth`
- Environment variables: LINEAR_CLIENT_ID, LINEAR_CLIENT_SECRET
- Token persistence: `.linear-tokens.json` created automatically
- Note that existing LINEAR_ACCESS_TOKEN (API key) still works for quick start
</acceptance_criteria>

<implementation_hints>
OAuth is optional for quick start (API key works), but recommended for production/v1.0.
Make it clear that OAuth is the "production" approach, API key is "quick start".
</implementation_hints>
</task>

<task id="3" name="Add Dev Agent section to README">
<description>
Document how to run the Dev Agent and what it does.
</description>

<files>
- MODIFY: README.md
</files>

<acceptance_criteria>
- Section titled "## Running the Dev Agent"
- Prerequisites: Temporal running, GitHub token, Linear token
- Start command: `npm run dev-agent`
- Explains Dev Agent workflow: Linear task → code → tests → PR
- Troubleshooting common issues
</acceptance_criteria>

<implementation_hints>
Mirror the structure of "Running the Product Agent" section for consistency.
</implementation_hints>
</task>

<task id="4" name="Rewrite Quick Start for clone-to-running">
<description>
Restructure Quick Start section to be a true "clone-to-running" experience with minimal friction.
</description>

<files>
- MODIFY: README.md
</files>

<acceptance_criteria>
- Quick Start gets someone from clone to running in 5 clear steps
- Steps: install, configure env, start infrastructure, authorize, start agent
- Each step has single command or minimal commands
- Links to detailed sections for configuration
- Works for someone who just cloned the repo
</acceptance_criteria>

<implementation_hints>
The "5-minute" version. Details are in later sections.

Example flow:
1. `npm install`
2. `cp .env.example .env.local` and edit
3. `npm run infra:up`
4. `npm run linear-oauth` (optional, for OAuth)
5. `npm run product-agent`
</implementation_hints>
</task>

<task id="5" name="Update Available Scripts table">
<description>
Update the Available Scripts table with new scripts from 9.1-01 and 9.1-02.
</description>

<files>
- MODIFY: README.md
</files>

<acceptance_criteria>
- Table includes: `linear-oauth`, `dev-agent`, `infra:up`, `infra:down`, `infra:logs`
- Descriptions are clear and consistent with existing style
</acceptance_criteria>
</task>

<task id="6" name="E2E Verification">
<description>
Manually verify the complete E2E flow works:
1. Clone-to-running experience
2. Product Agent responds in Slack
3. Linear tasks are created

Document any issues found.
</description>

<files>
- None (verification task)
</files>

<acceptance_criteria>
- `docker-compose up` starts Temporal successfully
- `npm run product-agent` starts and connects
- Bot responds to Slack @mention
- Linear tasks created with correct format

Note: This is a human-verify checkpoint. Claude executes the verification steps and reports results.
</acceptance_criteria>

<checkpoint type="human-verify">
After running verification, report results to user. If issues found, log them for follow-up.
</checkpoint>
</task>

---

## Verification

Clone-to-running test (simulated):
```bash
# Assume fresh clone
npm install
npm run infra:up
# Wait for services
npm run product-agent
```

Check README completeness:
- Does it explain Docker Compose setup? ✓
- Does it explain Linear OAuth? ✓
- Does it explain Dev Agent? ✓
- Can someone clone and follow steps? ✓

---

## Success Criteria

- [ ] README documents Docker Compose / Temporal setup
- [ ] README documents Linear OAuth flow
- [ ] README documents Dev Agent with entry point
- [ ] Quick Start is true clone-to-running (5 clear steps)
- [ ] Available Scripts table is complete
- [ ] E2E verification passes: Product Agent starts, responds to Slack, creates Linear tasks

---

## Output

```
README.md                        # Comprehensive documentation
```

## Notes

This plan depends on 9.1-01 (OAuth) and 9.1-02 (Docker Compose) being complete.
The E2E verification ensures all components work together.

Documentation serves dual purpose per user requirements:
1. Human developers onboarding
2. LLMs reading the project for understanding

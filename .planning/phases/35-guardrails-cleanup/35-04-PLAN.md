---
phase: 35-guardrails-cleanup
plan: 04
type: execute
wave: 2
depends_on: ["35-01", "35-03"]
files_modified:
  - packages/agents/src/dev-agent/index.ts
  - packages/agents/src/dev-agent/worker.ts
  - packages/agents/src/dev-agent/main.ts
  - packages/agents/src/shared/temporal/activities/index.ts
  - packages/agents/src/shared/temporal/workflows/index.ts
  - packages/agents/package.json
  - packages/agents/src/shared/temporal/types.ts
  - packages/platform/src/db/migrations/ (new migration file)
autonomous: true

must_haves:
  truths:
    - "dev-agent/index.ts has zero LangGraph re-exports (no graph, state, prompts, nodes, code-workflow)"
    - "worker.ts bootstrap() calls createOrchestratorWorker(), NOT createDevAgentWorker()"
    - "createDevAgentWorker() function is completely removed"
    - "No @langchain/* imports exist anywhere in packages/agents/"
    - "package.json has zero @langchain/* dependencies"
    - "activities/index.ts has no imports from deleted files"
    - "workflows/index.ts has no exports from dev-agent-workflow.ts"
    - "A migration file exists to DROP checkpoint tables"
    - "pnpm typecheck passes with zero errors from LangGraph references"
  artifacts:
    - path: "packages/agents/src/dev-agent/index.ts"
      provides: "Clean barrel export with only v2.2 orchestrator exports"
    - path: "packages/agents/src/dev-agent/worker.ts"
      provides: "Worker with only createOrchestratorWorker, bootstrap uses it"
      exports: ["createOrchestratorWorker", "DevAgentWorkerOptions"]
    - path: "packages/agents/package.json"
      provides: "Package without any @langchain/* dependencies"
    - path: "packages/agents/src/shared/temporal/activities/index.ts"
      provides: "Activity index without legacy imports"
    - path: "packages/agents/src/shared/temporal/workflows/index.ts"
      provides: "Workflow index without devAgentWorkflow export"
  key_links:
    - from: "worker.ts bootstrap()"
      to: "createOrchestratorWorker()"
      via: "bootstrap calls orchestrator worker, not legacy"
      pattern: "createOrchestratorWorker"
    - from: "package.json"
      to: "zero @langchain deps"
      via: "all 4 @langchain/* removed from dependencies"
      pattern: "@langchain"
    - from: "dev-agent/index.ts"
      to: "orchestrator/system-prompts.js"
      via: "barrel re-exports only v2.2 modules, no deleted paths"
      pattern: "export.*from.*orchestrator"
    - from: "activities/index.ts"
      to: "orchestrator-activities.ts"
      via: "only imports surviving activity files, no legacy dev-agent-activities"
      pattern: "orchestrator-activities|infrastructure-activities"
---

<objective>
Clean up all barrel exports, the worker entry point, package.json dependencies, and database checkpoint tables after LangGraph file deletion. This makes the codebase compile and run cleanly without any LangGraph references.

Purpose: Plan 03 deleted the LangGraph files. This plan fixes all the surviving files that import from or reference deleted code, removes the npm dependencies, and drops orphaned database tables (GUAR-07, GUAR-08, GUAR-09).
Output: Clean-compiling codebase with zero @langchain references, updated worker, migration for checkpoint tables
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-guardrails-cleanup/35-RESEARCH.md
@.planning/phases/35-guardrails-cleanup/35-03-SUMMARY.md

@packages/agents/src/dev-agent/index.ts
@packages/agents/src/dev-agent/worker.ts
@packages/agents/src/shared/temporal/activities/index.ts
@packages/agents/src/shared/temporal/workflows/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up barrel exports and worker entry point</name>
  <files>
    packages/agents/src/dev-agent/index.ts
    packages/agents/src/dev-agent/worker.ts
    packages/agents/src/dev-agent/main.ts
    packages/agents/src/shared/temporal/activities/index.ts
    packages/agents/src/shared/temporal/workflows/index.ts
    packages/agents/src/shared/temporal/types.ts
  </files>
  <action>
**IMPORTANT: Read each file BEFORE editing it.** For every barrel export file below, first read its current contents to see all existing exports, then make targeted removals. Do not guess at file contents.

**Scope cap:** This task modifies ONLY the 6 files listed above. If you discover other files with broken imports (e.g., files that import from deleted modules but are NOT listed here), log them but do NOT fix them in this task -- they will be caught by the typecheck in the verify step and can be addressed as a follow-up.

1. **dev-agent/index.ts** -- First, read the current file. Then REWRITE it. Remove ALL LangGraph re-exports (anything from `code-workflow/`, `workflow/graph`, `workflow/nodes/`, `workflow/prompts`, `workflow/state`). Replace with v2.2 orchestrator exports:

   ```typescript
   /**
    * Dev Agent Module
    *
    * Agentic orchestrator that autonomously handles development tasks.
    * Uses sub-agents (researcher, coder, tester) coordinated by an
    * orchestrator loop, wrapped in Temporal for durability.
    *
    * Exports:
    * - Orchestrator entry point and system prompts
    * - Worker creation for Temporal
    * - API handlers for webhooks
    */

   // Orchestrator
   export {
     ORCHESTRATOR_SYSTEM_PROMPT,
     RESEARCHER_SYSTEM_PROMPT,
     CODER_SYSTEM_PROMPT,
     TESTER_SYSTEM_PROMPT,
   } from "./orchestrator/system-prompts.js";

   // Worker
   export {
     createOrchestratorWorker,
     type DevAgentWorkerOptions,
   } from "./worker.js";
   ```

   After writing, read the orchestrator directory (`packages/agents/src/dev-agent/orchestrator/`) to check if there are additional public APIs that should be exported. Include any that are genuinely needed by external consumers.

2. **dev-agent/worker.ts** -- First, read the current file. Then make these changes:
   a. Remove ALL legacy imports: `ChatAnthropic` from `@langchain/anthropic`, `PostgresSaver` from `@langchain/langgraph-checkpoint-postgres`, and all imports from `dev-agent-activities.js`.
   b. Remove the entire `createDevAgentWorker()` function (lines 90-217).
   c. Update `bootstrap()` to call `createOrchestratorWorker()` instead of `createDevAgentWorker()`.
   d. Update the top-level JSDoc comment to describe v2.2 orchestrator worker only.
   e. Keep `createOrchestratorWorker()` and `DevAgentWorkerOptions` exports.

3. **dev-agent/main.ts** -- First, read the current file. Check for any `@langchain` imports or references to deleted modules. Remove them if present. This file is the HTTP server entry point and likely only imports API handlers.

4. **shared/temporal/activities/index.ts** -- First, read the current file to see ALL existing exports. Then make targeted removals:
   a. Remove ALL imports from `dev-agent-activities.js` (entire import block: `initDevAgentActivities`, `runDevAgentGraphActivity`, etc.)
   b. Remove import of `executeDevWorkflow` from `dev-agent-activity.js`
   c. Remove import of `mergePRActivity`, `MergePRInput`, `MergePROutput` from `github-activities.js`
   d. Remove the `makeActivities` function and `BoundActivities` type (they reference deleted activities)
   e. Remove the `ActivityDependencies` interface (references Sandbox for deleted code-workflow)
   f. Remove ALL re-exports of deleted activity types and functions
   g. Keep all orchestrator, infrastructure, product-agent, linear, and slack activity imports and exports
   h. Clean up type re-exports to only include types from surviving files

5. **shared/temporal/workflows/index.ts** -- First, read the current file. Then remove the `devAgentWorkflow` export block:
   ```typescript
   // DELETE this block:
   export {
     type DevAgentQueryStatus,
     devAgentStatusQuery,
     devAgentWorkflow,
   } from "./dev-agent-workflow.js";
   ```
   Keep orchestratorWorkflow and productAgentConversationWorkflow exports.

6. **shared/temporal/types.ts** -- First, read this file. Check for any legacy types (DevAgentPhase references, legacy workflow types). Remove any types that only exist for the LangGraph workflow. Keep types used by the orchestrator and product agent workflows.

7. **dev-agent/classification/approval.ts** and **approval.test.ts** -- Check for @langchain imports. These files implement the approval intent classifier. They should NOT have LangGraph imports (they were already v2.2-ready in Phase 34), but verify. If any @langchain imports exist, remove them.
  </action>
  <verify>
Run: `cd /Users/roberto.sousa/Documents/Projects/aesir && grep -rn "@langchain" packages/agents/src/ --include="*.ts"` -- should return ZERO results.

Run: `cd /Users/roberto.sousa/Documents/Projects/aesir && grep -rn "langgraph" packages/agents/src/ --include="*.ts" -i` -- should return ZERO results (case-insensitive).

Run: `cd /Users/roberto.sousa/Documents/Projects/aesir && grep -rn "PostgresSaver" packages/agents/src/ --include="*.ts"` -- should return ZERO results.

Run: `cd /Users/roberto.sousa/Documents/Projects/aesir && grep -rn "ChatAnthropic" packages/agents/src/ --include="*.ts"` -- should return ZERO results.

Run: `cd /Users/roberto.sousa/Documents/Projects/aesir && grep -rn "routeByPhase" packages/agents/src/ --include="*.ts"` -- should return ZERO results.

Run: `cd /Users/roberto.sousa/Documents/Projects/aesir && pnpm --filter @aesir/agents run typecheck` -- should pass (no dangling references).
  </verify>
  <done>
All barrel exports cleaned. Worker boots with orchestrator worker only. Zero @langchain, langgraph, PostgresSaver, or ChatAnthropic references remain in packages/agents/. Typecheck passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove @langchain dependencies and create checkpoint table migration</name>
  <files>
    packages/agents/package.json
    packages/platform/src/db/migrations/ (new migration file)
  </files>
  <action>
1. **packages/agents/package.json** -- First, read the current file. Then remove all four @langchain/* dependencies from the `dependencies` object:
   - `"@langchain/anthropic": "^0.3.0"` -- REMOVE
   - `"@langchain/core": "^0.3.0"` -- REMOVE
   - `"@langchain/langgraph": "^0.2.0"` -- REMOVE
   - `"@langchain/langgraph-checkpoint-postgres": "^0.1.0"` -- REMOVE

   Do NOT remove any other dependencies. Leave all other entries intact.

2. **Run `pnpm install`** after editing package.json to update the lockfile and remove the packages from node_modules.

3. **Create a new migration file** in `packages/platform/src/db/migrations/` to drop the LangGraph checkpoint tables. Follow the existing migration naming convention (check what the last migration number is).

   The migration SQL:
   ```sql
   -- Drop LangGraph checkpoint tables (replaced by agents.context_snapshots in Phase 29)
   -- These tables were created by PostgresSaver.setup(), not by Drizzle migrations.
   -- They exist in the public schema.
   DROP TABLE IF EXISTS checkpoint_writes;
   DROP TABLE IF EXISTS checkpoint_blobs;
   DROP TABLE IF EXISTS checkpoints;
   ```

   Check the migration format used in the project (Drizzle-kit vs hand-written SQL). Read an existing migration file to match the pattern. Create the migration file following the same convention.

4. **packages/platform/src/services/cleanup.ts** -- Check if this file references checkpoint tables or PostgresSaver cleanup. If it does, remove those references. The research mentions this file may have checkpoint cleanup code.

5. **Verify** that `pnpm --filter @aesir/agents run typecheck` passes cleanly after dependency removal.

6. **Verify** with a comprehensive grep for any remaining @langchain references across the ENTIRE monorepo:
   `grep -rn "@langchain" packages/ --include="*.ts" --include="*.json" | grep -v node_modules | grep -v pnpm-lock`
   The only allowed matches are in other packages' documentation/comments, NOT in any import statements or package.json dependencies.
  </action>
  <verify>
Run: `cd /Users/roberto.sousa/Documents/Projects/aesir && grep "@langchain" packages/agents/package.json` -- ZERO results.

Run: `cd /Users/roberto.sousa/Documents/Projects/aesir && pnpm --filter @aesir/agents run typecheck` -- passes.

Run: `cd /Users/roberto.sousa/Documents/Projects/aesir && grep -rn "@langchain" packages/agents/ --include="*.ts" --include="*.json" | grep -v node_modules` -- ZERO results.

Migration file exists with DROP TABLE IF EXISTS for checkpoint tables.
  </verify>
  <done>
All 4 @langchain/* dependencies removed from package.json. pnpm lockfile updated. Checkpoint table migration created. Zero @langchain references remain in the agents package. Typecheck passes cleanly.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "@langchain" packages/agents/ --include="*.ts" --include="*.json" | grep -v node_modules` -- ZERO results
2. `grep -rn "langgraph\|LangGraph" packages/agents/src/ --include="*.ts"` -- ZERO results
3. `grep -rn "PostgresSaver" packages/agents/src/ --include="*.ts"` -- ZERO results
4. `grep -rn "ChatAnthropic" packages/agents/src/ --include="*.ts"` -- ZERO results
5. `grep -rn "routeByPhase\|DevAgentPhase\|code-workflow" packages/agents/src/ --include="*.ts"` -- ZERO results
6. `pnpm --filter @aesir/agents run typecheck` -- passes with zero errors
7. `npx vitest run packages/agents/src/` -- all surviving tests pass (skip pre-existing failures)
</verification>

<success_criteria>
- dev-agent/index.ts exports only v2.2 orchestrator APIs
- worker.ts has only createOrchestratorWorker, bootstrap calls it
- activities/index.ts has no legacy activity imports/exports
- workflows/index.ts has no devAgentWorkflow export
- Zero @langchain imports in packages/agents/src/
- Zero @langchain dependencies in packages/agents/package.json
- Typecheck passes with zero LangGraph-related errors
- Migration file exists for dropping checkpoint tables
- All surviving tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/35-guardrails-cleanup/35-04-SUMMARY.md`
</output>

---
phase: 33-product-agent
plan: 04
type: execute
wave: 4
depends_on: ["33-03"]
files_modified:
  - packages/agents/src/shared/temporal/activities/product-agent-activity.test.ts
  - packages/agents/src/shared/temporal/workflows/product-agent-workflow.test.ts
autonomous: true

must_haves:
  truths:
    - "extractPhase correctly maps all phase tag variants to ProductAgentWorkflowPhase values"
    - "extractPhase returns awaiting_reply when no phase tag is present (safe default)"
    - "extractIssueInfo finds issue details from linear_create_issue tool results in the trace"
    - "Activity test verifies runProductAgent is called with correct options including conversation history"
    - "Workflow test verifies conversation history is accumulated across turns"
    - "Workflow test verifies agent response is NOT sent to Slack via sendSlackReplyActivity"
    - "Workflow test verifies timeout and cancellation handling still works"
  artifacts:
    - path: "packages/agents/src/shared/temporal/activities/product-agent-activity.test.ts"
      provides: "Activity and utility function tests"
      min_lines: 80
    - path: "packages/agents/src/shared/temporal/workflows/product-agent-workflow.test.ts"
      provides: "Workflow state machine simulation tests"
      min_lines: 80
  key_links:
    - from: "packages/agents/src/shared/temporal/activities/product-agent-activity.test.ts"
      to: "packages/agents/src/shared/temporal/activities/product-agent-activity.ts"
      via: "extractPhase and extractIssueInfo imports"
      pattern: "extractPhase|extractIssueInfo"
---

<objective>
Write tests for the product agent activity and workflow.

Purpose: Verify that phase extraction, issue info extraction, activity wiring, and workflow state transitions work correctly. Tests follow patterns established in Phase 32 (orchestrator activity and workflow tests).

Output: Comprehensive test suite covering activity utilities and workflow behavior.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-product-agent/33-RESEARCH.md
@.planning/phases/33-product-agent/33-03-SUMMARY.md

@packages/agents/src/shared/temporal/activities/product-agent-activity.ts
@packages/agents/src/shared/temporal/workflows/product-agent-workflow.ts
@packages/agents/src/shared/temporal/activities/orchestrator-activities.test.ts
@packages/agents/src/shared/temporal/workflows/orchestrator-workflow.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write activity utility and integration tests</name>
  <files>
    packages/agents/src/shared/temporal/activities/product-agent-activity.test.ts
  </files>
  <action>
    Write tests for the product agent activity utilities and the activity function itself.

    Follow the testing pattern from `orchestrator-activities.test.ts` (Phase 32): mock the orchestrator, test parsing functions as pure units, test activity wiring.

    **Test Groups:**

    1. **extractPhase** (pure function, no mocks needed):
       - "returns 'complete' for <phase>complete</phase>"
       - "returns 'awaiting_reply' for <phase>clarifying</phase>"
       - "returns 'declined' for <phase>declined</phase>"
       - "returns 'cancelled' for <phase>cancelled</phase>"
       - "returns 'awaiting_reply' when no phase tag present (safe default)"
       - "returns 'awaiting_reply' for unknown phase value"
       - "extracts phase from output with surrounding text" (phase tag embedded in longer text)
       - "handles multiple phase tags (takes first match)"

    2. **extractIssueInfo** (pure function):
       - "returns issue info when linear_create_issue tool_result exists in trace"
       - "returns null when no linear_create_issue in trace"
       - "handles malformed JSON in tool result"
       - "extracts id, identifier, title, and url from tool result"

    3. **runProductAgentActivity** (mocked dependencies):
       - Use module-level `vi.mock` for the orchestrator module (mock `runProductAgent`)
       - Use `initProductAgentActivities` with mock db and logger
       - "calls runProductAgent with correct options from input"
       - "passes conversationHistory when provided"
       - "passes empty history when conversationHistory is undefined"
       - "extracts phase from agent result output"
       - "extracts issue info from agent result trace"
       - "returns slim output without full trace"
       - "throws if activity deps not initialized"

    **Patterns to follow:**
    - Import `describe, expect, it, vi, beforeEach` from vitest
    - Use `vi.mock()` at module level for the orchestrator
    - Create helper function `buildMockAgentResult(overrides)` that returns a valid AgentLoopResult shape
    - Use `beforeEach` to reset mocks and reinitialize deps
    - Mock db as `{ execute: vi.fn() }` cast to unknown, mock logger with `vi.fn()` methods

    **Test data:**
    - For extractPhase tests, create AgentLoopResult objects with different `output` strings
    - For extractIssueInfo tests, create trace arrays with tool_result steps
    - For activity tests, mock runProductAgent to return controlled results
  </action>
  <verify>
    - `npx vitest run packages/agents/src/shared/temporal/activities/product-agent-activity.test.ts` -- all tests pass
    - At least 15 test cases covering extractPhase, extractIssueInfo, and runProductAgentActivity
  </verify>
  <done>
    Activity tests pass covering: phase extraction (8 cases), issue info extraction (4 cases), activity wiring (7 cases). Pure functions tested without mocks, activity tested with mocked orchestrator.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write workflow state machine tests</name>
  <files>
    packages/agents/src/shared/temporal/workflows/product-agent-workflow.test.ts
  </files>
  <action>
    Write or update tests for the product agent Temporal workflow.

    Follow the state machine simulation pattern from `orchestrator-workflow.test.ts` (Phase 32): test the workflow logic by simulating activity calls and signal delivery. Do NOT use TestWorkflowEnvironment (deferred to integration tests).

    **Read the existing test file first** -- it may already have tests for the current workflow. Update them to match the new behavior.

    **Test Groups:**

    1. **Conversation history accumulation:**
       - "accumulates user messages and agent responses across turns"
       - "passes full conversation history to activity on each turn"

    2. **Agent handles its own messages:**
       - "does NOT call sendSlackReplyActivity after agent activity returns" (critical behavioral change)
       - "agent response field is internal reasoning, not sent to Slack"

    3. **Phase-based flow control:**
       - "completes workflow when activity returns phase 'complete' with issue info"
       - "returns declined when activity returns phase 'declined'"
       - "waits for user reply when activity returns phase 'awaiting_reply'"
       - "handles cancelled phase from activity"

    4. **Timeout handling (preserved from current):**
       - "sends reminder after 24h of inactivity"
       - "times out after 72h total"
       - "handles max iterations"

    5. **Signal handling:**
       - "processes userReplySignal to continue conversation"
       - "processes cancelConversationSignal"

    6. **Query handler:**
       - "conversationStatusQuery returns current state"

    **Simulation pattern (from orchestrator-workflow.test.ts):**
    Since we can't run real Temporal workflows in unit tests, simulate the workflow's state transitions:
    - Mock `proxyActivities` to return controlled activity functions
    - Mock `wf.condition` to simulate signal arrivals and timeouts
    - Track calls to activity mocks to verify behavior
    - Verify the workflow's return value matches expected state

    **IMPORTANT behavioral difference to test:**
    In the OLD workflow, after `runProductAgentActivity`, the workflow called `sendSlackReplyActivity(channelId, threadTs, agentResult.response)`. In the NEW workflow, this call is REMOVED because the agent sends its own messages via tools. The test must verify this call does NOT happen after agent turns, but DOES happen for timeout/reminder/cancellation messages.

    If the existing test file has tests that assert `sendSlackReplyActivity` is called after agent turns, those tests must be REMOVED or INVERTED (assert it's NOT called).
  </action>
  <verify>
    - `npx vitest run packages/agents/src/shared/temporal/workflows/product-agent-workflow.test.ts` -- all tests pass
    - At least 10 test cases covering conversation history, phase flow, timeouts, signals
    - Test explicitly verifies sendSlackReplyActivity is NOT called after agent turns
  </verify>
  <done>
    Workflow tests pass covering: conversation history accumulation, agent self-messaging (sendSlackReplyActivity NOT called after turns), phase-based flow control (complete/declined/awaiting_reply/cancelled), timeout handling (24h/72h), signal handling (userReply/cancel), and query handler.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run packages/agents/src/shared/temporal/activities/product-agent-activity.test.ts` passes
- `npx vitest run packages/agents/src/shared/temporal/workflows/product-agent-workflow.test.ts` passes
- `pnpm test:fast` passes (all tests including new ones)
- Total test count >= 25 (15 activity + 10 workflow)
</verification>

<success_criteria>
1. extractPhase tested with all phase tag variants including edge cases
2. extractIssueInfo tested with trace parsing including malformed data
3. Activity wiring tested with mocked runProductAgent
4. Workflow conversation history accumulation verified
5. Agent self-messaging behavior verified (no sendSlackReplyActivity after agent turns)
6. Timeout and signal handling preserved from existing workflow tests
7. All tests pass with `pnpm test:fast`
</success_criteria>

<output>
After completion, create `.planning/phases/33-product-agent/33-04-SUMMARY.md`
</output>

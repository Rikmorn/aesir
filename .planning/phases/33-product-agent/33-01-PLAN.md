---
phase: 33-product-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/integrations/linear/src/mcp/schemas.ts
  - packages/integrations/linear/src/mcp/tools/issues.ts
  - packages/integrations/linear/src/mcp/tools/index.ts
  - packages/integrations/linear/src/mcp/server.ts
  - packages/integrations/linear/scripts/seed-permissions.ts
  - packages/agents/src/shared/tools/integration/linear-tools.ts
  - packages/agents/src/shared/tools/toolkits.ts
autonomous: true

must_haves:
  truths:
    - "Linear MCP server exposes a search_issues tool that searches issues by text query"
    - "Agent-side linear_search_issues tool wrapper exists and calls the MCP search_issues endpoint"
    - "Product agent toolkit factory returns exactly 5 tools: slack_send_message, linear_search_issues, linear_create_issue, linear_get_issue, linear_list_labels"
    - "Seed permissions script includes search_issues permission for both dev-agent and product-agent"
  artifacts:
    - path: "packages/integrations/linear/src/mcp/schemas.ts"
      provides: "SearchIssuesInputSchema and SearchIssuesOutputSchema Zod schemas"
      contains: "SearchIssuesInputSchema"
    - path: "packages/integrations/linear/src/mcp/tools/issues.ts"
      provides: "handleSearchIssues MCP handler"
      exports: ["handleSearchIssues"]
    - path: "packages/integrations/linear/src/mcp/server.ts"
      provides: "search_issues tool registration in the MCP server"
      contains: "search_issues"
    - path: "packages/agents/src/shared/tools/integration/linear-tools.ts"
      provides: "linear_search_issues tool definition"
      contains: "linear_search_issues"
    - path: "packages/agents/src/shared/tools/toolkits.ts"
      provides: "createProductAgentToolkit factory"
      exports: ["createProductAgentToolkit"]
  key_links:
    - from: "packages/agents/src/shared/tools/toolkits.ts"
      to: "packages/agents/src/shared/tools/integration/linear-tools.ts"
      via: "createLinearTools import"
      pattern: "createLinearTools"
    - from: "packages/integrations/linear/src/mcp/server.ts"
      to: "packages/integrations/linear/src/mcp/tools/issues.ts"
      via: "handleSearchIssues routing"
      pattern: "handleSearchIssues"
---

<objective>
Add the Linear search_issues MCP tool and create the product agent toolkit factory.

Purpose: The product agent needs a search_issues tool for duplicate detection (PROD-05) and a dedicated toolkit with exactly 5 tools. The search_issues tool does not exist in the Linear MCP server -- it must be added end-to-end (server-side handler + agent-side wrapper). The product agent toolkit factory follows the same pattern as the existing orchestrator toolkit but with a much smaller tool set.

Output: Linear MCP server has 6 tools (was 5), agent tool library has `linear_search_issues`, and `createProductAgentToolkit()` exists in toolkits.ts returning 5 tools.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-product-agent/33-RESEARCH.md

@packages/integrations/linear/src/mcp/schemas.ts
@packages/integrations/linear/src/mcp/tools/issues.ts
@packages/integrations/linear/src/mcp/tools/index.ts
@packages/integrations/linear/src/mcp/server.ts
@packages/integrations/linear/scripts/seed-permissions.ts
@packages/agents/src/shared/tools/integration/linear-tools.ts
@packages/agents/src/shared/tools/integration/mcp-wrapper.ts
@packages/agents/src/shared/tools/toolkits.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add search_issues MCP tool to Linear integration</name>
  <files>
    packages/integrations/linear/src/mcp/schemas.ts
    packages/integrations/linear/src/mcp/tools/issues.ts
    packages/integrations/linear/src/mcp/tools/index.ts
    packages/integrations/linear/src/mcp/server.ts
    packages/integrations/linear/scripts/seed-permissions.ts
  </files>
  <action>
    Add the search_issues MCP tool to the Linear integration package, following the exact patterns of existing tools (get_issue, create_issue).

    1. In `schemas.ts`, add:
       - `SearchIssuesInputSchema` with fields: `query` (string, required), `teamId` (string, optional), `limit` (number, optional, default 10)
       - `SearchIssuesOutputSchema` with fields: `issues` (array of { id, identifier, title, description (nullable), url, state: { name, type } }), `count` (number), `query` (string)
       - Export both types (`SearchIssuesInput`, `SearchIssuesOutput`)

    2. In `tools/issues.ts`, add `handleSearchIssues` function:
       - Follow the exact pattern of `handleGetIssue` (permission check -> input validation -> client creation -> API call -> result formatting)
       - Permission check: `checkLinearToolPermission` with toolName `"search_issues"`
       - Use `client.issueSearch(query)` from the Linear SDK to search issues
       - If `teamId` is provided, filter results by team after search (the SDK's `issueSearch` doesn't have team filtering built in -- filter the `nodes` array)
       - Limit results to `limit` parameter (default 10)
       - Map results to output format: extract `id`, `identifier`, `title`, `description`, `url`, and fetch `state` for each issue (name, type)
       - Return human-readable text: "Found N issues matching 'query':" with list of identifiers and titles
       - Handle the `IssueConnection` return type from `issueSearch` -- access results via `.nodes`

    3. In `tools/index.ts`, add export for `handleSearchIssues`

    4. In `server.ts`:
       - Import `handleSearchIssues` from tools
       - Add `search_issues` to the `ListToolsRequestSchema` handler with JSON Schema: query (string, required), teamId (string, optional), limit (number, optional)
       - Add `"search_issues"` case to the `CallToolRequestSchema` switch routing to `handleSearchIssues`
       - Update the log message from "5 tools" to "6 tools"

    5. In `scripts/seed-permissions.ts`:
       - Add `{ agentId: "dev-agent", toolName: "search_issues", allowed: true }` to PERMISSIONS array
       - Add `{ agentId: "product-agent", toolName: "search_issues", allowed: true }` to PERMISSIONS array

    IMPORTANT: Follow exactOptionalPropertyTypes patterns from existing code (conditional property assignment, not spreading undefined).
  </action>
  <verify>
    - `pnpm --filter @aesir/integration-linear typecheck` passes
    - `pnpm --filter @aesir/integration-linear build` succeeds
    - Grep confirms `handleSearchIssues` is exported from tools/index.ts
    - Grep confirms `search_issues` appears in server.ts tool list and switch statement
    - Grep confirms seed-permissions.ts has search_issues entries for both agents
  </verify>
  <done>
    Linear MCP server has 6 tools (get_issue, create_issue, update_issue_status, list_teams, list_labels, search_issues). The search_issues handler follows the same permission-check + validation + client + result pattern as all other handlers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add linear_search_issues agent wrapper and createProductAgentToolkit</name>
  <files>
    packages/agents/src/shared/tools/integration/linear-tools.ts
    packages/agents/src/shared/tools/toolkits.ts
  </files>
  <action>
    1. In `linear-tools.ts`:
       - Add a `searchIssuesSchema` Zod object with fields:
         - `query`: z.string() with description "Search query text to find matching Linear issues"
         - `teamId`: z.string().optional() with description "Optional team ID to scope search results"
         - `limit`: z.number().optional() with description "Maximum results to return (default: 10)"
       - Add a new `createMcpToolWrapper` call to the returned array in `createLinearTools()`:
         - integration: "linear"
         - toolName: "search_issues"
         - displayName: "linear_search_issues"
         - description: "Search Linear issues by text query. Returns matching issues with titles, identifiers, and status. Use this BEFORE creating a new issue to check for duplicates. If similar issues exist, suggest updating them instead of creating duplicates."
         - inputSchema: searchIssuesSchema
       - Update the JSDoc comment: "Returns 6 tools" (was 5), add search_issues to the list

    2. In `toolkits.ts`:
       - Add a new `createProductAgentToolkit` factory function
       - It takes a simpler deps type than ToolkitDeps since it doesn't need containerManager, tokenBudget, traceRecorder, or abortSignal. Define `ProductAgentToolkitDeps` interface with just: `agentId: string`, `correlationId: string`
       - Inside the function:
         - Create McpToolDeps from agentId and correlationId
         - Call `createLinearTools(mcpDeps)` and filter to: "linear_create_issue", "linear_get_issue", "linear_list_labels", "linear_search_issues"
         - Call `createSlackTools(mcpDeps)` and filter to: "slack_send_message"
         - Return [...linearTools, ...slackTools] (5 tools total)
       - Export from the module
       - Add JSDoc documenting the 5 tools: linear_create_issue, linear_get_issue, linear_list_labels, linear_search_issues, slack_send_message

    IMPORTANT: The product agent toolkit does NOT need codebase tools, coordination tools (spawn_agent, request_human_input), or GitHub tools. It communicates via Slack and creates Linear issues -- that's it.
  </action>
  <verify>
    - `pnpm --filter @aesir/agents typecheck` passes
    - Grep confirms `linear_search_issues` appears in linear-tools.ts
    - Grep confirms `createProductAgentToolkit` is exported from toolkits.ts
    - Run existing toolkit tests: `npx vitest run packages/agents/src/shared/tools/toolkits.test.ts` (should still pass)
    - Run existing integration tools tests: `npx vitest run packages/agents/src/shared/tools/integration/integration-tools.test.ts` (should still pass)
  </verify>
  <done>
    `createLinearTools()` returns 6 tools (was 5) including `linear_search_issues`. `createProductAgentToolkit()` exists and returns exactly 5 tools for the product agent.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @aesir/integration-linear typecheck && pnpm --filter @aesir/integration-linear build` both pass
- `pnpm --filter @aesir/agents typecheck` passes
- All existing tests pass: `npx vitest run packages/agents/src/shared/tools/`
- `linear_search_issues` tool definition exists in agent tool library
- `createProductAgentToolkit` factory exists and returns 5 tools
- Linear MCP server registers 6 tools
</verification>

<success_criteria>
1. search_issues MCP tool added end-to-end: schema, handler, server registration, permission seeding
2. Agent-side linear_search_issues wrapper follows createMcpToolWrapper pattern
3. createProductAgentToolkit returns exactly 5 tools (4 Linear + 1 Slack)
4. All existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/33-product-agent/33-01-SUMMARY.md`
</output>

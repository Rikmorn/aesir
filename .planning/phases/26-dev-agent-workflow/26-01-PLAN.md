---
phase: 26-dev-agent-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/integrations/slack/src/mcp/schemas.ts
  - packages/integrations/slack/src/mcp/tools/messages.ts
  - packages/integrations/slack/src/api/mcp.ts
autonomous: true

must_haves:
  truths:
    - "Dev-agent can call slack.update_message to modify existing messages"
    - "Update message tool validates channel and timestamp inputs"
    - "Tool is registered in MCP tool list and callable via HTTP"
  artifacts:
    - path: "packages/integrations/slack/src/mcp/schemas.ts"
      provides: "UpdateMessageInputSchema"
      contains: "UpdateMessageInputSchema"
    - path: "packages/integrations/slack/src/mcp/tools/messages.ts"
      provides: "update_message tool handler"
      contains: "case \"update_message\""
  key_links:
    - from: "packages/integrations/slack/src/mcp/tools/messages.ts"
      to: "client.chat.update"
      via: "WebClient method call"
      pattern: "client\\.chat\\.update"
---

<objective>
Add slack.update_message MCP tool (MCP-02)

Purpose: Dev-agent needs to update Slack messages (e.g., update approval request status, update progress messages). This is a prerequisite for the dev-agent workflow notification patterns.

Output: Working MCP tool at POST /mcp/tools/update_message
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/integrations/slack/src/mcp/schemas.ts
@packages/integrations/slack/src/mcp/tools/messages.ts
@packages/integrations/slack/src/api/mcp.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UpdateMessage schema</name>
  <files>packages/integrations/slack/src/mcp/schemas.ts</files>
  <action>
Add UpdateMessageInputSchema with:
- channel: string (required, Channel ID)
- ts: string (required, Message timestamp to update)
- text: string (optional, new text content)
- blocks: z.array(z.any()).optional() (optional, Block Kit blocks)

Follow existing pattern from SendMessageInputSchema. Include descriptive .describe() calls for each field.

Also add UpdateMessageOutputSchema matching MessageOutputSchema (ts, channel).

Export types: UpdateMessageInput, UpdateMessageOutput
  </action>
  <verify>
`pnpm --filter @aesir/integration-slack typecheck` passes
Grep for "UpdateMessageInputSchema" shows export
  </verify>
  <done>Schema exported and type-checks without errors</done>
</task>

<task type="auto">
  <name>Task 2: Add update_message tool handler</name>
  <files>packages/integrations/slack/src/mcp/tools/messages.ts</files>
  <action>
In the tools/list handler, add update_message tool definition:
```typescript
{
  name: "update_message",
  description: "Update an existing Slack message. Can change text and/or blocks.",
  inputSchema: zodToJsonSchema(UpdateMessageInputSchema),
}
```

In the tools/call switch statement, add case "update_message":
1. Validate input with UpdateMessageInputSchema.safeParse(args)
2. Call client.chat.update({ channel, ts, text, blocks })
3. Return success result with updated ts and channel

Follow existing patterns from send_message handler. Handle the case where text is optional but blocks are provided (Slack requires at least one).

Import UpdateMessageInputSchema from ../schemas.js
  </action>
  <verify>
`pnpm --filter @aesir/integration-slack typecheck` passes
`pnpm --filter @aesir/integration-slack test` passes
  </verify>
  <done>update_message tool registered and handler implemented</done>
</task>

<task type="auto">
  <name>Task 3: Test update_message integration</name>
  <files>packages/integrations/slack/src/mcp/tools/messages.test.ts</files>
  <action>
If test file exists, add tests for update_message. If not, create minimal test coverage.

Test cases:
1. Valid update with text only - succeeds
2. Valid update with blocks only - succeeds
3. Missing ts - validation error
4. Missing channel - validation error

Mock the Slack WebClient.chat.update method following existing test patterns.
  </action>
  <verify>
`pnpm --filter @aesir/integration-slack test` passes
Test output shows update_message tests passing
  </verify>
  <done>update_message tool has passing tests</done>
</task>

</tasks>

<verification>
- `pnpm --filter @aesir/integration-slack build` succeeds
- `pnpm --filter @aesir/integration-slack test` shows all tests passing
- Tool appears in GET /mcp/tools response when server running
</verification>

<success_criteria>
- UpdateMessageInputSchema exported from schemas.ts
- update_message case in tools/call handler
- client.chat.update called with correct parameters
- Tests pass for valid and invalid inputs
</success_criteria>

<output>
After completion, create `.planning/phases/26-dev-agent-workflow/26-01-SUMMARY.md`
</output>

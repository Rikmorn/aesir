---
phase: 23-event-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - packages/integrations/github/src/dispatcher/routes.ts
  - packages/integrations/github/src/dispatcher/client.ts
  - packages/integrations/github/src/dispatcher/normalize.ts
  - packages/integrations/github/src/dispatcher/index.ts
  - packages/integrations/github/src/api/webhooks.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "GitHub webhook handler normalizes PR review events to NormalizedEvent format"
    - "GitHub dispatcher sends normalized events to configured agent endpoints"
    - "Dispatch is fire-and-forget (does not block webhook response)"
  artifacts:
    - path: "packages/integrations/github/src/dispatcher/routes.ts"
      provides: "Routing configuration for GitHub events"
      exports: ["DISPATCH_ROUTES"]
    - path: "packages/integrations/github/src/dispatcher/client.ts"
      provides: "HTTP dispatch client"
      exports: ["createDispatcher"]
    - path: "packages/integrations/github/src/dispatcher/normalize.ts"
      provides: "GitHub webhook to NormalizedEvent converter"
      exports: ["normalizePRReviewEvent"]
  key_links:
    - from: "packages/integrations/github/src/api/webhooks.ts"
      to: "packages/integrations/github/src/dispatcher/index.ts"
      via: "import and call"
      pattern: "import.*dispatcher"
    - from: "packages/integrations/github/src/dispatcher/client.ts"
      to: "http://dev-agent:3004/events"
      via: "HTTP POST"
      pattern: "dev-agent.*3004.*events"
---

<objective>
Add event normalization and dispatch capability to the GitHub integration.

Purpose: GitHub webhook events (PR review submitted, PR merged) are normalized to the common NormalizedEvent schema and dispatched to agent endpoints, enabling dev-agent to receive feedback on PRs.

Output: GitHub integration normalizes webhooks and dispatches to dev-agent /events endpoint.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-event-infrastructure/23-CONTEXT.md
@.planning/phases/23-event-infrastructure/23-RESEARCH.md
@.planning/phases/23-event-infrastructure/23-01-SUMMARY.md
@packages/integrations/github/src/api/webhooks.ts
@packages/integrations/github/src/webhooks/parser.ts
@packages/common/src/events/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub dispatcher module</name>
  <files>
    packages/integrations/github/src/dispatcher/routes.ts
    packages/integrations/github/src/dispatcher/client.ts
    packages/integrations/github/src/dispatcher/normalize.ts
    packages/integrations/github/src/dispatcher/index.ts
  </files>
  <action>
Create the dispatcher module for GitHub integration. This follows the same pattern as Linear (Plan 02) but for GitHub events.

**1. Create `packages/integrations/github/src/dispatcher/routes.ts`:**

```typescript
/**
 * GitHub Event Dispatch Routes
 *
 * Configuration for routing normalized GitHub events to agent endpoints.
 * Type-only matching for v2.1 (no payload field filters).
 */

export interface DispatchRoute {
  /** Event type to match (exact match) */
  eventType: string;
  /** Target URL for dispatch */
  target: string;
  /** Dispatch mode: async (5s timeout) or sync (30s timeout) */
  mode: "async" | "sync";
}

/**
 * GitHub dispatch routes
 *
 * pull_request.review_submitted: PR got a review, dev-agent should handle feedback
 * pull_request.merged: PR was merged, dev-agent should clean up
 */
export const DISPATCH_ROUTES: DispatchRoute[] = [
  {
    eventType: "github.pull_request.review_submitted",
    target: process.env.DEV_AGENT_URL || "http://dev-agent:3004/events",
    mode: "sync", // Dev-agent may process immediately
  },
  {
    eventType: "github.pull_request.merged",
    target: process.env.DEV_AGENT_URL || "http://dev-agent:3004/events",
    mode: "async", // Cleanup can be background
  },
];
```

**2. Create `packages/integrations/github/src/dispatcher/client.ts`:**

```typescript
/**
 * Event Dispatcher Client
 *
 * Fire-and-forget HTTP client for dispatching normalized events to agents.
 * Same implementation as Linear dispatcher - could be shared in future.
 */

import type { NormalizedEvent, PinoLogger } from "@aesir/common";
import type { DispatchRoute } from "./routes.js";

export interface DispatcherOptions {
  logger: PinoLogger;
  routes: DispatchRoute[];
}

export interface Dispatcher {
  /** Dispatch event to matching routes (fire-and-forget) */
  dispatch(event: NormalizedEvent): void;
}

/**
 * Create dispatcher instance
 *
 * Dispatch is fire-and-forget - does not await response.
 * Errors are logged but do not propagate.
 */
export function createDispatcher(options: DispatcherOptions): Dispatcher {
  const { logger, routes } = options;

  return {
    dispatch(event: NormalizedEvent): void {
      const matchingRoutes = routes.filter((r) => event.type === r.eventType);

      if (matchingRoutes.length === 0) {
        logger.debug({ eventType: event.type }, "No dispatch routes for event type");
        return;
      }

      for (const route of matchingRoutes) {
        const timeout = route.mode === "async" ? 5000 : 30000;

        // Fire-and-forget: don't await
        fetch(route.target, {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            "X-Correlation-ID": event.correlationId,
            "X-Event-ID": event.id,
          },
          body: JSON.stringify(event),
          signal: AbortSignal.timeout(timeout),
        })
          .then((res) => {
            if (!res.ok) {
              logger.warn(
                { eventId: event.id, target: route.target, status: res.status },
                "Event dispatch received non-OK response"
              );
            } else {
              logger.info(
                { eventId: event.id, target: route.target },
                "Event dispatched successfully"
              );
            }
          })
          .catch((err) => {
            logger.error(
              { err, eventId: event.id, target: route.target },
              "Event dispatch failed"
            );
          });
      }
    },
  };
}
```

**3. Create `packages/integrations/github/src/dispatcher/normalize.ts`:**

```typescript
/**
 * GitHub Event Normalization
 *
 * Converts GitHub webhook payloads to NormalizedEvent format.
 */

import { createId, type NormalizedEvent } from "@aesir/common";
import type { PRReviewPayload } from "../webhooks/parser.js";

/**
 * Normalize PR review webhook payload to NormalizedEvent
 *
 * @param payload - Validated PR review webhook payload
 * @param deliveryId - X-GitHub-Delivery header value (used as correlationId)
 * @returns Normalized event ready for dispatch
 */
export function normalizePRReviewEvent(
  payload: PRReviewPayload,
  deliveryId: string
): NormalizedEvent {
  return {
    id: createId.event(),
    type: `github.pull_request.review_${payload.review.state}`,
    source: "github",
    timestamp: new Date(payload.review.submitted_at).toISOString(),
    correlationId: deliveryId,
    payload: {
      action: payload.action,
      prNumber: payload.pull_request.number,
      prTitle: payload.pull_request.title,
      prUrl: payload.pull_request.html_url,
      reviewId: payload.review.id,
      reviewState: payload.review.state,
      reviewBody: payload.review.body,
      reviewerLogin: payload.review.user.login,
      repository: {
        owner: payload.repository.owner.login,
        name: payload.repository.name,
        fullName: payload.repository.full_name,
      },
    },
  };
}

/**
 * Normalize PR merged event (from pull_request webhook with action=closed and merged=true)
 *
 * Note: This is for future use - current webhook handler only processes PR reviews.
 * Add when PR merged handling is needed.
 */
export function normalizePRMergedEvent(
  prNumber: number,
  prTitle: string,
  prUrl: string,
  repository: { owner: string; name: string; fullName: string },
  deliveryId: string
): NormalizedEvent {
  return {
    id: createId.event(),
    type: "github.pull_request.merged",
    source: "github",
    timestamp: new Date().toISOString(),
    correlationId: deliveryId,
    payload: {
      prNumber,
      prTitle,
      prUrl,
      repository,
    },
  };
}
```

**4. Create `packages/integrations/github/src/dispatcher/index.ts`:**

```typescript
export * from "./client.js";
export * from "./normalize.js";
export * from "./routes.js";
```
  </action>
  <verify>
Run: `pnpm --filter @aesir/integration-github typecheck`
Verify: No type errors
  </verify>
  <done>GitHub dispatcher module created with routes, client, and normalization</done>
</task>

<task type="auto">
  <name>Task 2: Integrate dispatcher into GitHub webhook handler</name>
  <files>packages/integrations/github/src/api/webhooks.ts</files>
  <action>
Update the GitHub webhook handler to normalize and dispatch events after processing.

In `packages/integrations/github/src/api/webhooks.ts`:

1. Add imports at the top:
```typescript
import { createDispatcher, normalizePRReviewEvent, DISPATCH_ROUTES } from "../dispatcher/index.js";
```

2. Inside `createWebhookRouter`, create the dispatcher instance after the router is created:
```typescript
// Create dispatcher for event routing
const dispatcher = createDispatcher({
  logger: logger.child({ component: "dispatcher" }),
  routes: DISPATCH_ROUTES,
});
```

3. After the `onPRReview` callback (around line 131-133), add dispatch logic:
```typescript
// Normalize and dispatch event (fire-and-forget)
const normalizedEvent = normalizePRReviewEvent(payload, deliveryId);
dispatcher.dispatch(normalizedEvent);

childLogger.info(
  {
    action: payload.action,
    prNumber: payload.pull_request.number,
    reviewState: payload.review.state,
    eventId: normalizedEvent.id,
  },
  "PR review webhook processed and event dispatched"
);
```

The webhook should:
1. Verify signature (existing)
2. Check idempotency (existing)
3. Parse and validate payload (existing)
4. Call onPRReview callback if provided (existing)
5. **NEW:** Normalize to NormalizedEvent
6. **NEW:** Dispatch to agents (fire-and-forget)
7. Record delivery (existing)
8. Return 200 (existing)
  </action>
  <verify>
Run: `pnpm --filter @aesir/integration-github build`
Run: `pnpm --filter @aesir/integration-github typecheck`
Verify: Build and typecheck pass
  </verify>
  <done>GitHub webhook handler normalizes PR review events and dispatches to agents</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @aesir/integration-github build` succeeds
2. `pnpm --filter @aesir/integration-github typecheck` succeeds
3. Code review: webhook handler imports dispatcher and calls dispatch after processing
4. Code review: normalizePRReviewEvent produces valid NormalizedEvent structure
</verification>

<success_criteria>
- Dispatcher module exists at packages/integrations/github/src/dispatcher/
- DISPATCH_ROUTES configures dev-agent:3004/events as target
- normalizePRReviewEvent converts PR review to NormalizedEvent
- Webhook handler dispatches events after processing (fire-and-forget)
- All builds pass
</success_criteria>

<output>
After completion, create `.planning/phases/23-event-infrastructure/23-03-SUMMARY.md`
</output>

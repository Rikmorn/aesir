---
phase: 17-github-extraction
plan: 06
type: execute
wave: 3
depends_on: ["17-02", "17-03", "17-05"]
files_modified:
  - packages/integrations/github/src/api/webhooks.ts
  - packages/integrations/github/src/api/oauth.ts
  - packages/integrations/github/src/api/routes.ts
  - packages/integrations/github/src/api/index.ts
  - packages/integrations/github/src/main.ts
autonomous: true

must_haves:
  truths:
    - "Webhook endpoint verifies signatures and responds with 200 on valid requests"
    - "OAuth endpoint redirects to GitHub and handles callback"
    - "Express server starts and handles routes"
  artifacts:
    - path: "packages/integrations/github/src/api/webhooks.ts"
      provides: "Webhook HTTP handler"
      contains: "createWebhookRouter"
    - path: "packages/integrations/github/src/api/oauth.ts"
      provides: "OAuth HTTP handlers"
      contains: "createOAuthRouter"
    - path: "packages/integrations/github/src/main.ts"
      provides: "Service entry point"
      contains: "startServer"
  key_links:
    - from: "packages/integrations/github/src/api/webhooks.ts"
      to: "packages/integrations/github/src/webhooks/signature.ts"
      via: "signature verification"
      pattern: "verifySignature"
    - from: "packages/integrations/github/src/api/oauth.ts"
      to: "packages/integrations/github/src/oauth/flow.ts"
      via: "OAuth flow"
      pattern: "buildAuthorizationUrl"
    - from: "packages/integrations/github/src/main.ts"
      to: "packages/integrations/github/src/api/routes.ts"
      via: "route mounting"
      pattern: "createRoutes"
---

<objective>
Create HTTP API layer with Express routes for webhooks, OAuth, and service entry point.

Purpose: The HTTP layer exposes the GitHub integration as a standalone service with webhook and OAuth endpoints.

Output: Runnable Express server with /webhooks and /oauth routes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-github-extraction/17-RESEARCH.md
@packages/integrations/linear/src/api/webhooks.ts (reference pattern)
@packages/integrations/linear/src/api/oauth.ts (reference pattern)
@packages/integrations/linear/src/api/routes.ts (reference pattern)
@packages/integrations/linear/src/main.ts (reference pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create webhook and OAuth routers</name>
  <files>
    packages/integrations/github/src/api/webhooks.ts
    packages/integrations/github/src/api/oauth.ts
  </files>
  <action>
    1. Create packages/integrations/github/src/api/webhooks.ts:
       - Import Router from express
       - Import createPinoLogger, createChildLogger, generateCorrelationId from @aesir/common
       - Import verifySignature, verifyWebhookRequest from ../webhooks/signature.js
       - Import parsePRReviewPayload from ../webhooks/parser.js
       - Import config from ../types/config.js
       - Import GitHubError from ../types/errors.js

       - Define WebhookRouterDeps interface:
         - onPRReview?: (payload: PRReviewPayload, deliveryId: string) => Promise<void>

       - Create createWebhookRouter function:
         ```typescript
         export function createWebhookRouter(deps: WebhookRouterDeps = {}): Router
         ```
         - Create router with express.raw({ type: "application/json" }) middleware
         - POST / handler:
           1. Generate correlation ID
           2. Extract headers (x-hub-signature-256, x-github-delivery, x-github-event)
           3. Verify signature using verifySignature
           4. Return 401 if invalid signature
           5. Parse payload based on event type
           6. For pull_request_review: validate with parsePRReviewPayload, call onPRReview
           7. Return 200 with { received: true, deliveryId }
         - Log at appropriate levels (debug for parsing, info for received, warn for invalid)

    2. Create packages/integrations/github/src/api/oauth.ts:
       - Import Router from express
       - Import createPinoLogger, createChildLogger, generateCorrelationId from @aesir/common
       - Import buildAuthorizationUrl, generateOAuthState from ../oauth/flow.js
       - Import saveGitHubTokens from ../oauth/token-store.js
       - Import GitHubCredentialStore from ../db/credential-store.js
       - Import config from ../types/config.js
       - Import GitHubError from ../types/errors.js

       - Define OAuthRouterDeps interface:
         - credentialStore: GitHubCredentialStore
         - clientId: string
         - clientSecret: string
         - callbackUrl: string

       - Create in-memory state storage:
         ```typescript
         const oauthStates = new Map<string, { created: number; owner?: string }>();
         ```

       - Create createOAuthRouter function:
         ```typescript
         export function createOAuthRouter(deps: OAuthRouterDeps): Router
         ```
         - GET /authorize:
           1. Generate OAuth state
           2. Store in oauthStates map with timestamp
           3. Optionally accept owner from query param
           4. Build authorization URL
           5. Redirect to GitHub

         - GET /callback:
           1. Extract code and state from query
           2. Validate state exists and not expired (10 min)
           3. Exchange code for token via fetch to https://github.com/login/oauth/access_token
           4. Save tokens using saveGitHubTokens
           5. Return success page or redirect
           6. Clean up state from map
  </action>
  <verify>
    pnpm --filter @aesir/integration-github typecheck
  </verify>
  <done>Webhook and OAuth routers created</done>
</task>

<task type="auto">
  <name>Task 2: Create routes and main entry point</name>
  <files>
    packages/integrations/github/src/api/routes.ts
    packages/integrations/github/src/api/index.ts
    packages/integrations/github/src/main.ts
  </files>
  <action>
    1. Create packages/integrations/github/src/api/routes.ts:
       - Import Router, json from express
       - Import httpLogger from @aesir/common
       - Import createWebhookRouter from ./webhooks.js
       - Import createOAuthRouter from ./oauth.js
       - Import GitHubCredentialStore from ../db/credential-store.js

       - Define RoutesDeps interface:
         - credentialStore: GitHubCredentialStore
         - clientId: string
         - clientSecret: string
         - callbackUrl: string
         - onPRReview?: (payload, deliveryId) => Promise<void>

       - Create createRoutes function:
         ```typescript
         export function createRoutes(deps: RoutesDeps): Router
         ```
         - Create main router
         - Mount pino-http middleware
         - Mount webhook router at /webhooks/github
         - Mount OAuth router at /oauth/github
         - Add health check at GET /health (return { status: "ok" })
         - Return router

    2. Create packages/integrations/github/src/api/index.ts:
       - export { createWebhookRouter } from "./webhooks.js"
       - export { createOAuthRouter } from "./oauth.js"
       - export { createRoutes } from "./routes.js"

    3. Create packages/integrations/github/src/main.ts:
       - Import express from express
       - Import createPinoLogger from @aesir/common
       - Import config from ./types/config.js
       - Import createDatabaseClient from ./db/client.js
       - Import createGitHubCredentialStore from ./db/credential-store.js
       - Import createRoutes from ./api/routes.js

       - Create startServer function:
         ```typescript
         export async function startServer(): Promise<void>
         ```
         - Initialize logger
         - Create database client
         - Create credential store
         - Create routes
         - Create Express app
         - Mount routes at /
         - Listen on config.server.port
         - Log startup message with port

       - Add main execution:
         ```typescript
         if (import.meta.url.endsWith(process.argv[1]?.split("/").pop() ?? "")) {
           startServer().catch((err) => {
             logger.error({ err }, "Failed to start server");
             process.exit(1);
           });
         }
         ```
  </action>
  <verify>
    pnpm --filter @aesir/integration-github build
    # Verify main.ts compiles (can't run without database)
  </verify>
  <done>HTTP API layer complete with routes and main entry point</done>
</task>

</tasks>

<verification>
- `pnpm --filter @aesir/integration-github build` compiles without errors
- Webhook router uses express.raw for raw body (signature verification)
- OAuth router handles state generation and validation
- Main entry point starts Express server on configured port
</verification>

<success_criteria>
- Webhook endpoint at /webhooks/github verifies X-Hub-Signature-256
- OAuth endpoints at /oauth/github/authorize and /oauth/github/callback
- Health check at /health returns { status: "ok" }
- Server starts on PORT (default 3002)
</success_criteria>

<output>
After completion, create `.planning/phases/17-github-extraction/17-06-SUMMARY.md`
</output>

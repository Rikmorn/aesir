---
phase: 17-github-extraction
plan: 04
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - packages/integrations/github/src/client/types.ts
  - packages/integrations/github/src/client/factory.ts
  - packages/integrations/github/src/operations/branches.ts
  - packages/integrations/github/src/operations/commits.ts
  - packages/integrations/github/src/operations/pull-requests.ts
  - packages/integrations/github/src/client/index.ts
  - packages/integrations/github/src/operations/index.ts
autonomous: true

must_haves:
  truths:
    - "GitHub client operations move to the GitHub package"
    - "Client factory creates Octokit instances with token authentication"
    - "Branch, commit, and PR operations work through the extracted package"
  artifacts:
    - path: "packages/integrations/github/src/client/factory.ts"
      provides: "Octokit client factory"
      contains: "createGitHubClient"
    - path: "packages/integrations/github/src/operations/branches.ts"
      provides: "Branch operations"
      contains: "createBranch"
    - path: "packages/integrations/github/src/operations/pull-requests.ts"
      provides: "PR operations"
      contains: "createPullRequest"
  key_links:
    - from: "packages/integrations/github/src/operations/branches.ts"
      to: "@octokit/rest"
      via: "Octokit type"
      pattern: "import.*Octokit"
    - from: "packages/integrations/github/src/client/factory.ts"
      to: "@octokit/rest"
      via: "client instantiation"
      pattern: "new Octokit"
---

<objective>
Move GitHub client factory and operations from packages/integrations/src/github/ to the new GitHub package.

Purpose: These operations are the core of the GitHub integration. Moving them to the extracted package consolidates all GitHub-specific code.

Output: Branch, commit, and PR operations available through the new package.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-github-extraction/17-RESEARCH.md
@packages/integrations/src/github/types.ts (move from here)
@packages/integrations/src/github/client.ts (move from here)
@packages/integrations/src/github/branches.ts (move from here)
@packages/integrations/src/github/commits.ts (move from here)
@packages/integrations/src/github/pull-requests.ts (move from here)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move client types and factory</name>
  <files>
    packages/integrations/github/src/client/types.ts
    packages/integrations/github/src/client/factory.ts
    packages/integrations/github/src/client/index.ts
  </files>
  <action>
    1. Create packages/integrations/github/src/client/types.ts:
       - Copy types from packages/integrations/src/github/types.ts
       - Types to move:
         - GitHubConfig (token config)
         - BranchInfo
         - CreateBranchOptions
         - CommitInfo
         - FileChange
         - CreateCommitOptions
         - PullRequestInfo
         - CreatePROptions
         - PRComment
       - Keep documentation comments

    2. Create packages/integrations/github/src/client/factory.ts:
       - Copy from packages/integrations/src/github/client.ts
       - Update imports:
         - import { createPinoLogger } from "@aesir/common"
         - import { Octokit } from "@octokit/rest"
         - import type { GitHubConfig } from "./types.js"
       - Keep createGitHubClient and getOctokit functions
       - Update logger component to "integrations:github:client"

    3. Create packages/integrations/github/src/client/index.ts:
       - export * from "./types.js"
       - export * from "./factory.js"
  </action>
  <verify>
    pnpm --filter @aesir/integration-github typecheck
  </verify>
  <done>Client types and factory moved to new package</done>
</task>

<task type="auto">
  <name>Task 2: Move operations (branches, commits, pull-requests)</name>
  <files>
    packages/integrations/github/src/operations/branches.ts
    packages/integrations/github/src/operations/commits.ts
    packages/integrations/github/src/operations/pull-requests.ts
    packages/integrations/github/src/operations/index.ts
  </files>
  <action>
    1. Create packages/integrations/github/src/operations/branches.ts:
       - Copy from packages/integrations/src/github/branches.ts
       - Update imports:
         - import { createPinoLogger } from "@aesir/common"
         - import type { Octokit } from "@octokit/rest"
         - import type { BranchInfo, CreateBranchOptions } from "../client/types.js"
       - Keep all functions: getBranch, listBranches, createBranch
       - Update logger component to "integrations:github:branches"

    2. Create packages/integrations/github/src/operations/commits.ts:
       - Copy from packages/integrations/src/github/commits.ts
       - Update imports:
         - import { createPinoLogger } from "@aesir/common"
         - import type { Octokit } from "@octokit/rest"
         - import { getBranch } from "./branches.js"
         - import type { CommitInfo, CreateCommitOptions } from "../client/types.js"
       - Keep createCommit function
       - Update logger component to "integrations:github:commits"

    3. Create packages/integrations/github/src/operations/pull-requests.ts:
       - Copy from packages/integrations/src/github/pull-requests.ts
       - Update imports:
         - import { createPinoLogger } from "@aesir/common"
         - import type { Octokit } from "@octokit/rest"
         - import type { CreatePROptions, PRComment, PullRequestInfo } from "../client/types.js"
       - Keep all functions: createPullRequest, getPullRequest, listPRComments, addPRComment, mergePullRequest
       - Update logger component to "integrations:github:pull-requests"

    4. Create packages/integrations/github/src/operations/index.ts:
       - export * from "./branches.js"
       - export * from "./commits.js"
       - export * from "./pull-requests.js"
  </action>
  <verify>
    pnpm --filter @aesir/integration-github build
    pnpm --filter @aesir/integration-github typecheck
  </verify>
  <done>All operations (branches, commits, PRs) moved to new package</done>
</task>

</tasks>

<verification>
- `pnpm --filter @aesir/integration-github build` compiles without errors
- All types exported (GitHubConfig, BranchInfo, PullRequestInfo, etc.)
- All operations exported (createBranch, createCommit, createPullRequest, etc.)
- Client factory creates Octokit with token auth
</verification>

<success_criteria>
- Client types match original definitions
- Client factory functions unchanged in behavior
- All branch/commit/PR operations moved and working
- Logger components updated to integrations:github:* pattern
</success_criteria>

<output>
After completion, create `.planning/phases/17-github-extraction/17-04-SUMMARY.md`
</output>

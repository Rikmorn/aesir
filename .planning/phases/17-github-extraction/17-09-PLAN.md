---
phase: 17-github-extraction
plan: 09
type: execute
wave: 4
depends_on: ["17-03", "17-04"]
files_modified:
  - packages/integrations/github/src/webhooks/signature.test.ts
  - packages/integrations/github/src/webhooks/parser.test.ts
  - packages/integrations/github/src/client/factory.test.ts
  - packages/integrations/github/src/db/credential-store.test.ts
autonomous: true

must_haves:
  truths:
    - "Tests verify signature verification with X-Hub-Signature-256 format"
    - "Tests verify payload parsing with Zod schemas"
    - "Tests verify client factory creates Octokit instances"
  artifacts:
    - path: "packages/integrations/github/src/webhooks/signature.test.ts"
      provides: "Signature verification tests"
      contains: "verifySignature"
    - path: "packages/integrations/github/src/webhooks/parser.test.ts"
      provides: "Payload parser tests"
      contains: "parsePRReviewPayload"
    - path: "packages/integrations/github/src/client/factory.test.ts"
      provides: "Client factory tests"
      contains: "createGitHubClient"
  key_links:
    - from: "packages/integrations/github/src/webhooks/signature.test.ts"
      to: "packages/integrations/github/src/webhooks/signature.ts"
      via: "test import"
      pattern: "import.*verifySignature"
---

<objective>
Move and adapt tests from the old location to the new GitHub package.

Purpose: Tests ensure the extracted code works correctly in its new location and dependencies are wired properly.

Output: Passing test suite for the GitHub integration package.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-github-extraction/17-RESEARCH.md
@packages/integrations/src/github/client.test.ts (move from here)
@packages/integrations/src/github/branches.test.ts (reference)
@packages/integrations/linear/src/webhooks/signature.test.ts (reference)
@packages/integrations/linear/src/webhooks/parser.test.ts (reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create webhook tests (signature and parser)</name>
  <files>
    packages/integrations/github/src/webhooks/signature.test.ts
    packages/integrations/github/src/webhooks/parser.test.ts
  </files>
  <action>
    1. Create packages/integrations/github/src/webhooks/signature.test.ts:
       - Import describe, expect, it from vitest
       - Import verifySignature, verifyWebhookRequest from ./signature.js
       - Use vi.mock pattern to isolate from config validation

       - Test verifySignature:
         - Test with valid signature (compute expected signature manually)
         - Test with invalid signature (should return false)
         - Test with malformed signature (missing sha256= prefix handled by library)

       - Test verifyWebhookRequest:
         - Test with all valid headers
         - Test with missing signature header
         - Test with missing delivery ID header

       - Note: Use crypto.createHmac to compute expected signatures in tests

    2. Create packages/integrations/github/src/webhooks/parser.test.ts:
       - Import describe, expect, it from vitest
       - Import parsePRReviewPayload, PRReviewPayloadSchema from ./parser.js

       - Test parsePRReviewPayload:
         - Test with valid PR review payload (action: submitted, state: approved)
         - Test with valid payload (action: submitted, state: changes_requested)
         - Test with invalid JSON (should return ZodError with "Invalid JSON")
         - Test with missing required fields (should return ZodError)
         - Test with invalid action enum value
         - Test with invalid review state enum value

       - Provide sample payloads matching real GitHub webhook format
  </action>
  <verify>
    pnpm --filter @aesir/integration-github test -- --run webhooks
  </verify>
  <done>Webhook tests for signature verification and payload parsing</done>
</task>

<task type="auto">
  <name>Task 2: Create client and credential store tests</name>
  <files>
    packages/integrations/github/src/client/factory.test.ts
    packages/integrations/github/src/db/credential-store.test.ts
  </files>
  <action>
    1. Create packages/integrations/github/src/client/factory.test.ts:
       - Copy and adapt from packages/integrations/src/github/client.test.ts
       - Import describe, expect, it, vi, beforeEach from vitest
       - Mock @octokit/rest
       - Mock config to avoid env validation

       - Test createGitHubClient:
         - Test creates Octokit with token from config
         - Test passes auth option correctly

       - Test getOctokit:
         - Test creates Octokit with direct token
         - Test creates separate instances for different tokens

    2. Create packages/integrations/github/src/db/credential-store.test.ts:
       - Import describe, expect, it, vi from vitest
       - Import type definitions
       - Create type contract tests (no real database)

       - Test interface compliance:
         - Verify store returns ResultAsync
         - Verify getByOwner returns ResultAsync
         - Verify delete returns ResultAsync
         - Verify health returns Promise

       - Test error wrapping:
         - Mock database error, verify GitHubError returned
         - Verify error code is INT_GITHUB_TOKEN

       - Note: Full integration tests deferred to Phase 20 (Testing Pyramid)
  </action>
  <verify>
    pnpm --filter @aesir/integration-github test
  </verify>
  <done>Client factory and credential store tests passing</done>
</task>

</tasks>

<verification>
- `pnpm --filter @aesir/integration-github test` passes all tests
- Signature tests verify X-Hub-Signature-256 format
- Parser tests verify Zod schema validation
- Client tests verify Octokit instantiation
</verification>

<success_criteria>
- All test files compile and run without errors
- Signature verification tests cover valid/invalid cases
- Parser tests cover valid payloads and error cases
- Client factory tests verify token authentication
</success_criteria>

<output>
After completion, create `.planning/phases/17-github-extraction/17-09-SUMMARY.md`
</output>

---
phase: 20-testing-pyramid
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/common/src/state/dev-workflow-state.test.ts
  - packages/agents/src/nodes/pickup-task.test.ts
  - packages/agents/src/dev-workflow-runner.test.ts
  - packages/agents/src/tools/code-gen.test.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All 7 failing tests now pass"
    - "Test assertions match actual implementation behavior"
    - "No regressions introduced in passing tests"
  artifacts:
    - path: "packages/common/src/state/dev-workflow-state.test.ts"
      provides: "Fixed test assertions for createDevWorkflowInitialState"
    - path: "packages/agents/src/nodes/pickup-task.test.ts"
      provides: "Fixed test assertion using sessionId instead of taskId"
    - path: "packages/agents/src/dev-workflow-runner.test.ts"
      provides: "Fixed test assertions for Ready status and sessionId"
    - path: "packages/agents/src/tools/code-gen.test.ts"
      provides: "Fixed logging integration test"
  key_links:
    - from: "test assertions"
      to: "implementation behavior"
      via: "matching expected values"
      pattern: "expect.*toEqual|toHaveBeenCalledWith"
---

<objective>
Fix the 7 pre-existing test failures in the agents and common packages.

Purpose: These test failures prevent coverage verification and block clean test runs. The tests have incorrect assertions that don't match the actual implementation behavior.

Output: All 7 tests passing with assertions that accurately reflect implementation behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-testing-pyramid/20-VERIFICATION.md

# Implementation files (the source of truth)
@packages/common/src/state/dev-workflow-state.ts
@packages/agents/src/nodes/pickup-task.ts
@packages/agents/src/dev-workflow-runner.ts
@packages/agents/src/tools/code-gen.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dev-workflow-state.test.ts assertions</name>
  <files>packages/common/src/state/dev-workflow-state.test.ts</files>
  <action>
Fix 3 failing tests in dev-workflow-state.test.ts:

1. **"DevWorkflowState defaults > has correct default values via createDevWorkflowInitialState"** (line ~148):
   - Test passes arguments `"task-123", "session-123", "Test task"` but then expects hardcoded values
   - The test expects `taskId: ""` and `taskDescription: ""` but function returns the passed arguments
   - Fix: Either expect the passed values OR remove this test as it duplicates the other test

2. **"createDevWorkflowInitialState > creates initial state with required fields"** (line ~312):
   - Test passes `"task-123", "session-123", "Test task"` but expects `taskId: "TASK-123"` and `taskDescription: "Implement feature X"`
   - The function returns exactly what you pass in, not transformed values
   - Fix: Update expectations to match passed arguments

3. **"createDevWorkflowInitialState > creates initial state with optional repository URL"** (line ~324):
   - Test passes `"TASK-456", "Fix bug Y", "owner/repo"` but expects `taskDescription: "Fix bug Y"` and `repositoryUrl: "owner/repo"`
   - This is actually correct but the second argument should be sessionId, not taskDescription
   - The function signature is: `createDevWorkflowInitialState(taskId, sessionId, taskDescription, repositoryUrl?)`
   - Fix: Update the test call to use correct argument order

Examine the actual function signature in dev-workflow-state.ts:
```typescript
export function createDevWorkflowInitialState(
  taskId: string,
  sessionId: string,
  taskDescription: string,
  repositoryUrl?: string,
): Partial<DevWorkflowStateType>
```

Update assertions to match what the function actually returns.
  </action>
  <verify>
Run `pnpm --filter @aesir/common test src/state/dev-workflow-state.test.ts` and verify all tests pass.
  </verify>
  <done>All 3 tests in dev-workflow-state.test.ts pass with correct assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Fix pickup-task.test.ts emitThought assertion</name>
  <files>packages/agents/src/nodes/pickup-task.test.ts</files>
  <action>
Fix 1 failing test in pickup-task.test.ts:

**"createPickupTaskNode > should emit thought activity"** (line ~92):
- Test expects `emitThought` called with `taskId` ("ABC-123") as second argument
- Implementation correctly uses `sessionId` ("session-test") because emitThought goes to AgentSession, not Issue
- The implementation comment even explains: "Use sessionId (AgentSession ID) not taskId (Issue ID)"

Fix the test assertion at line 94:
```typescript
// Current (wrong):
expect(mockEmitThought).toHaveBeenCalledWith(
  mockLinearClient,
  "ABC-123",  // taskId - wrong
  "Starting work on: Test issue title",
);

// Fixed (correct):
expect(mockEmitThought).toHaveBeenCalledWith(
  mockLinearClient,
  "session-test",  // sessionId - correct (from baseState.sessionId)
  "Starting work on: Test issue title",
);
```
  </action>
  <verify>
Run `pnpm --filter @aesir/agents test src/nodes/pickup-task.test.ts` and verify all tests pass.
  </verify>
  <done>The emitThought test now expects sessionId matching the implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Fix dev-workflow-runner.test.ts assertions</name>
  <files>packages/agents/src/dev-workflow-runner.test.ts</files>
  <action>
Fix 2 failing tests in dev-workflow-runner.test.ts:

1. **"runDevWorkflow > should update Linear status on failure"** (line ~161):
   - Test expects status updated to "Todo" but implementation uses "Ready"
   - Check dev-workflow-runner.ts line 126: `await updateIssueStatus(deps.linearClient, taskId, "Ready");`
   - Fix: Change expected status from "Todo" to "Ready"

2. **"runDevWorkflow > should pass recursion limit from config"** (line ~208):
   - Test expects invoke called with `{ taskId: "ABC-123", status: "pending" }`
   - But implementation passes `{ taskId, sessionId, status: "pending" }` (includes sessionId)
   - Check dev-workflow-runner.ts line 89: `{ taskId, sessionId, status: "pending" }`
   - Fix: Add sessionId to expected state object

```typescript
// Current (wrong):
expect(mockWorkflow.invoke).toHaveBeenCalledWith(
  { taskId: "ABC-123", status: "pending" },
  ...
);

// Fixed (correct):
expect(mockWorkflow.invoke).toHaveBeenCalledWith(
  { taskId: "ABC-123", sessionId: "session-abc", status: "pending" },
  ...
);
```
  </action>
  <verify>
Run `pnpm --filter @aesir/agents test src/dev-workflow-runner.test.ts` and verify all tests pass.
  </verify>
  <done>Both dev-workflow-runner tests pass with correct expected values.</done>
</task>

<task type="auto">
  <name>Task 4: Fix code-gen.test.ts logging test</name>
  <files>packages/agents/src/tools/code-gen.ts
packages/agents/src/tools/code-gen.test.ts</files>
  <action>
Fix 1 failing test in code-gen.test.ts:

**"codeGenTool > logging integration > should call logger during invocation"** (line ~229):
- Test expects `console.info` to be called
- But codeGenTool may use pino logger instead of console.info

First check what code-gen.ts actually does for logging:
- If it uses pino logger, mock that instead
- If it doesn't log at all, either add logging or remove/update the test

Options:
1. If code-gen.ts logs via pino, mock the pino logger and check it
2. If code-gen.ts doesn't log, the test expectation is wrong - update to test actual behavior
3. If logging was removed during pino migration, mark test as todo or remove it

Review the codeGenTool implementation to determine correct approach. Most likely the tool doesn't call console.info anywhere, so either:
- Remove the logging assertion entirely (test just that result has code property)
- Or skip the test with a todo marker explaining pino migration removed console calls
  </action>
  <verify>
Run `pnpm --filter @aesir/agents test src/tools/code-gen.test.ts` and verify all tests pass.
  </verify>
  <done>The logging integration test either passes or is removed/skipped with explanation.</done>
</task>

</tasks>

<verification>
Run full test suite and verify:
1. `pnpm test` shows 0 failures
2. All 7 previously failing tests now pass
3. No regressions - same number of passing tests as before (plus the 7 fixes)
</verification>

<success_criteria>
- `pnpm test` completes with 0 test failures
- The 7 specific tests that were failing now pass:
  - dev-workflow-state.test.ts: 3 tests fixed
  - pickup-task.test.ts: 1 test fixed
  - dev-workflow-runner.test.ts: 2 tests fixed
  - code-gen.test.ts: 1 test fixed
- No new test failures introduced
</success_criteria>

<output>
After completion, create `.planning/phases/20-testing-pyramid/20-07-SUMMARY.md`
</output>

---
phase: 61-inbound-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agents/src/adapters/types.ts
  - packages/agents/src/framework/types.ts
  - packages/agents/src/shared/communication/message-utils.ts
  - packages/agents/src/shared/communication/index.ts
autonomous: true

must_haves:
  truths:
    - "IncomingEventSchema accepts an optional replyContext field that validates against ReplyContextSchema"
    - "SignalSchema accepts an optional replyContext field that validates against ReplyContextSchema"
    - "StartConversationParams has an optional replyContext field typed as ReplyContext"
    - "appendReplyContextTag() returns the original message unchanged when replyContext is undefined"
    - "appendReplyContextTag() appends a <reply_context>JSON</reply_context> XML tag when replyContext is present"
  artifacts:
    - path: "packages/agents/src/adapters/types.ts"
      provides: "IncomingEventSchema with optional replyContext"
      contains: "replyContext"
    - path: "packages/agents/src/framework/types.ts"
      provides: "SignalSchema and StartConversationParams with optional replyContext"
      contains: "replyContext"
    - path: "packages/agents/src/shared/communication/message-utils.ts"
      provides: "appendReplyContextTag helper function"
      exports: ["appendReplyContextTag"]
    - path: "packages/agents/src/shared/communication/index.ts"
      provides: "barrel export for message-utils"
      contains: "appendReplyContextTag"
  key_links:
    - from: "packages/agents/src/adapters/types.ts"
      to: "packages/agents/src/shared/communication/types.ts"
      via: "import ReplyContextSchema"
      pattern: "ReplyContextSchema"
    - from: "packages/agents/src/framework/types.ts"
      to: "packages/agents/src/shared/communication/types.ts"
      via: "import ReplyContextSchema and ReplyContext"
      pattern: "ReplyContextSchema|ReplyContext"
---

<objective>
Extend IncomingEvent, Signal, and StartConversationParams schemas with optional replyContext fields, and create the appendReplyContextTag() helper.

Purpose: Establish the type-level foundation for replyContext flow. All subsequent plans (adapter extraction, executor wiring) depend on these schema extensions existing.
Output: Extended schemas in adapters/types.ts and framework/types.ts, plus message-utils.ts helper.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/agents/src/adapters/types.ts
@packages/agents/src/framework/types.ts
@packages/agents/src/shared/communication/types.ts
@packages/agents/src/shared/communication/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend schemas with optional replyContext and create message utility</name>
  <files>
    packages/agents/src/adapters/types.ts
    packages/agents/src/framework/types.ts
    packages/agents/src/shared/communication/message-utils.ts
    packages/agents/src/shared/communication/index.ts
  </files>
  <action>
    1. In `packages/agents/src/adapters/types.ts`:
       - Import `ReplyContextSchema` from `"../shared/communication/types.js"`
       - Add `replyContext: ReplyContextSchema.optional()` to `IncomingEventSchema` (after the `taskId` field)

    2. In `packages/agents/src/framework/types.ts`:
       - Import `ReplyContextSchema` from `"../shared/communication/types.js"` and `type ReplyContext` from the same module
       - Add `replyContext: ReplyContextSchema.optional()` to `SignalSchema` (after the `deduplicationId` field)
       - Add `replyContext?: ReplyContext` to `StartConversationParams` interface (after the `taskId` field)

    3. Create `packages/agents/src/shared/communication/message-utils.ts`:
       - Import `type ReplyContext` from `"./types.js"`
       - Export function `appendReplyContextTag(message: string, replyContext: ReplyContext | undefined | null): string`
       - If replyContext is falsy, return message unchanged
       - Otherwise return `${message}\n\n<reply_context>${JSON.stringify(replyContext)}</reply_context>`

    4. In `packages/agents/src/shared/communication/index.ts`:
       - Add barrel export for `appendReplyContextTag` from `"./message-utils.js"`
  </action>
  <verify>
    Run `pnpm run typecheck` -- no type errors.
    Run `pnpm test:fast` -- existing tests still pass.
  </verify>
  <done>
    IncomingEventSchema, SignalSchema, and StartConversationParams all have optional replyContext fields. appendReplyContextTag() exists and is exported. All existing tests pass.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck` passes
- `pnpm test:fast` passes (no regressions from schema extensions -- optional fields are backward compatible)
- `appendReplyContextTag("hello", undefined)` returns `"hello"`
- `appendReplyContextTag("hello", { channel: "slack", teamId: "T1", channelId: "C1" })` returns string containing `<reply_context>`
</verification>

<success_criteria>
All three schemas extended with optional replyContext. Message utility helper created and exported. Zero regressions in existing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/61-inbound-pipeline/61-01-SUMMARY.md`
</output>

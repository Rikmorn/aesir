---
phase: 61-inbound-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - packages/agents/src/framework/conversation-executor.ts
  - packages/agents/src/framework/worker-loop.ts
  - packages/agents/src/framework/event-router.ts
  - packages/agents/src/router/router.ts
  - packages/agents/src/framework/conversation-executor.test.ts
  - packages/agents/src/framework/event-router.test.ts
autonomous: true

must_haves:
  truths:
    - "When a signal with replyContext is delivered to a waiting conversation, the conversation row's reply_context column is updated with the new replyContext value"
    - "When a signal WITHOUT replyContext is delivered, the existing reply_context column value is preserved (not set to null)"
    - "The signal message appended to conversation includes a <reply_context> XML tag when the signal has replyContext"
    - "The signal message has NO <reply_context> tag when the signal has no replyContext"
    - "Queued signals consumed by the worker loop (both pre-claim and post-wait_for paths) include <reply_context> tags when replyContext is present"
    - "The start() method stores replyContext on the conversation row and appends <reply_context> to the initial message when replyContext is provided"
    - "EventRouter forwards replyContext from IncomingEvent to Signal object in the signal routing path"
    - "Task routing in router.ts forwards replyContext from IncomingEvent to Signal object"
  artifacts:
    - path: "packages/agents/src/framework/conversation-executor.ts"
      provides: "signal() and start() with replyContext wiring"
      contains: "appendReplyContextTag"
    - path: "packages/agents/src/framework/worker-loop.ts"
      provides: "signal consumption with replyContext tag appending"
      contains: "appendReplyContextTag"
    - path: "packages/agents/src/framework/event-router.ts"
      provides: "EventRouter forwarding replyContext to Signal"
      contains: "replyContext"
    - path: "packages/agents/src/router/router.ts"
      provides: "Task routing forwarding replyContext to Signal"
      contains: "replyContext"
  key_links:
    - from: "packages/agents/src/framework/conversation-executor.ts"
      to: "packages/agents/src/shared/communication/message-utils.ts"
      via: "import appendReplyContextTag"
      pattern: "appendReplyContextTag"
    - from: "packages/agents/src/framework/worker-loop.ts"
      to: "packages/agents/src/shared/communication/message-utils.ts"
      via: "import appendReplyContextTag"
      pattern: "appendReplyContextTag"
    - from: "packages/agents/src/framework/event-router.ts"
      to: "event.replyContext"
      via: "forwarding replyContext from IncomingEvent to Signal"
      pattern: "replyContext.*event\\.replyContext"
---

<objective>
Wire replyContext through the executor (signal + start), worker loop (all 3 signal consumption sites), EventRouter, and task routing so that signals carry replyContext end-to-end from adapter to agent message.

Purpose: This is the critical wiring that makes replyContext actually flow through the system. Without it, adapters extract replyContext but it never reaches the agent.
Output: Updated executor, worker loop, event router, and core router with replyContext propagation.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-inbound-pipeline/61-01-SUMMARY.md
@packages/agents/src/framework/conversation-executor.ts
@packages/agents/src/framework/worker-loop.ts
@packages/agents/src/framework/event-router.ts
@packages/agents/src/framework/types.ts
@packages/agents/src/router/router.ts
@packages/agents/src/shared/communication/message-utils.ts
@packages/agents/src/shared/db/schema.ts
@packages/agents/src/framework/conversation-executor.test.ts
@packages/agents/src/framework/event-router.test.ts
@.planning/phases/61-inbound-pipeline/61-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire replyContext through executor signal() and start() methods</name>
  <files>
    packages/agents/src/framework/conversation-executor.ts
    packages/agents/src/framework/conversation-executor.test.ts
  </files>
  <action>
    **Import in conversation-executor.ts:**
    - Import `appendReplyContextTag` from `"../shared/communication/message-utils.js"`
    - Import `type ReplyContext` from `"../shared/communication/types.js"` (needed for type annotations in the worker-loop signal type assertion -- but actually the Signal type already includes it via the schema extension from Plan 01)

    **signal() method** (the `status === "waiting"` branch, around line 387-393):
    - After building `signalContent`, apply the tag: `const finalContent = appendReplyContextTag(signalContent, signal.replyContext);`
    - Use `finalContent` instead of `signalContent` in the `signalMessage` object
    - In the `tx.update(conversations).set({...})` call, add conditional reply_context update:
      `...(signal.replyContext && { reply_context: signal.replyContext }),`
    - This goes alongside the existing `status: "queued"`, `messages: updatedMessages`, etc.

    **start() method** -- both the new-conversation path (around line 276) and the re-trigger path (around line 246):

    For the NEW conversation path (no existing conversation, around line 272-286):
    - After building `fullMessage`, apply the tag: `const messageWithContext = appendReplyContextTag(fullMessage, params.replyContext);`
    - Use `messageWithContext` in the messages array instead of `fullMessage`
    - Add `reply_context: params.replyContext ?? null,` to the insert values

    For the RE-TRIGGER path (terminal status, around line 241-256):
    - After building `fullMessage`, apply the tag: `const messageWithContext = appendReplyContextTag(fullMessage, params.replyContext);`
    - Use `messageWithContext` in the messages array instead of `fullMessage`
    - Add `reply_context: params.replyContext ?? null,` to the insert values

    **Tests** (`conversation-executor.test.ts`):
    - Add a test: "signal() with replyContext updates reply_context column and appends XML tag to message"
      - Create a waiting conversation, deliver a signal with `replyContext: { channel: "slack", teamId: "T1", channelId: "C1", threadTs: "123.456" }`
      - Verify the `set()` call includes `reply_context` in its argument
      - Verify the message content contains `<reply_context>`
    - Add a test: "signal() without replyContext does NOT update reply_context column"
      - Deliver signal without replyContext, verify `set()` does NOT include `reply_context`
    - Add a test: "start() with replyContext stores it on conversation row and appends tag to message"
      - Call start with replyContext, verify insert includes `reply_context` and message contains `<reply_context>`
  </action>
  <verify>
    Run `pnpm test:fast -- packages/agents/src/framework/conversation-executor.test.ts` -- all pass.
    Run `pnpm run typecheck` -- no errors.
  </verify>
  <done>
    Executor signal() updates reply_context column conditionally and appends XML tag to message. Executor start() stores replyContext on insert and appends XML tag to initial message. Tests verify both paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire replyContext through worker loop, EventRouter, and task routing</name>
  <files>
    packages/agents/src/framework/worker-loop.ts
    packages/agents/src/framework/event-router.ts
    packages/agents/src/router/router.ts
    packages/agents/src/framework/event-router.test.ts
  </files>
  <action>
    **Worker loop** (`packages/agents/src/framework/worker-loop.ts`):

    Import `appendReplyContextTag` from `"../shared/communication/message-utils.js"`.

    There are TWO signal consumption sites in the worker loop (in addition to the executor's signal() already handled in Task 1). Both need the same treatment:

    1. **Pre-claim signal consumption** (around line 656-701):
       - The queued signals type assertion on line 656 currently casts to `Array<{ type: string; data?: Record<string, unknown>; message?: string }>`. Update to include `replyContext?: unknown` (or import the ReplyContext type).
       - After building `signalContent` on line 670-672, apply: `const finalContent = appendReplyContextTag(signalContent, matchedSignal.replyContext as ReplyContext | undefined);`
       - Import `type ReplyContext` from `"../shared/communication/types.js"`
       - Use `finalContent` in the message object on line 677

    2. **Post-wait_for signal consumption** (around line 901-945):
       - Same type assertion update on line 901 to include `replyContext?: unknown`
       - After building `signalContent` on line 919-921, apply: `const finalContent = appendReplyContextTag(signalContent, matchedQueuedSignal.replyContext as ReplyContext | undefined);`
       - Use `finalContent` in the message object on line 925

    **EventRouter** (`packages/agents/src/framework/event-router.ts`):

    In the `handle()` method, signal rule check section (around line 125-131):
    - When building the `signal` object, add `replyContext: event.replyContext` alongside the existing `type`, `data`, `message`, `source`, `deduplicationId` fields

    **Task routing** (`packages/agents/src/router/router.ts`):

    In the `routeViaTask()` function, when building the signal object for active conversations (around line 87-93):
    - Add `replyContext: event.replyContext` to the signal object alongside existing fields

    Also in the `routeEvent()` function, when calling `deps.executor.start()` in the "start" case (around line 228-231):
    - Add `replyContext: routeDecision.event.replyContext` to the start params (this enables the router's start path to forward replyContext, even though Phase 62 handles the router tool's input schema)

    **EventRouter tests** (`event-router.test.ts`):
    - Add a test: "signal route includes replyContext from IncomingEvent"
      - Create an IncomingEvent with type "approval", correlationKey, and replyContext: `{ channel: "slack", teamId: "T1", channelId: "C1" }`
      - Call handle(), verify the signal in the route result includes the replyContext
    - Add a test: "signal route works without replyContext (backward compatible)"
      - Create an IncomingEvent without replyContext, verify signal does not have replyContext
  </action>
  <verify>
    Run `pnpm test:fast -- packages/agents/src/framework/event-router.test.ts` -- all pass.
    Run `pnpm test:fast -- packages/agents/src/framework/worker-loop` -- existing tests pass (worker loop tests may be integration-only).
    Run `pnpm run typecheck` -- no errors.
  </verify>
  <done>
    All three worker loop signal consumption sites append `<reply_context>` tags via appendReplyContextTag(). EventRouter forwards replyContext from IncomingEvent to Signal. Task routing forwards replyContext in both signal and start paths. Tests verify EventRouter forwarding.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck` passes
- `pnpm test:fast` passes (all tests including executor, event-router, and adapter tests)
- Grep for `signalContent =` in the codebase confirms ALL signal message construction sites use `appendReplyContextTag()`
- Grep for `replyContext` in event-router.ts confirms it forwards from event to signal
- Grep for `replyContext` in router.ts confirms task routing forwards it in both signal and start paths
</verification>

<success_criteria>
ReplyContext flows end-to-end: adapters extract it (Plan 02), EventRouter forwards it to signals, executor stores it on conversation rows and appends XML tags to messages, worker loop signal consumption includes XML tags. All existing tests pass with zero regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/61-inbound-pipeline/61-03-SUMMARY.md`
</output>

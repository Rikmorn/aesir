---
phase: 22.2-agent-mcp-migration
plan: 06
type: execute
wave: 5
depends_on: ["22.2-04", "22.2-05"]
files_modified:
  - packages/agents/package.json
  - packages/agents/src/index.ts
  - .claude/CLAUDE.md
autonomous: false

must_haves:
  truths:
    - "Agent package no longer depends on @linear/sdk or @octokit/rest in nodes"
    - "Docker containers start without integration tokens"
    - "CLAUDE.md reflects MCP-based agent architecture"
  artifacts:
    - path: "packages/agents/package.json"
      provides: "Updated dependencies"
    - path: ".claude/CLAUDE.md"
      provides: "Updated architecture documentation"
  key_links:
    - from: ".claude/CLAUDE.md"
      to: "packages/agents/src/mcp/"
      via: "architecture documentation"
      pattern: "MCP|callMcpTool"
---

<objective>
Clean up dependencies and verify complete migration

Purpose: Final cleanup removes unused SDK dependencies from agents package, updates documentation, and verifies Docker containers start successfully without integration tokens.

Output: Clean agent package without SDK dependencies, updated CLAUDE.md, verified Docker startup
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22.2-agent-mcp-migration/22.2-CONTEXT.md
@.planning/phases/22.2-agent-mcp-migration/22.2-04-SUMMARY.md
@.planning/phases/22.2-agent-mcp-migration/22.2-05-SUMMARY.md

@packages/agents/package.json
@packages/agents/src/index.ts
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused SDK dependencies</name>
  <files>
    packages/agents/package.json
    packages/agents/src/index.ts
  </files>
  <action>
**Step 1: Check SDK usage in nodes vs temporal activities:**

```bash
# Check where @linear/sdk is used
grep -rn "@linear/sdk" packages/agents/src/

# Check where @octokit/rest is used
grep -rn "@octokit/rest" packages/agents/src/

# Check where integration packages are used
grep -rn "@aesir/integration-linear" packages/agents/src/
grep -rn "@aesir/integration-github" packages/agents/src/
```

**Step 2: Decision based on grep results:**

- **If SDK only in temporal/activities/**: KEEP the SDK dependency (Temporal activities are NOT migrated per context decisions)
- **If SDK in nodes/**: Something went wrong in Plan 02/03 - investigate and fix
- **If SDK not found anywhere**: Safe to remove

**Step 3: Remove unused dependencies:**

```bash
# Only run for SDKs NOT used in temporal/activities/
# Check first, then remove:

# If @linear/sdk NOT in temporal/activities/:
pnpm remove --filter @aesir/agents @linear/sdk

# If @octokit/rest NOT in temporal/activities/:
pnpm remove --filter @aesir/agents @octokit/rest
```

Note: Keep @slack/bolt and @slack/web-api if used in temporal activities.

**Step 4: Update packages/agents/src/index.ts:**
- Export MCP client: `export * from "./mcp/index.js"`
- Keep other exports unchanged

**Step 5: Verify:**
```bash
pnpm install
pnpm --filter @aesir/agents build
pnpm --filter @aesir/agents typecheck
```
  </action>
  <verify>
    - `pnpm --filter @aesir/agents build` succeeds
    - `pnpm --filter @aesir/agents typecheck` passes
    - Verify SDK usage was analyzed: Record in summary whether SDKs were kept (for temporal) or removed
    - If SDKs removed: `! grep -q "@linear/sdk" packages/agents/package.json` (unless temporal uses it)
    - If SDKs kept: Document WHY in summary (temporal activities need them)
  </verify>
  <done>Agent package dependencies analyzed and cleaned - SDKs removed if unused, kept if temporal activities need them</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md with MCP architecture</name>
  <files>
    .claude/CLAUDE.md
  </files>
  <action>
Update CLAUDE.md to document the new MCP-based agent architecture:

1. Add section under "Architecture" or "Agents":
   ```markdown
   ### Agent MCP Communication

   Agents communicate with integrations via MCP HTTP protocol, not direct SDK clients.

   **MCP Client:**
   - Located: `packages/agents/src/mcp/`
   - Function: `callMcpTool(options)` - makes HTTP POST to /mcp/tools/:name
   - Headers: X-Agent-ID (required), X-Correlation-ID (for tracing)
   - Retry: Exponential backoff on 5xx/429, fail immediately on network errors

   **MCP Endpoints:**
   - Linear: http://linear-integration:3001/mcp/tools/:name
   - GitHub: http://github-integration:3002/mcp/tools/:name
   - Slack: http://slack-integration:3003/mcp/tools/:name

   **Agent Configuration:**
   - Agents do NOT require integration tokens (LINEAR_ACCESS_TOKEN, GITHUB_TOKEN, etc.)
   - Agents only need: ANTHROPIC_API_KEY, workspace IDs (LINEAR_TEAM_ID, GITHUB_REPO)
   - MCP URLs configurable via env (defaults to Docker network names)

   **Note:** Temporal activities still use direct SDK clients. MCP is for LangGraph agent layer only.
   ```

2. Update "Code Patterns" section if it references direct client usage in agents

3. Update "Gotchas" section:
   - Add note about MCP vs direct client usage
   - Note that Temporal activities are NOT migrated

4. Update any examples that show agent node code
  </action>
  <verify>
    - `grep -q "MCP" .claude/CLAUDE.md`
    - `grep -q "callMcpTool" .claude/CLAUDE.md`
  </verify>
  <done>CLAUDE.md updated with MCP architecture documentation</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete MCP migration: agents communicate with integrations via HTTP instead of direct SDK calls.
  </what-built>
  <how-to-verify>
1. Start infrastructure: `docker compose up -d postgresql temporal`
2. Wait for healthy: `docker compose ps`
3. Start integration services: `docker compose up -d linear-integration github-integration slack-integration`
4. Wait for healthy: `docker compose ps` (all integrations show healthy)
5. Start dev-agent: `docker compose up dev-agent`
6. Verify agent starts without errors
7. Check logs: `docker compose logs dev-agent` - should see startup without credential errors

**Verification checklist:**
- [ ] Dev agent container starts successfully
- [ ] No errors about missing LINEAR_ACCESS_TOKEN or GITHUB_TOKEN
- [ ] Agent connects to MCP services (may see connection logs)
- [ ] SDKs were handled correctly (check summary from Task 1):
  - If removed: `grep "@linear/sdk" packages/agents/package.json` returns nothing
  - If kept: Summary explains Temporal activities need them

Note: If Linear/GitHub webhooks are configured, you can test end-to-end by triggering a webhook. Otherwise, startup verification is sufficient.
  </how-to-verify>
  <resume-signal>Type "approved" if Docker startup works and SDK handling is correct, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] Agent package builds without SDK dependencies in nodes
- [ ] Temporal activities still have access to SDK dependencies (if needed)
- [ ] CLAUDE.md documents MCP architecture
- [ ] Docker containers start successfully
- [ ] No integration token errors in agent logs
- [ ] SDK removal/retention decision is documented and verified
</verification>

<success_criteria>
- Agent package is clean (no unused dependencies)
- MCP client exported from agent index
- CLAUDE.md reflects new architecture
- Docker verification passes (agents start without integration tokens)
- SDK dependencies are either removed (if unused) or documented (if temporal needs them)
</success_criteria>

<output>
After completion, create `.planning/phases/22.2-agent-mcp-migration/22.2-06-SUMMARY.md`

**SUMMARY must include:**
- SDK dependency status: Which were removed vs kept (and why)
- grep output showing SDK usage locations (temporal vs nodes)
- Docker startup verification result
</output>

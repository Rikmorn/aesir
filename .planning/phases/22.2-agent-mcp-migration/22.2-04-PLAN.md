---
phase: 22.2-agent-mcp-migration
plan: 04
type: execute
wave: 3
depends_on: ["22.2-02", "22.2-03"]
files_modified:
  - packages/agents/src/config.ts
  - docker-compose.yml
autonomous: true

must_haves:
  truths:
    - "Agent config no longer requires LINEAR_ACCESS_TOKEN"
    - "Agent config no longer requires GITHUB_TOKEN"
    - "Agent config includes MCP_BASE_URL or per-integration URLs"
    - "Docker containers can start without integration tokens"
  artifacts:
    - path: "packages/agents/src/config.ts"
      provides: "Agent configuration with MCP URLs"
      exports: ["config", "env"]
  key_links:
    - from: "packages/agents/src/config.ts"
      to: "packages/agents/src/mcp/client.ts"
      via: "MCP URL env vars"
      pattern: "MCP.*URL"
---

<objective>
Remove integration tokens from agent config and add MCP URL configuration

Purpose: Agents no longer need direct integration credentials since they communicate via MCP HTTP. This removes unnecessary secrets from agent environment and proves the migration is complete.

Output: Updated config.ts without integration tokens, docker-compose.yml with minimal agent env vars
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22.2-agent-mcp-migration/22.2-CONTEXT.md
@.planning/phases/22.2-agent-mcp-migration/22.2-02-SUMMARY.md
@.planning/phases/22.2-agent-mcp-migration/22.2-03-SUMMARY.md

@packages/agents/src/config.ts
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update agent config schema</name>
  <files>
    packages/agents/src/config.ts
  </files>
  <action>
Update config.ts to remove integration tokens and add MCP configuration:

1. Remove from agentEnvSchema:
   - `LINEAR_ACCESS_TOKEN` (required)
   - `LINEAR_CLIENT_ID` (optional)
   - `LINEAR_CLIENT_SECRET` (optional)
   - `LINEAR_WEBHOOK_SECRET` (optional)
   - `OAUTH_CALLBACK_URL` (optional)
   - `GITHUB_TOKEN` (required)
   - Remove entire `slack` block requirements (SLACK_BOT_TOKEN, SLACK_APP_TOKEN are for Socket Mode which is optional)
   Note: Keep `SLACK_CHANNEL_ID` if agents need to know where to post

2. Add MCP URL configuration:
   ```typescript
   // MCP service URLs (Docker network defaults)
   LINEAR_MCP_URL: z.string().url().optional(),
   GITHUB_MCP_URL: z.string().url().optional(),
   SLACK_MCP_URL: z.string().url().optional(),
   ```

3. Keep required env vars:
   - `ANTHROPIC_API_KEY` (still needed for LLM calls)
   - `LINEAR_TEAM_ID` (passed to MCP calls)
   - `GITHUB_REPO` (used for branch/PR operations)
   - Database vars (DB_HOST, DB_PORT, etc.)
   - Temporal vars
   - Observability vars

4. Update config object:
   - Remove `linear.accessToken`, `linear.clientId`, etc.
   - Remove `github.token`
   - Keep `linear.teamId`
   - Keep `github.repo`
   - Add `mcp` section:
     ```typescript
     mcp: {
       linear: { url: env.LINEAR_MCP_URL || 'http://linear-integration:3001' },
       github: { url: env.GITHUB_MCP_URL || 'http://github-integration:3002' },
       slack: { url: env.SLACK_MCP_URL || 'http://slack-integration:3003' },
     },
     ```

5. Keep slack config minimal or remove if not needed by agents directly

6. Update type exports as needed
  </action>
  <verify>
    - `pnpm --filter @aesir/agents typecheck`
    - `! grep -q "LINEAR_ACCESS_TOKEN" packages/agents/src/config.ts`
    - `! grep -q "GITHUB_TOKEN" packages/agents/src/config.ts`
    - `grep -q "MCP_URL" packages/agents/src/config.ts`
  </verify>
  <done>Agent config no longer requires integration tokens, includes MCP URLs</done>
</task>

<task type="auto">
  <name>Task 2: Update docker-compose.yml for agents</name>
  <files>
    docker-compose.yml
  </files>
  <action>
Update docker-compose.yml to remove integration tokens from agent services:

1. Update dev-agent service environment:
   Remove these env vars:
   ```yaml
   # REMOVE:
   - LINEAR_ACCESS_TOKEN=${LINEAR_ACCESS_TOKEN}
   - LINEAR_CLIENT_ID=${LINEAR_CLIENT_ID}
   - LINEAR_CLIENT_SECRET=${LINEAR_CLIENT_SECRET}
   - LINEAR_WEBHOOK_SECRET=${LINEAR_WEBHOOK_SECRET}
   - GITHUB_TOKEN=${GITHUB_TOKEN}
   - SLACK_BOT_TOKEN=${SLACK_BOT_TOKEN}
   - SLACK_APP_TOKEN=${SLACK_APP_TOKEN}
   ```

   Keep these:
   ```yaml
   # Database
   - DB_HOST=postgresql
   - DB_PORT=5432
   - DB_USER=temporal
   - DB_PASSWORD=temporal
   - DB_NAME=temporal
   - DATABASE_URL=postgresql://temporal:temporal@postgresql:5432/temporal
   - CREDENTIAL_ENCRYPTION_KEY=${CREDENTIAL_ENCRYPTION_KEY}
   # Temporal
   - TEMPORAL_ADDRESS=temporal:7233
   - TEMPORAL_NAMESPACE=default
   # LLM
   - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
   # Workspace config (passed to MCP)
   - LINEAR_TEAM_ID=${LINEAR_TEAM_ID}
   - GITHUB_REPO=${GITHUB_REPO}
   - SLACK_CHANNEL_ID=${SLACK_CHANNEL_ID}
   # MCP URLs (use Docker network names)
   - LINEAR_MCP_URL=http://linear-integration:3001
   - GITHUB_MCP_URL=http://github-integration:3002
   - SLACK_MCP_URL=http://slack-integration:3003
   # Optional observability
   - NODE_ENV=${NODE_ENV:-production}
   - LANGSMITH_TRACING=${LANGSMITH_TRACING:-false}
   - LANGSMITH_API_KEY=${LANGSMITH_API_KEY:-}
   - LANGSMITH_PROJECT=${LANGSMITH_PROJECT:-aesir-agents}
   ```

2. Update product-agent service similarly:
   Remove integration tokens, add MCP URLs

3. Add depends_on for integration services:
   ```yaml
   depends_on:
     temporal:
       condition: service_started
     postgresql:
       condition: service_healthy
     linear-integration:
       condition: service_healthy
     github-integration:
       condition: service_healthy
     slack-integration:
       condition: service_healthy
   ```
   This ensures MCP services are available before agents start.

4. Update comments to explain MCP communication model
  </action>
  <verify>
    - `! grep "LINEAR_ACCESS_TOKEN" docker-compose.yml | grep -q "dev-agent" -A 50`
    - `grep -q "LINEAR_MCP_URL" docker-compose.yml`
    - YAML is valid: `docker compose config > /dev/null`
  </verify>
  <done>Agent services in docker-compose no longer have integration tokens, have MCP URLs instead</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter @aesir/agents build` succeeds
- [ ] docker-compose.yml is valid: `docker compose config`
- [ ] grep for integration tokens in docker-compose agent sections returns empty
- [ ] MCP URLs present in both agent config and docker-compose
</verification>

<success_criteria>
- Agent configuration schema doesn't require integration tokens
- docker-compose.yml agent services have no LINEAR_ACCESS_TOKEN, GITHUB_TOKEN, etc.
- MCP URLs configured with Docker network defaults
- Agent depends_on includes integration services
</success_criteria>

<output>
After completion, create `.planning/phases/22.2-agent-mcp-migration/22.2-04-SUMMARY.md`
</output>

---
phase: 79-dashboard-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/src/lib/schema.ts
  - packages/dashboard/src/lib/sse-types.ts
  - packages/dashboard/src/components/conversation-detail/event-icon.tsx
  - packages/dashboard/src/components/conversation-detail/event-timeline.tsx
  - packages/dashboard/src/components/conversation-detail/event-renderers/lifecycle-banner.tsx
autonomous: true
requirements:
  - DASH-08

must_haves:
  truths:
    - "event.routed appears in the dashboard conversation timeline as a lifecycle banner with distinct icon and color"
    - "event.routed events arrive via SSE in real-time (ALL_EVENT_TYPES includes the 18th type)"
    - "event.routed lifecycle banner shows routing method and disposition (e.g. 'trigger_match -> new')"
  artifacts:
    - path: "packages/dashboard/src/lib/schema.ts"
      provides: "18th event type in agentEventTypeValues"
      contains: "event.routed"
    - path: "packages/dashboard/src/lib/sse-types.ts"
      provides: "18th event type in ALL_EVENT_TYPES"
      contains: "event.routed"
    - path: "packages/dashboard/src/components/conversation-detail/event-icon.tsx"
      provides: "Navigation icon and indigo-400 color for event.routed"
      contains: "event.routed"
    - path: "packages/dashboard/src/components/conversation-detail/event-timeline.tsx"
      provides: "event.routed grouped as lifecycle_banner kind"
      contains: "event.routed"
    - path: "packages/dashboard/src/components/conversation-detail/event-renderers/lifecycle-banner.tsx"
      provides: "Description extraction for event.routed payload"
      contains: "event.routed"
  key_links:
    - from: "packages/dashboard/src/lib/schema.ts"
      to: "packages/dashboard/src/lib/sse-types.ts"
      via: "Both must include event.routed for DB reads and SSE to work"
      pattern: "event\\.routed"
    - from: "packages/dashboard/src/components/conversation-detail/event-timeline.tsx"
      to: "packages/dashboard/src/components/conversation-detail/event-renderers/lifecycle-banner.tsx"
      via: "Timeline groups event.routed as lifecycle_banner, banner renders description"
      pattern: "event\\.routed.*lifecycle_banner"
---

<objective>
Propagate the `event.routed` event type (the 18th type, already in the agents schema) to the dashboard: schema, SSE types, icon/color mapping, timeline grouping, and lifecycle banner description.

Purpose: Closes the second Phase 79 gap -- `event.routed` events are emitted by the router (Phase 78) and stored in the DB, but the dashboard doesn't know about them. Without this, routing decisions are invisible in the conversation timeline.

Output: 5 files updated to include event.routed across the full dashboard rendering pipeline.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-dashboard-gap-closure/79-RESEARCH.md
@packages/dashboard/src/lib/schema.ts
@packages/dashboard/src/lib/sse-types.ts
@packages/dashboard/src/components/conversation-detail/event-icon.tsx
@packages/dashboard/src/components/conversation-detail/event-timeline.tsx
@packages/dashboard/src/components/conversation-detail/event-renderers/lifecycle-banner.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add event.routed to schema, SSE types, and icon/color mapping</name>
  <files>
    packages/dashboard/src/lib/schema.ts
    packages/dashboard/src/lib/sse-types.ts
    packages/dashboard/src/components/conversation-detail/event-icon.tsx
  </files>
  <action>
  1. In `packages/dashboard/src/lib/schema.ts`, add `"event.routed"` as the 18th entry in the `agentEventTypeValues` array, after `"agent.retry_scheduled"`. This mirrors the canonical agents schema which already has this type.

  2. In `packages/dashboard/src/lib/sse-types.ts`, add `"event.routed"` as the 18th entry in the `ALL_EVENT_TYPES` array, after `"notification.failed"`. This ensures event.routed events flow through the SSE subscription and are not filtered out.

  3. In `packages/dashboard/src/components/conversation-detail/event-icon.tsx`:
     - Add `Navigation` to the lucide-react import list (routing/direction icon).
     - Add `"event.routed": Navigation` to `iconMap`.
     - Add `"event.routed": "text-indigo-400"` to `colorMap`. Indigo-400 matches the infrastructure/active color scheme used for `agent.reopened` and `llm.response`.
  </action>
  <verify>
    - `pnpm run typecheck` passes
    - `grep "event.routed" packages/dashboard/src/lib/schema.ts packages/dashboard/src/lib/sse-types.ts packages/dashboard/src/components/conversation-detail/event-icon.tsx` shows the type in all 3 files
  </verify>
  <done>
    Dashboard schema has 18 event types. SSE subscriptions include event.routed. Icon rendering uses Navigation icon with indigo-400 color.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add event.routed timeline grouping and lifecycle banner description</name>
  <files>
    packages/dashboard/src/components/conversation-detail/event-timeline.tsx
    packages/dashboard/src/components/conversation-detail/event-renderers/lifecycle-banner.tsx
  </files>
  <action>
  1. In `packages/dashboard/src/components/conversation-detail/event-timeline.tsx`, in the `groupTimelineEvents()` function, add a case for `event.routed` before the generic fallback (line ~155). Insert after the `notification.failed` block (~line 149-153):
     ```typescript
     // Routing events
     if (event.type === "event.routed") {
       items.push({ kind: "lifecycle_banner", event });
       continue;
     }
     ```
     This ensures event.routed renders as a lifecycle banner (matching its infrastructure/routing nature) and is filterable under the "Lifecycle" category chip.

  2. In `packages/dashboard/src/components/conversation-detail/event-renderers/lifecycle-banner.tsx`, add a case to `getLifecycleDescription()` for `event.routed`, before the `default` case:
     ```typescript
     case "event.routed": {
       const disposition = typeof p.disposition === "string" ? p.disposition : "unknown";
       const method = typeof p.routingMethod === "string" ? p.routingMethod : "unknown";
       const entity = p.entity as { entityType?: string; entityId?: string } | undefined;
       const entityLabel = entity?.entityId ? ` (${entity.entityId})` : "";
       return `${method} \u2192 ${disposition}${entityLabel}`;
     }
     ```
     This formats the routing payload as a readable description, e.g., `"trigger_match -> new"` or `"correlation_fallback -> signal (LIN-456)"`. The `\u2192` is the right arrow character.
  </action>
  <verify>
    - `pnpm run typecheck` passes
    - `pnpm --filter @aesir/dashboard run build` succeeds
    - `grep "event.routed" packages/dashboard/src/components/conversation-detail/event-timeline.tsx packages/dashboard/src/components/conversation-detail/event-renderers/lifecycle-banner.tsx` shows handling in both files
  </verify>
  <done>
    event.routed events render in the conversation timeline as lifecycle banners with routing method + disposition description. Visible under the Lifecycle filter chip. Dashboard build passes.
  </done>
</task>

</tasks>

<verification>
1. `pnpm run typecheck` -- all packages pass
2. `pnpm --filter @aesir/dashboard run build` -- Next.js build succeeds
3. Grep verification: `grep -r "event.routed" packages/dashboard/src/` returns hits in all 5 target files
4. Manual: navigate to a conversation that received a routed event -- lifecycle banner shows with Navigation icon, indigo-400 color, and routing description
</verification>

<success_criteria>
- `agentEventTypeValues` in schema.ts has 18 entries including `event.routed`
- `ALL_EVENT_TYPES` in sse-types.ts has 18 entries including `event.routed`
- event-icon.tsx maps event.routed to Navigation icon and text-indigo-400
- event-timeline.tsx groups event.routed as lifecycle_banner kind
- lifecycle-banner.tsx extracts readable description from event.routed payload
- Dashboard build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/79-dashboard-gap-closure/79-02-SUMMARY.md`
</output>

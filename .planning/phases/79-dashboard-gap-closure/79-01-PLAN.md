---
phase: 79-dashboard-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agents/src/framework/worker-loop.ts
  - packages/dashboard/src/lib/pricing.ts
  - packages/dashboard/src/components/conversation-detail/event-metrics-bar.tsx
  - packages/dashboard/src/components/conversation-detail/live-detail-panels.tsx
  - packages/dashboard/src/app/conversations/[id]/page.tsx
autonomous: true
requirements:
  - DASH-08

must_haves:
  truths:
    - "EventMetricsBar displays a cost estimate (e.g. ~$0.42) alongside existing metrics"
    - "Cost estimate uses per-event model pricing -- sub-agent Haiku events priced differently from orchestrator Sonnet events"
    - "Events without a model field fall back to Sonnet pricing (approximate but non-zero)"
  artifacts:
    - path: "packages/dashboard/src/lib/pricing.ts"
      provides: "Model pricing lookup table and cost estimation utilities"
      exports: ["estimateCost", "formatCost"]
    - path: "packages/agents/src/framework/worker-loop.ts"
      provides: "model field in llm.response event payload"
      contains: "model: definition.model"
    - path: "packages/dashboard/src/components/conversation-detail/event-metrics-bar.tsx"
      provides: "Cost estimate display in metrics bar"
      contains: "formatCost"
  key_links:
    - from: "packages/agents/src/framework/worker-loop.ts"
      to: "packages/dashboard/src/lib/pricing.ts"
      via: "model field flows through event payload -> SSE -> pricing lookup"
      pattern: "model.*definition\\.model"
    - from: "packages/dashboard/src/components/conversation-detail/live-detail-panels.tsx"
      to: "packages/dashboard/src/lib/pricing.ts"
      via: "per-event cost computation from events array"
      pattern: "estimateCost"
---

<objective>
Add cost estimation to the conversation detail metrics bar. The llm.response event payload gains a `model` field, a client-side pricing utility computes per-event costs, and EventMetricsBar displays the aggregate estimate.

Purpose: Closes the DASH-08 gap -- the metrics bar currently shows tokens, duration, tool success rate, and retry count but not cost. Cost is the most actionable metric for understanding agent economics.

Output: pricing.ts utility, updated worker-loop.ts emission, updated EventMetricsBar with cost display.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-dashboard-gap-closure/79-RESEARCH.md
@packages/agents/src/framework/worker-loop.ts
@packages/dashboard/src/components/conversation-detail/event-metrics-bar.tsx
@packages/dashboard/src/components/conversation-detail/live-detail-panels.tsx
@packages/dashboard/src/app/conversations/[id]/page.tsx
@packages/dashboard/src/lib/format.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add model to llm.response payload and create pricing utility</name>
  <files>
    packages/agents/src/framework/worker-loop.ts
    packages/dashboard/src/lib/pricing.ts
  </files>
  <action>
  1. In `packages/agents/src/framework/worker-loop.ts`, find the `onResponse` callback (~line 1468-1476) where `eventLog.append` emits the `llm.response` event. Change the payload from `{ stop_reason: response.stop_reason }` to `{ stop_reason: response.stop_reason, model: definition.model }`. The `definition` variable is already in scope (used at line 1484 for `loopOptions`). This is a backward-compatible change -- the payload column is JSONB, no schema migration needed.

  2. Create `packages/dashboard/src/lib/pricing.ts` with:
     - A `ModelPricing` interface: `{ inputPerMTok: number; outputPerMTok: number }`.
     - A `MODEL_PRICING` record keyed by model ID string. Include:
       - `"claude-sonnet-4-20250514"`: { inputPerMTok: 3, outputPerMTok: 15 }
       - `"claude-sonnet-4"`: { inputPerMTok: 3, outputPerMTok: 15 }
       - `"claude-haiku-4-5-20251001"`: { inputPerMTok: 1, outputPerMTok: 5 }
       - `"claude-haiku-4.5"`: { inputPerMTok: 1, outputPerMTok: 5 }
       - `"claude-opus-4"`: { inputPerMTok: 5, outputPerMTok: 25 }
     - A `DEFAULT_PRICING` constant using Sonnet pricing (most common model in Aesir).
     - `estimateCost(inputTokens: number, outputTokens: number, model?: string): number` -- looks up model in the pricing table, falls back to DEFAULT_PRICING. Formula: `(inputTokens / 1_000_000) * pricing.inputPerMTok + (outputTokens / 1_000_000) * pricing.outputPerMTok`.
     - `formatCost(cost: number): string` -- returns `"$0.00"` for zero, `"<$0.01"` for sub-cent, `"$X.XX"` otherwise. Always prefixed with `~` when displayed in the UI (the caller adds the tilde, not this function, to keep the utility pure).
  </action>
  <verify>
    - `pnpm run typecheck` passes (worker-loop.ts change compiles, pricing.ts exports resolve)
    - `grep -n "model: definition.model" packages/agents/src/framework/worker-loop.ts` shows the new field
    - `grep -n "estimateCost\|formatCost" packages/dashboard/src/lib/pricing.ts` shows both exports
  </verify>
  <done>
    llm.response events include model field in payload. pricing.ts exports estimateCost and formatCost with correct Anthropic pricing for Sonnet, Haiku, and Opus model families.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire cost estimate into EventMetricsBar via LiveDetailPanels</name>
  <files>
    packages/dashboard/src/components/conversation-detail/event-metrics-bar.tsx
    packages/dashboard/src/components/conversation-detail/live-detail-panels.tsx
    packages/dashboard/src/app/conversations/[id]/page.tsx
  </files>
  <action>
  1. In `event-metrics-bar.tsx`:
     - Add `costEstimate: number` to `EventMetricsBarProps` interface.
     - After the retry count section (line ~54-61), add a cost display block:
       ```
       {costEstimate > 0 && (
         <>
           <span className="text-border">|</span>
           <span className="font-mono tabular-nums">
             ~{formatCost(costEstimate)}
           </span>
         </>
       )}
       ```
     - Import `formatCost` from `@/lib/pricing`.

  2. In `live-detail-panels.tsx`:
     - Import `estimateCost` from `@/lib/pricing`.
     - Add `initialCostEstimate: number` to `LiveDetailPanelsProps`.
     - Add `costEstimate` state initialized from `initialCostEstimate`.
     - Add a reset useEffect syncing `initialCostEstimate` to state (same pattern as `initialTokenInput`).
     - In the SSE event handler (around line 175-189), after computing `newInput`/`newOutput`, compute the incremental cost for new events:
       ```typescript
       const newCost = newEvents.reduce((sum, e) => {
         if (e.type === "llm.response") {
           const model = typeof e.payload.model === "string" ? e.payload.model : undefined;
           return sum + estimateCost(e.tokenCountInput ?? 0, e.tokenCountOutput ?? 0, model);
         }
         return sum;
       }, 0);
       if (newCost > 0) setCostEstimate((prev) => prev + newCost);
       ```
     - Pass `costEstimate` to `<EventMetricsBar costEstimate={costEstimate} />`.

  3. In `page.tsx` (conversations/[id]/page.tsx):
     - Import `estimateCost` from `@/lib/pricing`.
     - After the `initialTokenOutput` reduce (line ~44-47), add:
       ```typescript
       const initialCostEstimate = events.reduce((sum, e) => {
         if (e.type === "llm.response") {
           const model = typeof (e.payload as Record<string, unknown>).model === "string"
             ? (e.payload as Record<string, unknown>).model as string
             : undefined;
           return sum + estimateCost(e.tokenCountInput ?? 0, e.tokenCountOutput ?? 0, model);
         }
         return sum;
       }, 0);
       ```
     - Pass `initialCostEstimate` to `<LiveDetailPanels>`.
  </action>
  <verify>
    - `pnpm run typecheck` passes
    - `pnpm --filter @aesir/dashboard run build` succeeds (Next.js build validates RSC/client boundary, imports, types)
  </verify>
  <done>
    EventMetricsBar displays `~$X.XX` cost estimate derived from per-event model pricing. Cost updates live via SSE for active conversations. Historical events without model field fall back to Sonnet pricing.
  </done>
</task>

</tasks>

<verification>
1. `pnpm run typecheck` -- all packages pass
2. `pnpm --filter @aesir/dashboard run build` -- Next.js build succeeds
3. Manual: navigate to a conversation detail page -- metrics bar shows cost estimate after token counts
4. Manual: watch an active conversation -- cost estimate increments as new llm.response events arrive via SSE
</verification>

<success_criteria>
- EventMetricsBar shows a `~$X.XX` cost estimate metric alongside existing metrics
- Cost is computed per-event using model from llm.response payload (Sonnet vs Haiku pricing)
- New llm.response events emitted by worker-loop.ts include `model` in their payload
- pricing.ts is a self-contained utility with no external dependencies
- Dashboard build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/79-dashboard-gap-closure/79-01-SUMMARY.md`
</output>

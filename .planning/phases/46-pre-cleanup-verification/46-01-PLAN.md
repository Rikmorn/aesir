---
phase: 46-pre-cleanup-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/46-pre-cleanup-verification/46-AUDIT-REPORT.md
  - .planning/phases/46-pre-cleanup-verification/46-DELETION-MANIFEST.md
autonomous: true

must_haves:
  truths:
    - "Every @temporalio/* import is mapped and confirmed to be in dead code only"
    - "Every file/directory for deletion is enumerated with verified reason"
    - "Barrel export chains crossing the live/dead boundary are identified with refactoring steps"
    - "Old database tables (tasks, context_snapshots, execution_traces) confirmed to have no active v2.3 writers"
    - "Package.json scripts and dependencies referencing Temporal or old services are catalogued"
    - "Deletion manifest provides three-phase ordered plan (refactor references, delete files, remove deps) with typecheck gates"
  artifacts:
    - path: ".planning/phases/46-pre-cleanup-verification/46-AUDIT-REPORT.md"
      provides: "Five-area audit findings with evidence"
    - path: ".planning/phases/46-pre-cleanup-verification/46-DELETION-MANIFEST.md"
      provides: "Ordered deletion manifest for Phase 47 execution"
  key_links:
    - from: "46-AUDIT-REPORT.md"
      to: "46-DELETION-MANIFEST.md"
      via: "Audit findings feed manifest ordering"
    - from: "46-DELETION-MANIFEST.md"
      to: "Phase 47 plans"
      via: "Phase 47 executes against this manifest"
---

<objective>
Audit all five verification areas and produce the ordered deletion manifest for Phase 47.

Purpose: This phase gates Phase 47 cleanup. The audit confirms v2.3 cutover is complete (zero Temporal in live code paths), maps the dead code boundary precisely, and the manifest gives Phase 47 an executable ordered plan with typecheck gates between steps.

Output: 46-AUDIT-REPORT.md (evidence-backed findings) and 46-DELETION-MANIFEST.md (three-phase ordered deletion plan).
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-pre-cleanup-verification/46-CONTEXT.md
@.planning/phases/46-pre-cleanup-verification/46-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Five-Area Codebase Audit</name>
  <files>.planning/phases/46-pre-cleanup-verification/46-AUDIT-REPORT.md</files>
  <action>
Run exhaustive codebase audits across five areas and write findings to 46-AUDIT-REPORT.md. The research document (46-RESEARCH.md) provides a strong starting point -- VALIDATE each finding against the actual codebase and extend with any missed items.

**Area 1: @temporalio/* Dependency Audit**
- Grep for all `@temporalio` imports across the entire codebase (packages/agents, packages/platform, packages/types)
- For each file with an import, trace whether it is reachable from `service/main.ts` (live) or only from dead entry points (dev-agent/main.ts, product-agent/main.ts, router/main.ts, worker.ts files)
- Check for transitive dependencies: run `pnpm ls @temporalio/client @temporalio/worker @temporalio/workflow @temporalio/activity` in both agents and platform packages
- Confirm: ZERO @temporalio imports in live code paths

**Area 2: Dead Code Boundary Mapping**
- Enumerate all files/directories that are dead code (unreachable from service/main.ts)
- Trace barrel export chains: `agents/src/index.ts` -> `shared/index.ts` -> `shared/temporal/index.ts`. Check what is re-exported transitively.
- Trace: `dev-agent/index.ts` re-exports from `worker.ts` -- identify exactly which exports
- Trace: `router/index.ts` re-exports `routeEventLegacy`, `executeFastPath`, `matchFastPath`, `RouterDeps` -- identify which are dead
- Trace: `platform/src/index.ts` -> `temporal/index.js` re-exports
- Trace: `types/src/index.ts` -> `temporal/index.js` re-exports
- For each file in `router/fast-path.ts`: determine if `matchFastPath` is called by any live code (v2.3 routeEvent does NOT call it -- confirm this). Determine if `DETERMINISTIC_RULES`, `executeFastPath` are live.
- Identify files that need REFACTORING (import changes, barrel export cleanup) vs files that can be DELETED outright
- Check `cost-tracking.ts` and `trace-recorder.ts` -- grep for their exported function names (`getTaskTokenUsage`, `createTraceRecorder`) in all non-test files to confirm they are dead
- Check `shared/db/index.ts` barrel for what it re-exports from dead modules

**Area 3: Database Migration Audit**
- Grep for `tasks`, `context_snapshots`, `execution_traces` table references in active code (exclude test files, exclude `schema.ts` definitions)
- Grep for `task-store`, `context-manager`, `trace-recorder`, `cost-tracking` imports in non-temporal, non-dead code
- Confirm old tables have NO active v2.3 writers
- Check which ID generators in `@aesir/types` (`createId.agentTask`, `createId.contextSnapshot`, `createId.executionTrace`) would become unused after table definition removal

**Area 4: Package.json Audit**
- Check `packages/agents/package.json` for @temporalio/* deps and legacy scripts
- Check `packages/platform/package.json` for @temporalio/* deps
- Check root `package.json` for scripts referencing Temporal, dev-agent, product-agent
- Run `pnpm typecheck` and `pnpm lint` to confirm they pass with zero errors
- Document all entries to remove

**Area 5: Router Module Integrity + Test Breakage Analysis**
- Verify `service/main.ts` -> `router/router.ts:routeEvent()` has NO Temporal imports in the call chain
- Check `router/router.ts` imports: confirm `matchFastPath` and `executeFastPath` are imported but only used by `routeEventLegacy` (dead)
- Grep for any non-skipped test files that import from `@aesir/agents` barrel and transitively depend on temporal exports
- Check `@aesir/test-utils` for any Temporal references
- Document exactly which test files in `shared/db/` (task-store.test.ts, context-manager.test.ts, trace-recorder.test.ts, cost-tracking.test.ts) should be deleted with their source files

**Format the report** with sections per area, each containing:
- Findings (with file paths and line numbers)
- Evidence (grep commands and results)
- Conclusion (what's safe, what needs attention)
- Items for the deletion manifest
  </action>
  <verify>
- 46-AUDIT-REPORT.md exists with all 5 areas covered
- `pnpm typecheck` passes (run it as part of audit, record result)
- `pnpm lint` passes (run it as part of audit, record result)
- Every @temporalio import is accounted for with live/dead classification
  </verify>
  <done>All five audit areas completed with evidence. typecheck and lint pass. Every finding traced to specific files with line references.</done>
</task>

<task type="auto">
  <name>Task 2: Three-Phase Deletion Manifest</name>
  <files>.planning/phases/46-pre-cleanup-verification/46-DELETION-MANIFEST.md</files>
  <action>
Using the audit findings from 46-AUDIT-REPORT.md (Task 1), produce the ordered deletion manifest in 46-DELETION-MANIFEST.md.

**Structure the manifest in three phases with typecheck gates:**

**PHASE A: Refactor Live-to-Dead References**
For each live file that imports from dead code, provide:
- File path
- Current import line(s) to change
- What to replace with (inline constants, remove import, etc.)
- Why this must happen before deletion

Known Phase A items (validate against audit):
1. `router/fast-path.ts` -- imports `planApprovalSignal`, `prCompletionSignal`, `escalationResolvedSignal` from `shared/temporal/signals.js`. These are used for `.name` string access. Replace with inline string constants OR delete entire functions if they are dead code (executeFastPath, matchFastPath, DETERMINISTIC_RULES -- all legacy Temporal-oriented).
2. `router/router.ts` -- imports `matchFastPath`, `executeFastPath` from `fast-path.js` and `routeViaAgentLoop` from `slow-path.js`. The legacy functions `routeEventLegacy`, `handleRoutingFailureLegacy`, `sendRoutingAlertLegacy` use these. If all legacy code is removed from router.ts, the imports can be removed.
3. `router/types.ts` -- imports `Client` from `@temporalio/client` for `RouterDeps` type. Remove `RouterDeps`, `SignalAction`, `StartAction` types.
4. `router/index.ts` -- re-exports `routeEventLegacy`, `executeFastPath`, `matchFastPath`, `RouterDeps`, etc. Remove dead re-exports.
5. `shared/index.ts` -- `export * from "./temporal/index.js"` line. Remove it.
6. `dev-agent/index.ts` -- re-exports from `worker.ts`. Remove dead re-exports.
7. `agents/src/index.ts` -- may need cleanup after shared/index.ts and dev-agent/index.ts changes. Check transitive exports.
8. `platform/src/index.ts` -- `export * from "./temporal/index.js"` line. Remove it.
9. `platform/src/logging/index.ts` -- `createTemporalLogger` export. Remove it.
10. `types/src/index.ts` -- `export * from "./temporal/index.js"` line. Remove it.

Gate: `pnpm typecheck && pnpm lint`

**PHASE B: Delete Dead Files and Directories**
Ordered list of directories and files to delete:
- `packages/agents/src/shared/temporal/` (entire directory including all test files)
- `packages/agents/src/dev-agent/api/` (entire directory)
- `packages/agents/src/product-agent/api/` (entire directory)
- `packages/agents/src/dev-agent/classification/` (entire directory)
- `packages/agents/src/dev-agent/main.ts`
- `packages/agents/src/dev-agent/worker.ts`
- `packages/agents/src/product-agent/main.ts`
- `packages/agents/src/product-agent/worker.ts`
- `packages/agents/src/router/main.ts`
- `packages/agents/src/router/tools/start-workflow.ts`
- `packages/agents/src/router/tools/query-workflows.ts`
- `packages/agents/src/router/tools/signal-workflow.ts`
- `packages/platform/src/temporal/` (entire directory)
- `packages/platform/src/logging/temporal-logger.ts`
- `packages/types/src/temporal/` (entire directory)
- Dead store files: `shared/db/task-store.ts`, `shared/db/context-manager.ts`, `shared/db/trace-recorder.ts`, `shared/db/cost-tracking.ts` and their test files
- Old table definitions from `shared/db/schema.ts` (contextSnapshots, tasks, executionTraces -- remove definitions, keep file)

For each entry, note:
- What it is
- Why it's dead (unreachable from service/main.ts)
- Any test files deleted with it

Gate: `pnpm typecheck && pnpm lint`

**PHASE C: Remove Dependencies and Config Artifacts**
- `packages/agents/package.json`: remove @temporalio/activity, @temporalio/client, @temporalio/worker, @temporalio/workflow. Remove legacy scripts (dev-agent, product-agent).
- `packages/platform/package.json`: remove @temporalio/client, @temporalio/worker, @temporalio/workflow.
- Root `package.json`: remove/update infra:up, infra:logs, docker:agent:dev, docker:agent:product, agent:dev, agent:product scripts.
- `docker-compose.yml`: rename POSTGRES_USER/PASSWORD/DB from "temporal" to "aesir" (cosmetic). Rename volume. Update healthcheck pg_isready user.
- `.env.example`: remove TEMPORAL_ADDRESS if present. Remove PRODUCT_AGENT_URL if still present.
- Create DROP TABLE migration for old tables (tasks, context_snapshots, execution_traces).
- Remove unused ID generators from `@aesir/types` (createId.agentTask, createId.contextSnapshot, createId.executionTrace) -- only if confirmed unused after Phase B.

Gate: `pnpm typecheck && pnpm lint && pnpm test:fast`

**For each step in each phase, include:**
- Step number (sequential within phase)
- Action: delete | refactor | remove-dependency
- Files affected (exact paths)
- What to change (before/after where useful)
- Risk: LOW | MEDIUM (with explanation)

**Phase 47 guidance section at the end:**
- Recommended execution order within Phase 47 plans
- Which steps can be parallelized
- Where to stop and run typecheck gates
- Test breakage expectations (168 skipped tests deleted with shared/temporal/, db store tests deleted with store files)
  </action>
  <verify>
- 46-DELETION-MANIFEST.md exists with three phases (A, B, C)
- Each phase has a typecheck gate
- Every file/directory from the audit report is accounted for in the manifest
- Phase A refactoring steps are ordered so typecheck passes after all are applied
- Phase B deletions are ordered so no Phase B step breaks a later Phase B step
- Phase C includes package.json, Docker, and migration items
- Phase 47 guidance section exists
  </verify>
  <done>Complete three-phase deletion manifest with every dead file/directory enumerated, every barrel export refactoring step specified, every dependency to remove listed, and typecheck gates between phases. Phase 47 can execute against this manifest without additional investigation.</done>
</task>

</tasks>

<verification>
1. 46-AUDIT-REPORT.md covers all 5 areas with evidence
2. 46-DELETION-MANIFEST.md has three ordered phases with typecheck gates
3. pnpm typecheck passes (zero errors)
4. pnpm lint passes (zero errors)
5. Every @temporalio/* import is accounted for (confirmed dead)
6. No live code path imports from shared/temporal/ (confirmed)
7. Old database tables confirmed to have no active v2.3 writers
</verification>

<success_criteria>
- Audit confirms zero @temporalio imports in live code paths
- Deletion manifest provides complete, ordered, executable plan for Phase 47
- pnpm typecheck and pnpm lint pass with zero errors
- All barrel export chains crossing live/dead boundary are identified with specific refactoring steps
- Database audit confirms old tables are safe to drop
</success_criteria>

<output>
After completion, create `.planning/phases/46-pre-cleanup-verification/46-01-SUMMARY.md`
</output>

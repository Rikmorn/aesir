---
phase: 50-conversations-list
plan: 02
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - packages/dashboard/src/components/conversations/status-badge.tsx
  - packages/dashboard/src/components/conversations/columns.tsx
  - packages/dashboard/src/components/conversations/data-table.tsx
  - packages/dashboard/src/components/conversations/data-table-toolbar.tsx
  - packages/dashboard/src/components/conversations/data-table-pagination.tsx
  - packages/dashboard/src/app/conversations/page.tsx
  - packages/dashboard/src/app/conversations/loading.tsx
autonomous: true

must_haves:
  truths:
    - "Navigating to /conversations shows a table of all conversations with columns for agent name, status badge, duration, token usage, and last activity"
    - "Filtering by status (multi-select) narrows the list correctly"
    - "Filtering by agent type (multi-select) narrows the list correctly"
    - "Filtering by time range (1h, 24h, 7d) narrows the list correctly"
    - "Filtering by has-errors (boolean toggle) narrows the list correctly"
    - "Changing any filter resets pagination to page 1"
    - "Page-based pagination works and shows correct page count"
    - "Filter state is persisted in the URL (shareable links)"
    - "Empty state is shown when no conversations match filters"
    - "Loading skeleton appears during page transitions"
  artifacts:
    - path: "packages/dashboard/src/app/conversations/page.tsx"
      provides: "Server component that reads searchParams, calls service, renders DataTable"
      min_lines: 30
    - path: "packages/dashboard/src/components/conversations/data-table.tsx"
      provides: "Client DataTable component using TanStack Table with manual pagination"
      contains: "manualPagination"
    - path: "packages/dashboard/src/components/conversations/data-table-toolbar.tsx"
      provides: "Filter toolbar with status, agent, time range, and has-errors controls"
      contains: "useQueryStates"
    - path: "packages/dashboard/src/components/conversations/columns.tsx"
      provides: "TanStack column definitions for conversation table"
      contains: "ColumnDef"
    - path: "packages/dashboard/src/components/conversations/status-badge.tsx"
      provides: "Color-coded status badge component"
      contains: "statusConfig"
    - path: "packages/dashboard/src/components/conversations/data-table-pagination.tsx"
      provides: "Page-based pagination controls"
      contains: "pageCount"
    - path: "packages/dashboard/src/app/conversations/loading.tsx"
      provides: "Skeleton loading state matching table layout"
      contains: "Skeleton"
  key_links:
    - from: "packages/dashboard/src/app/conversations/page.tsx"
      to: "packages/dashboard/src/services/conversations.ts"
      via: "Server-side data fetch in server component"
      pattern: "listConversations|getDistinctAgentDefinitions"
    - from: "packages/dashboard/src/components/conversations/data-table-toolbar.tsx"
      to: "URL search params"
      via: "nuqs useQueryStates with shallow: false"
      pattern: "shallow.*false"
    - from: "packages/dashboard/src/components/conversations/columns.tsx"
      to: "packages/dashboard/src/lib/format.ts"
      via: "Format utility imports for cell rendering"
      pattern: "formatDuration|formatTokenCount|formatRelativeTime"
    - from: "packages/dashboard/src/components/conversations/data-table.tsx"
      to: "@tanstack/react-table"
      via: "useReactTable with manualPagination"
      pattern: "useReactTable"
---

<objective>
Build the complete conversations list UI: column definitions, status badges, DataTable with TanStack Table, filter toolbar with nuqs URL state, pagination controls, loading skeleton, and the server component page at `/conversations`.

Purpose: This is the user-facing deliverable of Phase 50 -- the actual page where users see all agent conversations with filtering and pagination.

Output: A fully functional `/conversations` page with filterable, paginated table showing agent name, status badge, duration, token usage, last activity, and error indicators.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-conversations-list/50-CONTEXT.md
@.planning/phases/50-conversations-list/50-RESEARCH.md
@.planning/phases/50-conversations-list/50-01-SUMMARY.md

@packages/dashboard/src/lib/schema.ts
@packages/dashboard/src/services/conversations.ts
@packages/dashboard/src/lib/format.ts
@packages/dashboard/src/lib/utils.ts
@packages/dashboard/src/app/layout.tsx
@packages/dashboard/components.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create status badge and column definitions</name>
  <files>
    packages/dashboard/src/components/conversations/status-badge.tsx
    packages/dashboard/src/components/conversations/columns.tsx
  </files>
  <action>
    **1. Create `src/components/conversations/status-badge.tsx`:**

    A reusable component for rendering conversation status as a colored badge. This will be reused in Phase 51+ views.

    - Import `Badge` from `@/components/ui/badge` and `cn` from `@/lib/utils`
    - Define a `statusConfig` record mapping status strings to `{ label: string; className: string }`:
      - `queued`: slate colors (bg-slate-100 text-slate-700, dark: bg-slate-800 text-slate-300)
      - `running`: blue colors (bg-blue-100 text-blue-700, dark: bg-blue-900 text-blue-300)
      - `waiting`: amber colors (bg-amber-100 text-amber-700, dark: bg-amber-900 text-amber-300)
      - `completed`: emerald colors (bg-emerald-100 text-emerald-700, dark: bg-emerald-900 text-emerald-300)
      - `failed`: red colors (bg-red-100 text-red-700, dark: bg-red-900 text-red-300)
      - `cancelled`: gray colors (bg-gray-100 text-gray-700, dark: bg-gray-800 text-gray-300)
    - Export `StatusBadge({ status }: { status: string })` -- looks up config, falls back to raw status string and empty className for unknown statuses
    - Use Badge `variant="outline"` with the config className applied via `cn("font-medium", config.className)`
    - This is a server component (no "use client" needed) since it takes props and renders static markup

    **2. Create `src/components/conversations/columns.tsx`:**

    "use client" directive required (TanStack Table column defs use accessors that run client-side).

    - Import `ColumnDef` type from `@tanstack/react-table`
    - Import `ConversationListItem` type from `@/services/conversations`
    - Import `StatusBadge` from `./status-badge`
    - Import `formatDuration`, `formatTokenCount`, `formatRelativeTime` from `@/lib/format`
    - Import `AlertCircle` from `lucide-react`
    - Import `Link` from `next/link`

    Export `columns: ColumnDef<ConversationListItem>[]` with these columns in order:

    1. **Agent** (`agentDefinitionId`): header "Agent", render as `<Link>` wrapping a `font-medium` span. Link href to `/conversations?agent=${agentDefinitionId}` is NOT needed here -- just display the ID as text with `font-medium`. Format the agent ID for display: replace hyphens with spaces and title-case it, or just display as-is (the raw ID like "dev-agent" is clear enough).

    2. **Status** (`status`): header "Status", render `<StatusBadge status={row.original.status} />`

    3. **Duration** (`duration`): id "duration" (computed, not a direct accessor), header "Duration". For `status === "running"`, show `<span className="text-blue-600">Running...</span>`. Otherwise call `formatDuration(row.original.createdAt, row.original.updatedAt)`.

    4. **Tokens** (`tokenUsage`): id "tokenUsage" (computed), header "Tokens". Show `formatTokenCount(row.original.tokenInput + row.original.tokenOutput)`. Use `text-muted-foreground` styling. Optionally show a tooltip or secondary text with input/output breakdown like "In: X / Out: Y" -- but keep it simple for now, just the total.

    5. **Last Activity** (`lastActivity`): header "Last Activity", render `formatRelativeTime(row.original.lastActivity)` with `text-muted-foreground` styling.

    6. **Error indicator**: id "error", header "" (empty -- icon-only column), narrow width. If `status === "failed"`, show `<AlertCircle className="h-4 w-4 text-destructive" />`. Otherwise render null.

    Make each row clickable by wrapping the Agent column cell in `<Link href={/conversations/${row.original.id}} className="hover:underline">`. The detail page (Phase 51) will be built later, but the link wiring should be in place now.
  </action>
  <verify>
    - `pnpm --filter @aesir/dashboard run typecheck` passes
    - `grep -r "statusConfig" packages/dashboard/src/components/conversations/status-badge.tsx` confirms config object
    - `grep -r "ColumnDef" packages/dashboard/src/components/conversations/columns.tsx` confirms column definitions
    - `grep -r "formatDuration\|formatTokenCount\|formatRelativeTime" packages/dashboard/src/components/conversations/columns.tsx` confirms format utility usage
  </verify>
  <done>
    - StatusBadge component renders color-coded badges for all 6 conversation statuses
    - Column definitions cover agent name, status, duration, tokens, last activity, and error indicator
    - Agent column links to conversation detail page
    - Typecheck passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Build DataTable, toolbar, pagination, loading skeleton, and page</name>
  <files>
    packages/dashboard/src/components/conversations/data-table.tsx
    packages/dashboard/src/components/conversations/data-table-toolbar.tsx
    packages/dashboard/src/components/conversations/data-table-pagination.tsx
    packages/dashboard/src/app/conversations/page.tsx
    packages/dashboard/src/app/conversations/loading.tsx
  </files>
  <action>
    **1. Create `src/components/conversations/data-table-pagination.tsx`:**

    "use client" directive.

    Props: `page: number`, `pageSize: number`, `total: number`.

    - Calculate `pageCount = Math.ceil(total / pageSize)`
    - Import `Button` from `@/components/ui/button`
    - Import `ChevronLeft`, `ChevronRight`, `ChevronsLeft`, `ChevronsRight` from `lucide-react`
    - Import `useQueryStates`, `parseAsInteger` from `nuqs`
    - Use nuqs to manage the `page` param: `const [{ page: currentPage }, setParams] = useQueryStates({ page: parseAsInteger.withDefault(1) }, { shallow: false })`
    - Show "Page X of Y" text with muted-foreground styling
    - Show "Showing X-Y of Z results" (calculate range from page, pageSize, total)
    - Render 4 navigation buttons: First (ChevronsLeft), Previous (ChevronLeft), Next (ChevronRight), Last (ChevronsRight)
    - Disable First/Previous when `page <= 1`, disable Next/Last when `page >= pageCount`
    - Each button updates the page param via `setParams({ page: newPage })`
    - Use `shallow: false` so navigation triggers server re-render
    - Layout: `flex items-center justify-between` with results text on left, page info + buttons on right

    **2. Create `src/components/conversations/data-table-toolbar.tsx`:**

    "use client" directive.

    Props: `agentDefinitions: string[]` (list of distinct agent IDs for the agent filter dropdown).

    - Import `useQueryStates`, `parseAsArrayOf`, `parseAsString`, `parseAsBoolean` from `nuqs`
    - Define filter parsers:
      ```
      status: parseAsArrayOf(parseAsString, ",")
      agent: parseAsArrayOf(parseAsString, ",")
      timeRange: parseAsString.withDefault("24h")
      hasErrors: parseAsBoolean
      ```
    - Use `useQueryStates(parsers, { shallow: false })` -- shallow: false is CRITICAL for server re-render
    - When ANY filter changes, also set `page: null` (resets to default page 1) to avoid empty page on filtered results. Use a separate `pageParser = { page: parseAsInteger }` or include page in the same parsers object.

    Actually, combine all URL params into one `useQueryStates` call to batch updates:
    ```
    const parsers = {
      page: parseAsInteger.withDefault(1),
      status: parseAsArrayOf(parseAsString, ","),
      agent: parseAsArrayOf(parseAsString, ","),
      timeRange: parseAsString.withDefault("24h"),
      hasErrors: parseAsBoolean,
    }
    const [filters, setFilters] = useQueryStates(parsers, { shallow: false })
    ```

    Build the toolbar UI:
    - **Status filter**: Use `Popover` + `Command` for a multi-select dropdown. Show a `Button` variant="outline" with text "Status" and a count badge if filters are active. Inside the popover, use `Command` with `CommandList` showing checkable items for each status: queued, running, waiting, completed, failed. Toggle status in/out of the array. When toggling, reset page to 1: `setFilters({ status: newArray, page: 1 })`. Import statuses from the conversationStatusValues in schema or hardcode the 5 filter-relevant values.
    - **Agent filter**: Same Popover + Command pattern as status. Show button "Agent" with count badge. List items from the `agentDefinitions` prop. Toggle agent IDs in/out of array.
    - **Time range filter**: Use `Select` component. Options: "Last hour" (1h), "Last 24 hours" (24h), "Last 7 days" (7d), "All time" (null/empty). On change: `setFilters({ timeRange: value, page: 1 })`.
    - **Has errors toggle**: Use `Button` variant="outline" with destructive styling when active. Text "Errors only". On click toggle: `setFilters({ hasErrors: filters.hasErrors ? null : true, page: 1 })`.
    - **Clear filters**: If any filter is active, show a "Clear filters" button (or X icon) that resets all filters to null/defaults: `setFilters({ status: null, agent: null, timeRange: "24h", hasErrors: null, page: 1 })`.

    Layout: `flex items-center gap-2` toolbar with filter controls in a row. Use `Separator` between filter groups if needed for visual clarity.

    **3. Create `src/components/conversations/data-table.tsx`:**

    "use client" directive.

    Props: `data: ConversationListItem[]`, `total: number`, `page: number`, `pageSize: number`, `agentDefinitions: string[]`.

    - Import `flexRender`, `getCoreRowModel`, `useReactTable` from `@tanstack/react-table`
    - Import `Table`, `TableBody`, `TableCell`, `TableHead`, `TableHeader`, `TableRow` from `@/components/ui/table`
    - Import `columns` from `./columns`
    - Import `DataTableToolbar` from `./data-table-toolbar`
    - Import `DataTablePagination` from `./data-table-pagination`

    - Create table instance with `useReactTable`:
      ```
      const table = useReactTable({
        data,
        columns,
        getCoreRowModel: getCoreRowModel(),
        manualPagination: true,
        pageCount: Math.ceil(total / pageSize),
      })
      ```
      `manualPagination: true` is CRITICAL -- without it TanStack Table tries to paginate client-side.

    - Render layout:
      ```
      <div className="space-y-4">
        <DataTableToolbar agentDefinitions={agentDefinitions} />
        <div className="rounded-md border">
          <Table>
            <TableHeader>
              {table.getHeaderGroups().map(headerGroup => (
                <TableRow key={headerGroup.id}>
                  {headerGroup.headers.map(header => (
                    <TableHead key={header.id}>
                      {header.isPlaceholder ? null : flexRender(header.column.columnDef.header, header.getContext())}
                    </TableHead>
                  ))}
                </TableRow>
              ))}
            </TableHeader>
            <TableBody>
              {table.getRowModel().rows?.length ? (
                table.getRowModel().rows.map(row => (
                  <TableRow key={row.id}>
                    {row.getVisibleCells().map(cell => (
                      <TableCell key={cell.id}>
                        {flexRender(cell.column.columnDef.cell, cell.getContext())}
                      </TableCell>
                    ))}
                  </TableRow>
                ))
              ) : (
                <TableRow>
                  <TableCell colSpan={columns.length} className="h-24 text-center">
                    No conversations found.
                  </TableCell>
                </TableRow>
              )}
            </TableBody>
          </Table>
        </div>
        <DataTablePagination page={page} pageSize={pageSize} total={total} />
      </div>
      ```

    - The empty state ("No conversations found.") handles when filters return zero results.

    **4. Create `src/app/conversations/page.tsx`:**

    Server component (NO "use client" directive).

    - Import `listConversations`, `getDistinctAgentDefinitions` from `@/services/conversations`
    - Import `ConversationsTable` (the DataTable component) from `@/components/conversations/data-table`
    - Import `getTimeRangeDate` from `@/lib/format`

    - Define `searchParams` as `Promise<{ page?: string; status?: string; agent?: string; timeRange?: string; hasErrors?: string }>` (Next.js 15 -- searchParams is a Promise that MUST be awaited)

    - Parse search params:
      ```
      const params = await searchParams;
      const page = Number(params.page) || 1;
      const pageSize = 25;
      const status = params.status?.split(",").filter(Boolean);
      const agent = params.agent?.split(",").filter(Boolean);
      const timeRange = params.timeRange || "24h";
      const hasErrors = params.hasErrors === "true";
      ```

    - Compute time range filter:
      - If `timeRange` is "1h", "24h", or "7d": `{ from: getTimeRangeDate(timeRange) }`
      - If `timeRange` is empty/null/"all": no time range filter (pass undefined)

    - Call service layer:
      ```
      const [{ items, total }, agentDefinitions] = await Promise.all([
        listConversations({
          status: status?.length ? status : undefined,
          agentDefinitionId: agent?.length ? agent : undefined,
          timeRange: timeRangeFilter,
          hasErrors: hasErrors || undefined,
          limit: pageSize,
          offset: (page - 1) * pageSize,
        }),
        getDistinctAgentDefinitions(),
      ]);
      ```

    - Render:
      ```
      <div className="flex flex-col gap-4 p-6">
        <div>
          <h1 className="text-2xl font-bold tracking-tight">Conversations</h1>
          <p className="text-muted-foreground">Monitor agent conversation history and status</p>
        </div>
        <ConversationsTable
          data={items}
          total={total}
          page={page}
          pageSize={pageSize}
          agentDefinitions={agentDefinitions}
        />
      </div>
      ```

    **5. Create `src/app/conversations/loading.tsx`:**

    Server component. Next.js automatically shows this during navigation/data fetching.

    - Import `Skeleton` from `@/components/ui/skeleton`
    - Render a skeleton matching the page layout:
      - Title skeleton (h-8 w-48)
      - Subtitle skeleton (h-4 w-72)
      - Toolbar skeleton: 4 buttons (h-9 w-32 each) in a flex row
      - Table skeleton: border wrapper with header row (6 skeleton cells) and 10 body rows (6 skeleton cells each), all with proper padding matching the real table
    - Match the gap/padding structure of the real page (flex flex-col gap-4 p-6)
  </action>
  <verify>
    - `pnpm --filter @aesir/dashboard run typecheck` passes
    - `pnpm --filter @aesir/dashboard run build` succeeds (Next.js build confirms server/client component boundaries are correct)
    - `ls packages/dashboard/src/components/conversations/` shows: status-badge.tsx, columns.tsx, data-table.tsx, data-table-toolbar.tsx, data-table-pagination.tsx
    - `ls packages/dashboard/src/app/conversations/` shows: page.tsx, loading.tsx
    - `grep -r "manualPagination" packages/dashboard/src/components/conversations/data-table.tsx` confirms server-side pagination
    - `grep -r "shallow.*false" packages/dashboard/src/components/conversations/data-table-toolbar.tsx` confirms server re-render trigger
    - `grep -r "await searchParams" packages/dashboard/src/app/conversations/page.tsx` confirms Next.js 15 async searchParams
  </verify>
  <done>
    - /conversations page renders a table of all conversations with agent name, status badge, duration, tokens, last activity, error indicator
    - Filter toolbar allows multi-select status, multi-select agent, time range dropdown, and has-errors toggle
    - All filters update URL search params (shareable links) and trigger server-side data re-fetch
    - Changing any filter resets page to 1
    - Page-based pagination shows correct page count and navigates between pages
    - Empty state shows when no conversations match filters
    - Loading skeleton appears during navigation and data fetching
    - typecheck and build pass
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @aesir/dashboard run typecheck` passes with zero errors
- `pnpm --filter @aesir/dashboard run build` succeeds
- `pnpm run lint` passes (or lint:fix resolves all issues)
- The /conversations route exists and renders the DataTable
- Filter URL params (status, agent, timeRange, hasErrors, page) are handled correctly
- Empty state renders when no data matches
- Loading skeleton appears during page transitions
</verification>

<success_criteria>
- Navigating to /conversations shows a table with all required columns (agent name, status badge, duration, token usage, last activity, error indicator)
- Filtering by status, agent type, time range, and has-errors narrows the list and updates the URL
- Page-based pagination works with correct page count
- The page is visually clean with proper alignment, status badge colors, and empty state
- All filter combinations produce correct results (no stale data, no wrong page)
</success_criteria>

<output>
After completion, create `.planning/phases/50-conversations-list/50-02-SUMMARY.md`
</output>

---
phase: 50-conversations-list
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/package.json
  - packages/dashboard/src/services/conversations.ts
  - packages/dashboard/src/lib/format.ts
  - packages/dashboard/src/app/layout.tsx
  - packages/dashboard/src/components/ui/*.tsx
autonomous: true

must_haves:
  truths:
    - "listConversations returns items with token usage (input + output) aggregated from llm.response events"
    - "listConversations returns items with triggerEventType derived from the first agent_events row"
    - "listConversations supports multi-value filters for status, agent type, time range, and has-errors"
    - "listConversations returns total count for pagination alongside filtered items"
    - "getDistinctAgentDefinitions returns unique agent IDs for filter dropdown population"
    - "Format utilities produce human-readable duration, token count, and relative time strings"
    - "Root layout wraps children with NuqsAdapter for URL state management"
  artifacts:
    - path: "packages/dashboard/src/services/conversations.ts"
      provides: "Full conversations list query with token aggregation, trigger event type, multi-filter, pagination"
      exports: ["listConversations", "getDistinctAgentDefinitions", "ConversationListItem", "ConversationListParams"]
    - path: "packages/dashboard/src/lib/format.ts"
      provides: "Duration, token count, relative time, and time range date formatters"
      exports: ["formatDuration", "formatTokenCount", "formatRelativeTime", "getTimeRangeDate"]
    - path: "packages/dashboard/src/app/layout.tsx"
      provides: "Root layout with NuqsAdapter wrapping children"
      contains: "NuqsAdapter"
    - path: "packages/dashboard/src/components/ui/table.tsx"
      provides: "shadcn/ui Table component"
    - path: "packages/dashboard/src/components/ui/badge.tsx"
      provides: "shadcn/ui Badge component"
  key_links:
    - from: "packages/dashboard/src/services/conversations.ts"
      to: "packages/dashboard/src/lib/schema.ts"
      via: "Drizzle schema imports for conversations, agentEvents, agentSessions"
      pattern: "import.*from.*@/lib/schema"
    - from: "packages/dashboard/src/services/conversations.ts"
      to: "packages/dashboard/src/lib/db.ts"
      via: "Database client import"
      pattern: "import.*db.*from.*@/lib/db"
---

<objective>
Install all Phase 50 dependencies (TanStack Table, nuqs, shadcn/ui components), expand the conversations service layer with the full token-aggregation query and multi-filter support, create formatting utilities, and wire NuqsAdapter into the root layout.

Purpose: Establish the data layer and foundational components that the conversations table UI (Plan 02) will consume. Separating data from UI ensures the service contract is solid before building the view.

Output: Expanded `conversations.ts` with `listConversations` (token aggregation, trigger event type, filters, pagination count) and `getDistinctAgentDefinitions`, `format.ts` with date/token/duration formatters, shadcn/ui components installed, NuqsAdapter wired into layout.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-conversations-list/50-CONTEXT.md
@.planning/phases/50-conversations-list/50-RESEARCH.md

@packages/dashboard/src/lib/schema.ts
@packages/dashboard/src/lib/db.ts
@packages/dashboard/src/services/conversations.ts
@packages/dashboard/src/app/layout.tsx
@packages/dashboard/package.json
@packages/dashboard/components.json
@packages/dashboard/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and shadcn/ui components</name>
  <files>packages/dashboard/package.json</files>
  <action>
    From the `packages/dashboard/` directory:

    1. Install runtime dependencies:
       ```bash
       pnpm add @tanstack/react-table nuqs
       ```

    2. Install shadcn/ui components (run each separately -- shadcn CLI prompts can conflict when batched):
       ```bash
       pnpm dlx shadcn@latest add table badge skeleton button select popover command separator input --yes
       ```
       If the CLI prompts for overwrite on any component, accept. These are fresh installs (no existing ui/ components).

    3. Verify all components were generated in `src/components/ui/`:
       - table.tsx, badge.tsx, skeleton.tsx, button.tsx, select.tsx, popover.tsx, command.tsx, separator.tsx, input.tsx
       If any are missing, run the individual `pnpm dlx shadcn@latest add <component> --yes` command.

    4. Run `pnpm run typecheck` from the dashboard directory to confirm no type errors from new deps.
  </action>
  <verify>
    - `packages/dashboard/package.json` lists `@tanstack/react-table` and `nuqs` in dependencies
    - `ls packages/dashboard/src/components/ui/` shows at least: table.tsx, badge.tsx, skeleton.tsx, button.tsx, select.tsx, popover.tsx, command.tsx, separator.tsx, input.tsx
    - `pnpm --filter @aesir/dashboard run typecheck` passes
  </verify>
  <done>All Phase 50 dependencies installed and shadcn/ui components generated with zero type errors</done>
</task>

<task type="auto">
  <name>Task 2: Expand conversations service and create format utilities</name>
  <files>
    packages/dashboard/src/services/conversations.ts
    packages/dashboard/src/lib/format.ts
    packages/dashboard/src/app/layout.tsx
  </files>
  <action>
    **1. Rewrite `src/services/conversations.ts`:**

    Replace the existing simple `listConversations` with the full implementation. Keep `countConversationsByStatus` (used by Phase 54 later). The new version must:

    - Export `ConversationListParams` interface with: `status?: string[]`, `agentDefinitionId?: string[]`, `timeRange?: { from: Date; to?: Date }`, `hasErrors?: boolean`, `limit?: number`, `offset?: number`
    - Export `ConversationListItem` interface with: `id: string`, `agentDefinitionId: string`, `status: string`, `triggerEventType: string | null`, `tokenInput: number`, `tokenOutput: number`, `createdAt: Date`, `updatedAt: Date`, `lastActivity: Date | null`, `errorMessage: string | null`
    - `listConversations(params: ConversationListParams)` returns `Promise<{ items: ConversationListItem[]; total: number }>`
    - Token aggregation via subquery: select `coalesce(sum(token_count_input), 0)` and `coalesce(sum(token_count_output), 0)` from `agent_events` where `type = 'llm.response'`, grouped by `conversation_id`, aliased as `token_agg`
    - Trigger event type via subquery: select `type` from `agent_events` where `conversation_id = c.id` ORDER BY `sequence ASC` LIMIT 1, aliased as `trigger_event`. This returns the type of the first event logged for the conversation (typically `agent.started` or `agent.resumed`). Use a SQL subquery: `sql<string | null>\`(SELECT type FROM agents.agent_events WHERE conversation_id = ${conversations.id} ORDER BY sequence ASC LIMIT 1)\`` and alias it as `trigger_event_type`.
    - Main query: `conversations` LEFT JOIN `agent_sessions` (on `conversation_id`) LEFT JOIN `token_agg` (on `conversation_id`), with the trigger event subquery as a selected column
    - Dynamic filter building: build array of SQL conditions, use `and(...conditions)` if non-empty, `undefined` if empty
    - Order by `created_at DESC` (newest first)
    - Separate COUNT query for pagination total using same WHERE clause
    - Map snake_case DB columns to camelCase in the return object. Use `Number()` on token values to ensure numeric type. Map the `trigger_event_type` subquery result to `triggerEventType`.

    - Export `getDistinctAgentDefinitions(): Promise<string[]>` -- selectDistinct `agent_definition_id` from conversations, return sorted array of strings.

    - Keep the existing `countConversationsByStatus` function unchanged.

    Follow the research document's code examples closely (Pattern 4, Pattern 5, Pattern 6). Import `sql, and, inArray, gte, lte, eq, desc, count` from `drizzle-orm`. Import `agentEvents, agentSessions` from `@/lib/schema` (they are already defined there).

    **2. Create `src/lib/format.ts`:**

    Export these four functions:

    - `formatDuration(start: Date, end: Date): string` -- returns "<1s", "Xs", "Xm Xs", "Xh Xm" based on diff
    - `formatTokenCount(tokens: number): string` -- returns "0", raw number if <1000, "X.Xk" if <1M, "X.XXM" otherwise
    - `formatRelativeTime(date: Date | null): string` -- returns "-" for null, "just now" for <60s, "Xm ago", "Xh ago", "Xd ago"
    - `getTimeRangeDate(range: string): Date` -- maps "1h", "24h", "7d" to Date objects; defaults to 24h for unknown values

    Follow the research document's code examples exactly.

    **3. Update `src/app/layout.tsx`:**

    Wrap the `{children}` with `NuqsAdapter` from `nuqs/adapters/next/app`. This is required for nuqs URL state hooks to work. The import path must be exactly `nuqs/adapters/next/app`. Preserve the existing `<html>`, `<body>`, metadata export, and className attributes.
  </action>
  <verify>
    - `pnpm --filter @aesir/dashboard run typecheck` passes
    - `grep -r "NuqsAdapter" packages/dashboard/src/app/layout.tsx` shows the adapter wrapping children
    - `grep -r "listConversations" packages/dashboard/src/services/conversations.ts` shows the new signature with `ConversationListParams`
    - `grep -r "getDistinctAgentDefinitions" packages/dashboard/src/services/conversations.ts` shows the function
    - `grep -r "formatDuration\|formatTokenCount\|formatRelativeTime\|getTimeRangeDate" packages/dashboard/src/lib/format.ts` shows all four formatters
    - `grep -r "token_agg\|coalesce" packages/dashboard/src/services/conversations.ts` confirms token aggregation subquery
    - `grep -r "triggerEventType\|trigger_event_type" packages/dashboard/src/services/conversations.ts` confirms trigger event type field is returned
  </verify>
  <done>
    - conversations.ts exports listConversations with token aggregation, trigger event type, multi-filter, and pagination count
    - ConversationListItem includes triggerEventType field derived from the first agent_events row
    - conversations.ts exports getDistinctAgentDefinitions for filter population
    - format.ts exports four formatting utilities (duration, tokens, relative time, time range)
    - layout.tsx wraps children with NuqsAdapter
    - typecheck passes with zero errors
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @aesir/dashboard run typecheck` passes with zero errors
- All shadcn/ui components exist in `src/components/ui/`
- `@tanstack/react-table` and `nuqs` are in package.json dependencies
- Service layer returns typed `ConversationListItem[]` with token aggregation and triggerEventType
- NuqsAdapter is wired in root layout
</verification>

<success_criteria>
- conversations.ts supports the full query pattern: multi-status filter, multi-agent filter, time range, has-errors, pagination with total count, token usage from llm.response events, trigger event type from first agent_events row
- ConversationListItem interface includes triggerEventType: string | null
- format.ts provides reusable formatters that future phases will also use
- All shadcn/ui table-related components are installed and type-check clean
- NuqsAdapter is configured so Plan 02 can use nuqs hooks immediately
</success_criteria>

<output>
After completion, create `.planning/phases/50-conversations-list/50-01-SUMMARY.md`
</output>

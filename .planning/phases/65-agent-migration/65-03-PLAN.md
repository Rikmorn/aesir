---
phase: 65-agent-migration
plan: 03
type: execute
wave: 3
depends_on: ["65-02"]
files_modified:
  - packages/agents/definitions/dev-agent/definition.yaml
  - packages/agents/definitions/dev-agent/prompt.md
autonomous: true

must_haves:
  truths:
    - "Dev-agent definition.yaml lists communication:reply, communication:ask, communication:notify instead of slack:send_message and slack:send_approval_request"
    - "Dev-agent prompt.md describes communication in domain terms without referencing Slack, Linear, or GitHub channel specifics"
    - "Dev-agent prompt.md contains a 'Working with humans' section explaining reply/ask/notify distinction and replyContext pass-through"
    - "Dev-agent prompt.md includes few-shot example showing explicit replyContext extraction from <reply_context> tag and pass-through to reply()"
  artifacts:
    - path: "packages/agents/definitions/dev-agent/definition.yaml"
      provides: "Communication tool references replacing Slack tools"
      contains: "communication:reply"
    - path: "packages/agents/definitions/dev-agent/prompt.md"
      provides: "Domain-language communication guidance"
      contains: "reply_context"
  key_links:
    - from: "packages/agents/definitions/dev-agent/prompt.md"
      to: "packages/agents/src/shared/tools/communication/"
      via: "Tool references in prompt match registered tool names"
      pattern: "reply|ask|notify"
---

<objective>
Migrate dev-agent from channel-specific Slack tools to domain-language communication primitives. Update definition.yaml tool list and rewrite prompt.md to use communication:reply/ask/notify.

Purpose: Dev-agent currently references slack:send_message and slack:send_approval_request directly. After migration, the agent reasons about communication intent (reply to the user, ask for approval, notify a channel) while infrastructure handles delivery channel resolution via replyContext.

Output: Updated definition.yaml with communication tools. Rewritten prompt.md with domain-language communication guidance, few-shot examples, and constitutional constraint about channel-agnostic messaging.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/agents/definitions/dev-agent/definition.yaml
@packages/agents/definitions/dev-agent/prompt.md
@packages/agents/definitions/PROMPT_GUIDE.md
@packages/agents/src/shared/tools/communication/reply.ts
@packages/agents/src/shared/tools/communication/ask.ts
@packages/agents/src/shared/tools/communication/notify.ts
@.planning/phases/65-agent-migration/65-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dev-agent definition.yaml tool list</name>
  <files>
    packages/agents/definitions/dev-agent/definition.yaml
  </files>
  <action>
In the `tools:` list of dev-agent's definition.yaml:

1. **Remove:**
   - `slack:send_message`
   - `slack:send_approval_request`

2. **Add:**
   - `communication:reply`
   - `communication:ask`
   - `communication:notify`

Place the communication tools in the same position where the Slack tools were (after GitHub tools, before subAgents). All other tools remain unchanged (codebase, coordination, task, linear, github tools stay).

The resulting tools section should look like:
```yaml
tools:
  - codebase:read_file
  - codebase:search_codebase
  - codebase:list_directory
  - coordination:spawn_agent
  - coordination:request_human_input
  - coordination:wait_for
  - task:create_task
  - task:complete_task
  - task:pause_task
  - task:handoff_task
  - task:list_tasks
  - task:get_task_context
  - linear:get_issue
  - linear:update_issue_status
  - github:create_branch
  - github:create_commit
  - github:create_pull_request
  - github:get_pull_request
  - communication:reply
  - communication:ask
  - communication:notify
```
  </action>
  <verify>
    pnpm --filter @aesir/agents run build
  </verify>
  <done>
    Dev-agent definition.yaml lists communication:reply, communication:ask, communication:notify. No slack:* tools remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite dev-agent prompt.md for domain-language communication</name>
  <files>
    packages/agents/definitions/dev-agent/prompt.md
  </files>
  <action>
Make three specific changes to dev-agent prompt.md per the locked decisions, plus add a communication guidance section. Preserve the existing prompt structure (identity, constraints, domain_knowledge, examples, tools, context sections).

**Change 1: Replace `<tools>` Slack category (lines ~137-138)**

Replace the current Slack section:
```
**Slack:**
Sending status updates, notifications, and interactive approval requests with approve/reject buttons.
```

With a Communication section:
```
**Communication:**
Replying to the human who triggered this conversation, asking questions or requesting approvals, and sending proactive notifications to channels. Delivery follows the replyContext — the infrastructure determines whether the message goes to Slack, Linear, or GitHub based on where the human is talking to you.
```

**Change 2: Replace `<constraints>` line about Slack (line ~12)**

Replace:
```
- Get human approval before creating a pull request for non-trivial changes. Send a plan via Slack and use request_human_input to pause for their decision.
```

With:
```
- Get human approval before creating a pull request for non-trivial changes. Present your plan using ask() and pause for their decision with request_human_input.
```

**Change 3: Replace `<tools>` Human interaction section (line ~129)**

Replace:
```
**Human interaction:**
Requesting human input pauses execution until a human responds. Send a Slack notification first so the human knows to check -- request_human_input only pauses, it does not send any message on its own.
```

With:
```
**Human interaction:**
Requesting human input pauses execution until a human responds. Send a message first using reply() or ask() so the human knows to check -- request_human_input only pauses, it does not send any message on its own.
```

**Change 4: Add "Working with humans" section in `<domain_knowledge>`**

After the "Task Lifecycle" section (after line ~54) and before `</domain_knowledge>`, add a new subsection:

```markdown
## Working with Humans

You communicate with humans through three tools: reply, ask, and notify. Reply where they're talking to you.

- **reply()** sends a message back to whoever triggered the current conversation or signal. It needs a replyContext — the address of the channel the human is talking to you from. Extract replyContext from the `<reply_context>` tag in the signal that resumed your conversation.
- **ask()** is like reply but signals that you need a response before continuing. Use it when presenting plans for approval or asking clarifying questions. Include clear options when the decision is discrete.
- **notify()** sends a message to an explicit target channel — use it for proactive updates that aren't replies (status updates, escalation alerts). Pass the defaultNotifyTarget from the `<default_notify_target>` block in your context.

Reply and ask need replyContext (from a signal). Notify needs an explicit target (no signal needed).

The replyContext is the address where the human is talking to you. Pass it through to reply() and ask() exactly as received — do not inspect or modify it. Never change your message based on what's in replyContext. Your response should read the same whether the human is on Slack, Linear, or GitHub.
```

**Change 5: Add few-shot example showing replyContext pass-through**

Add a new example after Example 6 (or replace the existing Example 6 follow-up pattern with an expanded version that shows the communication tool usage). Add as Example 7:

```markdown
**Example 7: Responding to a signal with replyContext**

A PR review signal arrives with a `<reply_context>` tag containing `{"channel":"github","owner":"my-org","repo":"my-repo","prNumber":42}`.

Reasoning: The reviewer left feedback on the PR. I need to acknowledge their comments and explain my approach. The reply_context tells me where to send my response — I pass it through to reply() without inspecting its internals. The infrastructure will route my message to the right place.

Action: Extract the replyContext JSON from the `<reply_context>` tag. Call reply() with that replyContext and my response message. Then address the review feedback — read the current code, understand the concern, and either implement the fix or explain the tradeoff.
```

**Important PROMPT_GUIDE.md compliance checks:**
- No procedural tool sequences prescribed (agent decides when to use which tool)
- Constitutional constraint about channel-agnostic messaging (locked decision)
- Few-shot example teaches reasoning about intent, not channel mechanics
- Working with humans section is goal-oriented, not a procedure
- Directive density: check total MUST/ALWAYS/NEVER count stays reasonable
  </action>
  <verify>
    Manually inspect the prompt.md to verify: (1) no "Slack" or "slack_" references remain in instructions (Slack can appear in examples as context), (2) communication:reply/ask/notify are described, (3) replyContext pass-through is explained, (4) constitutional constraint present, (5) PROMPT_GUIDE.md compliance
  </verify>
  <done>
    Dev-agent prompt.md uses domain-language communication throughout. Three Slack references replaced. Working with humans section added. Few-shot example shows replyContext pass-through. No channel-specific messaging guidance remains.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @aesir/agents run build` compiles (definition.yaml validated by AgentRegistry)
- Dev-agent definition.yaml has communication:reply, communication:ask, communication:notify
- Dev-agent definition.yaml does NOT have slack:send_message or slack:send_approval_request
- Dev-agent prompt.md contains "Working with humans" section
- Dev-agent prompt.md contains replyContext explanation and constitutional constraint
- Dev-agent prompt.md contains few-shot example with replyContext pass-through
- grep -c "slack:" definition.yaml returns 0 (no Slack tool references)
- grep -ci "slack" prompt.md shows only necessary references (none in instructions, possible in examples for context)
</verification>

<success_criteria>
- definition.yaml: 3 communication tools replace 2 Slack tools
- prompt.md: 3 Slack references replaced with domain-language equivalents
- prompt.md: "Working with humans" section explains reply/ask/notify and replyContext
- prompt.md: constitutional constraint about channel-agnostic messages
- prompt.md: few-shot example demonstrates replyContext pass-through pattern
- prompt.md: complies with PROMPT_GUIDE.md (no procedural sequences, minimal directives)
</success_criteria>

<output>
After completion, create `.planning/phases/65-agent-migration/65-03-SUMMARY.md`
</output>

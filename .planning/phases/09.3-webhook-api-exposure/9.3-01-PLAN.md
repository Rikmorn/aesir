---
phase: 9.3-webhook-api-exposure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - README.md
autonomous: false
user_setup:
  - service: cloudflare
    why: "Tunnel for exposing local webhooks to internet"
    env_vars:
      - name: CLOUDFLARE_TUNNEL_TOKEN
        source: "Cloudflare Zero Trust Dashboard -> Networks -> Tunnels -> Configure"
    dashboard_config:
      - task: "Create tunnel named 'aesir-dev'"
        location: "dash.cloudflare.com -> Zero Trust -> Networks -> Tunnels"
      - task: "Add public hostname routing to dev-agent:3001"
        location: "Tunnel config -> Public Hostname tab"
      - task: "Update Linear webhook URL to tunnel URL"
        location: "Linear -> Settings -> Webhooks"
      - task: "Update GitHub webhook URL to tunnel URL"
        location: "GitHub repo -> Settings -> Webhooks"

must_haves:
  truths:
    - "cloudflared container runs alongside dev-agent in Docker Compose"
    - "External services can reach /webhooks/linear through tunnel"
    - "External services can reach /webhooks/github through tunnel"
    - "Developer knows how to set up Cloudflare tunnel from README"
  artifacts:
    - path: "docker-compose.yml"
      provides: "cloudflared service definition"
      contains: "cloudflared"
    - path: ".env.example"
      provides: "CLOUDFLARE_TUNNEL_TOKEN placeholder"
      contains: "CLOUDFLARE_TUNNEL_TOKEN"
    - path: "README.md"
      provides: "Cloudflare Tunnel setup documentation"
      contains: "Cloudflare Tunnel"
  key_links:
    - from: "cloudflared container"
      to: "dev-agent:3001"
      via: "Docker network routing"
      pattern: "dev-agent:3001"
---

<objective>
Add Cloudflare Tunnel to expose local webhook endpoints for Linear and GitHub.

Purpose: Enable external services (Linear, GitHub) to deliver webhooks to the local dev-agent during development. Without this, webhooks sent to localhost:3001 are unreachable from the internet.

Output: Updated docker-compose.yml with cloudflared service, .env.example with tunnel token placeholder, README with setup instructions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.3-webhook-api-exposure/09.3-RESEARCH.md

# Existing files to modify
@docker-compose.yml
@.env.example
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cloudflared service to Docker Compose</name>
  <files>docker-compose.yml</files>
  <action>
Add cloudflared service to docker-compose.yml:

1. Add service `cloudflared` after `product-agent`:
   - Image: `cloudflare/cloudflared:2025.2.0`
   - Container name: `aesir-cloudflared`
   - Restart: `unless-stopped`
   - Command: `tunnel --no-autoupdate run --metrics=0.0.0.0:2000`
   - Environment: `TUNNEL_TOKEN=${CLOUDFLARE_TUNNEL_TOKEN}`
   - Networks: `aesir-network`
   - Depends_on: `dev-agent` with `condition: service_started`

2. Add optional health check sidecar `cloudflared-health`:
   - Image: `quay.io/curl/curl:8.10.1`
   - Init: true
   - Command: `sleep infinity`
   - Depends_on: `cloudflared`
   - Healthcheck: `curl -fsS http://cloudflared:2000/ready` (interval 1s, timeout 2s, retries 60)
   - Networks: `aesir-network`

3. Update header comment to include cloudflared access info:
   - Add: `Cloudflared (requires setup): exposes webhooks via Cloudflare Tunnel`

4. DO NOT remove port 3001 from dev-agent yet - keep it for local testing without tunnel.

Reference the research file for exact YAML structure.
  </action>
  <verify>
`docker compose config` validates without errors.
`grep -A 10 'cloudflared:' docker-compose.yml` shows the new service.
  </verify>
  <done>
cloudflared and cloudflared-health services defined in docker-compose.yml, using CLOUDFLARE_TUNNEL_TOKEN from environment.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Cloudflare environment variables to .env.example</name>
  <files>.env.example</files>
  <action>
Add a new section to .env.example for Cloudflare Tunnel configuration:

1. Add section after "Observability (optional)" section:

```
# ===================
# Cloudflare Tunnel (for webhook exposure)
# ===================

# Tunnel token from Cloudflare Zero Trust dashboard
# Required for external webhooks (Linear, GitHub) to reach local dev environment
# Found at: dash.cloudflare.com -> Zero Trust -> Networks -> Tunnels -> Configure
# Leave empty to run without tunnel (webhooks won't work from external services)
CLOUDFLARE_TUNNEL_TOKEN=
```

Note: Leave value empty (not a placeholder) since tunnel is optional for local-only development.
  </action>
  <verify>
`grep -A 5 'CLOUDFLARE_TUNNEL_TOKEN' .env.example` shows the new variable with documentation.
  </verify>
  <done>
CLOUDFLARE_TUNNEL_TOKEN documented in .env.example with clear instructions for obtaining it.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document Cloudflare Tunnel setup in README</name>
  <files>README.md</files>
  <action>
Add comprehensive Cloudflare Tunnel documentation to README.md:

1. Add new section "## Webhook Tunnel Setup (Cloudflare)" after "Running with Docker (Recommended)" section.

2. Content structure:
   - Brief explanation of why tunnel is needed (external webhooks can't reach localhost)
   - Prerequisites: Cloudflare account (free tier), domain added to Cloudflare
   - Step-by-step setup:
     a. Create tunnel in Zero Trust dashboard
     b. Copy tunnel token to .env
     c. Configure public hostname (dev-agent:3001)
     d. Start services with `npm run docker:up`
     e. Update webhook URLs in Linear and GitHub
   - Verification commands:
     - `docker compose logs cloudflared` to check tunnel health
     - `curl https://your-subdomain.your-domain.com/health` to test endpoint
   - Troubleshooting:
     - "Tunnel not connecting": Check CLOUDFLARE_TUNNEL_TOKEN is set
     - "Webhooks not received": Verify public hostname routes to dev-agent:3001

3. Update "Service URLs" table to include conditional cloudflared URL:
   | Cloudflared | https://your-subdomain.your-domain.com | Webhook tunnel (when configured) |

4. Add note that tunnel is optional - port 3001 still exposed for local testing.

Keep documentation concise - link to Cloudflare docs for deep dives.
  </action>
  <verify>
`grep -A 3 'Cloudflare Tunnel' README.md` shows the new section exists.
The section includes: setup steps, verification commands, troubleshooting.
  </verify>
  <done>
README documents complete Cloudflare Tunnel setup flow with prerequisites, step-by-step guide, verification, and troubleshooting.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Cloudflare Tunnel integration:
1. docker-compose.yml with cloudflared service
2. .env.example with CLOUDFLARE_TUNNEL_TOKEN
3. README with setup documentation
  </what-built>
  <how-to-verify>
1. Review docker-compose.yml:
   - cloudflared service exists with correct image and config
   - Uses CLOUDFLARE_TUNNEL_TOKEN environment variable
   - Depends on dev-agent

2. Review .env.example:
   - CLOUDFLARE_TUNNEL_TOKEN documented with instructions

3. Review README.md:
   - "Webhook Tunnel Setup" section exists
   - Setup steps are clear and complete
   - Verification commands provided

4. (Optional) If you have Cloudflare configured:
   - Set CLOUDFLARE_TUNNEL_TOKEN in .env
   - Run `npm run docker:up`
   - Check `docker compose logs cloudflared`
   - Verify tunnel connects successfully
  </how-to-verify>
  <resume-signal>Type "approved" to complete phase, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
Phase-level verification:

1. Docker Compose validates:
   ```bash
   docker compose config > /dev/null && echo "Valid"
   ```

2. All required components present:
   ```bash
   grep -q 'cloudflared:' docker-compose.yml && echo "cloudflared service: OK"
   grep -q 'CLOUDFLARE_TUNNEL_TOKEN' .env.example && echo "env var: OK"
   grep -q 'Cloudflare Tunnel' README.md && echo "docs: OK"
   ```

3. Network connectivity (with tunnel configured):
   ```bash
   docker compose up -d
   docker compose exec cloudflared curl -fsS http://localhost:2000/ready
   ```
</verification>

<success_criteria>
1. cloudflared container runs as part of `docker compose up`
2. Tunnel connects when CLOUDFLARE_TUNNEL_TOKEN is configured
3. External requests to tunnel URL reach dev-agent:3001
4. Developer can follow README to set up tunnel from scratch
5. Development still works without tunnel (port 3001 exposed)
</success_criteria>

<output>
After completion, create `.planning/phases/09.3-webhook-api-exposure/9.3-01-SUMMARY.md`
</output>

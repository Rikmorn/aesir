---
phase: 58.1-task-schema-and-service
verified: 2026-02-06T22:34:03Z
status: passed
score: 11/11 must-haves verified
---

# Phase 58.1: Task Schema and Service Verification Report

**Phase Goal:** The task and handoff data model exists in PostgreSQL with a validated service layer, enabling the tool and routing layers to be built on top

**Verified:** 2026-02-06T22:34:03Z
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | ToolContext.taskId no longer exists for sandbox container IDs — the field is now ToolContext.sandboxId | ✓ VERIFIED | `packages/agents/src/framework/types.ts:320` defines `sandboxId?: string`, worker-loop.ts:469 sets it from `conv.id`, all codebase tools use it |
| 2 | CodebaseToolDeps.taskId no longer exists — the field is now CodebaseToolDeps.sandboxId | ✓ VERIFIED | `packages/agents/src/shared/tools/types.ts:27` defines `sandboxId: string`, all 5 codebase tools destructure it |
| 3 | The query_conversations router tool uses correlationRef (not taskId) for its input parameter | ✓ VERIFIED | `packages/agents/src/router/tools/query-conversations.ts:22` defines `correlationRef` field, line 66 destructures it |
| 4 | The router system-prompt.ts references correlationRef (not taskId) in prompt text | ✓ VERIFIED | Lines 116 and 261 both reference `correlationRef` in query_conversations instructions |
| 5 | The agents.tasks and agents.task_handoffs tables exist with proper indexes, and conversations.task_id is a nullable FK to tasks | ✓ VERIFIED | Migration `0005_add_task_tables.sql` creates both tables with all specified indexes, conversations.task_id added at line 75 (no .notNull()) |
| 6 | TaskService provides CRUD operations with Zod-validated inputs | ✓ VERIFIED | `task-service.ts` exports interface with create/get/update/addHandoff/getHandoffs/getLatestHandoff/listByAssignee, all parse with Zod schemas |
| 7 | Task status transitions are enforced via CHECK constraint, handoff context requires summary (max 2000 chars, ~4KB total), metadata capped at 10KB | ✓ VERIFIED | Migration line 28 has CHECK constraint, HandoffContextSchema (lines 53-66) requires summary.min(1).max(2000) + 4KB refine, TaskMetadataSchema (lines 46-50) has 10KB refine |
| 8 | TaskService.update() sets updated_at on every update and completed_at when status becomes completed | ✓ VERIFIED | Lines 203-210 show `updated_at: now` always set, `completed_at: now` conditionally set when `status === "completed"` |
| 9 | createId.task() and createId.handoff() exist in @aesir/types | ✓ VERIFIED | `packages/types/src/utils/ids.ts:58-59` define both with correct prefixes (`task_` and `ho_`) |
| 10 | ToolContext.taskId is set from conv.task_id in the worker loop (undefined when null) | ✓ VERIFIED | `packages/agents/src/framework/worker-loop.ts:464` sets `taskId: conv.task_id ?? undefined` in ToolContext construction |
| 11 | Existing conversations without tasks continue to work unchanged | ✓ VERIFIED | All existing tests pass (552/553, only 1 pre-existing failure from Phase 57), task_id is nullable, taskId field is optional on ToolContext |

**Score:** 11/11 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/agents/src/framework/types.ts` | ToolContext with sandboxId and taskId | ✓ VERIFIED | Line 320: `sandboxId?: string`, Line 322: `taskId?: string`, both optional with clear JSDoc |
| `packages/agents/src/shared/tools/types.ts` | CodebaseToolDeps with sandboxId | ✓ VERIFIED | Line 27: `sandboxId: string` with JSDoc "Sandbox container ID identifying which dev container to use" |
| `packages/agents/src/router/tools/query-conversations.ts` | Router tool with correlationRef input | ✓ VERIFIED | Line 22: `correlationRef` field in QueryConversationsInputSchema, line 66: destructured in execute |
| `packages/agents/src/shared/db/migrations/0005_add_task_tables.sql` | Migration with correct DROP/CREATE/ALTER ordering | ✓ VERIFIED | 80 lines, proper order: DROP 3 legacy tables → CREATE tasks → indexes → CREATE handoffs → index → ALTER conversations → index |
| `packages/agents/src/shared/db/schema.ts` | Drizzle schemas for tasks and taskHandoffs | ✓ VERIFIED | Lines 252-296: tasks table, lines 300-326: taskHandoffs table, line 82: conversations.task_id FK, line 94: idx_conversations_task |
| `packages/agents/src/shared/db/schema.drizzle.ts` | Synced schema with legacy removed | ✓ VERIFIED | Contains new tasks/taskHandoffs tables, no contextSnapshots/executionTraces/old tasks definitions found |
| `packages/types/src/utils/ids.ts` | createId.task() and createId.handoff() | ✓ VERIFIED | Lines 52-53: handoff, lines 58-59: task, both return correctly prefixed IDs |
| `packages/agents/src/shared/services/task-service.ts` | TaskService factory with Zod validation | ✓ VERIFIED | 10,216 bytes, exports interface + factory, all CRUD methods present with Zod parsing |
| `packages/agents/src/shared/services/task-service.test.ts` | Unit tests for validation | ✓ VERIFIED | 22 tests, all passing, coverage for metadata 10KB limit, summary required/2000 char limit, 4KB context limit |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `packages/agents/src/framework/tool-factories.ts` | `packages/agents/src/shared/tools/types.ts` | codebaseAdapter passes ctx.sandboxId to CodebaseToolDeps.sandboxId | ✓ WIRED | Line 74: `sandboxId: ctx.sandboxId as string` in codebaseAdapter function |
| `packages/agents/src/framework/worker-loop.ts` | `packages/agents/src/framework/types.ts` | ToolContext construction uses sandboxId: conv.id and taskId: conv.task_id | ✓ WIRED | Line 469: `sandboxId: conv.id`, Line 464: `taskId: conv.task_id ?? undefined` both present in ToolContext construction |
| `packages/agents/src/shared/services/task-service.ts` | `packages/agents/src/shared/db/schema.ts` | Drizzle queries against tasks and taskHandoffs tables | ✓ WIRED | Line 21: imports tasks and taskHandoffs from schema, used in insert/select/update queries throughout |
| `packages/agents/src/shared/services/task-service.ts` | `packages/types/src/utils/ids.ts` | createId.task() and createId.handoff() for ID generation | ✓ WIRED | Line 15: `import { createId } from "@aesir/types"`, line 158: `createId.task()`, line 252: `createId.handoff()` |
| Router system-prompt.ts references | Router tool parameter | correlationRef used in prompt instructions | ✓ WIRED | Lines 116 and 261 correctly reference correlationRef (not taskId) when instructing the router to query conversations |

### Requirements Coverage

From ROADMAP.md Phase 58.1 requirements (TASK-01 through TASK-27):

| Requirement | Status | Evidence |
|-------------|--------|----------|
| TASK-01: Schema migration with tasks table | ✓ SATISFIED | Migration file exists with proper structure |
| TASK-02: Schema migration with task_handoffs table | ✓ SATISFIED | Both tables present in migration |
| TASK-03: ToolContext.taskId renamed to sandboxId | ✓ SATISFIED | All 16 files updated |
| TASK-04: New ToolContext.taskId from conv.task_id | ✓ SATISFIED | Wired in worker loop |
| TASK-05: TaskService CRUD operations | ✓ SATISFIED | All methods implemented |
| TASK-06: Handoff context validation | ✓ SATISFIED | Zod schema enforces summary required, 2000 char limit, 4KB total |
| TASK-07: Metadata size limit | ✓ SATISFIED | 10KB limit enforced by Zod refine |
| TASK-08: Status CHECK constraint | ✓ SATISFIED | Database constraint in migration |
| TASK-21-27: Various schema constraints | ✓ SATISFIED | All CHECK constraints, indexes, and FKs present |

### Anti-Patterns Found

No blocking anti-patterns found.

| Severity | Pattern | Impact |
|----------|---------|--------|
| ℹ️ Info | Self-referential FK handled by SQL migration only (not Drizzle references()) | Documented decision to avoid TypeScript circular reference issue — FK enforced by database |

### Human Verification Required

**1. Database Migration Execution**

**Test:** Run `pnpm db:migrate` against a database that has the legacy tasks table present
**Expected:** Migration succeeds, drops 3 legacy tables, creates new tasks + task_handoffs tables, adds task_id to conversations
**Why human:** Requires actual database with legacy schema state, can't verify programmatically without running migration

**2. TaskService Runtime Behavior**

**Test:** Create a task, update its status to "completed", verify completed_at is set
**Expected:** The database row has completed_at populated with the current timestamp
**Why human:** Requires actual database round-trip to verify ORM behavior matches expectations

## Verification Summary

All must-haves verified. Phase goal achieved.

**Plan 01 (Rename taskId to sandboxId):**
- ✓ ToolContext.sandboxId exists and is used by worker loop, codebase tools, spawn-agent
- ✓ CodebaseToolDeps.sandboxId exists and is destructured by all 5 codebase tools
- ✓ Router tool uses correlationRef parameter
- ✓ Router system prompt references correlationRef
- ✓ TypeScript compiles cleanly
- ✓ All tests pass (552/553, 1 pre-existing failure from Phase 57)

**Plan 02 (Task Schema and Service):**
- ✓ Migration file creates tasks and task_handoffs tables with proper structure
- ✓ Migration drops 3 legacy tables (tasks, context_snapshots, execution_traces)
- ✓ Drizzle schema.ts and schema.drizzle.ts both in sync with new tables
- ✓ conversations.task_id FK column added (nullable)
- ✓ createId.task() and createId.handoff() generate correctly prefixed IDs
- ✓ TaskService implements all CRUD methods with Zod validation
- ✓ Handoff context requires summary (max 2000 chars, 4KB total enforced)
- ✓ Task metadata capped at 10KB
- ✓ Status CHECK constraint at database level
- ✓ TaskService.update() sets updated_at always, completed_at conditionally
- ✓ ToolContext.taskId wired from conv.task_id ?? undefined
- ✓ 22 TaskService unit tests all pass

**Success Criteria Met:**
1. ✓ Tables exist with indexes, conversations.task_id nullable FK — verified by migration file structure
2. ✓ TaskService CRUD with Zod validation, CHECK constraints, handoff summary required, metadata/context size limits — verified by service implementation and tests
3. ✓ ToolContext.taskId renamed to sandboxId across codebase, new taskId from conv.task_id — verified by grep and worker loop code
4. ✓ Null task_id handled gracefully — verified by existing tests passing and nullable column definition

The foundation is in place for Phase 58.2 (agent task tools), Phase 58.3 (integration correlation), and Phase 58.4 (task-aware routing).

---

_Verified: 2026-02-06T22:34:03Z_
_Verifier: Claude (gsd-verifier)_

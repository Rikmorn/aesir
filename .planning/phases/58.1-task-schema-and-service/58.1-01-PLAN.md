---
phase: 58.1-task-schema-and-service
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agents/src/framework/types.ts
  - packages/agents/src/framework/worker-loop.ts
  - packages/agents/src/framework/tool-factories.ts
  - packages/agents/src/framework/tool-factories.test.ts
  - packages/agents/src/framework/worker-loop.test.ts
  - packages/agents/src/shared/tools/types.ts
  - packages/agents/src/shared/tools/codebase/read-file.ts
  - packages/agents/src/shared/tools/codebase/write-file.ts
  - packages/agents/src/shared/tools/codebase/run-command.ts
  - packages/agents/src/shared/tools/codebase/search-codebase.ts
  - packages/agents/src/shared/tools/codebase/list-directory.ts
  - packages/agents/src/shared/tools/codebase/codebase-tools.test.ts
  - packages/agents/src/shared/tools/coordination/spawn-agent.ts
  - packages/agents/src/shared/tools/coordination/spawn-agent.test.ts
  - packages/agents/src/router/tools/query-conversations.ts
  - packages/agents/src/router/system-prompt.ts
autonomous: true

must_haves:
  truths:
    - "ToolContext.taskId no longer exists -- the field for sandbox container IDs is now ToolContext.sandboxId"
    - "CodebaseToolDeps.taskId no longer exists -- the field is now CodebaseToolDeps.sandboxId"
    - "The query_conversations router tool uses correlationRef (not taskId) for its input parameter"
    - "The router system-prompt.ts references correlationRef (not taskId) in prompt text"
    - "All existing tests pass -- behavior is identical, only field names changed"
    - "TypeScript typecheck passes with zero errors"
  artifacts:
    - path: "packages/agents/src/framework/types.ts"
      provides: "ToolContext interface with sandboxId (renamed from taskId)"
      contains: "sandboxId"
    - path: "packages/agents/src/shared/tools/types.ts"
      provides: "CodebaseToolDeps interface with sandboxId (renamed from taskId)"
      contains: "sandboxId"
    - path: "packages/agents/src/router/tools/query-conversations.ts"
      provides: "Router tool with correlationRef input parameter"
      contains: "correlationRef"
  key_links:
    - from: "packages/agents/src/framework/tool-factories.ts"
      to: "packages/agents/src/shared/tools/types.ts"
      via: "codebaseAdapter passes ctx.sandboxId to CodebaseToolDeps.sandboxId"
      pattern: "sandboxId.*ctx\\.sandboxId"
    - from: "packages/agents/src/framework/worker-loop.ts"
      to: "packages/agents/src/framework/types.ts"
      via: "ToolContext construction uses sandboxId: conv.id"
      pattern: "sandboxId.*conv\\.id"
---

<objective>
Rename ToolContext.taskId to sandboxId across the agents package

Purpose: Clear the `taskId` name for v2.5 task primitive usage (Phase 58.1 Plan 2 and Phase 58.2). The current `taskId` field on ToolContext means "sandbox container ID" and must be renamed to avoid semantic collision with the new task concept.

Output: All 17 files in `packages/agents/src/` that use `taskId` for sandbox container identification are updated to use `sandboxId`. The router tool's `taskId` parameter (which means "correlation key") is renamed to `correlationRef`. Zero behavior changes -- only names.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/agents/gsd-executor.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58.1-task-schema-and-service/58.1-CONTEXT.md
@.planning/phases/58.1-task-schema-and-service/58.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename taskId to sandboxId in framework and codebase tools</name>
  <files>
    packages/agents/src/framework/types.ts
    packages/agents/src/framework/worker-loop.ts
    packages/agents/src/framework/tool-factories.ts
    packages/agents/src/framework/tool-factories.test.ts
    packages/agents/src/framework/worker-loop.test.ts
    packages/agents/src/shared/tools/types.ts
    packages/agents/src/shared/tools/codebase/read-file.ts
    packages/agents/src/shared/tools/codebase/write-file.ts
    packages/agents/src/shared/tools/codebase/run-command.ts
    packages/agents/src/shared/tools/codebase/search-codebase.ts
    packages/agents/src/shared/tools/codebase/list-directory.ts
    packages/agents/src/shared/tools/codebase/codebase-tools.test.ts
    packages/agents/src/shared/tools/coordination/spawn-agent.ts
    packages/agents/src/shared/tools/coordination/spawn-agent.test.ts
  </files>
  <action>
    Mechanical rename of `taskId` to `sandboxId` across these 14 files. This is a name change only -- zero logic changes.

    **ToolContext interface** (`framework/types.ts`):
    - Rename the `taskId` field to `sandboxId`
    - Update the JSDoc comment from "Task ID identifying which dev container to use" to "Sandbox container ID identifying which dev container to use"
    - Do NOT touch any other field in ToolContext

    **CodebaseToolDeps interface** (`shared/tools/types.ts`):
    - Rename `taskId` to `sandboxId`
    - Update JSDoc from "Task ID identifying which dev container to use" to "Sandbox container ID identifying which dev container to use"

    **Codebase adapter** (`framework/tool-factories.ts`):
    - In the `codebaseAdapter` function, change `taskId: ctx.taskId as string` to `sandboxId: ctx.sandboxId as string`
    - Update the JSDoc comment that mentions "taskId" to say "sandboxId"

    **Worker loop** (`framework/worker-loop.ts`):
    - In `setupSandbox` function: rename the `taskId` parameter to `sandboxId` in the opts type and all usages within the function (spawn, execute, logger calls, git operations)
    - In the ToolContext construction (~line 465): change `taskId: conv.id` to `sandboxId: conv.id`
    - In the setupSandbox call (~line 443): change `taskId: conv.id` to `sandboxId: conv.id`

    **Five codebase tool files** (read-file, write-file, run-command, search-codebase, list-directory):
    - Each file destructures `{ containerManager, taskId, logger }` from deps -- change to `{ containerManager, sandboxId, logger }`
    - Each file passes `taskId` to `containerManager.execute(taskId, ...)` -- change to `containerManager.execute(sandboxId, ...)`
    - Each file includes `taskId` in logger context objects -- change to `sandboxId`

    **spawn-agent.ts** (`shared/tools/coordination/`):
    - Line ~111: `taskId: ctx.taskId` in sub-agent context -- change to `sandboxId: ctx.sandboxId`

    **Test files** (tool-factories.test.ts, worker-loop.test.ts, codebase-tools.test.ts, spawn-agent.test.ts):
    - Update all mock/test context objects: `taskId: "..."` becomes `sandboxId: "..."`
    - Update any assertions that reference `taskId` on ToolContext to reference `sandboxId`

    **IMPORTANT -- DO NOT rename in these files (different semantic meaning):**
    - Anything in `packages/integrations/` (taskId = Linear issue ID)
    - Anything in `packages/platform/` (DevContainerManager uses taskId as its own parameter -- separate abstraction level)
    - Anything in `packages/test-utils/` (DevWorkflowState taskId = Linear task)
    - `packages/agents/src/adapters/slack.ts` (local variable from payload.taskIdentifier)
    - `packages/agents/src/shared/tools/integration/slack-tools.ts` (Slack MCP tool input field)
    - The integration test `http-layer.integration.test.ts` uses `taskIdentifier` not `taskId` -- leave it alone
  </action>
  <verify>
    Run `pnpm run typecheck` from the monorepo root -- must pass with zero errors.
    Run `pnpm test:fast` -- all existing unit tests must pass.
    Grep for remaining `taskId` in the modified files: `grep -r "taskId" packages/agents/src/framework/types.ts packages/agents/src/shared/tools/types.ts packages/agents/src/framework/tool-factories.ts` should return zero matches.
  </verify>
  <done>
    ToolContext.taskId is renamed to ToolContext.sandboxId. CodebaseToolDeps.taskId is renamed to CodebaseToolDeps.sandboxId. All 14 files updated. Typecheck passes. All unit tests pass. No behavior changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename taskId to correlationRef in router tools</name>
  <files>
    packages/agents/src/router/tools/query-conversations.ts
    packages/agents/src/router/system-prompt.ts
  </files>
  <action>
    Rename the `taskId` parameter in the router's query_conversations tool to `correlationRef`. This parameter is used to pattern-match conversation IDs (e.g., `dev-agent-{value}`, `product-agent-{value}`), not for the v2.5 task concept.

    **query-conversations.ts:**
    - In `QueryConversationsInputSchema`: rename the `taskId` field to `correlationRef`
    - Update the `.describe()` text: change "Filter by task ID to check..." to "Filter by correlation reference to check if a specific conversation exists (checks dev-agent-{correlationRef} and product-agent-{correlationRef} patterns)"
    - In the `execute` function: update the destructure from `{ taskId, agentDefinitionId }` to `{ correlationRef, agentDefinitionId }`
    - Update the conditional: `if (taskId)` becomes `if (correlationRef)`
    - Update the pattern construction: `dev-agent-${taskId}` becomes `dev-agent-${correlationRef}`, same for `product-agent-`

    **system-prompt.ts:**
    - Line 116: Change "Call query_conversations with taskId set to the threadTs value" to "Call query_conversations with correlationRef set to the threadTs value"
    - Line 261: Change "Call query_conversations with taskId = threadTs from payload" to "Call query_conversations with correlationRef = threadTs from payload"
    - Search for any other occurrences of `taskId` in the file and update them to `correlationRef` where they reference the query_conversations parameter

    This is a prompt text change (not type-checked) so manually verify the system-prompt.ts changes are correct.
  </action>
  <verify>
    Run `pnpm run typecheck` -- must pass.
    Run `pnpm test:fast` -- must pass.
    Read `packages/agents/src/router/tools/query-conversations.ts` and confirm no remaining `taskId` references.
    Read `packages/agents/src/router/system-prompt.ts` and confirm no remaining `taskId` references.
  </verify>
  <done>
    Router tool uses `correlationRef` instead of `taskId`. System prompt references updated. The `taskId` name is now completely free across the agents package for v2.5 task primitive usage.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pnpm run typecheck` passes with zero errors
2. `pnpm test:fast` passes (all existing unit tests)
3. No remaining `taskId` references in ToolContext, CodebaseToolDeps, or router tool input schema
4. Files outside `packages/agents/src/` are untouched (grep verification)
5. `containerManager.execute()` calls still pass the correct ID (now via `sandboxId` variable)
</verification>

<success_criteria>
- The name `taskId` on ToolContext and CodebaseToolDeps no longer exists
- The name `taskId` on the query_conversations router tool no longer exists
- All TypeScript compilation succeeds
- All existing tests pass without modification beyond the rename
- Zero behavior changes (identical runtime behavior)
</success_criteria>

<output>
After completion, create `.planning/phases/58.1-task-schema-and-service/58.1-01-SUMMARY.md`
</output>

---
phase: 62-router-updates
plan: 03
type: execute
wave: 2
depends_on: ["62-01", "62-02"]
files_modified:
  - packages/agents/src/router/tools/signal-conversation.test.ts
  - packages/agents/src/router/tools/start-conversation.test.ts
  - packages/agents/src/router/router.test.ts
autonomous: true

must_haves:
  truths:
    - "signal_conversation auto-injects eventReplyContext when LLM omits replyContext"
    - "signal_conversation uses LLM-provided replyContext when explicitly given (override)"
    - "start_conversation auto-injects eventReplyContext when LLM omits replyContext"
    - "start_conversation uses LLM-provided replyContext when explicitly given (override)"
    - "Slow-path dispatch passes eventReplyContext from incoming event to tool deps"
    - "Signal built by signal_conversation includes replyContext from auto-injection"
  artifacts:
    - path: "packages/agents/src/router/tools/signal-conversation.test.ts"
      provides: "Tests for replyContext auto-injection and override in signal_conversation"
      min_lines: 50
    - path: "packages/agents/src/router/tools/start-conversation.test.ts"
      provides: "Tests for replyContext auto-injection and override in start_conversation"
      min_lines: 50
    - path: "packages/agents/src/router/router.test.ts"
      provides: "Tests for slow-path eventReplyContext threading"
      contains: "eventReplyContext"
  key_links:
    - from: "packages/agents/src/router/tools/signal-conversation.test.ts"
      to: "packages/agents/src/router/tools/signal-conversation.ts"
      via: "imports createSignalConversationTool"
      pattern: "createSignalConversationTool"
    - from: "packages/agents/src/router/tools/start-conversation.test.ts"
      to: "packages/agents/src/router/tools/start-conversation.ts"
      via: "imports createStartConversationTool"
      pattern: "createStartConversationTool"
---

<objective>
Create test coverage for Phase 62 changes: signal_conversation replyContext auto-injection and override, start_conversation replyContext auto-injection and override, and slow-path eventReplyContext deps threading in router.ts.

Purpose: Validates all four Phase 62 success criteria through automated tests, ensuring replyContext propagation works correctly in both tool-level and router-level scenarios.

Output: Two new test files (signal-conversation.test.ts, start-conversation.test.ts) and additions to existing router.test.ts.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-router-updates/62-01-SUMMARY.md
@packages/agents/src/router/tools/signal-conversation.ts
@packages/agents/src/router/tools/start-conversation.ts
@packages/agents/src/router/router.ts
@packages/agents/src/router/router.test.ts
@packages/agents/src/router/types.ts
@packages/agents/src/shared/communication/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create signal_conversation and start_conversation tool tests</name>
  <files>
    packages/agents/src/router/tools/signal-conversation.test.ts
    packages/agents/src/router/tools/start-conversation.test.ts
  </files>
  <action>
**signal-conversation.test.ts:**

Create test file with these test cases for `createSignalConversationTool`:

1. Mock setup:
   - Create mock `EventRouterDeps` with mock executor (signal returns `{ action: "resumed" }`), mock logger
   - Define test replyContexts: `slackReplyContext = { channel: "slack", teamId: "T123", channelId: "C456", threadTs: "1234.5678" }` and `linearReplyContext = { channel: "linear", issueId: "issue-uuid-123" }`

2. Test cases:
   a. **"auto-injects eventReplyContext when LLM omits replyContext"**
      - Create tool with deps that have `eventReplyContext: slackReplyContext`
      - Call execute with input that has NO replyContext field
      - Verify `executor.signal` was called with a signal containing `replyContext: slackReplyContext`

   b. **"uses LLM-provided replyContext over eventReplyContext"**
      - Create tool with deps that have `eventReplyContext: slackReplyContext`
      - Call execute with input that HAS `replyContext: linearReplyContext`
      - Verify `executor.signal` was called with a signal containing `replyContext: linearReplyContext` (LLM override wins)

   c. **"omits replyContext from signal when neither LLM nor deps provides it"**
      - Create tool with deps that have NO eventReplyContext
      - Call execute with input that has NO replyContext field
      - Verify `executor.signal` was called with a signal that does NOT have a `replyContext` property (not even undefined)

   d. **"validates replyContext with ReplyContextSchema (rejects invalid)"**
      - Call execute with input that has `replyContext: { channel: "invalid" }`
      - Verify result has `isError: true`

   e. **"includes replyContext in signal alongside existing fields"**
      - Verify the signal has type, data, message, source, AND replyContext all present

**start-conversation.test.ts:**

Create test file with these test cases for `createStartConversationTool`:

1. Mock setup:
   - Create mock `EventRouterDeps` with mock executor (start returns `"conv-123"`), mock logger

2. Test cases:
   a. **"auto-injects eventReplyContext when LLM omits replyContext"**
      - Create tool with deps that have `eventReplyContext: slackReplyContext`
      - Call execute with valid input (agentDefinitionId, correlationKey, input) but NO replyContext
      - Verify `executor.start` was called with params containing `replyContext: slackReplyContext`

   b. **"uses LLM-provided replyContext over eventReplyContext"**
      - Create tool with deps that have `eventReplyContext: slackReplyContext`
      - Call execute with input that HAS `replyContext: linearReplyContext`
      - Verify `executor.start` was called with params containing `replyContext: linearReplyContext`

   c. **"omits replyContext from start params when neither LLM nor deps provides it"**
      - Create tool with deps that have NO eventReplyContext
      - Verify `executor.start` was called with params that do NOT have a `replyContext` property

Follow the existing test patterns in `router.test.ts` for mock creation (createMockLogger, createMockExecutor patterns). Use `describe`/`it` from vitest. Use `vi.fn()` for mocks.
  </action>
  <verify>
    `npx vitest run packages/agents/src/router/tools/signal-conversation.test.ts` -- all tests pass.
    `npx vitest run packages/agents/src/router/tools/start-conversation.test.ts` -- all tests pass.
  </verify>
  <done>
    - signal_conversation has 5 tests covering auto-injection, LLM override, no-context, validation, field coexistence
    - start_conversation has 3 tests covering auto-injection, LLM override, no-context
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Add slow-path eventReplyContext threading tests to router.test.ts</name>
  <files>
    packages/agents/src/router/router.test.ts
  </files>
  <action>
Add a new `describe("slow-path replyContext threading")` block to the existing `router.test.ts` file.

The slow-path is mocked (`vi.mock("./slow-path.js")`), so we can verify the deps passed to `routeViaAgentLoopV2`.

Import `routeViaAgentLoopV2` from the mock at the top (it's already mocked, just need to import and access the mock):
```typescript
import { routeViaAgentLoopV2 } from "./slow-path.js";
const mockRouteViaAgentLoop = vi.mocked(routeViaAgentLoopV2);
```

Test cases:

a. **"passes eventReplyContext to slow-path deps when incoming event has replyContext"**
   - Create an IncomingEvent with `replyContext: { channel: "slack", teamId: "T1", channelId: "C1", threadTs: "123.456" }`
   - Configure EventRouter to return `{ action: "slow_path", event: incomingEvent }`
   - Call `routeEvent()`
   - Verify `mockRouteViaAgentLoop` was called, and the second argument (deps) contains `eventReplyContext` matching the incoming event's replyContext

b. **"does not set eventReplyContext when incoming event has no replyContext"**
   - Create an IncomingEvent with NO replyContext
   - Configure EventRouter to return `{ action: "slow_path", event: incomingEvent }`
   - Call `routeEvent()`
   - Verify `mockRouteViaAgentLoop` was called, and the second argument (deps) does NOT have `eventReplyContext` set (or it's undefined)

c. **"passes Linear replyContext through slow-path deps"**
   - Create an IncomingEvent with `replyContext: { channel: "linear", issueId: "uuid-abc" }`
   - Configure EventRouter to return slow_path
   - Verify the deps passed include `eventReplyContext: { channel: "linear", issueId: "uuid-abc" }`

These tests validate that router.ts correctly threads eventReplyContext from the incoming event to the slow-path tool deps.
  </action>
  <verify>
    `npx vitest run packages/agents/src/router/router.test.ts` -- all tests pass (existing + new).
  </verify>
  <done>
    - 3 new tests in router.test.ts covering slow-path eventReplyContext threading
    - Tests verify deps argument to routeViaAgentLoopV2 contains/omits eventReplyContext correctly
    - All existing router tests remain green
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run packages/agents/src/router/tools/signal-conversation.test.ts` -- all pass
2. `npx vitest run packages/agents/src/router/tools/start-conversation.test.ts` -- all pass
3. `npx vitest run packages/agents/src/router/router.test.ts` -- all pass (existing + new)
4. `pnpm run typecheck` passes
5. `pnpm test:fast` passes (full test suite)
</verification>

<success_criteria>
- signal_conversation has tests for auto-injection, LLM override, no-context, validation, and field coexistence
- start_conversation has tests for auto-injection, LLM override, and no-context
- router.test.ts has tests for slow-path eventReplyContext deps threading with and without replyContext
- All tests pass
- Full test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/62-router-updates/62-03-SUMMARY.md`
</output>

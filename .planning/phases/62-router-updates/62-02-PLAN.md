---
phase: 62-router-updates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agents/src/router/system-prompt.ts
autonomous: true

must_haves:
  truths:
    - "Router prompt handles Linear issue comments as a routing path equivalent to Slack thread replies"
    - "Follow-up routing uses a single channel-agnostic procedure with correlation key lookup table"
    - "Intent classification section is channel-neutral (not Linear-specific)"
    - "Prompt includes minimal replyContext guidance (auto-forwarded, no LLM action needed)"
  artifacts:
    - path: "packages/agents/src/router/system-prompt.ts"
      provides: "Channel-agnostic router prompt with follow_up_routing section"
      contains: "follow_up_routing"
  key_links:
    - from: "packages/agents/src/router/system-prompt.ts"
      to: "packages/agents/src/router/slow-path.ts"
      via: "ROUTER_SYSTEM_PROMPT consumed as systemPrompt"
      pattern: "ROUTER_SYSTEM_PROMPT"
---

<objective>
Rewrite the router system prompt to replace the Slack-specific `<slack_thread_reply_routing>` section with a channel-agnostic `<follow_up_routing>` section, generalize intent classification, and add minimal replyContext guidance. Supports Linear issue comments as a first-class routing path.

Purpose: Enables the slow-path LLM to route Linear comments identically to Slack thread replies using a single unified procedure, completing the router's channel-agnostic follow-up routing.

Output: Modified system-prompt.ts with unified follow-up routing.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-router-updates/62-RESEARCH.md
@.planning/phases/62-router-updates/62-CONTEXT.md
@packages/agents/src/router/system-prompt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace slack_thread_reply_routing with channel-agnostic follow_up_routing</name>
  <files>packages/agents/src/router/system-prompt.ts</files>
  <action>
Replace the entire `<slack_thread_reply_routing>...</slack_thread_reply_routing>` XML section (lines 111-126) with a new `<follow_up_routing>` section.

The new section must:
1. Describe ONE procedure for all follow-up messages regardless of source channel
2. Include a correlation key lookup table:
   - Slack thread replies: correlationRef = threadTs from payload
   - Linear issue comments: correlationRef = issueId from payload
   - GitHub PR comments: correlationRef = owner/repo/prNumber (future, note only)
3. Procedure:
   a. Extract the correlation key from the event payload using the lookup table
   b. Call query_conversations with correlationRef set to the extracted key
   c. Examine results and conversation STATUS:
      - Active (waiting/running/queued) -> signal it (user_reply for product-agent, classified intent for dev-agent)
      - Terminal (completed/failed) -> reopen_conversation with the message text as reason, THEN signal_conversation with the appropriate signal type
      - No conversation found -> ignore with reason "No conversation found for this follow-up"
4. Product-agent conversations are conversational -- forward all replies as user_reply. Intent classification only matters for dev-agent conversations.
5. For top-level messages without a correlation key (e.g., Slack message with no threadTs), ignore unless clear actionable routing context exists.

Per CONTEXT.md locked decisions:
- Correlation key for Linear = issueId (NOT issue identifier like "AES-42")
- No match found = ignore (issues assigned via agent_session.created, not comments)
- Reopen flow: reopen first, then signal with replyContext -- the signal carries the new event's replyContext ("classify by intent, reply by origin")
- Agent echo filtering is NOT in scope -- flag as a known prerequisite with a brief note

Per CONTEXT.md: Kill the existing Linear-specific "derive conversationId" shortcut in the `<tools>` section.
  </action>
  <verify>
    File compiles: `pnpm run typecheck` (system-prompt.ts is a simple string export, should always pass).
    Grep confirms: no remaining `slack_thread_reply_routing` text. Confirm `follow_up_routing` is present.
  </verify>
  <done>
    - `<slack_thread_reply_routing>` section fully replaced by `<follow_up_routing>`
    - Correlation key lookup table includes Slack threadTs and Linear issueId
    - Single unified procedure for all follow-up routing
    - Product-agent gets user_reply; dev-agent gets classified intent
    - No conversation found = ignore
  </done>
</task>

<task type="auto">
  <name>Task 2: Generalize intent_classification and add replyContext guidance</name>
  <files>packages/agents/src/router/system-prompt.ts</files>
  <action>
Two changes to the system prompt:

**1. Generalize `<intent_classification>` section (lines 128-237):**
- The opening line currently says "Linear comments or Slack replies" -- generalize to "follow-up messages from any channel (Slack thread replies, Linear issue comments, GitHub PR comments)"
- The `user_reply` signal type description (line 102-103) currently says "Sent to product-agent conversations when a user replies in a Slack thread" -- generalize to "Sent to conversations when a user sends a follow-up message (Slack thread reply, Linear comment, etc.)"
- In Classification Guidelines (line 231-236), item 5 currently says "consider the event source (Linear comment vs Slack message) and type" -- generalize to "consider the event source and type"
- Remove any remaining Slack-specific or Linear-specific language from the intent classification section. The classification logic is channel-agnostic.

**2. Add minimal replyContext guidance:**
Per CONTEXT.md: Two sentences maximum. Add a new `<reply_context>` section AFTER `<follow_up_routing>` and BEFORE `<intent_classification>`:

```
<reply_context>
replyContext is automatically forwarded from the incoming event to signal_conversation and start_conversation calls. You do not need to pass it explicitly. Only provide an explicit replyContext if the event lacks one and you can construct the correct channel address from conversation context.
</reply_context>
```

**3. Update `<tools>` section (lines 251-279):**
- Remove the Linear-specific "derive conversationId" shortcut (lines 274-278: "For Linear comments..." block)
- Replace the "TYPICAL ROUTING FLOWS" with a single unified flow description that references the `<follow_up_routing>` procedure:
  ```
  TYPICAL ROUTING FLOW (follow-up messages):
  1. Extract correlation key from event payload (see <follow_up_routing> for key mapping)
  2. Call query_conversations with correlationRef = extracted key
  3. Check conversation status and route accordingly (see <follow_up_routing> procedure)
  ```
- Keep tool descriptions 1-5 as-is (they are correct)

**4. Update `<fast_path_context>` if needed:**
- Verify `linear.comment.created` is listed in "Events that DO reach you" -- it already is (line 41). No change needed.
  </action>
  <verify>
    `pnpm run typecheck` passes. `pnpm run lint` passes.
    Grep confirms: no "Slack thread" in isolation (should only appear in correlation key table context), "reply_context" section exists, "<tools>" section has unified flow.
  </verify>
  <done>
    - Intent classification is channel-neutral -- no Slack-specific or Linear-specific language
    - user_reply signal type description is generalized
    - `<reply_context>` section added with minimal guidance (auto-forwarded, no LLM action needed)
    - `<tools>` section has single unified routing flow, no Linear-specific shortcut
    - Classification guidelines reference generic "event source and type"
  </done>
</task>

</tasks>

<verification>
1. `pnpm run typecheck` passes with zero errors
2. `pnpm run lint` passes with zero errors
3. Grep: no occurrences of `slack_thread_reply_routing` in the codebase
4. Grep: `follow_up_routing` appears in system-prompt.ts
5. Grep: `reply_context` section appears in system-prompt.ts
6. Grep: no "derive conversationId" shortcut for Linear in the tools section
7. The prompt text is well-formed: all XML tags properly opened and closed
</verification>

<success_criteria>
- Router prompt has channel-agnostic `<follow_up_routing>` replacing Slack-specific section
- Correlation key lookup table covers Slack (threadTs) and Linear (issueId)
- Single unified procedure for all follow-up message routing
- Intent classification is channel-neutral
- Minimal `<reply_context>` guidance section exists
- Tools section has unified flow, no channel-specific shortcuts
</success_criteria>

<output>
After completion, create `.planning/phases/62-router-updates/62-02-SUMMARY.md`
</output>

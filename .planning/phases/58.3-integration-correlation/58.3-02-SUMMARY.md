---
phase: 58.3-integration-correlation
plan: 02
subsystem: api
tags: [linear, mcp, webhooks, correlation, task-tracking]

# Dependency graph
requires:
  - phase: 58.3-01
    provides: task_correlations table schema, X-Task-ID header chain, IncomingEvent.taskId field
provides:
  - Linear recordTaskCorrelation and lookupTaskCorrelation helper functions
  - MCP handler records correlations on create_issue and create_comment
  - Webhook handler looks up correlations on Comment and AgentSession events
  - Adapter extracts taskId from NormalizedEvent payload into IncomingEvent
affects: [58.3-03, 58.3-04, event-router, agent-conversations]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Fire-and-forget correlation recording via .catch(() => {})"
    - "Non-fatal lookup with undefined fallback on error"
    - "taskId injection into NormalizedEvent payload for cross-boundary propagation"

key-files:
  created:
    - packages/integrations/linear/src/db/task-correlations.ts
  modified:
    - packages/integrations/linear/src/api/mcp.ts
    - packages/integrations/linear/src/api/webhooks.ts
    - packages/integrations/linear/src/api/routes.ts
    - packages/integrations/linear/src/main.ts
    - packages/agents/src/adapters/linear.ts

key-decisions:
  - "Correlation lookup injects taskId into NormalizedEvent.payload (not a new NormalizedEvent field) to avoid schema changes"

patterns-established:
  - "Integration correlation helper pattern: record on MCP write, lookup on webhook receive"
  - "Fire-and-forget for outgoing correlations, await for incoming lookups"

# Metrics
duration: 3min
completed: 2026-02-07
---

# Phase 58.3 Plan 02: Linear Integration Correlation Summary

**Bidirectional task correlation for Linear: records on MCP create_issue/create_comment, looks up on webhook Comment/AgentSession events**

## Performance

- **Duration:** 3 min 22s
- **Started:** 2026-02-07T13:17:14Z
- **Completed:** 2026-02-07T13:20:36Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Created reusable record/lookup helper functions for Linear task correlations
- Wired correlation recording into MCP handler for create_issue and create_comment (fire-and-forget)
- Wired correlation lookup into webhook handler for Comment and AgentSession events
- Extended Linear adapter to propagate taskId from NormalizedEvent payload into IncomingEvent

## Task Commits

Each task was committed atomically:

1. **Task 1: Create Linear correlation helper functions** - `c755378` (feat)
2. **Task 2: Wire correlation recording in MCP handlers and lookup in webhooks** - `12235a7` (feat)

## Files Created/Modified
- `packages/integrations/linear/src/db/task-correlations.ts` - Record and lookup task correlation helpers
- `packages/integrations/linear/src/api/mcp.ts` - Extract X-Task-ID header, record correlations after create_issue/create_comment
- `packages/integrations/linear/src/api/webhooks.ts` - Look up task correlations for Comment and AgentSession events
- `packages/integrations/linear/src/api/routes.ts` - Pass db to webhook router deps
- `packages/integrations/linear/src/main.ts` - Pass db to createRoutes call
- `packages/agents/src/adapters/linear.ts` - Extract taskId from payload into IncomingEvent

## Decisions Made
- Injected taskId into `NormalizedEvent.payload` (not a new schema field) to avoid modifying the NormalizedEvent Zod schema and keep the change contained to Linear

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Wired db through routes.ts and main.ts to webhook handler**
- **Found during:** Task 2 (webhook correlation lookup)
- **Issue:** WebhookRouterDeps needed `db: NodePgDatabase` but `db` was not passed through the createRoutes chain
- **Fix:** Added `db` to `CreateRoutesDeps` in routes.ts and `WebhookRouterDeps` in webhooks.ts, updated main.ts to pass `db`
- **Files modified:** routes.ts, main.ts (in addition to webhooks.ts)
- **Verification:** Typecheck passes, db available in webhook handler
- **Committed in:** 12235a7 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary to wire database access for correlation lookup. No scope creep.

## Issues Encountered
- Biome formatter required multi-line formatting for `recordTaskCorrelation` calls and type assertions -- auto-fixed by running `pnpm run format`

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Linear integration fully wired for bidirectional task correlation
- GitHub (plan 03) and Slack (plan 04) follow the same pattern
- Event router can now receive taskId on Linear-originated events

## Self-Check: PASSED

---
*Phase: 58.3-integration-correlation*
*Completed: 2026-02-07*

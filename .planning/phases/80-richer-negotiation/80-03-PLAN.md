---
phase: 80-richer-negotiation
plan: 03
type: execute
wave: 2
depends_on: [80-01]
files_modified:
  - packages/agents/definitions/dev-agent/definition.yaml
  - packages/agents/definitions/dev-agent/prompt.md
  - packages/agents/definitions/qa-agent/definition.yaml
  - packages/agents/definitions/qa-agent/prompt.md
  - packages/agents/definitions/product-agent/definition.yaml
  - packages/agents/definitions/product-agent/prompt.md
autonomous: true
requirements: [NEG-07]

must_haves:
  truths:
    - "All delegation-capable agents (dev-agent, qa-agent, product-agent) have task:clarify and task:answer in their tool lists"
    - "Agent prompts describe when to counter-propose vs reject and when to clarify vs proceed with assumptions"
    - "Negotiation guidance follows the accommodating disposition hierarchy: accept > counter-propose > reject"
    - "Cost-of-being-wrong framework guides clarification decisions"
  artifacts:
    - path: "packages/agents/definitions/dev-agent/definition.yaml"
      provides: "Dev-agent tool list with task:clarify and task:answer"
      contains: "task:clarify"
    - path: "packages/agents/definitions/dev-agent/prompt.md"
      provides: "Negotiation personality guidance for dev-agent"
      contains: "counter-propose"
    - path: "packages/agents/definitions/qa-agent/definition.yaml"
      provides: "QA agent tool list with task:clarify and task:answer"
      contains: "task:clarify"
    - path: "packages/agents/definitions/qa-agent/prompt.md"
      provides: "Negotiation personality guidance for QA agent"
      contains: "counter-propose"
    - path: "packages/agents/definitions/product-agent/definition.yaml"
      provides: "Product agent tool list with task:clarify and task:answer"
      contains: "task:clarify"
    - path: "packages/agents/definitions/product-agent/prompt.md"
      provides: "Negotiation personality guidance for product agent"
      contains: "counter-propose"
  key_links:
    - from: "packages/agents/definitions/dev-agent/definition.yaml"
      to: "packages/agents/src/framework/tool-factories.ts"
      via: "Tool reference resolution at runtime"
      pattern: "task:clarify"
---

<objective>
Add the new negotiation tools (task:clarify, task:answer) to all delegation-capable agent definitions and update their system prompts with negotiation personality guidance covering when to counter-propose vs reject and when to clarify vs proceed with assumptions.

Purpose: Agents need both the tools and the judgment criteria to use them effectively. Without prompt guidance, agents default to accept/reject binary behavior even when counter-proposing or clarifying would produce better outcomes.
Output: Updated definition.yaml and prompt.md for dev-agent, qa-agent, and product-agent.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-richer-negotiation/80-CONTEXT.md
@packages/agents/definitions/PROMPT_GUIDE.md

# Agent definitions
@packages/agents/definitions/dev-agent/definition.yaml
@packages/agents/definitions/dev-agent/prompt.md
@packages/agents/definitions/qa-agent/definition.yaml
@packages/agents/definitions/qa-agent/prompt.md
@packages/agents/definitions/product-agent/definition.yaml
@packages/agents/definitions/product-agent/prompt.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add task:clarify and task:answer to agent definitions</name>
  <files>
    packages/agents/definitions/dev-agent/definition.yaml
    packages/agents/definitions/qa-agent/definition.yaml
    packages/agents/definitions/product-agent/definition.yaml
  </files>
  <action>
    Add `task:clarify` and `task:answer` to the tools list of all three delegation-capable agents. These agents already have `task:delegate` and/or `task:respond` -- they need the new negotiation tools to participate in counter-proposals and clarification flows.

    **dev-agent/definition.yaml:**
    Add after `task:respond` in the tools list:
    ```yaml
      - task:clarify
      - task:answer
    ```
    This agent both delegates (to QA, sub-agents) and can be delegated to (by product-agent). It needs both tools.

    **qa-agent/definition.yaml:**
    Add after `task:respond` in the tools list (same as above, but QA is primarily a target agent that may need to clarify):
    ```yaml
      - task:clarify
      - task:answer
    ```
    QA agent delegates to dev-agent for fixes (so needs task:answer). QA is delegated to by dev-agent (so needs task:clarify).

    **product-agent/definition.yaml:**
    Add after `task:respond` in the tools list:
    ```yaml
      - task:clarify
      - task:answer
    ```
    Product agent delegates to dev-agent. It needs task:answer for when dev-agent sends clarifications. It can receive delegations too, so task:clarify is also needed.
  </action>
  <verify>
    `grep -n "task:clarify\|task:answer" packages/agents/definitions/*/definition.yaml` shows both tools in all three agents.
  </verify>
  <done>
    - All three delegation-capable agents have task:clarify and task:answer in their tool lists
    - Tool ordering is logical (grouped with other task tools)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add negotiation personality guidance to agent prompts</name>
  <files>
    packages/agents/definitions/dev-agent/prompt.md
    packages/agents/definitions/qa-agent/prompt.md
    packages/agents/definitions/product-agent/prompt.md
  </files>
  <action>
    Add a `<negotiation>` section to each agent's prompt with generic negotiation principles. Per PROMPT_GUIDE.md rules: goal-oriented (not procedural), judgment criteria (not rigid rules), minimal directive density.

    **IMPORTANT:** Follow the locked decisions exactly:
    - Accommodating by default, disposition hierarchy: accept > counter-propose > reject
    - Counter-propose to honestly scope, reject only for capability mismatches
    - Clarify when ambiguity is about intent (what do you want?), assume when about implementation (how to build it?)
    - Cost-of-being-wrong framework for clarification decisions
    - Generic principles for all agents (no role-specific calibration in v2.9)

    **Add the following section to all three prompt files.** Place it after the existing `<domain_knowledge>` section (specifically after the delegation-related content). Use the same section in all three prompts -- the principles are generic per the locked decisions.

    ```markdown
    <negotiation>
    ## Delegation Negotiation

    When you receive a delegation, you have three options: accept, counter-propose, or reject. When you delegate work and receive a counter-proposal, you evaluate whether the modified scope works for your goals.

    **Disposition hierarchy:** Prefer accepting over counter-proposing, and counter-proposing over rejecting. Move work forward.

    Counter-propose when you can do the work with a different scope or approach. "I can handle the API changes, but the database migration should go to someone with schema expertise" is more useful than accepting and delivering poorly, or rejecting entirely. The delegator sees your proposal and decides: accept the modification, reject and try someone else, or re-scope and re-delegate.

    Reserve rejection for genuine capability mismatches -- when the work fundamentally doesn't match what you can do. "I'm a test runner, I can't write a product brief" is a valid rejection. "The scope is too large" is better handled as a counter-proposal with reduced scope.

    **Clarification vs assumptions:** Ask for clarification when getting it wrong would waste significant work. Proceed with assumptions when the cost of being wrong is low.

    Clarify when the ambiguity is about *intent* -- what the delegator actually wants. "Build the auth system" needs clarification: OAuth vs JWT vs session-based are fundamentally different approaches. Getting this wrong means rebuilding from scratch.

    Proceed with assumptions when the ambiguity is about *implementation* -- how to build what's clearly wanted. "Create a Linear ticket for this bug" doesn't need clarification about label choices or priority -- make reasonable decisions and let the delegator adjust if needed.

    When answering a clarification question from a delegated agent, be direct and specific. The agent is paused waiting for your answer -- a vague response forces another clarification round.

    Clarification time counts against the task timeout. Each round trip costs time, so ask focused questions. When the question has discrete answers (e.g., "OAuth, JWT, or session-based?"), provide the options to help the delegator answer quickly.
    </negotiation>
    ```

    **Placement specifics:**

    For **dev-agent/prompt.md**: Add the `<negotiation>` section after the existing `## Cross-Agent Delegation` subsection within `<domain_knowledge>`. The dev-agent already has delegation guidance -- the negotiation section extends it.

    For **qa-agent/prompt.md**: Add the `<negotiation>` section after whatever the last `<domain_knowledge>` subsection is. If there's no domain_knowledge section, add it as a new top-level section before `<constraints>` or at the end.

    For **product-agent/prompt.md**: Add the `<negotiation>` section after the delegation-related content in `<domain_knowledge>`.

    **Do NOT:**
    - Add role-specific calibration (deferred to v3.0)
    - Add procedural if/then rules about when to counter-propose
    - Add MUST/ALWAYS/NEVER directives (these are judgment criteria, not safety boundaries)
    - Rewrite existing prompt content -- only add the new section
  </action>
  <verify>
    `grep -c "counter-propose\|clarification\|negotiation" packages/agents/definitions/dev-agent/prompt.md packages/agents/definitions/qa-agent/prompt.md packages/agents/definitions/product-agent/prompt.md` shows non-zero counts for all three files. The negotiation section is present and follows the cost-of-being-wrong framework.
  </verify>
  <done>
    - All three agent prompts include negotiation personality guidance
    - Guidance follows disposition hierarchy (accept > counter-propose > reject)
    - Cost-of-being-wrong framework guides clarification decisions
    - No role-specific calibration (generic for v2.9)
    - No procedural state machines or rigid MUST/ALWAYS directives
    - Prompt additions are goal-oriented and trust the model's judgment
  </done>
</task>

</tasks>

<verification>
1. All three definition.yaml files include task:clarify and task:answer
2. All three prompt.md files include negotiation guidance
3. No role-specific calibration in prompts (deferred to v3.0)
4. Prompt guidance is goal-oriented, not procedural
</verification>

<success_criteria>
- dev-agent, qa-agent, and product-agent definitions include task:clarify and task:answer
- All three prompts describe when to counter-propose vs reject
- All three prompts describe when to clarify vs proceed with assumptions
- Negotiation guidance uses cost-of-being-wrong framework
- No MUST/ALWAYS/NEVER directives in the negotiation section
</success_criteria>

<output>
After completion, create `.planning/phases/80-richer-negotiation/80-03-SUMMARY.md`
</output>

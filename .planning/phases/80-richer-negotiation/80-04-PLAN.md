---
phase: 80-richer-negotiation
plan: 04
type: execute
wave: 3
depends_on: [80-01, 80-02]
files_modified:
  - packages/dashboard/src/services/tasks.ts
  - packages/dashboard/src/components/tasks/task-detail-panel.tsx
  - packages/dashboard/src/components/tasks/timeline-event-row.tsx
  - packages/dashboard/src/lib/schema.ts
autonomous: true
requirements: [NEG-01, NEG-03, NEG-04, NEG-05]

must_haves:
  truths:
    - "Counter-proposal tool calls and signals appear in the task timeline"
    - "Clarification questions and answers appear in the task timeline"
    - "The counter_proposed task status is recognized in the dashboard"
    - "DELEGATION_TOOL_NAMES includes the new negotiation tools"
  artifacts:
    - path: "packages/dashboard/src/services/tasks.ts"
      provides: "Updated DELEGATION_TOOL_NAMES with new tools"
      contains: "task:clarify"
    - path: "packages/dashboard/src/components/tasks/timeline-event-row.tsx"
      provides: "Rendering for counter-proposal and clarification timeline events"
      contains: "counter_propose"
    - path: "packages/dashboard/src/lib/schema.ts"
      provides: "counter_proposed in TaskStatus type"
      contains: "counter_proposed"
  key_links:
    - from: "packages/dashboard/src/services/tasks.ts"
      to: "packages/dashboard/src/components/tasks/task-detail-panel.tsx"
      via: "Timeline events filtered through DELEGATION_TOOL_NAMES"
      pattern: "DELEGATION_TOOL_NAMES"
---

<objective>
Update the dashboard to display counter-proposals and clarifications in the task timeline, recognize the new `counter_proposed` task status, and include the new tools in delegation event filtering.

Purpose: Human operators need visibility into agent negotiation through the existing dashboard timeline. Without these updates, counter-proposal and clarification events would be filtered out or displayed as generic tool calls.
Output: Updated dashboard service, timeline rendering, and schema to support richer negotiation events.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-richer-negotiation/80-01-SUMMARY.md
@.planning/phases/80-richer-negotiation/80-02-SUMMARY.md
@.interface-design/system.md

# Dashboard source files
@packages/dashboard/src/services/tasks.ts
@packages/dashboard/src/components/tasks/task-detail-panel.tsx
@packages/dashboard/src/components/tasks/timeline-event-row.tsx
@packages/dashboard/src/lib/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dashboard schema and delegation tool names</name>
  <files>
    packages/dashboard/src/lib/schema.ts
    packages/dashboard/src/services/tasks.ts
  </files>
  <action>
    **1. Update `schema.ts` to recognize `counter_proposed` task status:**

    The dashboard maintains its own schema types (does NOT import from @aesir/agents per project conventions). Find the `TaskStatus` type or task status values and add `"counter_proposed"` to the union/enum. This may be a string literal union, a Zod enum, or a const array depending on how the dashboard defines it.

    Search for where task status is defined in `packages/dashboard/src/lib/schema.ts` and add `"counter_proposed"` alongside the existing statuses (created, active, paused, completed, cancelled).

    **2. Update `DELEGATION_TOOL_NAMES` in `tasks.ts`:**

    Add the four new tool names to the set. The current set is:
    ```typescript
    const DELEGATION_TOOL_NAMES = new Set([
      "task:delegate",
      "task:respond",
      "task:complete",
      "wait_for_task",
    ]);
    ```

    Update to:
    ```typescript
    const DELEGATION_TOOL_NAMES = new Set([
      "task:delegate",
      "task:respond",
      "task:complete",
      "task:clarify",
      "task:answer",
      "wait_for_task",
    ]);
    ```

    Note: Tool names in the dashboard event filtering use the `namespace:tool_name` format from the tool call events, matching the registration keys in tool-factories.ts. Verify by checking how the existing tool names appear in the timeline events. If the events use `clarify_task`/`answer_task` (the tool's internal name), use those instead. Check the existing pattern -- `task:respond` maps to internal name `respond_task`, so if the set currently uses `task:respond`, the new entries should be `task:clarify` and `task:answer`.

    Wait -- looking at the current set more carefully: `"task:delegate"`, `"task:respond"` use the namespace format, but `"wait_for_task"` uses the internal tool name. The dashboard filtering logic (`tasks.ts:216`) checks `toolName` from the event data. Check what format tool names are stored in events. The events likely store the internal name (e.g., `respond_task`, `delegate_task`) since that's what the agent loop records. Read the event filtering code to verify and use the correct format.

    If the tool events use internal names (likely): use `"clarify_task"` and `"answer_task"`.
    If they use namespace names: use `"task:clarify"` and `"task:answer"`.

    Look at how existing events are matched to determine the correct format. The safest approach: add BOTH formats to the set so neither is missed.
  </action>
  <verify>
    `grep -n "counter_proposed" packages/dashboard/src/lib/schema.ts` shows the new status. `grep -n "clarify\|answer" packages/dashboard/src/services/tasks.ts` shows the new tools in DELEGATION_TOOL_NAMES.
  </verify>
  <done>
    - counter_proposed is a recognized task status in the dashboard
    - DELEGATION_TOOL_NAMES includes task:clarify and task:answer (in the correct name format)
    - Timeline event filtering includes negotiation tool events
  </done>
</task>

<task type="auto">
  <name>Task 2: Render counter-proposal and clarification events in timeline</name>
  <files>
    packages/dashboard/src/components/tasks/timeline-event-row.tsx
    packages/dashboard/src/components/tasks/task-detail-panel.tsx
  </files>
  <action>
    **1. Add counter-proposal and clarification rendering in `timeline-event-row.tsx`:**

    The timeline event row component renders individual events in the task detail panel. It likely switches on event type and/or tool name to render different displays. Add handling for the new negotiation events:

    **Counter-proposal events** (tool.called/tool.succeeded for `respond_task` with type "counter_propose"):
    - Display with an orange/amber accent (negotiation in progress, distinct from green accept and red reject)
    - Show the proposal text from the tool input/result
    - Label: "Counter-proposed" with the proposal summary
    - Use an icon that conveys negotiation (e.g., arrows, exchange, or a message bubble)

    **Clarification events** (tool.called/tool.succeeded for `clarify_task`):
    - Display with a blue/info accent (question, not error or warning)
    - Show the question text from the tool input
    - If options were provided, display them as a compact list
    - Label: "Asked clarification" with the question

    **Clarification answer events** (tool.called/tool.succeeded for `answer_task`):
    - Display with a blue/info accent matching the question
    - Show the answer text
    - Label: "Answered clarification"

    **Signal events** (`signal.received` with type `task_counter_proposed`, `task_clarification`, `task_clarification_response`):
    - These are the receiving side of the above. They already show in the timeline as signal events.
    - Enhance the signal rendering for these specific types:
      - `task_counter_proposed`: Show "Counter-proposal received" with proposal text from signal data
      - `task_clarification`: Show "Clarification requested" with the question
      - `task_clarification_response`: Show "Clarification answered" with the answer

    **2. Update `task-detail-panel.tsx` if needed:**

    Check if the task detail panel has any status-specific rendering (e.g., a status badge). If so, add handling for `counter_proposed` status:
    - Display as amber/orange badge (negotiation in progress)
    - Text: "Counter-Proposed"

    Check if `computeTreeHealth` or similar functions need updates for the new status. If `counter_proposed` should be treated like `created` (waiting, not yet active), map it accordingly.

    **Design system compliance:** Read `.interface-design/system.md` for color tokens. Use:
    - Amber/orange for counter-proposals (in-progress negotiation): likely `warning` or `amber` tokens
    - Blue for clarifications (informational): likely `info` or `blue` tokens
    - Existing green/red for accept/reject

    **Keep changes minimal.** The timeline already has a rendering pattern -- extend it, don't redesign. The goal is that negotiation events are visible and distinguishable, not that they're a completely new visual paradigm.

    Run `pnpm --filter @aesir/dashboard run build` to verify the dashboard compiles.
  </action>
  <verify>
    `pnpm --filter @aesir/dashboard run build` succeeds. `grep -n "counter.propose\|clarif" packages/dashboard/src/components/tasks/timeline-event-row.tsx` shows rendering for both event types.
  </verify>
  <done>
    - Counter-proposal events render with amber accent and proposal text in the timeline
    - Clarification question events render with blue accent and question text
    - Clarification answer events render with blue accent and answer text
    - counter_proposed status has a visual indicator in the task detail panel
    - Dashboard builds successfully
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @aesir/dashboard run build` succeeds
2. Counter-proposal and clarification events are visually distinct in the timeline
3. counter_proposed task status is recognized and displayed
4. DELEGATION_TOOL_NAMES includes the new tools
</verification>

<success_criteria>
- Dashboard compiles and builds successfully
- DELEGATION_TOOL_NAMES includes clarify and answer tools
- counter_proposed is a recognized task status in the dashboard schema
- Timeline renders counter-proposal and clarification events with appropriate visual treatment
- Design follows the project's design system tokens
</success_criteria>

<output>
After completion, create `.planning/phases/80-richer-negotiation/80-04-SUMMARY.md`
</output>

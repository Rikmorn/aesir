---
phase: 82-transparent-materialization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agents/src/shared/db/migrations/0018_add_materialization_records.sql
  - packages/agents/src/shared/db/schema.ts
  - packages/agents/src/shared/db/schema.drizzle.ts
  - packages/agents/src/shared/services/materialization/types.ts
  - packages/agents/src/shared/services/materialization/index.ts
  - packages/integrations/linear/src/mcp/schemas.ts
  - packages/integrations/linear/src/mcp/tools/issues.ts
autonomous: true
requirements:
  - MAT-05
  - MAT-06

must_haves:
  truths:
    - "MaterializationAdapter interface defines create(), syncStatus(), and handleWebhook() methods"
    - "MaterializationConfig Zod schema validates { type: 'transparent', target: 'linear', properties: { priority?, labels?, teamId? } }"
    - "agents.materialization_records table exists with task_id PK, target, external_id, external_url, conversation_id, agent_id, config columns"
    - "Linear MCP create_issue accepts optional parentId parameter and passes it to the SDK"
    - "Linear MCP update_issue_status accepts optional stateType parameter and resolves by type when provided"
  artifacts:
    - path: "packages/agents/src/shared/services/materialization/types.ts"
      provides: "MaterializationAdapter interface, MaterializationConfig schema, MaterializationRecord type"
      min_lines: 40
    - path: "packages/agents/src/shared/db/migrations/0018_add_materialization_records.sql"
      provides: "Migration for materialization_records table"
      contains: "CREATE TABLE agents.materialization_records"
    - path: "packages/agents/src/shared/services/materialization/index.ts"
      provides: "Barrel export for materialization module"
  key_links:
    - from: "packages/agents/src/shared/services/materialization/types.ts"
      to: "packages/agents/src/shared/db/schema.ts"
      via: "MaterializationRecord type aligns with materializationRecords table"
      pattern: "materializationRecords"
    - from: "packages/integrations/linear/src/mcp/tools/issues.ts"
      to: "packages/integrations/linear/src/mcp/schemas.ts"
      via: "handleCreateIssue uses CreateIssueInputSchema with parentId"
      pattern: "parentId"
---

<objective>
Establish the foundation types, database schema, and Linear MCP extensions needed by all subsequent materialization plans.

Purpose: Every downstream plan depends on the MaterializationAdapter interface, the MaterializationConfig schema, the materialization_records table, and the extended Linear MCP tools. Getting these right first enables parallel development of the adapter implementation and tool extensions.

Output: Migration file, Drizzle schema additions, TypeScript types/interfaces, and extended Linear MCP schemas and handlers.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-transparent-materialization/82-CONTEXT.md
@.planning/phases/82-transparent-materialization/82-RESEARCH.md
@packages/agents/src/shared/db/schema.ts
@packages/agents/src/shared/db/migrations/0017_add_task_groups.sql
@packages/integrations/linear/src/mcp/schemas.ts
@packages/integrations/linear/src/mcp/tools/issues.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration + Drizzle schema + materialization types</name>
  <files>
    packages/agents/src/shared/db/migrations/0018_add_materialization_records.sql
    packages/agents/src/shared/db/schema.ts
    packages/agents/src/shared/db/schema.drizzle.ts
    packages/agents/src/shared/services/materialization/types.ts
    packages/agents/src/shared/services/materialization/index.ts
  </files>
  <action>
    1. Create migration `0018_add_materialization_records.sql`:
       ```sql
       CREATE TABLE agents.materialization_records (
         task_id TEXT NOT NULL PRIMARY KEY,
         target TEXT NOT NULL,               -- "linear" (extensible for future targets)
         external_id TEXT NOT NULL,           -- Linear issue UUID
         external_url TEXT,                   -- Linear issue URL
         conversation_id TEXT NOT NULL,       -- Owning conversation
         agent_id TEXT NOT NULL,              -- Agent that requested materialization
         config JSONB NOT NULL DEFAULT '{}',  -- Full MaterializationConfig snapshot
         sync_status TEXT NOT NULL DEFAULT 'active', -- active, completed, failed
         created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
         updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
       );
       CREATE INDEX idx_materialization_external ON agents.materialization_records (target, external_id);
       CREATE INDEX idx_materialization_conversation ON agents.materialization_records (conversation_id);
       ```

    2. Add `materializationRecords` table to `packages/agents/src/shared/db/schema.ts` matching the migration. Place it after the `workCorrelations` table. Use the same patterns: `agentsSchema.table()`, `text()` columns, `jsonb()` for config, `timestamp()` with timezone for dates. Add indexes matching the migration.

    3. Add the same table definition to `schema.drizzle.ts` (required for drizzle-kit migration tracking -- see CLAUDE.md gotchas about not cleaning up schema.drizzle.ts).

    4. Create `packages/agents/src/shared/services/materialization/types.ts`:
       - `MaterializationConfigSchema` (Zod): `{ type: z.literal("transparent"), target: z.enum(["linear"]), properties: z.object({ priority: z.enum(["urgent", "high", "medium", "low", "none"]).optional(), labels: z.array(z.string()).optional(), teamId: z.string().optional() }).optional() }`
       - Export inferred type `MaterializationConfig`
       - `MaterializationAdapter` interface with three methods:
         - `create(params: MaterializationCreateParams): Promise<MaterializationCreateResult | null>` -- null means graceful failure
         - `syncStatus(params: { externalId: string; taskId: string; newStatus: 'active' | 'completed' | 'cancelled' }): Promise<void>` -- fire-and-forget, swallow errors internally
         - `handleWebhook(params: { externalId: string; eventType: string; eventData: Record<string, unknown> }): { signalType: string; signalData: Record<string, unknown>; conversationId: string; deduplicationId: string } | null`
       - `MaterializationCreateParams` type: `{ taskId, conversationId, agentId, description, properties, parentIssueId?, correlationId }`
       - `MaterializationCreateResult` type: `{ externalId: string; externalUrl: string }`
       - `MaterializationRecord` type matching the DB table shape

    5. Create `packages/agents/src/shared/services/materialization/index.ts` with barrel exports from `./types.js`.
  </action>
  <verify>
    - `pnpm --filter @aesir/agents run build` compiles without errors
    - Migration file exists at `packages/agents/src/shared/db/migrations/0018_add_materialization_records.sql`
    - TypeScript types import cleanly: `MaterializationAdapter`, `MaterializationConfig`, `MaterializationConfigSchema`
  </verify>
  <done>
    - materialization_records table defined in migration, schema.ts, and schema.drizzle.ts with matching columns
    - MaterializationAdapter interface exported with create/syncStatus/handleWebhook
    - MaterializationConfigSchema validates the locked config shape from CONTEXT.md
    - Barrel export re-exports all public types
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend Linear MCP create_issue with parentId and update_issue_status with stateType</name>
  <files>
    packages/integrations/linear/src/mcp/schemas.ts
    packages/integrations/linear/src/mcp/tools/issues.ts
  </files>
  <action>
    1. In `packages/integrations/linear/src/mcp/schemas.ts`, add `parentId: z.string().optional()` to `CreateIssueInputSchema` (after `labelIds`). Add a JSDoc comment: "Parent issue ID for creating sub-issues".

    2. In `packages/integrations/linear/src/mcp/schemas.ts`, add `stateType: z.enum(["triage", "backlog", "unstarted", "started", "completed", "canceled"]).optional()` to `UpdateIssueStatusInputSchema`. Add JSDoc: "Target state type. When provided, resolves the first workflow state matching this type. Takes precedence over statusName."

    3. In `handleCreateIssue` (`packages/integrations/linear/src/mcp/tools/issues.ts`):
       - Update the `createParams` type to include `parentId?: string`
       - After the `labelIds` conditional, add: `if (input.parentId !== undefined) { createParams.parentId = input.parentId; }`
       - The Linear SDK `IssueCreateInput` already supports `parentId` (verified in research).

    4. In `handleUpdateIssueStatus`:
       - After validating input, check if `stateType` is provided (from the extended schema -- note: the safeParse uses the updated schema)
       - When `stateType` is provided: fetch team states as current code does, but filter by `state.type === input.stateType` instead of `state.name === statusName`. Use the first matching state. If no state matches the type, return error with available state types.
       - When `stateType` is NOT provided: keep existing behavior (resolve by name).
       - Make `statusName` optional in the schema (`.optional()`) but require at least one of `statusName` or `stateType` via a `.refine()` check: `(data) => data.statusName !== undefined || data.stateType !== undefined`. Error message: "Either statusName or stateType must be provided".

    Note: The `UpdateIssueStatusInput` type is inferred from the schema, so downstream consumers auto-get the new optional field.
  </action>
  <verify>
    - `pnpm --filter @aesir/integration-linear run build` compiles without errors
    - `CreateIssueInputSchema.parse({ teamId: "t1", title: "Test", parentId: "p1" })` succeeds
    - `UpdateIssueStatusInputSchema.parse({ issueId: "i1", stateType: "completed" })` succeeds
    - `UpdateIssueStatusInputSchema.parse({ issueId: "i1" })` fails (neither statusName nor stateType)
  </verify>
  <done>
    - create_issue MCP tool accepts and passes parentId to Linear SDK for sub-issue creation
    - update_issue_status MCP tool accepts stateType and resolves by state type (not name) when provided
    - Both schema extensions are backward-compatible (new fields are optional)
  </done>
</task>

</tasks>

<verification>
- Both packages build: `pnpm --filter @aesir/agents run build && pnpm --filter @aesir/integration-linear run build`
- Migration file is valid SQL (no syntax errors)
- MaterializationAdapter interface has exactly 3 methods: create, syncStatus, handleWebhook
- MaterializationConfigSchema matches the locked shape from CONTEXT.md decisions
- Linear MCP schemas are backward-compatible (existing calls without new fields still work)
</verification>

<success_criteria>
Foundation types, database schema, and Linear MCP extensions are in place. All subsequent materialization plans can import and use these artifacts without circular dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/82-transparent-materialization/82-01-SUMMARY.md`
</output>

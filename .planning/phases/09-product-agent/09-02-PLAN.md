---
phase: 09-product-agent
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/integrations/slack/bolt-app.ts
  - src/integrations/slack/bolt-app.test.ts
  - src/integrations/slack/index.ts
  - src/integrations/slack/types.ts
autonomous: true

user_setup:
  - service: slack
    why: "Bolt app requires Socket Mode for local development"
    env_vars:
      - name: SLACK_APP_TOKEN
        source: "Slack API → Your App → Basic Information → App-Level Tokens → Generate Token with connections:write scope"
    dashboard_config:
      - task: "Enable Socket Mode"
        location: "Slack API → Your App → Socket Mode → Enable Socket Mode"
        details: "Required for Bolt app to receive events via WebSocket"
      - task: "Add app_mention event subscription"
        location: "Slack API → Your App → Event Subscriptions → Subscribe to bot events"
        details: "Add app_mention event to receive @mentions"
      - task: "Add message.im event subscription"
        location: "Slack API → Your App → Event Subscriptions → Subscribe to bot events"
        details: "Add message.im event to receive DMs"

must_haves:
  truths:
    - "Bolt app can connect to Slack via Socket Mode"
    - "Bolt app can receive and respond to messages"
  artifacts:
    - path: "src/integrations/slack/bolt-app.ts"
      provides: "Bolt app factory with Socket Mode"
      exports: ["createBoltApp", "startBoltApp", "stopBoltApp"]
  key_links:
    - from: "bolt-app.ts"
      to: "@slack/bolt"
      via: "App constructor with socketMode"
      pattern: "new App\\(.*socketMode.*true"
---

<objective>
Set up Slack Bolt app with Socket Mode for receiving messages and events.

Purpose: Enable the Product Agent to receive and respond to Slack messages using Bolt's event-driven architecture.
Output: Bolt app factory with Socket Mode connection and graceful shutdown.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-product-agent/09-RESEARCH.md

@src/integrations/slack/client.ts
@src/integrations/slack/types.ts
@src/integrations/slack/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @slack/bolt and update types</name>
  <files>package.json, src/integrations/slack/types.ts</files>
  <action>
1. Install Slack Bolt:
   ```
   npm install @slack/bolt
   ```

2. Add Bolt-specific types to src/integrations/slack/types.ts:
   ```typescript
   export interface BoltAppConfig {
     botToken: string;      // xoxb-... token
     appToken: string;      // xapp-... token for Socket Mode
     socketMode: true;      // Always true for this setup
   }
   ```

The appToken is the App-Level Token with connections:write scope, required for Socket Mode.
  </action>
  <verify>npm ls @slack/bolt shows installed version</verify>
  <done>@slack/bolt installed and types defined</done>
</task>

<task type="auto">
  <name>Task 2: Create Bolt app factory</name>
  <files>src/integrations/slack/bolt-app.ts, src/integrations/slack/bolt-app.test.ts</files>
  <action>
Create Bolt app management functions:

1. `createBoltApp(config: BoltAppConfig)` - Create App instance:
   - Configure with socketMode: true
   - Pass botToken (token) and appToken
   - Return App instance
   - Add logging on creation

2. `startBoltApp(app: App)` - Start the app:
   - Call app.start()
   - Log successful connection
   - Return void (or throw on error)

3. `stopBoltApp(app: App)` - Graceful shutdown:
   - Call app.stop()
   - Log shutdown
   - Handle cleanup

Follow existing slack client patterns:
- Use createLogger for module logging
- Handle errors with descriptive messages
- Keep functions simple and composable

Tests should:
- Mock App constructor and methods
- Verify config is passed correctly
- Test start/stop lifecycle
- NOT actually connect to Slack
  </action>
  <verify>npm test -- bolt-app.test.ts passes</verify>
  <done>Bolt app factory creates and manages app lifecycle</done>
</task>

<task type="auto">
  <name>Task 3: Update module exports</name>
  <files>src/integrations/slack/index.ts</files>
  <action>
Update src/integrations/slack/index.ts to export:
- createBoltApp, startBoltApp, stopBoltApp from "./bolt-app.js"
- BoltAppConfig from "./types.js"

Keep existing exports (WebClient, notifications, etc.) - this adds to them.

Ensure .js extensions for ESM compatibility.
  </action>
  <verify>npm run build succeeds with no errors</verify>
  <done>Bolt app functions publicly exported</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm test -- bolt-app` passes all tests
- [ ] @slack/bolt is in dependencies (not devDependencies)
- [ ] BoltAppConfig type is exported
- [ ] createBoltApp, startBoltApp, stopBoltApp are exported
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Bolt app factory tested with mocked App
- No TypeScript errors
- Socket Mode configuration is correct
</success_criteria>

<output>
After completion, create `.planning/phases/09-product-agent/09-02-SUMMARY.md`
</output>

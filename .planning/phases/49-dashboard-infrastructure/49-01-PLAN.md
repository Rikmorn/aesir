---
phase: 49-dashboard-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/package.json
  - packages/dashboard/tsconfig.json
  - packages/dashboard/next.config.ts
  - packages/dashboard/postcss.config.mjs
  - packages/dashboard/components.json
  - packages/dashboard/.gitignore
  - packages/dashboard/public/.gitkeep
  - packages/dashboard/src/app/layout.tsx
  - packages/dashboard/src/app/page.tsx
  - packages/dashboard/src/app/globals.css
  - packages/dashboard/src/app/api/health/route.ts
  - packages/dashboard/src/middleware.ts
  - packages/dashboard/src/lib/utils.ts
  - tsconfig.json
  - biome.json
autonomous: true

must_haves:
  truths:
    - "`packages/dashboard/` is a valid pnpm workspace package named `@aesir/dashboard`"
    - "`pnpm install` resolves `@aesir/dashboard` without errors"
    - "`pnpm --filter @aesir/dashboard run typecheck` passes with zero errors"
    - "`pnpm run lint` passes for dashboard files with zero errors"
    - "Next.js dev server starts on port 3005 and serves the placeholder page"
    - "`curl http://localhost:3005/api/health` returns 200 with JSON body"
    - "middleware.ts exists and passes all requests through (auth-ready passthrough)"
  artifacts:
    - path: "packages/dashboard/package.json"
      provides: "pnpm workspace package definition"
      contains: "@aesir/dashboard"
    - path: "packages/dashboard/tsconfig.json"
      provides: "Next.js-compatible TypeScript config"
      contains: "jsx"
    - path: "packages/dashboard/next.config.ts"
      provides: "Standalone output config with monorepo tracing"
      contains: "outputFileTracingRoot"
    - path: "packages/dashboard/postcss.config.mjs"
      provides: "Tailwind CSS 4 PostCSS plugin"
      contains: "@tailwindcss/postcss"
    - path: "packages/dashboard/public/.gitkeep"
      provides: "Empty public directory for Docker COPY"
    - path: "packages/dashboard/src/app/layout.tsx"
      provides: "Root layout with Tailwind CSS"
      min_lines: 10
    - path: "packages/dashboard/src/app/page.tsx"
      provides: "Placeholder home page"
      min_lines: 5
    - path: "packages/dashboard/src/app/globals.css"
      provides: "Tailwind imports + shadcn/ui CSS variables"
      contains: "@import \"tailwindcss\""
    - path: "packages/dashboard/src/app/api/health/route.ts"
      provides: "Health check endpoint"
      exports: ["GET"]
    - path: "packages/dashboard/src/middleware.ts"
      provides: "Auth-ready middleware passthrough"
      exports: ["middleware", "config"]
    - path: "packages/dashboard/src/lib/utils.ts"
      provides: "cn() utility for shadcn/ui"
      exports: ["cn"]
  key_links:
    - from: "packages/dashboard/package.json"
      to: "pnpm-workspace.yaml"
      via: "pnpm workspace resolution (packages/* glob)"
      pattern: "@aesir/dashboard"
    - from: "packages/dashboard/src/app/layout.tsx"
      to: "packages/dashboard/src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
    - from: "tsconfig.json"
      to: "packages/dashboard"
      via: "TypeScript project references"
      pattern: "packages/dashboard"
---

<objective>
Create the `@aesir/dashboard` pnpm workspace package with a fully configured Next.js 15.5.x App Router application including Tailwind CSS 4, shadcn/ui setup, health check endpoint, auth-ready middleware, and passing typecheck/lint.

Purpose: Establish the foundational package structure that all subsequent dashboard phases (50-55) will build upon. Without this, no dashboard views can be created.
Output: A working Next.js application in `packages/dashboard/` that builds, typechecks, lints, and serves a placeholder page with a health endpoint.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-dashboard-infrastructure/49-RESEARCH.md

Key existing files to reference:
@tsconfig.base.json
@tsconfig.json
@biome.json
@package.json
@pnpm-workspace.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard package configuration files</name>
  <files>
    packages/dashboard/package.json
    packages/dashboard/tsconfig.json
    packages/dashboard/next.config.ts
    packages/dashboard/postcss.config.mjs
    packages/dashboard/components.json
    packages/dashboard/.gitignore
    packages/dashboard/public/.gitkeep
  </files>
  <action>
Create the `packages/dashboard/` directory and all configuration files.

**package.json** (`@aesir/dashboard`):
- `"name": "@aesir/dashboard"`, `"version": "0.1.0"`, `"private": true`, `"type": "module"`
- Scripts: `"dev": "next dev --port 3005"`, `"build": "next build"`, `"start": "next start --port 3005"`, `"typecheck": "tsc --noEmit"`
- Dependencies: `next@15.5.9`, `react@^19.0.3`, `react-dom@^19.0.3`, `tailwindcss@^4.0.0`, `@tailwindcss/postcss@^4.0.0`, `class-variance-authority@^0.7.0`, `clsx@^2.1.0`, `tailwind-merge@^3.0.0`, `lucide-react@^0.400.0`, `tw-animate-css@^1.0.0`
- DevDependencies: `typescript@^5.7.0`, `@types/node@^22.0.0`, `@types/react@^19.0.0`, `@types/react-dom@^19.0.0`
- Do NOT add drizzle-orm or pg yet (Plan 02 handles that)

**tsconfig.json** (does NOT extend tsconfig.base.json -- see RESEARCH.md Pitfall 2):
- `"target": "ES2022"`, `"lib": ["dom", "dom.iterable", "ES2022"]`
- `"module": "ESNext"`, `"moduleResolution": "bundler"` (NOT NodeNext)
- `"jsx": "preserve"`, `"noEmit": true`, `"incremental": true`
- `"strict": true`, `"skipLibCheck": true`, `"esModuleInterop": true`, `"resolveJsonModule": true`, `"isolatedModules": true`
- Replicate strict options from tsconfig.base.json: `"forceConsistentCasingInFileNames": true`, `"noImplicitReturns": true`, `"noFallthroughCasesInSwitch": true`, `"noUncheckedIndexedAccess": true`
- `"paths": { "@/*": ["./src/*"] }`
- `"plugins": [{ "name": "next" }]`
- `"include": ["src/**/*", "next-env.d.ts", ".next/types/**/*.ts"]`, `"exclude": ["node_modules"]`

**next.config.ts**:
- `output: "standalone"`
- `outputFileTracingRoot: path.join(__dirname, "../../")` -- CRITICAL for monorepo Docker builds
- `basePath: "/dashboard"` -- for Nginx sub-path routing
- `eslint: { ignoreDuringBuilds: true }` -- Biome handles linting
- `typescript: { ignoreBuildErrors: false }` -- keep type safety
- Use `import path from "node:path"` and `import { fileURLToPath } from "node:url"` for `__dirname`

**postcss.config.mjs**:
```javascript
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
```

**components.json** (shadcn/ui config for Tailwind v4):
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/app/globals.css",
    "baseColor": "zinc",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide"
}
```

**.gitignore** (Next.js-specific):
```
.next/
out/
node_modules/
next-env.d.ts
```

**public/.gitkeep**: Create an empty `packages/dashboard/public/.gitkeep` file. This ensures the `public/` directory exists for the Docker COPY in Plan 03's Dockerfile (`COPY --from=builder /app/packages/dashboard/public ./packages/dashboard/public`). Without this, the Docker build fails because COPY requires the source directory to exist.

After creating all files, run `pnpm install` from the monorepo root to resolve the new package.
  </action>
  <verify>
Run `pnpm install` from monorepo root -- should succeed without errors. Run `ls packages/dashboard/node_modules/next` to confirm Next.js is installed.
  </verify>
  <done>
All 7 config files exist in `packages/dashboard/` (including `public/.gitkeep`), `pnpm install` resolves `@aesir/dashboard` as a workspace package, and `next` is installed in the package.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create app files, middleware, and update root configs</name>
  <files>
    packages/dashboard/src/app/layout.tsx
    packages/dashboard/src/app/page.tsx
    packages/dashboard/src/app/globals.css
    packages/dashboard/src/app/api/health/route.ts
    packages/dashboard/src/middleware.ts
    packages/dashboard/src/lib/utils.ts
    tsconfig.json
    biome.json
  </files>
  <action>
Create the Next.js application source files and update root monorepo configs.

**src/app/globals.css**:
- `@import "tailwindcss";` and `@import "tw-animate-css";`
- Full shadcn/ui CSS variable block using oklch color space (see RESEARCH.md Pattern 5 and the shadcn/ui Tailwind v4 docs)
- Include both `:root` (light) and `.dark` theme variable sets
- Include the `@theme inline` block mapping CSS variables to Tailwind utilities:
  ```css
  @theme inline {
    --color-background: var(--background);
    --color-foreground: var(--foreground);
    --color-card: var(--card);
    --color-card-foreground: var(--card-foreground);
    --color-popover: var(--popover);
    --color-popover-foreground: var(--popover-foreground);
    --color-primary: var(--primary);
    --color-primary-foreground: var(--primary-foreground);
    --color-secondary: var(--secondary);
    --color-secondary-foreground: var(--secondary-foreground);
    --color-muted: var(--muted);
    --color-muted-foreground: var(--muted-foreground);
    --color-accent: var(--accent);
    --color-accent-foreground: var(--accent-foreground);
    --color-destructive: var(--destructive);
    --color-border: var(--border);
    --color-input: var(--input);
    --color-ring: var(--ring);
    --color-chart-1: var(--chart-1);
    --color-chart-2: var(--chart-2);
    --color-chart-3: var(--chart-3);
    --color-chart-4: var(--chart-4);
    --color-chart-5: var(--chart-5);
    --color-sidebar: var(--sidebar);
    --color-sidebar-foreground: var(--sidebar-foreground);
    --color-sidebar-primary: var(--sidebar-primary);
    --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
    --color-sidebar-accent: var(--sidebar-accent);
    --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
    --color-sidebar-border: var(--sidebar-border);
    --color-sidebar-ring: var(--sidebar-ring);
    --font-sans: var(--font-sans);
    --font-mono: var(--font-mono);
    --radius-sm: calc(var(--radius) - 4px);
    --radius-md: calc(var(--radius) - 2px);
    --radius-lg: var(--radius);
    --radius-xl: calc(var(--radius) + 4px);
  }
  ```
- Include `@layer base` with `* { @apply border-border; }` and `body { @apply bg-background text-foreground; }` styles
- Reference the exact shadcn/ui Tailwind v4 CSS from https://ui.shadcn.com/docs/tailwind-v4 for complete variable values

**src/app/layout.tsx**:
```tsx
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "Aesir Dashboard",
  description: "Agent execution monitoring and management",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="min-h-screen bg-background font-sans antialiased">
        {children}
      </body>
    </html>
  );
}
```

**src/app/page.tsx**:
```tsx
export default function HomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <h1 className="text-2xl font-bold">Aesir Dashboard</h1>
    </main>
  );
}
```

**src/app/api/health/route.ts**:
```typescript
import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({
    status: "ok",
    service: "dashboard",
    timestamp: new Date().toISOString(),
  });
}
```

**src/middleware.ts** (auth-ready passthrough):
```typescript
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";

export function middleware(_request: NextRequest) {
  return NextResponse.next();
}

export const config = {
  matcher: [
    "/((?!_next/static|_next/image|favicon.ico|api/health).*)",
  ],
};
```

**src/lib/utils.ts** (shadcn/ui cn utility):
```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

**Update root tsconfig.json**: Add `{ "path": "packages/dashboard" }` to the `references` array. Note: The dashboard uses `"noEmit": true` so it won't produce build artifacts through tsc project references, avoiding conflicts with Next.js's own compilation.

**Update biome.json**: Add a new override entry for the dashboard to prevent importing integration SDKs directly:
```json
{
  "includes": ["packages/dashboard/**/*.ts", "packages/dashboard/**/*.tsx"],
  "linter": {
    "rules": {
      "style": {
        "noRestrictedImports": {
          "level": "error",
          "options": {
            "paths": {},
            "patterns": [
              {
                "group": ["@aesir/integration-linear", "@aesir/integration-linear/*"],
                "message": "Dashboard reads from Postgres and agent-service API only. Do not import integration packages directly."
              },
              {
                "group": ["@aesir/integration-github", "@aesir/integration-github/*"],
                "message": "Dashboard reads from Postgres and agent-service API only. Do not import integration packages directly."
              },
              {
                "group": ["@aesir/integration-slack", "@aesir/integration-slack/*"],
                "message": "Dashboard reads from Postgres and agent-service API only. Do not import integration packages directly."
              }
            ]
          }
        }
      }
    }
  }
}
```

Also update the biome.json `"includes"` for the function naming convention to also allow PascalCase for functions in TSX files (React component exports use PascalCase function names like `export default function HomePage()`). Add an override:
```json
{
  "includes": ["packages/dashboard/**/*.tsx"],
  "linter": {
    "rules": {
      "style": {
        "useNamingConvention": {
          "level": "error",
          "options": {
            "strictCase": false,
            "conventions": [
              {
                "selector": { "kind": "variable" },
                "formats": ["camelCase", "CONSTANT_CASE", "PascalCase"]
              },
              {
                "selector": { "kind": "function" },
                "formats": ["camelCase", "PascalCase"]
              },
              {
                "selector": { "kind": "typeLike" },
                "formats": ["PascalCase"]
              },
              {
                "selector": { "kind": "const", "scope": "global" },
                "formats": ["camelCase", "CONSTANT_CASE", "PascalCase"]
              },
              {
                "selector": { "kind": "objectLiteralProperty" },
                "formats": ["camelCase", "CONSTANT_CASE", "snake_case", "PascalCase"]
              },
              {
                "selector": { "kind": "typeProperty" },
                "formats": ["camelCase", "snake_case"]
              }
            ]
          }
        }
      }
    }
  }
}
```

After creating all files:
1. Run `pnpm --filter @aesir/dashboard run typecheck` -- must pass with zero errors
2. Run `pnpm run lint` -- must pass for dashboard files with zero errors
3. If typecheck or lint fails, fix the issues before proceeding
  </action>
  <verify>
`pnpm --filter @aesir/dashboard run typecheck` exits 0. `pnpm run lint` exits 0 (or at least no errors in dashboard files). All 6 source files exist in expected locations.

**Runtime verification:** Start the Next.js dev server with `pnpm --filter @aesir/dashboard run dev &`, wait ~10 seconds for startup, then verify:
1. `curl -s http://localhost:3005/dashboard/api/health` returns 200 with JSON body containing `"status":"ok"`
2. `curl -s -o /dev/null -w "%{http_code}" http://localhost:3005/dashboard/` returns 200
After verification, kill the dev server background process.
  </verify>
  <done>
Next.js app source files exist, globals.css has Tailwind + shadcn/ui variables, health endpoint returns 200 JSON, middleware passes all requests, cn() utility works, root tsconfig.json includes dashboard reference, biome.json has dashboard-specific overrides, typecheck passes, lint passes, and Next.js dev server starts on port 3005 serving the placeholder page.
  </done>
</task>

</tasks>

<verification>
1. `pnpm install` succeeds with `@aesir/dashboard` resolved as workspace package
2. `pnpm --filter @aesir/dashboard run typecheck` exits with 0
3. `pnpm run lint` exits with 0 (no errors in dashboard files)
4. All expected files exist: package.json, tsconfig.json, next.config.ts, postcss.config.mjs, components.json, .gitignore, public/.gitkeep, layout.tsx, page.tsx, globals.css, health/route.ts, middleware.ts, utils.ts
5. Root tsconfig.json has dashboard in references
6. biome.json has dashboard import restriction override
7. Next.js dev server starts on port 3005 and `curl http://localhost:3005/dashboard/api/health` returns 200
</verification>

<success_criteria>
- `packages/dashboard/` is a valid pnpm workspace package (`@aesir/dashboard`)
- Next.js 15.5.x is installed and configured with standalone output
- Tailwind CSS 4 with shadcn/ui variables is set up
- Health check at `/api/health` returns 200 JSON
- Auth-ready middleware passes all requests through
- TypeScript compilation passes
- Biome linting passes
- Root configs updated (tsconfig references, biome overrides)
</success_criteria>

<output>
After completion, create `.planning/phases/49-dashboard-infrastructure/49-01-SUMMARY.md`
</output>

---
phase: 25-product-agent-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agents/src/product-agent/nodes/classify.ts
  - packages/agents/src/product-agent/nodes/index.ts
  - packages/agents/src/product-agent/state.ts
  - packages/agents/src/product-agent/prompts.ts
autonomous: true

must_haves:
  truths:
    - "Product-agent can determine if a message is a feature request, bug report, question, or off-topic"
    - "Uncertain messages result in a clarifying question"
    - "Classification uses structured output with confidence scoring"
  artifacts:
    - path: "packages/agents/src/product-agent/nodes/classify.ts"
      provides: "Intent classification node"
      exports: ["classifyNode", "ClassificationSchema"]
    - path: "packages/agents/src/product-agent/state.ts"
      provides: "Updated state with classification fields"
      contains: "classification"
  key_links:
    - from: "packages/agents/src/product-agent/nodes/classify.ts"
      to: "@langchain/anthropic"
      via: "withStructuredOutput"
      pattern: "withStructuredOutput.*ClassificationSchema"
---

<objective>
Create intent classification node for product-agent

Purpose: Enable product-agent to filter incoming Slack messages into actionable types (feature_request, bug_report, question, off_topic, unclear) before entering the conversation workflow.

Output: classify.ts node with structured output classification, updated state schema with classification fields
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-product-agent-workflow/25-CONTEXT.md
@.planning/phases/25-product-agent-workflow/25-RESEARCH.md
@packages/agents/src/product-agent/state.ts
@packages/agents/src/product-agent/nodes/analyze-requirements.ts
@packages/agents/src/product-agent/prompts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add classification fields to state</name>
  <files>packages/agents/src/product-agent/state.ts</files>
  <action>
Update ProductAgentStateAnnotation to include classification fields:

1. Add ClassificationTypeSchema enum: 'feature_request' | 'bug_report' | 'question' | 'off_topic' | 'unclear'

2. Add ClassificationConfidenceSchema enum: 'high' | 'medium' | 'low'

3. Add to ProductAgentStateAnnotation:
   - classification: Annotation with ClassificationType or null, default null
   - classificationConfidence: Annotation with ClassificationConfidence or null, default null

4. Update ProductAgentPhaseSchema to add 'declined' phase (for questions/off-topic)

5. Export the new schemas for use in classify node

Follow existing patterns - use Zod enums, Annotation with replace reducer, export types.
  </action>
  <verify>pnpm --filter @aesir/agents typecheck</verify>
  <done>State schema includes classification, classificationConfidence fields with proper types</done>
</task>

<task type="auto">
  <name>Task 2: Create classification node and prompt</name>
  <files>
packages/agents/src/product-agent/nodes/classify.ts
packages/agents/src/product-agent/nodes/index.ts
packages/agents/src/product-agent/prompts.ts
  </files>
  <action>
Create classify.ts node following analyze-requirements.ts pattern:

1. In prompts.ts, add CLASSIFY_MESSAGE_PROMPT:
```
You are a product agent that helps create Linear issues.

Classify the user's message:
- feature_request: User wants new functionality built
- bug_report: Something is broken or not working
- question: User asking for help/information (not requesting work)
- off_topic: Not product/development related
- unclear: Message is ambiguous, need clarification

Guidelines:
- @mentions trigger you - classify the message content
- If confidence is low, return "unclear"
- Feature requests and bug reports -> proceed to gather requirements
- Questions -> polite decline ("I create issues. For questions, ask the team.")
- Off-topic -> polite decline ("I help with features and bugs.")
```

2. Create classify.ts:
   - ClassificationOutputSchema with Zod: type, confidence, reasoning, response (optional)
   - classifyNode factory function accepting llm option
   - Use withStructuredOutput pattern from analyze-requirements.ts
   - Return state update with classification, classificationConfidence, phase
   - If feature_request or bug_report -> phase 'gathering'
   - If question or off_topic -> phase 'declined', include polite response
   - If unclear -> phase 'clarifying', include "could you clarify" response
   - Log classification result

3. Export from nodes/index.ts

Follow existing patterns: factory function, optional llm param, child logger, error handling.
  </action>
  <verify>pnpm --filter @aesir/agents typecheck && pnpm --filter @aesir/agents test -- --testPathPattern="classify" --passWithNoTests</verify>
  <done>classifyNode exists, exports properly, handles all classification types with appropriate responses</done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for classification node</name>
  <files>packages/agents/src/product-agent/nodes/classify.test.ts</files>
  <action>
Create classify.test.ts following existing test patterns:

1. Mock ChatAnthropic with vi.mock('@langchain/anthropic')

2. Test cases:
   - Feature request message -> returns feature_request classification, phase 'gathering'
   - Bug report message -> returns bug_report classification, phase 'gathering'
   - Question message -> returns question classification, phase 'declined', has decline response
   - Off-topic message -> returns off_topic classification, phase 'declined', has decline response
   - Unclear message -> returns unclear classification, phase 'clarifying', has clarifying response
   - Low confidence on any type -> phase 'clarifying'

3. Test that node:
   - Logs classification result
   - Handles LLM errors gracefully (returns gathering phase)
   - Uses provided LLM if passed in options

Follow existing test patterns from analyze-requirements or graph.test.ts.
  </action>
  <verify>pnpm --filter @aesir/agents test -- --testPathPattern="classify"</verify>
  <done>All classification node tests pass, covering all message types and edge cases</done>
</task>

</tasks>

<verification>
All checks pass:
- pnpm --filter @aesir/agents typecheck
- pnpm --filter @aesir/agents test -- --testPathPattern="classify"
- State schema updated with classification fields
- Classify node exported from nodes/index.ts
</verification>

<success_criteria>
- Classification node created with structured output
- State includes classification, classificationConfidence fields
- Five classification types handled: feature_request, bug_report, question, off_topic, unclear
- Polite decline responses for non-actionable messages
- Unit tests pass for all classification scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/25-product-agent-workflow/25-01-SUMMARY.md`
</output>

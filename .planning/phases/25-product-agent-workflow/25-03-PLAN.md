---
phase: 25-product-agent-workflow
plan: 03
type: execute
wave: 2
depends_on: ["25-01", "25-02"]
files_modified:
  - packages/agents/src/product-agent/graph.ts
  - packages/agents/src/product-agent/graph.test.ts
autonomous: true

must_haves:
  truths:
    - "Graph starts with classification before analysis"
    - "Declined messages exit immediately with response"
    - "Confirmation step happens before task creation"
    - "Graph flow: classify -> (analyze -> clarify | confirm) -> createTasks"
  artifacts:
    - path: "packages/agents/src/product-agent/graph.ts"
      provides: "Updated StateGraph with classify and confirm nodes"
      contains: "classifyNode"
  key_links:
    - from: "packages/agents/src/product-agent/graph.ts"
      to: "nodes/classify.ts"
      via: "import and addNode"
      pattern: "addNode.*classify"
    - from: "packages/agents/src/product-agent/graph.ts"
      to: "nodes/confirm.ts"
      via: "import and addNode"
      pattern: "addNode.*confirm"
---

<objective>
Update product-agent graph with classification and confirmation flow

Purpose: Wire the new classify and confirm nodes into the product-agent graph, creating the complete conversation flow: classify -> (declined | gathering -> analyze -> (clarify | confirm -> createTasks)).

Output: Updated graph.ts with full conversation flow, updated tests
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-product-agent-workflow/25-CONTEXT.md
@.planning/phases/25-product-agent-workflow/25-RESEARCH.md
@packages/agents/src/product-agent/graph.ts
@packages/agents/src/product-agent/graph.test.ts
@packages/agents/src/product-agent/state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update graph with classify and confirm nodes</name>
  <files>packages/agents/src/product-agent/graph.ts</files>
  <action>
Refactor graph.ts to include new flow:

1. Import classifyNode and confirmNode from nodes/index.js

2. Add new route types:
   - AfterClassifyRoute: 'analyze' | 'declined' | 'end'
   - AfterAnalysisRoute: 'clarify' | 'confirm' (rename from current)

3. Add routeAfterClassify function:
   - If phase === 'declined' -> 'end' (exit with decline message)
   - If phase === 'clarifying' -> 'clarify' (unclear, ask for clarification)
   - Else -> 'analyze' (proceed with gathering)

4. Update routeAfterAnalysis:
   - If phase === 'creating' or 'confirming' -> 'confirm' (not createTasks directly)
   - If phase === 'clarifying' -> 'clarify'
   - Default -> 'clarify'

5. Add routeAfterConfirm function:
   - If phase === 'creating' -> 'createTasks' (user confirmed)
   - If phase === 'clarifying' -> 'clarify' (user gave feedback)
   - Default -> 'clarify'

6. Update graph structure:
   ```
   __start__ -> classify
   classify -> [routeAfterClassify]
              |-> analyze (gathering/clarifying from unclear)
              |-> __end__ (declined)

   analyze -> [routeAfterAnalysis]
              |-> clarify (need more info)
              |-> confirm (ready for confirmation)

   clarify -> __end__ (return question to user)

   confirm -> [routeAfterConfirm]
              |-> createTasks (user confirmed)
              |-> clarify (user gave feedback)

   createTasks -> __end__
   ```

7. Build nodes with options, add all nodes, add edges

Follow existing patterns. Maintain backwards compatibility with ProductAgentGraphOptions.
  </action>
  <verify>pnpm --filter @aesir/agents typecheck</verify>
  <done>Graph includes classify, confirm nodes with proper routing</done>
</task>

<task type="auto">
  <name>Task 2: Update graph tests for new flow</name>
  <files>packages/agents/src/product-agent/graph.test.ts</files>
  <action>
Update graph.test.ts to cover new flow:

1. Update existing tests to account for classify node at start

2. Add new test cases:
   - routeAfterClassify: declined -> 'end'
   - routeAfterClassify: clarifying (unclear) -> 'clarify'
   - routeAfterClassify: gathering -> 'analyze'
   - routeAfterAnalysis: confirming -> 'confirm'
   - routeAfterConfirm: creating -> 'createTasks'
   - routeAfterConfirm: clarifying (feedback) -> 'clarify'

3. Test graph compilation with all nodes:
   - Graph compiles without error
   - All node names present: classify, analyze, clarify, confirm, createTasks

4. Test end-to-end flow mocking:
   - Declined message exits at classify
   - Feature request proceeds through full flow

Use existing mocking patterns from graph.test.ts.
  </action>
  <verify>pnpm --filter @aesir/agents test -- --testPathPattern="graph"</verify>
  <done>All graph tests pass including new routing scenarios</done>
</task>

</tasks>

<verification>
All checks pass:
- pnpm --filter @aesir/agents typecheck
- pnpm --filter @aesir/agents test -- --testPathPattern="graph"
- Graph routes through classify first
- Declined messages exit immediately
- Confirmation happens before createTasks
</verification>

<success_criteria>
- Graph includes classify node at entry point
- Declined phase exits graph with response
- Confirmation step before task creation
- All routing functions tested
- Graph compiles and all nodes wired correctly
</success_criteria>

<output>
After completion, create `.planning/phases/25-product-agent-workflow/25-03-SUMMARY.md`
</output>

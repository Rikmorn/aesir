---
phase: 25-product-agent-workflow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agents/src/product-agent/nodes/confirm.ts
  - packages/agents/src/product-agent/nodes/index.ts
  - packages/agents/src/product-agent/prompts.ts
  - packages/agents/src/product-agent/state.ts
autonomous: true

must_haves:
  truths:
    - "Product-agent shows issue draft before creating"
    - "User must confirm before issue is created"
    - "Draft includes title, description, and acceptance criteria preview"
  artifacts:
    - path: "packages/agents/src/product-agent/nodes/confirm.ts"
      provides: "Confirmation node that drafts issue preview"
      exports: ["confirmNode"]
    - path: "packages/agents/src/product-agent/state.ts"
      provides: "Updated state with confirming phase and draft"
      contains: "confirming"
  key_links:
    - from: "packages/agents/src/product-agent/nodes/confirm.ts"
      to: "state.requirements"
      via: "builds draft from requirements"
      pattern: "state\\.requirements"
---

<objective>
Create confirmation node for issue draft preview

Purpose: Enable product-agent to show a draft issue to the user before creating it in Linear, following the "confirmation before action" pattern from research.

Output: confirm.ts node that generates issue draft preview, updated state with confirming phase
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-product-agent-workflow/25-CONTEXT.md
@.planning/phases/25-product-agent-workflow/25-RESEARCH.md
@packages/agents/src/product-agent/state.ts
@packages/agents/src/product-agent/nodes/create-tasks.ts
@packages/agents/src/product-agent/prompts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update state for confirmation flow</name>
  <files>packages/agents/src/product-agent/state.ts</files>
  <action>
Add confirmation-related fields to state:

1. Add IssueDraftSchema with Zod:
   - title: string
   - description: string
   - acceptanceCriteria: string[]
   - priority: 'urgent' | 'high' | 'medium' | 'low'
   - labels: string[] (suggested labels like 'feature', 'bug')
   - slackThreadUrl: string | null (link back to conversation)

2. Add to ProductAgentStateAnnotation:
   - issueDraft: Annotation<IssueDraft | null>, default null
   - awaitingConfirmation: Annotation<boolean>, default false

3. Ensure 'confirming' is in ProductAgentPhaseSchema (add if missing)

4. Export IssueDraft type

Follow existing state patterns with replace reducers.
  </action>
  <verify>pnpm --filter @aesir/agents typecheck</verify>
  <done>State includes issueDraft and awaitingConfirmation fields, confirming phase exists</done>
</task>

<task type="auto">
  <name>Task 2: Create confirmation node with draft generation</name>
  <files>
packages/agents/src/product-agent/nodes/confirm.ts
packages/agents/src/product-agent/nodes/index.ts
packages/agents/src/product-agent/prompts.ts
  </files>
  <action>
Create confirm.ts node:

1. In prompts.ts, add GENERATE_ISSUE_DRAFT_PROMPT:
```
You are a product agent creating a Linear issue draft.

Based on the gathered requirements, generate a well-structured issue:
- Title: Clear, actionable (e.g., "Add user authentication with JWT")
- Description: Context paragraph explaining what and why
- Acceptance Criteria: Specific, testable checkboxes
- Priority: Based on urgency indicators in conversation
- Labels: Suggest appropriate labels (feature, bug, frontend, backend, etc.)

Format the issue so a developer can understand and implement it.
```

2. Create confirm.ts:
   - IssueDraftOutputSchema matching IssueDraftSchema
   - confirmNode factory function accepting llm option
   - Build context from state.requirements (what, why, who, acceptanceCriteria, constraints)
   - Use withStructuredOutput to generate draft
   - Build Slack-formatted preview message:
     ```
     I'll create this issue:

     **Title:** {title}

     **Description:**
     {description}

     **Acceptance Criteria:**
     - [ ] {criteria1}
     - [ ] {criteria2}

     **Priority:** {priority}
     **Labels:** {labels.join(', ')}

     Reply "confirm" to create, or provide feedback to revise.
     ```
   - Return state update: issueDraft, awaitingConfirmation: true, phase: 'confirming', messages: [AIMessage with preview]

3. Export from nodes/index.ts

Follow existing patterns: factory function, logger, error handling (fallback to gathering on error).
  </action>
  <verify>pnpm --filter @aesir/agents typecheck</verify>
  <done>confirmNode generates issue draft and formatted preview message for Slack</done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for confirmation node</name>
  <files>packages/agents/src/product-agent/nodes/confirm.test.ts</files>
  <action>
Create confirm.test.ts:

1. Mock ChatAnthropic

2. Test cases:
   - Given complete requirements, generates valid issue draft
   - Draft includes title, description, acceptanceCriteria, priority, labels
   - AIMessage contains formatted preview with "confirm" instruction
   - Phase set to 'confirming'
   - awaitingConfirmation set to true
   - LLM error handled gracefully (returns gathering phase)

3. Test draft formatting:
   - Preview message is Slack-markdown compatible
   - Acceptance criteria formatted as checkboxes
   - Labels joined with commas

Follow existing test patterns.
  </action>
  <verify>pnpm --filter @aesir/agents test -- --testPathPattern="confirm"</verify>
  <done>All confirmation node tests pass</done>
</task>

</tasks>

<verification>
All checks pass:
- pnpm --filter @aesir/agents typecheck
- pnpm --filter @aesir/agents test -- --testPathPattern="confirm"
- State updated with issueDraft, awaitingConfirmation
- Confirm node exported from nodes/index.ts
</verification>

<success_criteria>
- Confirmation node creates issue draft from requirements
- Draft includes all required fields (title, description, criteria, priority, labels)
- Preview message formatted for Slack with confirmation instruction
- State tracks awaitingConfirmation flag
- Unit tests cover draft generation and formatting
</success_criteria>

<output>
After completion, create `.planning/phases/25-product-agent-workflow/25-02-SUMMARY.md`
</output>

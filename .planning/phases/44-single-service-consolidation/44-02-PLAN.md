---
phase: 44-single-service-consolidation
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - docker-compose.yml
  - docker-config/nginx.conf
  - .env.example
  - Dockerfile
autonomous: true

must_haves:
  truths:
    - "docker-compose.yml defines agent-service instead of dev-agent, dev-agent-worker, product-agent, router, temporal, temporal-ui"
    - "Integration services (linear, github, slack) point ROUTER_URL to agent-service:3004/events"
    - "nginx.conf routes /agent/* to agent-service:3004 and removes product-agent, router upstreams"
    - ".env.example documents new service vars (AGENT_SERVICE_PORT, MAX_CONCURRENT_CONVERSATIONS, etc.) and removes Temporal vars"
    - "Dockerfile CMD defaults to node dist/service/main.js"
    - "agent-service has Docker socket mount, root user, stop_grace_period: 35s"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Updated Docker Compose with agent-service replacing 6 services"
      contains: "agent-service"
    - path: "docker-config/nginx.conf"
      provides: "Updated nginx config routing to agent-service"
      contains: "upstream agent-service"
    - path: ".env.example"
      provides: "Updated env template with service config vars"
      contains: "AGENT_SERVICE_PORT"
    - path: "Dockerfile"
      provides: "Updated default CMD for agent-service"
      contains: "dist/service/main.js"
  key_links:
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "agent-service build context references Dockerfile"
      pattern: "dockerfile: Dockerfile"
    - from: "docker-compose.yml (integration services)"
      to: "docker-compose.yml (agent-service)"
      via: "ROUTER_URL env var points to agent-service:3004/events"
      pattern: "ROUTER_URL=http://agent-service:3004/events"
    - from: "docker-config/nginx.conf"
      to: "docker-compose.yml (agent-service)"
      via: "upstream block references agent-service:3004"
      pattern: "server agent-service:3004"
---

<objective>
Update Docker infrastructure to replace 6 services with a single agent-service.

Purpose: Docker Compose, nginx, Dockerfile, and .env.example must reflect the new single-service architecture. Integration services must route events to the new agent-service instead of the old router service.

Output: Updated docker-compose.yml (6 services removed, 1 added), nginx.conf (3 upstreams removed, 1 unified), .env.example (Temporal vars removed, service vars added), Dockerfile (CMD updated).
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-single-service-consolidation/44-CONTEXT.md
@.planning/phases/44-single-service-consolidation/44-RESEARCH.md
@.planning/phases/44-single-service-consolidation/44-01-SUMMARY.md

@docker-compose.yml
@docker-config/nginx.conf
@.env.example
@Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docker-compose.yml -- replace 6 services with agent-service</name>
  <files>docker-compose.yml</files>
  <action>
Modify `docker-compose.yml` to remove old services and add the unified agent-service.

**Remove these service definitions entirely:**
1. `temporal` (lines ~76-101) -- Temporal server no longer needed
2. `temporal-ui` (lines ~102-114) -- Temporal UI no longer needed
3. `router` (lines ~299-345) -- replaced by agent-service
4. `dev-agent` (lines ~347-431) -- replaced by agent-service
5. `dev-agent-worker` (lines ~433-498) -- replaced by agent-service
6. `product-agent` (lines ~500-567) -- replaced by agent-service

**Add `agent-service` definition** (after the slack-integration service, before cloudflared):

```yaml
  # Agent Service - unified agent runtime replacing dev-agent, product-agent, and router
  # Handles all event routing, conversation execution, and worker polling in a single process.
  # Events: POST /events (from integration dispatchers)
  # Management: GET /conversations/:id, POST /conversations/:id/cancel
  # Health: GET /health
  agent-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aesir-agent-service
    # Run as root to access Docker socket for DevContainerManager
    # The container is already isolated; Docker access is required for code sandboxing
    user: root
    depends_on:
      postgresql:
        condition: service_healthy
      linear-integration:
        condition: service_healthy
      github-integration:
        condition: service_healthy
      slack-integration:
        condition: service_healthy
    environment:
      # Database
      - DB_HOST=postgresql
      - DB_PORT=5432
      - DB_USER=temporal
      - DB_PASSWORD=temporal
      - DB_NAME=temporal
      - CREDENTIAL_ENCRYPTION_KEY=${CREDENTIAL_ENCRYPTION_KEY}
      # LLM
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      # Workspace config
      - LINEAR_TEAM_ID=${LINEAR_TEAM_ID}
      - GITHUB_REPO=${GITHUB_REPO}
      - GITHUB_OWNER=${GITHUB_OWNER}
      - GITHUB_REPO_URL=${GITHUB_REPO_URL}
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - GITHUB_BASE_BRANCH=${GITHUB_BASE_BRANCH:-main}
      - SLACK_CHANNEL_ID=${SLACK_CHANNEL_ID}
      - DEV_AGENT_SLACK_CHANNEL=${DEV_AGENT_SLACK_CHANNEL}
      - PRODUCT_AGENT_ALLOWED_CHANNELS=${PRODUCT_AGENT_ALLOWED_CHANNELS:-}
      # MCP URLs
      - LINEAR_MCP_URL=http://linear-integration:3001
      - GITHUB_MCP_URL=http://github-integration:3002
      - SLACK_MCP_URL=http://slack-integration:3003
      # Service config
      - AGENT_SERVICE_PORT=3004
      - MAX_CONCURRENT_CONVERSATIONS=${MAX_CONCURRENT_CONVERSATIONS:-5}
      - WORKER_POLL_INTERVAL_MS=${WORKER_POLL_INTERVAL_MS:-5000}
      - FORCE_SHUTDOWN_TIMEOUT_MS=${FORCE_SHUTDOWN_TIMEOUT_MS:-30000}
      # Router config
      - ROUTER_ALERTS_CHANNEL=${ROUTER_ALERTS_CHANNEL:-}
      # Observability
      - NODE_ENV=${NODE_ENV:-production}
      - LANGSMITH_TRACING=${LANGSMITH_TRACING:-false}
      - LANGSMITH_API_KEY=${LANGSMITH_API_KEY:-}
      - LANGSMITH_PROJECT=${LANGSMITH_PROJECT:-aesir-agents}
    ports:
      - "3004:3004"
    volumes:
      # Mount Docker socket for DevContainerManager
      - /var/run/docker.sock:/var/run/docker.sock
      # macOS alternative path
      - ${HOME}/.docker/run/docker.sock:/var/run/docker.sock:ro
    command: ["node", "dist/service/main.js"]
    stop_grace_period: 35s
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://localhost:3004/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))\""]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - aesir-network
    restart: unless-stopped
    develop:
      watch:
        - action: rebuild
          path: ./packages/agents/src
        - action: rebuild
          path: ./packages/agents/package.json
        - action: rebuild
          path: ./packages/agents/definitions
        - action: rebuild
          path: ./packages/types/src
        - action: rebuild
          path: ./packages/platform/src
```

**Update integration services ROUTER_URL** -- change in ALL three:
- `linear-integration`: `ROUTER_URL=http://router:3006/events` -> `ROUTER_URL=http://agent-service:3004/events`
- `github-integration`: `ROUTER_URL=http://router:3006/events` -> `ROUTER_URL=http://agent-service:3004/events`
- `slack-integration`: `ROUTER_URL=http://router:3006/events` -> `ROUTER_URL=http://agent-service:3004/events`

**Update nginx depends_on** -- replace old service dependencies:
```yaml
  nginx:
    depends_on:
      linear-integration:
        condition: service_healthy
      github-integration:
        condition: service_healthy
      slack-integration:
        condition: service_healthy
      agent-service:
        condition: service_healthy
```
Remove `router`, `dev-agent`, `product-agent` from nginx depends_on.

**Update the header comments** to reflect new architecture:
- Remove references to Temporal services and ports
- Remove direct access entries for router:3006, dev-agent:3004 (separate), product-agent:3005
- Update the /router/* route comment to /agent-service/*
- Update "Access (via nginx proxy)" section
- Remove "Infrastructure Only" line about temporal/temporal-ui

**IMPORTANT: Keep these services unchanged:** postgresql, nginx, cloudflared, cloudflared-health, linear-integration (except ROUTER_URL), github-integration (except ROUTER_URL), slack-integration (except ROUTER_URL).
  </action>
  <verify>Run `docker compose config --quiet 2>&1 | head -10` to verify YAML syntax is valid. Check that `docker compose config 2>/dev/null | grep -c "temporal"` returns 0 (no temporal references in service definitions -- volume name is OK). Verify `docker compose config 2>/dev/null | grep "ROUTER_URL"` shows `agent-service:3004/events` for all three integration services.</verify>
  <done>docker-compose.yml has agent-service replacing 6 removed services. All integration services point ROUTER_URL to agent-service:3004/events. nginx depends on agent-service. No temporal/temporal-ui service definitions remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update nginx.conf, .env.example, and Dockerfile</name>
  <files>docker-config/nginx.conf, .env.example, Dockerfile</files>
  <action>
**1. Update `docker-config/nginx.conf`:**

Replace the 3 agent/router upstream blocks with a single agent-service upstream:

**Remove these upstream blocks:**
```
upstream dev-agent { server dev-agent:3004; }
upstream product-agent { server product-agent:3005; }
upstream router { server router:3006; }
```

**Add this upstream block** (after the slack upstream):
```
upstream agent-service {
    server agent-service:3004;
}
```

**Replace the 3 location blocks** (`/agent/`, `/product/`, `/router/`) with a single unified block:
```nginx
# Agent Service (unified event routing + conversation management)
# Events: POST /agent/events (from integration dispatchers via tunnel)
# Management: GET /agent/conversations/:id, POST /agent/conversations/:id/cancel
# Health: GET /agent/health
location /agent/ {
    proxy_pass http://agent-service/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    # Extended timeout for LLM-based routing (slow path)
    proxy_connect_timeout 5s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;
    proxy_buffering off;
}
```

**Update the root `/` route** to show updated available routes (remove /product/*, /router/*, update descriptions):
```nginx
location = / {
    return 200 '{"routes":{"/linear/*":"Linear integration","/github/*":"GitHub integration","/slack/*":"Slack integration","/agent/*":"Agent service (events, conversations, health)","/health":"Nginx health"}}';
    add_header Content-Type application/json;
}
```

**2. Update `.env.example`:**

**Remove the Temporal Configuration section entirely** (lines ~49-59):
```
# ===================
# Temporal Configuration
# ===================
TEMPORAL_ADDRESS=localhost:7233
TEMPORAL_NAMESPACE=default
```

**Add Agent Service Configuration section** (after the Agent Configuration section):
```
# ===================
# Agent Service Configuration
# ===================

# Port for the unified agent service (default: 3004)
AGENT_SERVICE_PORT=3004

# Maximum number of conversations running simultaneously (default: 5)
MAX_CONCURRENT_CONVERSATIONS=5

# How often the worker polls for queued conversations in ms (default: 5000)
WORKER_POLL_INTERVAL_MS=5000

# Grace period before forced shutdown in ms (default: 30000)
FORCE_SHUTDOWN_TIMEOUT_MS=30000

# Slack channel for routing failure alerts (optional)
ROUTER_ALERTS_CHANNEL=
```

**Update the PRODUCT_AGENT_URL comment** -- this var is no longer needed since events go through the unified service:
```
# DEPRECATED: Product agent URL -- events now routed through unified agent service
# PRODUCT_AGENT_URL=http://localhost:3005/events
```
Or simply remove the PRODUCT_AGENT_URL entry.

**Update the Database Configuration section comment** to remove the Temporal reference:
- `# Docker Compose internal: postgresql://temporal:temporal@postgresql:5432/temporal` -> keep as-is (DB credentials are still named "temporal" for backward compat)

**3. Update `Dockerfile`:**

Change the default CMD (last line) from:
```dockerfile
CMD ["node", "dist/dev-agent/main.js"]
```
to:
```dockerfile
CMD ["node", "dist/service/main.js"]
```

Update the comment above CMD:
```dockerfile
# Default command - start unified agent service
CMD ["node", "dist/service/main.js"]
```

Remove the old "Other entry points" comments that referenced dev-agent/worker, product-agent, router.

Also remove the comment about Temporal SDK native bindings from the runtime stage:
```dockerfile
# Production image (slim uses glibc, required for Temporal SDK native bindings)
```
Replace with:
```dockerfile
# Production image
```
  </action>
  <verify>
Run `nginx -t -c /dev/stdin < docker-config/nginx.conf 2>&1 || echo "nginx not installed locally, skipping syntax check"` -- if nginx is available locally, verify config. Otherwise just verify no obvious YAML/config errors by reading the file.

Verify `.env.example` does not contain `TEMPORAL_ADDRESS` or `TEMPORAL_NAMESPACE`. Verify it contains `AGENT_SERVICE_PORT`.

Verify `Dockerfile` CMD is `["node", "dist/service/main.js"]`.
  </verify>
  <done>
- nginx.conf has single `agent-service` upstream replacing 3 old upstreams, single `/agent/` location replacing 3 old locations
- .env.example has Agent Service Configuration section, no Temporal section, no PRODUCT_AGENT_URL
- Dockerfile CMD defaults to `node dist/service/main.js` with updated comments
  </done>
</task>

</tasks>

<verification>
1. `docker compose config --quiet` passes without YAML errors
2. No references to temporal, dev-agent, product-agent, or router service names in docker-compose.yml (except volume name which stays)
3. All 3 integration services have ROUTER_URL=http://agent-service:3004/events
4. nginx.conf has exactly 4 upstream blocks: linear, github, slack, agent-service
5. .env.example documents all new env vars and removes Temporal vars
6. Dockerfile CMD is `node dist/service/main.js`
</verification>

<success_criteria>
- Docker Compose defines agent-service as the sole agent runtime service
- Integration services route events to agent-service
- nginx proxies /agent/* to agent-service
- .env.example is a complete template for the new architecture
- Dockerfile builds and defaults to the new entry point
</success_criteria>

<output>
After completion, create `.planning/phases/44-single-service-consolidation/44-02-SUMMARY.md`
</output>

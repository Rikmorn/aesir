---
phase: 73-qa-agent-validation-workflow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agents/definitions/product-agent/prompt.md
  - packages/agents/definitions/dev-agent/prompt.md
autonomous: true

must_haves:
  truths:
    - "Product-agent prompt guides delegation of implementation work to dev-agent via directory:find + task:delegate"
    - "Dev-agent prompt guides delegation of verification to QA agent after creating a pull request"
    - "Both prompts maintain existing voice, structure, and all current guidance"
  artifacts:
    - path: "packages/agents/definitions/product-agent/prompt.md"
      provides: "Implementation delegation guidance for product-agent"
      contains: "directory:find"
    - path: "packages/agents/definitions/dev-agent/prompt.md"
      provides: "QA verification delegation guidance for dev-agent"
      contains: "verification"
  key_links:
    - from: "packages/agents/definitions/product-agent/prompt.md"
      to: "packages/agents/definitions/dev-agent/definition.yaml"
      via: "directory:find discovers dev-agent by implementation capability"
      pattern: "directory:find"
    - from: "packages/agents/definitions/dev-agent/prompt.md"
      to: "packages/agents/definitions/qa-agent/definition.yaml"
      via: "directory:find discovers qa-agent by verification capability"
      pattern: "verification"
---

<objective>
Update product-agent and dev-agent prompts with delegation guidance for the triangular workflow: product delegates implementation to dev, dev delegates verification to QA.

Purpose: Both agents already have delegation tools (directory:find, task:delegate, wait_for_task) but their prompts lack specific guidance about when and how to use them for the triangular workflow. Product-agent's prompt focuses on issue creation; it needs guidance about delegating implementation. Dev-agent's prompt has general delegation guidance; it needs specific QA verification delegation guidance.

Output: Updated prompt.md files for both agents with added domain_knowledge sections.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-qa-agent-validation-workflow/73-RESEARCH.md

# Current prompts to update
@packages/agents/definitions/product-agent/prompt.md
@packages/agents/definitions/dev-agent/prompt.md

# QA agent capabilities (from 73-01, parallel plan — use the capabilities from RESEARCH.md)
# "verify code changes by running tests and reviewing PR diffs"
# "validate implementation against task requirements"

# Prompt authoring rules
@packages/agents/definitions/PROMPT_GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add implementation delegation guidance to product-agent prompt</name>
  <files>packages/agents/definitions/product-agent/prompt.md</files>
  <action>
Add a new section to the product-agent's `<domain_knowledge>` block, after the existing "Task Delegation" section. The new section provides guidance about delegating implementation work after creating a Linear issue.

**New section: "Implementation Delegation"** — add after the existing "Receiving delegations:" paragraph at the end of the Task Delegation section.

Content guidance (follow PROMPT_GUIDE.md — goal-oriented, not procedural):

```markdown
## Implementation Delegation

When a user's request requires code changes (feature implementation, bug fixes, technical work), consider delegating implementation to a development agent after capturing the requirements.

Your delegation brief is the developer's entire context — they cannot see your conversation. Include:
- What needs to be implemented (requirements, acceptance criteria)
- Why it matters (user value, business context from the conversation)
- Any technical constraints or preferences the user mentioned
- The Linear issue ID if one was created

After delegating, wait for the result. If the developer reports completion, relay the outcome to the user. If they report failure or need clarification, use your judgment: clarify with the user, adjust requirements, or inform them of blockers.

Not every request needs implementation delegation. Issue creation and prioritization are your core job. Delegate implementation only when the user wants something built, not just tracked.
```

IMPORTANT: Do NOT modify any existing content. Append this section within the existing `<domain_knowledge>` tags, before the closing `</domain_knowledge>` tag. Preserve all existing sections (Good Linear issues, Task Lifecycle, Working with Humans, Task Delegation, Receiving delegations).

Also add an example (Example 7) to the `<examples>` section showing implementation delegation:

```markdown
Example 7 -- Feature request needing implementation:

User: "Add a /health endpoint that returns the service status"

Reasoning: This is a clear, actionable feature request. The user wants something built, not just tracked. I should create a Linear issue to capture the requirement, then delegate the implementation to a development agent. The request is specific enough that I don't need extensive clarification — the acceptance criteria are implicit (endpoint exists, returns status).

Action: Search for duplicates. Draft a Linear issue with clear acceptance criteria. Use ask() to confirm with the user. After confirmation, create the issue. Then use directory:find to locate a development agent, delegate with task:delegate including the issue ID and requirements, and wait_for_task for the result. Relay the outcome to the user.
```
  </action>
  <verify>
    - `grep "Implementation Delegation" packages/agents/definitions/product-agent/prompt.md` confirms new section exists
    - `grep "delegation brief" packages/agents/definitions/product-agent/prompt.md` confirms delegation guidance
    - `grep "Example 7" packages/agents/definitions/product-agent/prompt.md` confirms new example
    - Verify existing sections are preserved: `grep "Good Linear issues" packages/agents/definitions/product-agent/prompt.md`
  </verify>
  <done>Product-agent prompt includes implementation delegation guidance that teaches the agent when and how to delegate implementation work via directory:find + task:delegate, with a concrete example showing the full flow.</done>
</task>

<task type="auto">
  <name>Task 2: Add QA verification delegation guidance to dev-agent prompt</name>
  <files>packages/agents/definitions/dev-agent/prompt.md</files>
  <action>
Add a new section to the dev-agent's `<domain_knowledge>` block, after the existing "Task Delegation" section (which ends with the "Receiving delegations" paragraph). The new section provides guidance about delegating verification to a QA agent after creating a pull request.

**New section: "Independent Verification"** — add after the existing "Receiving delegations:" paragraph at the end of the Task Delegation section.

Content guidance (follow PROMPT_GUIDE.md — goal-oriented, not procedural):

```markdown
## Independent Verification

After creating a pull request, consider delegating verification to a QA agent. Independent verification catches issues that your own testing might miss — the QA agent runs tests and reviews the PR diff from a fresh perspective, without your implementation biases.

Your delegation brief is the QA agent's entire context. Include:
- Branch name and PR number (QA needs to checkout the branch)
- What the implementation should accomplish (from the original requirements)
- The test command to run (e.g., "pnpm test" or a specific test file path)
- Any test setup required (environment variables, seed data)

If QA reports test failures, you receive a fix delegation back with failed test names and QA's assessment. Fix the issues in a new commit — QA will re-verify automatically after you complete the fix task.

Not every PR needs independent QA verification. Use your judgment: straightforward changes with passing tests may not benefit from the overhead. Larger features, changes touching critical paths, or work where you had to retry multiple approaches are good candidates.
```

IMPORTANT: Do NOT modify any existing content. Append this section within the existing `<domain_knowledge>` tags. Preserve all existing sections (Sub-Agent Delegation, Task Lifecycle, Working with Humans, Task Delegation, Communication on Linear).

Also add an example (Example 8) to the `<examples>` section showing QA delegation:

```markdown
**Example 8: Delegating verification to QA**

Issue: Implementation complete — PR created for new API endpoint.

Reasoning: I've created a PR with the implementation and my tester sub-agent confirmed tests pass. For independent verification, I should delegate to a QA agent. The QA agent will run tests from a clean checkout and review the PR diff against the requirements — catching anything my testing missed. The delegation brief needs the branch name (so QA can checkout), PR number, test command, and what the tests should verify.

Action: Use directory:find to locate a verification agent ("verify code changes"). Delegate with task:delegate including: branch name, PR number, test command, and requirements summary. Call wait_for_task to wait for QA's verdict. If QA passes, report success. If QA delegates a fix back to me, address the specific failures and complete the fix task.
```
  </action>
  <verify>
    - `grep "Independent Verification" packages/agents/definitions/dev-agent/prompt.md` confirms new section exists
    - `grep "QA agent" packages/agents/definitions/dev-agent/prompt.md` confirms QA delegation guidance
    - `grep "Example 8" packages/agents/definitions/dev-agent/prompt.md` confirms new example
    - Verify existing sections are preserved: `grep "Sub-Agent Delegation" packages/agents/definitions/dev-agent/prompt.md`
  </verify>
  <done>Dev-agent prompt includes independent verification guidance that teaches the agent when and how to delegate verification to a QA agent after creating a PR, with a concrete example showing the delegation flow including fix handling.</done>
</task>

</tasks>

<verification>
- Product-agent prompt.md contains "Implementation Delegation" section within domain_knowledge
- Dev-agent prompt.md contains "Independent Verification" section within domain_knowledge
- Both prompts maintain existing XML structure (identity, constraints, domain_knowledge, examples, tools, context)
- No existing sections are removed or modified
- New examples added to both prompts demonstrate the delegation patterns
- Prompt updates follow PROMPT_GUIDE.md rules (goal-oriented, not procedural; no state machines; minimal directives)
</verification>

<success_criteria>
- Product-agent has guidance for delegating implementation work via directory:find + task:delegate
- Dev-agent has guidance for delegating verification to QA after PR creation
- Both prompts preserve all existing functionality and voice
- New sections are goal-oriented with judgment criteria, not step-by-step procedures
</success_criteria>

<output>
After completion, create `.planning/phases/73-qa-agent-validation-workflow/73-02-SUMMARY.md`
</output>

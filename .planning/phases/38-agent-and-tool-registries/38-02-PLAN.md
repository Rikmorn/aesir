---
phase: 38-agent-and-tool-registries
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agents/definitions/dev-agent/definition.yaml
  - packages/agents/definitions/dev-agent/prompt.md
  - packages/agents/definitions/product-agent/definition.yaml
  - packages/agents/definitions/product-agent/prompt.md
  - packages/agents/definitions/researcher/definition.yaml
  - packages/agents/definitions/researcher/prompt.md
  - packages/agents/definitions/coder/definition.yaml
  - packages/agents/definitions/coder/prompt.md
  - packages/agents/definitions/tester/definition.yaml
  - packages/agents/definitions/tester/prompt.md
autonomous: true

must_haves:
  truths:
    - "Each agent definition directory contains a definition.yaml and prompt.md file"
    - "YAML files contain identical configuration to the current hardcoded values (model, tools, iterations, token budget)"
    - "prompt.md files contain the exact system prompt text from the current TypeScript constants"
    - "Tool references use namespace:tool_name format (e.g., linear:get_issue not linear_get_issue)"
    - "Version fields are quoted strings in YAML (not bare numbers)"
  artifacts:
    - path: "packages/agents/definitions/dev-agent/definition.yaml"
      provides: "Dev agent configuration"
      contains: "id: dev-agent"
    - path: "packages/agents/definitions/dev-agent/prompt.md"
      provides: "Dev agent system prompt"
      contains: "You are the dev agent orchestrator"
    - path: "packages/agents/definitions/product-agent/definition.yaml"
      provides: "Product agent configuration"
      contains: "id: product-agent"
    - path: "packages/agents/definitions/product-agent/prompt.md"
      provides: "Product agent system prompt"
      contains: "You are the product agent"
    - path: "packages/agents/definitions/researcher/definition.yaml"
      provides: "Researcher sub-agent configuration"
      contains: "id: researcher"
    - path: "packages/agents/definitions/coder/definition.yaml"
      provides: "Coder sub-agent configuration"
      contains: "id: coder"
    - path: "packages/agents/definitions/tester/definition.yaml"
      provides: "Tester sub-agent configuration"
      contains: "id: tester"
  key_links:
    - from: "packages/agents/definitions/dev-agent/definition.yaml"
      to: "packages/agents/definitions/researcher/definition.yaml"
      via: "subAgents.researcher reference"
      pattern: "researcher: researcher"
---

<objective>
Extract all 5 agent definitions (dev-agent, product-agent, researcher, coder, tester) from hardcoded TypeScript into YAML config + prompt.md data files.

Purpose: Transforms agent configuration from code into data. This is the content extraction that makes agents declarative -- adding a new agent requires only a new definition directory, zero code changes.
Output: 5 definition directories under packages/agents/definitions/, each with definition.yaml and prompt.md.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-agent-and-tool-registries/38-RESEARCH.md

@packages/agents/src/dev-agent/orchestrator/system-prompts.ts
@packages/agents/src/dev-agent/orchestrator/orchestrator.ts
@packages/agents/src/product-agent/orchestrator/system-prompts.ts
@packages/agents/src/product-agent/orchestrator/orchestrator.ts
@packages/agents/src/shared/tools/toolkits.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract dev-agent and product-agent definitions</name>
  <files>
    packages/agents/definitions/dev-agent/definition.yaml
    packages/agents/definitions/dev-agent/prompt.md
    packages/agents/definitions/product-agent/definition.yaml
    packages/agents/definitions/product-agent/prompt.md
  </files>
  <action>
Create the directory structure: `packages/agents/definitions/dev-agent/` and `packages/agents/definitions/product-agent/`.

**dev-agent/definition.yaml** -- Extract from orchestrator.ts and toolkits.ts:
```yaml
id: dev-agent
name: Development Agent
description: >
  Autonomous development agent that resolves Linear issues through
  codebase research, planning, implementation via sub-agents, testing,
  and pull request creation.
version: "1"

model: claude-sonnet-4-20250514
temperature: 0

tools:
  - codebase:read_file
  - codebase:search_codebase
  - codebase:list_directory
  - coordination:spawn_agent
  - coordination:request_human_input
  - coordination:wait_for
  - linear:get_issue
  - linear:update_issue_status
  - github:create_branch
  - github:create_commit
  - github:create_pull_request
  - github:get_pull_request
  - slack:send_message
  - slack:send_approval_request

subAgents:
  researcher: researcher
  coder: coder
  tester: tester

maxIterations: 100
tokenBudget: 500000

history:
  pruneThreshold: 80000
  protectedMessages: 20
  summaryThreshold: 120000
  summaryModel: claude-haiku-4-5-20251001

triggers:
  - event: linear.agent_session.created
```

Verify tool list matches orchestrator toolkit:
- 3 codebase: read_file, search_codebase, list_directory (orchestrator is read-only, NO write_file, NO run_command)
- 2 coordination: spawn_agent, request_human_input (+ wait_for for v2.3)
- 2 linear: get_issue, update_issue_status
- 4 github: create_branch, create_commit, create_pull_request, get_pull_request
- 2 slack: send_message, send_approval_request
Total: 14 tools (13 from v2.2 + wait_for from v2.3)

Verify config values from orchestrator.ts:
- maxIterations: 100 (options.maxIterations ?? 100)
- tokenBudget: 500000 (options.maxTokenBudget ?? 500_000)
- model: claude-sonnet-4-20250514 (loop default from agent-loop types)

**dev-agent/prompt.md** -- Copy the EXACT text from ORCHESTRATOR_SYSTEM_PROMPT constant in `packages/agents/src/dev-agent/orchestrator/system-prompts.ts`. Copy the string content verbatim -- everything between the backtick delimiters. Do NOT add markdown headers or frontmatter. The file IS the prompt text.

**product-agent/definition.yaml** -- Extract from product-agent orchestrator.ts and toolkits.ts:
```yaml
id: product-agent
name: Product Agent
description: >
  Conversational product agent that helps teams capture feature requests
  and bug reports from Slack conversations and turns them into
  well-structured Linear issues.
version: "1"

model: claude-sonnet-4-20250514

tools:
  - linear:create_issue
  - linear:get_issue
  - linear:list_labels
  - linear:search_issues
  - slack:send_message

maxIterations: 10
tokenBudget: 50000

history:
  pruneThreshold: 30000
  protectedMessages: 10
  summaryThreshold: 50000
  summaryModel: claude-haiku-4-5-20251001

triggers:
  - event: slack.app_mention.created
```

Verify tool list matches product agent toolkit:
- 4 linear: create_issue, get_issue, list_labels, search_issues
- 1 slack: send_message
Total: 5 tools

Verify config values from product-agent orchestrator.ts:
- maxIterations: 10 (options.maxIterations ?? 10)
- tokenBudget: 50000 (options.maxTokenBudget ?? 50_000)

NOTE: product-agent has NO subAgents, NO temperature override, NO coordination tools.

**product-agent/prompt.md** -- Copy the EXACT text from PRODUCT_AGENT_SYSTEM_PROMPT constant in `packages/agents/src/product-agent/orchestrator/system-prompts.ts`.
  </action>
  <verify>
Verify files exist and have correct content:
- `ls packages/agents/definitions/dev-agent/` shows definition.yaml and prompt.md
- `ls packages/agents/definitions/product-agent/` shows definition.yaml and prompt.md
- Verify prompt.md content matches source: compare line counts of prompt.md vs the constant in system-prompts.ts
  </verify>
  <done>dev-agent and product-agent definition directories exist with correct YAML configs and exact system prompt text. Tool lists and config values match the hardcoded TypeScript equivalents.</done>
</task>

<task type="auto">
  <name>Task 2: Extract researcher, coder, and tester sub-agent definitions</name>
  <files>
    packages/agents/definitions/researcher/definition.yaml
    packages/agents/definitions/researcher/prompt.md
    packages/agents/definitions/coder/definition.yaml
    packages/agents/definitions/coder/prompt.md
    packages/agents/definitions/tester/definition.yaml
    packages/agents/definitions/tester/prompt.md
  </files>
  <action>
Create directories: `packages/agents/definitions/researcher/`, `packages/agents/definitions/coder/`, `packages/agents/definitions/tester/`.

**researcher/definition.yaml** -- Extract from toolkits.ts createResearcherToolkit() and sub-agent config in createOrchestratorToolkit():
```yaml
id: researcher
name: Code Researcher
description: >
  Explores codebases to gather information needed for implementation
  planning. Reads files, searches code, and produces structured research
  reports.
version: "1"

model: claude-haiku-4-5-20251001

tools:
  - codebase:read_file
  - codebase:search_codebase
  - codebase:list_directory
  - codebase:run_command

maxIterations: 30
tokenBudget: 100000

history:
  pruneThreshold: 40000
  protectedMessages: 10
  summaryThreshold: 60000
  summaryModel: claude-haiku-4-5-20251001
```

Verify: 4 tools (read_file, search_codebase, list_directory, run_command) matching createResearcherToolkit(). Model: claude-haiku-4-5-20251001. maxIterations: 30 (from subAgentConfigs in toolkits.ts).

NOTE: Sub-agents do NOT have their own tokenBudget in v2.2 (they share the parent's). For v2.3 definitions, set a reasonable standalone tokenBudget (100000 for researcher). The executor will override this with the parent's shared budget when spawning. Sub-agents do NOT have triggers (they are spawned, not triggered by events). Sub-agents do NOT have subAgents (no recursive spawning).

**researcher/prompt.md** -- Copy EXACT text from RESEARCHER_SYSTEM_PROMPT.

**coder/definition.yaml**:
```yaml
id: coder
name: Code Implementer
description: >
  Writes and modifies code according to implementation plans. Produces
  working, type-safe code that compiles and passes linting.
version: "1"

model: claude-haiku-4-5-20251001

tools:
  - codebase:read_file
  - codebase:write_file
  - codebase:search_codebase
  - codebase:run_command

maxIterations: 40
tokenBudget: 100000

history:
  pruneThreshold: 40000
  protectedMessages: 10
  summaryThreshold: 60000
  summaryModel: claude-haiku-4-5-20251001
```

Verify: 4 tools (read_file, write_file, search_codebase, run_command) matching createCoderToolkit(). maxIterations: 40.

**coder/prompt.md** -- Copy EXACT text from CODER_SYSTEM_PROMPT.

**tester/definition.yaml**:
```yaml
id: tester
name: Test Runner
description: >
  Runs tests, analyzes failures, and provides clear diagnoses with
  categorized root cause analysis.
version: "1"

model: claude-haiku-4-5-20251001

tools:
  - codebase:read_file
  - codebase:search_codebase
  - codebase:run_command

maxIterations: 20
tokenBudget: 100000

history:
  pruneThreshold: 40000
  protectedMessages: 10
  summaryThreshold: 60000
  summaryModel: claude-haiku-4-5-20251001
```

Verify: 3 tools (read_file, search_codebase, run_command) matching createTesterToolkit(). maxIterations: 20.

**tester/prompt.md** -- Copy EXACT text from TESTER_SYSTEM_PROMPT.

IMPORTANT for all prompt.md files: The content must be the EXACT text from the TypeScript string constants. No added headers, no frontmatter, no modifications. These files ARE the system prompts -- static strings as specified in the codebase patterns.
  </action>
  <verify>
- `ls packages/agents/definitions/researcher/` shows definition.yaml and prompt.md
- `ls packages/agents/definitions/coder/` shows definition.yaml and prompt.md
- `ls packages/agents/definitions/tester/` shows definition.yaml and prompt.md
- Total: 5 directories, 10 files in packages/agents/definitions/
  </verify>
  <done>All 5 agent definition directories exist with YAML configs matching the hardcoded TypeScript values (tool lists, models, iteration limits) and prompt.md files containing exact system prompt text.</done>
</task>

</tasks>

<verification>
1. `ls -R packages/agents/definitions/` shows 5 directories each with definition.yaml + prompt.md
2. Tool counts match: dev-agent=14, product-agent=5, researcher=4, coder=4, tester=3
3. Models match: dev-agent=claude-sonnet-4-20250514, all others=claude-haiku-4-5-20251001 (except product-agent=claude-sonnet-4-20250514)
4. maxIterations match: dev-agent=100, product-agent=10, researcher=30, coder=40, tester=20
5. version fields are quoted strings ("1") in all YAML files
</verification>

<success_criteria>
- 5 agent definition directories exist under packages/agents/definitions/
- Each contains definition.yaml + prompt.md
- YAML configs are the data equivalent of the hardcoded TypeScript values
- prompt.md files are exact copies of the system prompt string constants
- Tool references use namespace:tool_name format throughout
</success_criteria>

<output>
After completion, create `.planning/phases/38-agent-and-tool-registries/38-02-SUMMARY.md`
</output>

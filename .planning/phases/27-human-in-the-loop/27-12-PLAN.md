---
phase: 27-human-in-the-loop
plan: 12
type: execute
wave: 6
depends_on: ["27-11"]
files_modified:
  - packages/agents/src/dev-agent/integration.test.ts
autonomous: false

must_haves:
  truths:
    - "Approval via Slack button resumes workflow"
    - "Workflow continues to execution after approval"
    - "Linear status and Slack message updated after approval"
  artifacts:
    - path: "packages/agents/src/dev-agent/integration.test.ts"
      provides: "Integration test for HITL flow"
      contains: "approval.*signal"
  key_links:
    - from: "packages/agents/src/dev-agent/integration.test.ts"
      to: "temporal workflows"
      via: "test assertions"
      pattern: "expect.*phase.*executing"
---

<objective>
E2E verification of HITL approval flow.

Purpose: Verify the complete human-in-the-loop flow works end-to-end: plan posted -> approval via Slack -> workflow resumes -> execution continues. This is the critical path that validates Phase 27 success.

Output: Verified HITL flow with documented test results
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-human-in-the-loop/27-CONTEXT.md
@.planning/phases/27-human-in-the-loop/27-RESEARCH.md
@.planning/phases/27-human-in-the-loop/27-11-SUMMARY.md

# Reference patterns
@packages/agents/src/dev-agent/integration.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HITL integration tests</name>
  <files>packages/agents/src/dev-agent/integration.test.ts</files>
  <action>
Add integration tests for HITL approval flow.

In packages/agents/src/dev-agent/integration.test.ts:

Add test cases (may need to skip if infrastructure not available):

```typescript
describe("Human-in-the-Loop Approval Flow", () => {
  describe("Approval Signal Handling", () => {
    it("should update workflow state on approval signal", async () => {
      // This test verifies signal handling logic
      // May need mocking for Temporal client

      // Setup: Create a mock workflow state in awaiting_approval
      // Action: Send planApprovalSignal with approved: true
      // Assert: Workflow state transitions to executing
    });

    it("should include approver metadata in signal", async () => {
      // Verify approverName and source are passed through
    });

    it("should handle rejection with feedback", async () => {
      // Setup: Workflow in awaiting_approval
      // Action: Send planApprovalSignal with approved: false, feedback
      // Assert: Workflow routes to re-planning
    });

    it("should handle WorkflowNotFoundError gracefully", async () => {
      // Send signal to non-existent workflow
      // Assert: Returns { signaled: false, error: "Workflow not found" }
    });
  });

  describe("Cross-Channel Sync", () => {
    it("should sync Slack approval to Linear", async () => {
      // Verify Linear comment and status update on Slack approval
      // May need MCP tool mocking
    });

    it("should update Slack message after approval", async () => {
      // Verify update_message called with correct params
    });
  });

  describe("PR Completion Flow", () => {
    it("should trigger completion on PR merge signal", async () => {
      // Send prCompletionSignal with merged: true
      // Assert: Completion flow runs
    });

    it("should handle PR closed without merge", async () => {
      // Send prCompletionSignal with merged: false
      // Assert: Cancellation handling
    });
  });
});
```

Note: These tests may need significant mocking for Temporal, MCP tools, etc.
Mark as .skip() if infrastructure not available, with TODO comments.
  </action>
  <verify>
    - pnpm run typecheck
    - Tests compile (may skip execution)
  </verify>
  <done>
    - Test cases cover approval, rejection, completion flows
    - Tests document expected behavior
    - Skipped tests have clear TODO comments
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Human-in-the-Loop approval flow for dev-agent workflow:
- Slack button clicks send approval signals to Temporal workflows
- Approval updates Slack message and syncs to Linear
- Rejection routes to re-planning flow
- PR merge triggers task completion
  </what-built>
  <how-to-verify>
1. Start services: `docker compose up -d`

2. Trigger dev-agent workflow:
   - Create Linear issue with "agent-ready" label
   - Or trigger via agent_session webhook

3. Wait for plan approval request:
   - Check Linear for plan comment
   - Check Slack for approval buttons

4. Test approval via Slack:
   - Click "Approve" button in Slack
   - Verify: Slack message updates to show "Approved by..."
   - Verify: Linear gets comment "Plan approved via Slack by..."
   - Verify: Workflow continues to execution phase

5. (Optional) Test rejection:
   - On a new issue, click "Reject" button
   - Verify: Agent asks for feedback in thread
   - Provide feedback
   - Verify: Revised plan posted in thread

6. Test completion (after PR created):
   - Merge the PR
   - Verify: Linear status updated to "Done"
   - Verify: Slack gets completion notification
   - Verify: Container cleaned up
  </how-to-verify>
  <resume-signal>
Type "verified" with results, or describe issues found
  </resume-signal>
</task>

</tasks>

<verification>
Manual E2E verification following the steps above.
</verification>

<success_criteria>
- Approval via Slack button resumes workflow
- Slack message updated after approval
- Linear synced with approval status
- PR merge triggers completion flow
- E2E flow documented and working
</success_criteria>

<output>
After completion, create `.planning/phases/27-human-in-the-loop/27-12-SUMMARY.md`
</output>

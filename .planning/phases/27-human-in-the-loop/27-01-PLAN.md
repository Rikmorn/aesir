---
phase: 27-human-in-the-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agents/src/dev-agent/classification/approval.ts
  - packages/agents/src/dev-agent/classification/approval.test.ts
autonomous: true

must_haves:
  truths:
    - "LLM can classify natural language as approve/reject/unclear/question"
    - "Classification returns confidence level (high/medium/low)"
    - "Rejection feedback is extracted when rejecting"
  artifacts:
    - path: "packages/agents/src/dev-agent/classification/approval.ts"
      provides: "LLM-based approval intent classification"
      exports: ["classifyApprovalIntent", "ApprovalClassificationSchema"]
    - path: "packages/agents/src/dev-agent/classification/approval.test.ts"
      provides: "Unit tests for approval classification"
      min_lines: 50
  key_links:
    - from: "packages/agents/src/dev-agent/classification/approval.ts"
      to: "@langchain/anthropic"
      via: "ChatAnthropic.withStructuredOutput"
      pattern: "withStructuredOutput.*ApprovalClassificationSchema"
---

<objective>
Implement LLM-based approval intent classification for human responses to execution plans.

Purpose: Enable natural language approval/rejection detection (e.g., "looks good", "ship it", "hold on, missing tests") instead of requiring exact keywords. This is the core classification used by both Slack message handlers and Linear comment handlers.

Output: Classification module with schema, classifier function, and comprehensive tests
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-human-in-the-loop/27-CONTEXT.md
@.planning/phases/27-human-in-the-loop/27-RESEARCH.md

# Reference patterns
@packages/agents/src/product-agent/nodes/classify.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create approval classification module</name>
  <files>packages/agents/src/dev-agent/classification/approval.ts</files>
  <action>
Create LLM-based approval classification module following the product-agent classify.ts pattern.

Create file at packages/agents/src/dev-agent/classification/approval.ts with:

1. Zod schema for ApprovalClassification:
   - intent: enum ["approve", "reject", "unclear", "question"]
   - confidence: enum ["high", "medium", "low"]
   - feedback: string | null (extracted feedback if rejection or changes requested)
   - reasoning: string (explanation for classification)

2. Classification prompt constant:
   - Context: Human responded to an implementation plan
   - Clear examples for each intent type:
     - approve: "looks good", "ship it", "approved", "go ahead", thumbs up references
     - reject: "no", "don't do this", specific concerns, "missing X"
     - question: "what about X?", "did you consider Y?", clarifying questions
     - unclear: ambiguous, off-topic, can't determine

3. classifyApprovalIntent function:
   - Takes ChatAnthropic instance and message string
   - Uses withStructuredOutput with ApprovalClassificationSchema
   - Returns ApprovalClassification

Follow the exact pattern from product-agent classify.ts for structured output.
  </action>
  <verify>
    - pnpm run typecheck (no type errors)
    - File exists with correct exports
  </verify>
  <done>
    - Module exports ApprovalClassificationSchema, classifyApprovalIntent
    - Schema validates intent, confidence, feedback, reasoning fields
    - Function uses LLM structured output for reliable classification
  </done>
</task>

<task type="auto">
  <name>Task 2: Create approval classification tests</name>
  <files>packages/agents/src/dev-agent/classification/approval.test.ts</files>
  <action>
Create comprehensive unit tests for approval classification.

Test cases to cover:

1. Schema validation tests:
   - Valid approve classification
   - Valid reject classification with feedback
   - Valid question classification
   - Invalid missing fields rejected

2. Mock LLM tests (use vi.mock for ChatAnthropic):
   - Test approve intent detection for various phrases
   - Test reject intent detection with feedback extraction
   - Test question detection
   - Test unclear fallback
   - Test confidence levels

3. Edge cases:
   - Empty message
   - Very long message (truncation behavior)
   - Emoji-only messages (should handle gracefully)

Use vitest patterns from existing test files.
  </action>
  <verify>
    - pnpm --filter @aesir/agents test packages/agents/src/dev-agent/classification/approval.test.ts
    - All tests pass
  </verify>
  <done>
    - Schema validation tests cover valid and invalid inputs
    - Classification function tests cover all intent types
    - Edge cases handled appropriately
  </done>
</task>

</tasks>

<verification>
- pnpm run typecheck
- pnpm --filter @aesir/agents test packages/agents/src/dev-agent/classification/approval.test.ts
</verification>

<success_criteria>
- Approval classification module exists with typed schema
- Function uses LLM structured output for natural language classification
- Tests cover approve, reject, question, unclear intents
- Module follows product-agent classify.ts patterns
</success_criteria>

<output>
After completion, create `.planning/phases/27-human-in-the-loop/27-01-SUMMARY.md`
</output>

---
phase: 12-observability
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/platform/src/temporal/worker.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Temporal worker uses Runtime.install with the pino-based logger adapter"
    - "Temporal internal logs (workflow execution, task polling) flow through pino"
    - "Stale dist artifacts from deleted legacy logger are cleaned"
  artifacts:
    - path: "packages/platform/src/temporal/worker.ts"
      provides: "Temporal worker with Runtime.install logger configuration"
      contains: "Runtime.install"
  key_links:
    - from: "packages/platform/src/temporal/worker.ts"
      to: "@aesir/common"
      via: "import createTemporalLogger"
      pattern: "Runtime\\.install.*createTemporalLogger"
---

<objective>
Wire Temporal logger adapter and clean build artifacts

Purpose: Close verification gaps 2 and 3 from 12-VERIFICATION.md:
- Gap 2: createTemporalLogger adapter exists but is not wired to Runtime.install
- Gap 3: Stale logger.js/logger.d.ts files in packages/common/dist/logging/

Output: Temporal worker using pino-based logging via Runtime.install, and clean dist directory.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-observability/12-VERIFICATION.md
@.planning/phases/12-observability/12-03-SUMMARY.md

# Source files
@packages/platform/src/temporal/worker.ts
@packages/common/src/logging/temporal-logger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Temporal logger adapter to Runtime.install</name>
  <files>packages/platform/src/temporal/worker.ts</files>
  <action>
Modify the Temporal worker to use the pino-based logger adapter:

1. Import `Runtime` from `@temporalio/worker`
2. Import `createTemporalLogger` from `@aesir/common`
3. Call `Runtime.install({ logger: createTemporalLogger() })` at module initialization (before any worker creation)

Important placement: Runtime.install MUST be called before Worker.create is called. Add it at the top of the file after imports, or as the first line in createTemporalWorker before any other Temporal calls.

Per Temporal docs, Runtime.install should be called once per process before any other Temporal operations.

Pattern:
```typescript
import { Runtime, NativeConnection, Worker } from "@temporalio/worker";
import { createPinoLogger, createTemporalLogger } from "@aesir/common";

// Install Temporal runtime with pino-based logger
// Must be called before creating any workers
Runtime.install({
  logger: createTemporalLogger(),
});

const logger = createPinoLogger({ component: "platform:temporal" });
// ... rest of file unchanged
```

Do NOT:
- Change any existing worker creation logic
- Modify activity or workflow configuration
- Remove existing logging calls (they're for application logs, not Temporal internals)
  </action>
  <verify>
`pnpm --filter @aesir/platform build` succeeds without errors
`grep -n "Runtime.install" packages/platform/src/temporal/worker.ts` shows the call
`grep -n "createTemporalLogger" packages/platform/src/temporal/worker.ts` shows the import and usage
  </verify>
  <done>
Temporal Runtime configured with pino-based logger before worker creation
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean stale dist artifacts</name>
  <files>packages/common/dist/logging/</files>
  <action>
Remove stale build artifacts from the deleted legacy Logger class:

1. Delete packages/common/dist/logging/logger.js
2. Delete packages/common/dist/logging/logger.d.ts
3. Delete packages/common/dist/logging/logger.js.map
4. Delete packages/common/dist/logging/logger.d.ts.map
5. Delete packages/common/dist/logging/logger.test.js (if exists)
6. Delete packages/common/dist/logging/logger.test.d.ts (if exists)
7. Delete packages/common/dist/logging/logger.test.js.map (if exists)
8. Delete packages/common/dist/logging/logger.test.d.ts.map (if exists)

Then run a clean rebuild to ensure no orphan references:
```bash
pnpm --filter @aesir/common run build
```

Alternative: If the files keep appearing, add them to .gitignore and delete from git tracking:
```bash
git rm --cached packages/common/dist/logging/logger.*
```

Note: dist/ should ideally be in .gitignore. If it's not and this is intentional (checked into repo), the deletion should be permanent.
  </action>
  <verify>
`ls packages/common/dist/logging/ | grep -c "^logger\\."` returns 0 (no logger.* files)
`pnpm --filter @aesir/common build` succeeds
  </verify>
  <done>
Stale legacy logger build artifacts removed, dist directory clean
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `pnpm build` - full monorepo build succeeds
2. `grep -r "Runtime.install" packages/platform/src/temporal/` - shows configuration
3. `ls packages/common/dist/logging/` - no logger.js or logger.d.ts files
4. Import check: `@aesir/common` exports work correctly
</verification>

<success_criteria>
- Temporal Runtime uses createTemporalLogger for internal logging
- Temporal internal logs flow through pino (verified by marker: temporal: true in logs)
- No stale logger.* files in packages/common/dist/logging/
- Build passes for all affected packages
- Gaps 2 and 3 from 12-VERIFICATION.md are closed
</success_criteria>

<output>
After completion, create `.planning/phases/12-observability/12-08-SUMMARY.md`
</output>

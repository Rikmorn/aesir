---
phase: 12-observability
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agents/src/api/webhooks/github-pr-review.ts
  - packages/agents/src/api/webhooks/linear-agent-session.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Every HTTP request generates a unique correlation ID visible in all resulting log entries"
    - "Webhook handlers use per-request loggers instead of module-level static loggers"
    - "Correlation ID appears in all log entries from webhook processing"
  artifacts:
    - path: "packages/agents/src/api/webhooks/github-pr-review.ts"
      provides: "Webhook handler with per-request correlation logging"
      contains: "generateCorrelationId"
    - path: "packages/agents/src/api/webhooks/linear-agent-session.ts"
      provides: "Webhook handler with per-request correlation logging"
      contains: "generateCorrelationId"
  key_links:
    - from: "packages/agents/src/api/webhooks/github-pr-review.ts"
      to: "@aesir/common"
      via: "import generateCorrelationId, createChildLogger"
      pattern: "generateCorrelationId.*req"
    - from: "packages/agents/src/api/webhooks/linear-agent-session.ts"
      to: "@aesir/common"
      via: "import generateCorrelationId, createChildLogger"
      pattern: "generateCorrelationId.*req"
---

<objective>
Wire correlation ID generation into webhook handlers

Purpose: Close verification gap OBSV-02 - webhook handlers currently use module-level static loggers without per-request correlation IDs. This makes it impossible to trace logs from the same request.

Output: Webhook handlers that generate a correlation ID per request and pass it through all log entries during request processing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-observability/12-VERIFICATION.md
@.planning/phases/12-observability/12-03-SUMMARY.md

# Source files to modify
@packages/agents/src/api/webhooks/github-pr-review.ts
@packages/agents/src/api/webhooks/linear-agent-session.ts

# Available utilities
@packages/common/src/logging/correlation.ts
@packages/common/src/logging/pino-logger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-request correlation to GitHub webhook handler</name>
  <files>packages/agents/src/api/webhooks/github-pr-review.ts</files>
  <action>
Modify the GitHub PR review webhook handler to generate correlation IDs per request:

1. Keep the module-level logger as a base logger (rename to `baseLogger`)
2. Import `generateCorrelationId` and `createChildLogger` from `@aesir/common`
3. In `prReviewWebhookHandler`:
   - Generate a correlation ID at the start: `const correlationId = generateCorrelationId('req')`
   - Create a child logger: `const logger = createChildLogger(baseLogger, { correlationId })`
   - Pass this logger to `handlePRReviewEvent` (add logger parameter)
4. In `handlePRReviewEvent`:
   - Add optional `logger?: PinoLogger` parameter with fallback to base logger
   - Use the passed logger for all log calls

Pattern to follow:
```typescript
const baseLogger: PinoLogger = createPinoLogger({
  component: "agents:webhooks:github-pr-review",
});

export async function prReviewWebhookHandler(
  req: WebhookRequest,
  res: WebhookResponse,
): Promise<void> {
  const correlationId = generateCorrelationId("req");
  const logger = createChildLogger(baseLogger, { correlationId });

  // ... validation ...

  const result = await handlePRReviewEvent(req.body, logger);
  res.status(200).json(result);
}
```

Do NOT:
- Remove the base logger (it's needed for component context)
- Change the public API signatures other than adding optional logger
- Modify any business logic
  </action>
  <verify>
`pnpm --filter @aesir/agents build` succeeds without errors
`grep -n "generateCorrelationId" packages/agents/src/api/webhooks/github-pr-review.ts` shows import and usage
  </verify>
  <done>
GitHub webhook handler generates correlation ID per request and passes logger through handler chain
  </done>
</task>

<task type="auto">
  <name>Task 2: Add per-request correlation to Linear webhook handler</name>
  <files>packages/agents/src/api/webhooks/linear-agent-session.ts</files>
  <action>
Apply the same pattern to the Linear AgentSession webhook handler:

1. Keep the module-level logger as `baseLogger`
2. Import `generateCorrelationId` and `createChildLogger` from `@aesir/common`
3. In `linearWebhookHandler`:
   - Generate correlation ID at start
   - Create child logger with correlationId binding
   - Pass logger to `handleAgentSessionWebhook`
4. In `handleAgentSessionWebhook`:
   - Add optional `logger?: PinoLogger` parameter
   - Use passed logger for all log calls

Same pattern as Task 1. Ensure:
- Signature change is backward compatible (optional parameter)
- All existing log calls use the request-scoped logger
- Module-level baseLogger only used for initialization
  </action>
  <verify>
`pnpm --filter @aesir/agents build` succeeds without errors
`grep -n "generateCorrelationId" packages/agents/src/api/webhooks/linear-agent-session.ts` shows import and usage
  </verify>
  <done>
Linear webhook handler generates correlation ID per request and passes logger through handler chain
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `pnpm --filter @aesir/agents build` - builds without errors
2. `pnpm --filter @aesir/agents test` - existing tests pass
3. Each webhook handler file contains:
   - Import of `generateCorrelationId` from `@aesir/common`
   - Import of `createChildLogger` from `@aesir/common`
   - Correlation ID generation in HTTP handler function
   - Child logger creation with correlationId binding
</verification>

<success_criteria>
- Webhook handlers generate unique correlation ID per HTTP request
- Correlation ID appears in all log entries from that request
- No module-level logger calls remain in request path (only base logger creation)
- Build passes, existing tests pass
- Gap 1 from 12-VERIFICATION.md is closed
</success_criteria>

<output>
After completion, create `.planning/phases/12-observability/12-07-SUMMARY.md`
</output>

---
phase: 12-observability
plan: 04
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - packages/integrations/src/slack/bolt-app.ts
  - packages/integrations/src/slack/notifications.ts
  - packages/integrations/src/slack/client.ts
  - packages/integrations/src/linear/issues.ts
  - packages/integrations/src/linear/client.ts
  - packages/integrations/src/github/pull-requests.ts
  - packages/integrations/src/github/commits.ts
  - packages/integrations/src/github/client.ts
  - packages/integrations/src/github/branches.ts
  - packages/platform/src/sandbox/docker-sandbox.ts
  - packages/platform/src/temporal/client.ts
  - packages/platform/src/temporal/worker.ts
autonomous: true

must_haves:
  truths:
    - "All integration modules use pino logger, not legacy Logger class"
    - "All platform modules use pino logger, not legacy Logger class"
    - "Component naming follows layer:module format in all files"
    - "No console.log calls remain in integrations or platform packages"
  artifacts:
    - path: "packages/integrations/src/slack/bolt-app.ts"
      provides: "Slack Bolt app with pino logging"
      contains: "createLogger"
    - path: "packages/platform/src/temporal/worker.ts"
      provides: "Temporal worker with pino logging"
      contains: "createLogger"
  key_links:
    - from: "packages/integrations/src/slack/bolt-app.ts"
      to: "@aesir/common"
      via: "import createLogger"
      pattern: "import.*createLogger.*from [\"']@aesir/common[\"']"
---

<objective>
Migrate integrations and platform packages to pino logger

Purpose: Update all logging calls in integrations/ and platform/ packages to use the new pino-based createLogger, eliminating usage of the legacy Logger class and any console.log statements.

Output: All files in integrations/ and platform/ use pino logger with proper component naming.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-observability/12-CONTEXT.md
@packages/common/src/logging/pino-logger.ts
@packages/integrations/src/slack/bolt-app.ts
@packages/platform/src/temporal/worker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate integrations package to pino</name>
  <files>
    packages/integrations/src/slack/bolt-app.ts
    packages/integrations/src/slack/notifications.ts
    packages/integrations/src/slack/client.ts
    packages/integrations/src/linear/issues.ts
    packages/integrations/src/linear/client.ts
    packages/integrations/src/github/pull-requests.ts
    packages/integrations/src/github/commits.ts
    packages/integrations/src/github/client.ts
    packages/integrations/src/github/branches.ts
  </files>
  <action>
Update all files in packages/integrations/ that use logging to use pino.

**Migration pattern:**

BEFORE (legacy Logger):
```typescript
import { createLogger } from "@aesir/common";
const logger = createLogger({ defaultContext: { module: "slack-bolt-app" } });
logger.info("bolt_app_created", { outcome: "success", message: "Bolt app created" });
```

AFTER (pino):
```typescript
import { createLogger } from "@aesir/common";
const logger = createLogger({ component: "integrations:slack" });
logger.info({ outcome: "success" }, "Bolt app created");
```

**Key changes:**
1. Change `defaultContext: { module: "xxx" }` to `component: "integrations:xxx"`
2. Change `logger.info("action", { message: "...", ...data })` to `logger.info({ ...data }, "message")`
3. pino signature: `logger.info(mergeObject, message)` not `logger.info(action, options)`
4. Remove `outcome` from log calls (pino doesn't have this concept - use level instead)
5. Remove `durationMs` - use pino-http for automatic timing or add as field

**Component naming:**
- Slack files: `integrations:slack`
- Linear files: `integrations:linear`
- GitHub files: `integrations:github`

**For each file:**

1. `packages/integrations/src/slack/bolt-app.ts`:
   - Component: `integrations:slack`
   - Convert all logger calls to pino signature

2. `packages/integrations/src/slack/notifications.ts`:
   - Component: `integrations:slack`

3. `packages/integrations/src/slack/client.ts`:
   - Component: `integrations:slack`

4. `packages/integrations/src/linear/issues.ts`:
   - Component: `integrations:linear`

5. `packages/integrations/src/linear/client.ts`:
   - Component: `integrations:linear`

6. `packages/integrations/src/github/pull-requests.ts`:
   - Component: `integrations:github`

7. `packages/integrations/src/github/commits.ts`:
   - Component: `integrations:github`
   - Remove any console.log statements

8. `packages/integrations/src/github/client.ts`:
   - Component: `integrations:github`

9. `packages/integrations/src/github/branches.ts`:
   - Component: `integrations:github`

Also remove console.log from:
- `packages/integrations/src/linear/token-store.ts` (if any)
  </action>
  <verify>
- `pnpm build --filter @aesir/integrations` succeeds
- `grep -r "console.log" packages/integrations/src --include="*.ts" | grep -v ".test.ts"` returns nothing
- All files use `createLogger` from @aesir/common with component parameter
  </verify>
  <done>All integrations files use pino logger with layer:module component naming</done>
</task>

<task type="auto">
  <name>Task 2: Migrate platform package to pino</name>
  <files>
    packages/platform/src/sandbox/docker-sandbox.ts
    packages/platform/src/temporal/client.ts
    packages/platform/src/temporal/worker.ts
  </files>
  <action>
Update all files in packages/platform/ that use logging to use pino.

**Same migration pattern as Task 1.**

**Component naming:**
- Sandbox files: `platform:sandbox`
- Temporal files: `platform:temporal`

**For each file:**

1. `packages/platform/src/sandbox/docker-sandbox.ts`:
   - Component: `platform:sandbox`
   - Remove console.log statements (convert to logger.debug or logger.info)

2. `packages/platform/src/temporal/client.ts`:
   - Component: `platform:temporal`

3. `packages/platform/src/temporal/worker.ts`:
   - Component: `platform:temporal`
   - Consider using createTemporalLogger for Temporal Runtime integration

Also remove console.log from test files if they're not intentional test output:
- `packages/platform/src/sandbox/docker-sandbox.test.ts`
  </action>
  <verify>
- `pnpm build --filter @aesir/platform` succeeds
- `grep -r "console.log" packages/platform/src --include="*.ts" | grep -v ".test.ts"` returns nothing (or only intentional test output)
  </verify>
  <done>All platform files use pino logger with layer:module component naming</done>
</task>

<task type="auto">
  <name>Task 3: Remove console.log from config/env.ts</name>
  <files>packages/common/src/config/env.ts</files>
  <action>
The env.ts file has console.log for startup messages. Convert these to proper logging or remove if not needed.

Options:
1. If it's logging env validation errors: Keep but convert to process.stderr.write (pino may not be initialized yet)
2. If it's debug/info output: Remove or convert to conditional debug logging

Check the file and apply appropriate fix. The startup sequence matters here - if pino depends on env config, we can't use pino to log env errors.

For early startup logging before pino is available, use:
```typescript
if (process.env.NODE_ENV === 'development') {
  process.stderr.write(`[config] Loading environment: ${process.env.NODE_ENV}\n`);
}
```

Or simply remove if not critical.
  </action>
  <verify>
- `pnpm build --filter @aesir/common` succeeds
- No console.log in env.ts (process.stderr.write is acceptable for early startup)
  </verify>
  <done>env.ts uses appropriate logging for startup phase</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. All packages build:
```bash
pnpm build
```

2. No console.log in production code:
```bash
grep -r "console\.log" packages/*/src --include="*.ts" | grep -v ".test.ts" | grep -v "node_modules"
```

3. All loggers use component parameter:
```bash
grep -r "createLogger" packages/integrations/src packages/platform/src --include="*.ts" | head -20
```
</verification>

<success_criteria>
- All integrations/ files use pino with integrations:* component names
- All platform/ files use pino with platform:* component names
- Zero console.log calls in non-test source files
- All packages build successfully
</success_criteria>

<output>
After completion, create `.planning/phases/12-observability/12-04-SUMMARY.md`
</output>

---
phase: 71-completion-signaling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agents/src/shared/db/migrations/0010_completion_signaling.sql
  - packages/agents/src/shared/db/migrations/meta/_journal.json
  - packages/agents/src/shared/db/schema.ts
  - packages/agents/src/shared/db/schema.drizzle.ts
  - packages/agents/src/framework/types.ts
  - packages/agents/src/framework/signal-matching.ts
  - packages/agents/src/framework/wait-for-tool.ts
  - packages/agents/src/framework/wait-for-task-tool.ts
  - packages/agents/src/framework/tool-factories.ts
  - packages/agents/src/framework/conversation-executor.ts
  - packages/agents/src/framework/worker-loop.ts
autonomous: true

must_haves:
  truths:
    - "wait_for tool accepts both string and string[] for the type field and normalizes to array internally"
    - "wait_for_task tool accepts a taskId and auto-registers for task_completion, task_failure, task_timeout signal types"
    - "Signal matching in both executor.signal() and worker-loop queued signal checks uses types array membership, not single-type equality"
    - "Signal matching for wait_for_task filters by signal.data.taskId in addition to type"
    - "Existing single-type wait_for callers (handshake, approval, user_reply) continue working without changes"
    - "Schema migration adds completion_result JSONB column to tasks and active_delegations JSONB column to conversations"
    - "signal.orphaned is a valid agent event type"
  artifacts:
    - path: "packages/agents/src/shared/db/migrations/0010_completion_signaling.sql"
      provides: "completion_result on tasks, active_delegations on conversations"
      contains: "completion_result"
    - path: "packages/agents/src/framework/signal-matching.ts"
      provides: "Shared signal matching helper"
      exports: ["signalMatchesPendingWait"]
    - path: "packages/agents/src/framework/wait-for-task-tool.ts"
      provides: "wait_for_task tool factory"
      exports: ["createWaitForTaskTool"]
  key_links:
    - from: "packages/agents/src/framework/conversation-executor.ts"
      to: "packages/agents/src/framework/signal-matching.ts"
      via: "import signalMatchesPendingWait"
      pattern: "signalMatchesPendingWait"
    - from: "packages/agents/src/framework/worker-loop.ts"
      to: "packages/agents/src/framework/signal-matching.ts"
      via: "import signalMatchesPendingWait"
      pattern: "signalMatchesPendingWait"
    - from: "packages/agents/src/framework/tool-factories.ts"
      to: "packages/agents/src/framework/wait-for-task-tool.ts"
      via: "coordination:wait_for_task registration"
      pattern: "wait_for_task"
---

<objective>
Multi-type wait_for, wait_for_task tool, shared signal matching, and Phase 71 schema migration.

Purpose: Establishes the foundation for completion signaling -- agents can wait for multiple signal types, wait specifically for task lifecycle events with taskId scoping, and the database schema supports completion_result and active_delegations for plans 02 and 03.

Output: Migration 0010, signal-matching.ts, wait-for-task-tool.ts, updated wait-for-tool.ts, updated executor and worker-loop signal matching.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-completion-signaling/71-CONTEXT.md
@.planning/phases/71-completion-signaling/71-RESEARCH.md

# Key source files
@packages/agents/src/framework/wait-for-tool.ts
@packages/agents/src/framework/types.ts
@packages/agents/src/framework/tool-factories.ts
@packages/agents/src/framework/conversation-executor.ts
@packages/agents/src/framework/worker-loop.ts
@packages/agents/src/shared/db/schema.ts
@packages/agents/src/shared/db/schema.drizzle.ts
@packages/agents/src/shared/db/migrations/meta/_journal.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration and type system updates</name>
  <files>
    packages/agents/src/shared/db/migrations/0010_completion_signaling.sql
    packages/agents/src/shared/db/migrations/meta/_journal.json
    packages/agents/src/shared/db/schema.ts
    packages/agents/src/shared/db/schema.drizzle.ts
    packages/agents/src/framework/types.ts
  </files>
  <action>
    **Migration 0010_completion_signaling.sql:**
    Create SQL migration with two ALTER TABLE statements separated by `--&gt; statement-breakpoint`:
    1. `ALTER TABLE agents.tasks ADD COLUMN completion_result JSONB;` -- stores signal payload + delivery metadata for completed/failed delegations
    2. `ALTER TABLE agents.conversations ADD COLUMN active_delegations JSONB DEFAULT '[]';` -- tracks currently pending delegations, survives history compaction

    Note: pending_wait stays as JSONB. The shape changes from `{ type: string }` to `{ types: string[] }` at the application layer only. No column type change needed. Existing waiting conversations with old `{ type: "..." }` format are handled by backward-compat normalization in signal matching code.

    **_journal.json:**
    Add entry for migration 0010. The journal currently has entries idx 0-5 (missing entries for 0007-0009 which were hand-written). Add idx 6 for `0010_completion_signaling` with `breakpoints: true`.

    **schema.ts:**
    1. Add `completion_result` JSONB column to the `tasks` table definition: `completion_result: jsonb("completion_result").$type<Record<string, unknown> | null>(),`
    2. Add `active_delegations` JSONB column to the `conversations` table definition: `active_delegations: jsonb("active_delegations").$type<unknown[]>().notNull().default([]),`
    3. Add `"signal.orphaned"` to `agentEventTypeValues` array (after `"signal.received"`)

    **schema.drizzle.ts:**
    Mirror all three changes from schema.ts: completion_result on tasks, active_delegations on conversations, signal.orphaned in event types.

    **types.ts:**
    Update `WaitForState` interface: change `waitType: string | null` to `waitTypes: string[] | null`. This is the internal representation -- always an array.
  </action>
  <verify>
    `pnpm run typecheck` passes (may have errors from wait-for-tool.ts reading old field -- that's expected and fixed in Task 2).
    Migration file exists at expected path. _journal.json has 7 entries. schema.ts has completion_result, active_delegations, and signal.orphaned.
  </verify>
  <done>
    Schema migration ready for `pnpm db:migrate`. WaitForState uses `waitTypes: string[]` internally. `signal.orphaned` is a valid event type.
  </done>
</task>

<task type="auto">
  <name>Task 2: Multi-type wait_for, signal matching helper, wait_for_task tool, and executor/worker-loop updates</name>
  <files>
    packages/agents/src/framework/signal-matching.ts
    packages/agents/src/framework/wait-for-tool.ts
    packages/agents/src/framework/wait-for-task-tool.ts
    packages/agents/src/framework/tool-factories.ts
    packages/agents/src/framework/conversation-executor.ts
    packages/agents/src/framework/worker-loop.ts
  </files>
  <action>
    **signal-matching.ts (NEW):**
    Create a shared signal matching helper used by both conversation-executor and worker-loop. Export `signalMatchesPendingWait(signal, pendingWait)`:
    - Handle both old-format (`pendingWait.type` string) and new-format (`pendingWait.types` array) for backward compatibility
    - Normalize: `const types = pendingWait.types ? (pendingWait.types as string[]) : pendingWait.type ? [pendingWait.type as string] : [];`
    - If types array empty, return false
    - If `!types.includes(signal.type)`, return false
    - TaskId-scoped matching: if `pendingWait.metadata?.taskId` exists, check `signal.data?.taskId === pendingWait.metadata.taskId`. If no match, return false
    - Return true

    **wait-for-tool.ts updates:**
    1. Update `WaitForInputSchema.type` field to accept both `string` and `string[]` using `z.union([z.string().min(1), z.array(z.string().min(1)).min(1)])`. Update the describe text to mention array support.
    2. In the execute function, normalize to array: `const types = Array.isArray(parsed.type) ? parsed.type : [parsed.type];`
    3. Set `waitForState.waitTypes = types` (instead of old `waitForState.waitType = parsed.type`)
    4. Update confirmation message to show all types: ``Waiting for: ${types.join(", ")}``

    **wait-for-task-tool.ts (NEW):**
    Create the `wait_for_task` tool factory. This is a SEPARATE tool from wait_for with distinct input semantics:
    - Input schema: `{ taskId: z.string().min(1), timeout: z.string().optional() }`
    - When called, set `waitForState.triggered = true`, `waitForState.waitTypes = ["task_completion", "task_failure", "task_timeout"]`, `waitForState.reason = "Waiting for delegated task ${taskId}"`, `waitForState.timeout = timeout ?? null`, `waitForState.metadata = { taskId }`
    - Return confirmation message: `"Conversation paused. Waiting for task ${taskId} to complete, fail, or timeout.${timeout ? ` Timeout: ${timeout}.` : ''}"`
    - Safety by design: no exposed types parameter -- agents cannot forget to listen for failure/timeout
    - Export `createWaitForTaskTool(waitForState: WaitForState): ToolDefinition`

    **tool-factories.ts:**
    1. Import `createWaitForTaskTool` from `./wait-for-task-tool.js`
    2. Register `coordination:wait_for_task` using the same pattern as `coordination:wait_for` (creates a default WaitForState, executor replaces at runtime)
    3. Update tool count comment from 46 to 47 in the JSDoc

    **conversation-executor.ts:**
    1. Import `signalMatchesPendingWait` from `./signal-matching.js`
    2. Replace the inline signal matching logic at the `if (pendingWait?.type && pendingWait.type !== signal.type)` check (around line 380) with: `if (!signalMatchesPendingWait(signal, pendingWait)) { ... reject ... }`
    3. Update the warn log to show `expectedTypes` (from normalized types array) instead of single `expectedType`

    **worker-loop.ts:**
    1. Import `signalMatchesPendingWait` from `./signal-matching.js`
    2. Replace the inline queued signal matching (the `if (queuedSignals.length > 0 && pendingWait?.type)` block around lines 823-828) with the shared helper: iterate over queuedSignals and use `signalMatchesPendingWait(sig, pendingWait)` to find the first match
    3. In the wait_for persistence section (around line 1142-1159), change `type: waitForState.waitType` to `types: waitForState.waitTypes` in the `pendingWaitValue` object

    IMPORTANT: The executor creates and replaces the WaitForState at runtime. For coordination:wait_for_task, follow the exact same pattern as coordination:wait_for -- the tool-factories registration creates a dummy state; the worker loop creates a real state and wires it to both wait_for and wait_for_task tools at execution time. Check how the worker loop currently creates the WaitForState and wires it to wait_for, then extend that to also wire wait_for_task.
  </action>
  <verify>
    `pnpm run typecheck` passes. `pnpm test:fast` passes. Verify:
    1. `grep -r "signalMatchesPendingWait" packages/agents/src/framework/` shows imports in conversation-executor.ts and worker-loop.ts
    2. `grep "wait_for_task" packages/agents/src/framework/tool-factories.ts` shows registration
    3. The old inline `pendingWait?.type && pendingWait.type !== signal.type` pattern is REMOVED from conversation-executor.ts
    4. The old inline `sig.type === pendingWait.type` pattern is REMOVED from worker-loop.ts
  </verify>
  <done>
    wait_for accepts `string | string[]` and normalizes to array. wait_for_task auto-registers for task lifecycle signals with taskId scoping. Signal matching is extracted to a shared helper used by both executor and worker-loop. Existing single-type callers work unchanged due to backward-compat normalization.
  </done>
</task>

</tasks>

<verification>
1. `pnpm run typecheck` -- no type errors
2. `pnpm test:fast` -- existing tests pass (signal matching backward compatibility)
3. Migration file exists: `packages/agents/src/shared/db/migrations/0010_completion_signaling.sql`
4. signal-matching.ts exports `signalMatchesPendingWait`
5. wait-for-task-tool.ts exports `createWaitForTaskTool`
6. tool-factories.ts registers `coordination:wait_for_task` (tool #47)
7. No inline signal matching remains in conversation-executor.ts or worker-loop.ts
</verification>

<success_criteria>
- Multi-type wait_for works backward-compatibly (string input normalizes to single-element array)
- wait_for_task registers for all 3 task lifecycle signal types automatically
- Signal matching helper handles both old `type` and new `types` format
- TaskId-scoped matching prevents cross-task signal wakeup
- Schema migration adds completion_result and active_delegations columns
- signal.orphaned is a valid event type
</success_criteria>

<output>
After completion, create `.planning/phases/71-completion-signaling/71-01-SUMMARY.md`
</output>

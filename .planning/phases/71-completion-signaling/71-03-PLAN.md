---
phase: 71-completion-signaling
plan: 03
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - packages/agents/src/shared/tools/task/delegate-task.ts
  - packages/agents/src/framework/worker-loop.ts
autonomous: true

must_haves:
  truths:
    - "task:delegate writes an active_delegations entry on the conversation row after successful delegation"
    - "When a conversation resumes from waiting, the signal message includes an <active_delegations> XML block showing all pending delegations"
    - "Non-delegation signals (user_reply, approval) also include the active_delegations block so the agent knows what is in flight"
    - "active_delegations entries are removed after the agent loop completes for the signal that matched a task lifecycle type"
    - "active_delegations survives history compaction because it is a separate column from messages"
  artifacts:
    - path: "packages/agents/src/shared/tools/task/delegate-task.ts"
      provides: "Writes active_delegations entry on delegation success"
      contains: "active_delegations"
    - path: "packages/agents/src/framework/worker-loop.ts"
      provides: "active_delegations context injection on resume and removal after processing"
      contains: "active_delegations"
  key_links:
    - from: "packages/agents/src/shared/tools/task/delegate-task.ts"
      to: "packages/agents/src/shared/db/schema.ts"
      via: "writes active_delegations JSONB array on conversations table"
      pattern: "active_delegations"
    - from: "packages/agents/src/framework/worker-loop.ts"
      to: "packages/agents/src/shared/db/schema.ts"
      via: "reads and writes active_delegations on conversation resume and post-loop cleanup"
      pattern: "active_delegations"
---

<objective>
active_delegations lifecycle management -- tracking pending delegations on conversation rows with context injection on resume and cleanup after processing.

Purpose: Delegation context survives history compaction because it lives in a separate column, not in the compactable messages array. Agents resuming from any wait always see their pending delegations for full orientation.

Output: Updated delegate-task.ts writes entries, updated worker-loop.ts injects context and removes entries.
</objective>

<execution_context>
@/Users/roberto.sousa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roberto.sousa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-completion-signaling/71-CONTEXT.md
@.planning/phases/71-completion-signaling/71-RESEARCH.md
@.planning/phases/71-completion-signaling/71-01-SUMMARY.md

# Key source files
@packages/agents/src/shared/tools/task/delegate-task.ts
@packages/agents/src/framework/worker-loop.ts
@packages/agents/src/shared/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write active_delegations entry on successful delegation</name>
  <files>
    packages/agents/src/shared/tools/task/delegate-task.ts
  </files>
  <action>
    After the successful `executor.start()` call in createDelegateTaskTool (around the success return, step 7):

    1. Read the current `active_delegations` array from the delegator's conversation row. The delegator's conversation ID is the correlationId on ToolContext (ctx.correlationId). Use `deps.executor` to get the conversation or query directly -- actually, the tool doesn't have direct DB access. Instead, have DelegationDeps include a `db` reference, OR use a simpler approach: import and use the `conversations` table from schema.ts with a direct db query.

    BEST APPROACH: DelegationDeps already includes executor and taskService. We need to add the DB reference to DelegationDeps so delegate-task can write active_delegations. Add `db` to the DelegationDeps interface in types.ts (this is a minimal change -- the db is already available in worker-loop where DelegationDeps is populated).

    2. Build the delegation entry:
    ```typescript
    const delegationEntry = {
      taskId: task.id,
      targetEntityId: entity.id,
      description: description.slice(0, 200),
      delegatedAt: new Date().toISOString(),
      handshakeStatus: "pending",
    };
    ```

    3. Append to active_delegations:
    ```typescript
    // Read current delegations
    const [conv] = await deps.db
      .select({ active_delegations: conversations.active_delegations })
      .from(conversations)
      .where(eq(conversations.id, ctx.correlationId))
      .limit(1);

    const currentDelegations = (conv?.active_delegations ?? []) as unknown[];
    const updatedDelegations = [...currentDelegations, delegationEntry];

    await deps.db
      .update(conversations)
      .set({ active_delegations: updatedDelegations, updated_at: new Date() })
      .where(eq(conversations.id, ctx.correlationId));
    ```

    4. Update DelegationDeps interface in `packages/agents/src/framework/types.ts` to include `db`:
    ```typescript
    export interface DelegationDeps {
      executor: ConversationExecutor;
      directoryService: DirectoryService;
      taskService: TaskService;
      db: NodePgDatabase<typeof agentsSchemaModule>;
    }
    ```

    5. Update worker-loop.ts where DelegationDeps is constructed to include `db` (the db reference is already in scope there).

    NOTE: Wrap the active_delegations write in try/catch -- failure to write delegation context should NOT fail the delegation itself. Log a warning and continue.
  </action>
  <verify>
    `pnpm run typecheck` passes. `grep "active_delegations" packages/agents/src/shared/tools/task/delegate-task.ts` shows the write logic. DelegationDeps in types.ts includes `db`.
  </verify>
  <done>
    task:delegate writes an active_delegations entry to the delegator's conversation row on successful delegation. Entry includes taskId, targetEntityId, description, delegatedAt, and handshakeStatus="pending".
  </done>
</task>

<task type="auto">
  <name>Task 2: Context injection on resume and delegation entry removal after processing</name>
  <files>
    packages/agents/src/framework/worker-loop.ts
  </files>
  <action>
    **Context injection on resume (when conversation transitions from waiting to running):**

    Find the section in worker-loop.ts where a waiting conversation resumes (where the queued signal match happens and the signal message is built). This is the section that:
    1. Finds a matching signal in queued_signals
    2. Builds `signalContent` with signal.message
    3. Persists updated messages with the signal message appended

    ALSO find the section where executor.signal() transitions a conversation from waiting -> queued (this path is in conversation-executor.ts, but the actual execution with context injection happens in worker-loop when the conversation is next picked up and run). The signal message is already in the messages array at that point.

    The best injection point is where the worker loop picks up a conversation that was just resumed (status=queued, has signal message in messages). However, the simplest approach matching the CONTEXT.md decision is:

    **Approach: Inject active_delegations block into the signal message content.**

    1. Create a `formatActiveDelegations(delegations)` helper function (can be in the same file or extracted):
       - Takes the active_delegations array
       - Returns an XML block: `<active_delegations>...</active_delegations>`
       - Each entry: `<delegation task_id="..." target="..." status="...">description\nDelegated: timestamp</delegation>`
       - Returns empty string if no delegations
       - Follow the XML block pattern used in `<task_context>` and `<delegation>` blocks

    2. In the queued signal matching section (where `matchedSignal` is found and signal content is built), read `active_delegations` from the conversation row (it's already loaded from the SELECT), format it, and prepend to the signal message:
       ```typescript
       const activeDelegations = conv.active_delegations as unknown[] ?? [];
       const delegationContext = formatActiveDelegations(activeDelegations);
       const fullContent = delegationContext ? `${delegationContext}\n\n${signalContent}` : signalContent;
       ```
       Use `fullContent` instead of `signalContent` when building the signal message that gets appended to messages.

    3. ALSO in the section of conversation-executor.ts where a signal resumes a waiting conversation (executor.signal() method, around line 402-410) -- actually this is tricky because conversation-executor.ts is NOT in this plan's files_modified. Let me reconsider.

       The conversation-executor.signal() method builds the signal message and writes it to the conversation's messages. This is where we should inject active_delegations context. BUT this plan owns worker-loop.ts, not conversation-executor.ts.

       ALTERNATIVE: The worker loop already handles the queued signal path. For the direct signal path (executor.signal() resumes a waiting conversation), the signal message is already written to messages before the worker loop picks it up. We need to inject the active_delegations context into that message too.

       RESOLUTION: Since conversation-executor.ts is owned by plan 01 and plan 01 is Wave 1 (already complete by Wave 2), we can safely modify it here. Add conversation-executor.ts to this task's file list. In the executor.signal() method, after building signalContent and before writing to messages, read active_delegations from the conversation row, format it, and prepend.

       ACTUALLY -- conversation-executor.ts loads the row in the signal() method's transaction. The row already has active_delegations (added in migration 0010 from plan 01). So read it from `row`, format, and prepend to signalContent.

    **Delegation entry removal after agent processes the signal:**

    Per CONTEXT.md: "removal happens after agent processes the signal, not at delivery time."

    In the worker-loop, after the agent loop completes (the `executeConversation` function), check if the resuming signal was a task lifecycle signal (task_completion, task_failure, task_timeout). If so, remove the matching entry from active_delegations:

    1. After the agent loop exits and before persisting final messages, check if this was a signal-resumed conversation (look at the last user message or the queued signal that was matched)
    2. If the matched signal's type is one of `["task_completion", "task_failure", "task_timeout"]` and signal.data.taskId exists:
       - Filter active_delegations to remove the entry matching that taskId
       - Include the updated active_delegations in the conversation update
    3. For handshake signals (task_handshake with response=accepted), update the matching entry's handshakeStatus to "accepted" and add the estimate if provided
    4. For handshake signals (task_handshake with response=rejected), remove the matching entry (delegation cancelled)

    The simplest implementation: track the matched signal data in the executeConversation scope (it's available from the queued signal matching step), then after the loop, compute the updated active_delegations based on signal type.
  </action>
  <verify>
    `pnpm run typecheck` passes. `pnpm test:fast` passes. Verify:
    1. `grep "formatActiveDelegations" packages/agents/src/framework/worker-loop.ts` shows the helper
    2. `grep "active_delegations" packages/agents/src/framework/worker-loop.ts` shows read/write/removal logic
    3. Active delegations context appears in signal messages as `<active_delegations>` XML block
  </verify>
  <done>
    Delegation context injected into signal messages on resume. active_delegations entries are added on delegation, updated on handshake, and removed after the agent processes task lifecycle signals. Context survives history compaction because it lives in a separate column.
  </done>
</task>

</tasks>

<verification>
1. `pnpm run typecheck` -- no type errors
2. `pnpm test:fast` -- existing tests pass
3. delegate-task.ts writes active_delegations entry on success
4. Worker-loop injects `<active_delegations>` XML block into signal messages
5. Worker-loop removes entries when agent processes task_completion/failure/timeout signals
6. Worker-loop updates entries when agent processes task_handshake signals (accepted/rejected)
7. DelegationDeps includes `db` for direct conversation row access
</verification>

<success_criteria>
- Delegating agent's conversation row tracks all pending delegations in active_delegations column
- Resuming from any signal (not just task signals) shows active_delegations context
- Task lifecycle signals trigger removal of the corresponding delegation entry
- Handshake signals update handshakeStatus on the corresponding entry
- active_delegations is a separate column from messages, unaffected by history compaction
</success_criteria>

<output>
After completion, create `.planning/phases/71-completion-signaling/71-03-SUMMARY.md`
</output>

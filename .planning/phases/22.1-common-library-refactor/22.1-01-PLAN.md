---
phase: 22.1-common-library-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/common/src/config/env.ts
  - packages/common/src/config/index.ts
  - packages/common/src/config/agent-config.ts
  - packages/common/src/config/agent-config.test.ts
  - packages/common/src/db/index.ts
  - packages/common/src/db/ids.ts
  - packages/common/src/logging/pino-logger.ts
  - packages/common/src/logging/index.ts
  - packages/common/src/index.ts
autonomous: true

must_haves:
  truths:
    - "Importing @aesir/common does NOT trigger environment variable validation"
    - "createLogger() works with no arguments using process.env defaults"
    - "createLogger({ level, service, component }) accepts explicit options"
  artifacts:
    - path: "packages/common/src/logging/pino-logger.ts"
      provides: "Pure logger factory with optional config"
      exports: ["createLogger", "createChildLogger"]
    - path: "packages/common/src/utils/ids.ts"
      provides: "ID generation utility (renamed from db/)"
      exports: ["createId", "IdPrefix"]
  key_links:
    - from: "packages/common/src/index.ts"
      to: "packages/common/src/utils/index.ts"
      via: "barrel export"
      pattern: 'export.*from.*utils'
---

<objective>
Refactor @aesir/common to pure library pattern by removing environment validation at import time.

Purpose: Unblock Docker container startup by eliminating cascading env validation failures when services import common utilities.

Output: Pure @aesir/common library that accepts all dependencies as function arguments. No Proxy objects, no module-level side effects.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22.1-common-library-refactor/22.1-CONTEXT.md
@.planning/phases/22.1-common-library-refactor/22.1-RESEARCH.md
@packages/common/src/index.ts
@packages/common/src/config/env.ts
@packages/common/src/config/index.ts
@packages/common/src/logging/pino-logger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete config folder and remove config exports</name>
  <files>
    packages/common/src/config/env.ts
    packages/common/src/config/index.ts
    packages/common/src/config/agent-config.ts
    packages/common/src/config/agent-config.test.ts
    packages/common/src/index.ts
  </files>
  <action>
1. Delete the entire config folder: `packages/common/src/config/`
   - env.ts (the problematic lazy Proxy validation)
   - index.ts (re-exports env and config)
   - agent-config.ts (will be moved to agents package in Plan 03)
   - agent-config.test.ts (will be moved to agents package in Plan 03)

2. Update packages/common/src/index.ts to remove the config export:
   - Delete line: `export * from "./config/index.js";`
   - Keep all other exports (db, errors, logging, mcp, state, temporal, types)

3. Verify no TypeScript compilation errors in common package:
   - The package should now have no env validation at module level
   - Other packages will temporarily break (fixed in subsequent plans)
  </action>
  <verify>
cd /Users/roberto.sousa/Documents/Projects/aesir && ls packages/common/src/config 2>/dev/null && echo "FAIL: config folder still exists" || echo "PASS: config folder deleted"
  </verify>
  <done>
Common package no longer exports config, env, or agent configuration. The config folder is deleted entirely.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename db folder to utils and update exports</name>
  <files>
    packages/common/src/db/index.ts
    packages/common/src/db/ids.ts
    packages/common/src/utils/index.ts
    packages/common/src/utils/ids.ts
    packages/common/src/index.ts
  </files>
  <action>
1. Rename the db folder to utils:
   - Move `packages/common/src/db/` to `packages/common/src/utils/`
   - The folder contains only `ids.ts` and `index.ts` (pure utilities, no database logic)

2. Update packages/common/src/index.ts:
   - Change `export * from "./db/index.js";` to `export * from "./utils/index.js";`

3. Verify the exports remain the same:
   - `createId` function
   - `IdPrefix` type

Note: The name "db" was misleading since these are pure utility functions (nanoid wrappers). "utils" better reflects the actual content.
  </action>
  <verify>
cd /Users/roberto.sousa/Documents/Projects/aesir && ls packages/common/src/utils/ids.ts && echo "PASS: utils folder exists with ids.ts"
  </verify>
  <done>
The db folder is renamed to utils. Exports from @aesir/common for createId and IdPrefix continue to work.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactor logger factory to pure function with optional config</name>
  <files>
    packages/common/src/logging/pino-logger.ts
    packages/common/src/logging/index.ts
  </files>
  <action>
1. Update createLogger in pino-logger.ts to accept optional config:
   - Current signature already supports options with defaults from process.env
   - Verify it works with no arguments: `createLogger()` uses process.env.LOG_LEVEL and process.env.SERVICE_NAME
   - Verify it works with explicit options: `createLogger({ level: 'debug', service: 'my-service' })`
   - No Zod validation of env vars - just read process.env directly with fallback defaults

2. Ensure the logger factory has NO side effects at module level:
   - The `parseComponentLevels()` function runs at module load - this is OK because it's pure (reads process.env, no validation/exit)
   - The `componentLevelOverrides` cache is OK - it's a read-only Map
   - Confirm: No Proxy objects, no lazy validation, no process.exit in module scope

3. Update logging/index.ts exports if needed:
   - Should already export createLogger and createChildLogger
   - Verify createPinoLogger alias exists for backward compatibility

The logger factory should follow the pattern from RESEARCH.md:
```typescript
export function createLogger(options: CreateLoggerOptions = {}): Logger {
  const {
    service = process.env.SERVICE_NAME ?? "aesir",
    component,
    level = process.env.LOG_LEVEL ?? "info",
    correlationId,
    // ... other options
  } = options;
  // No validation, just use defaults
  return pino({ ... });
}
```
  </action>
  <verify>
cd /Users/roberto.sousa/Documents/Projects/aesir && pnpm --filter @aesir/common build && echo "PASS: common builds successfully"
  </verify>
  <done>
The logger factory accepts optional configuration with process.env defaults. No environment validation occurs at import time.
  </done>
</task>

</tasks>

<verification>
1. Common package builds: `pnpm --filter @aesir/common build`
2. No config folder exists: `ls packages/common/src/config` returns error
3. Utils folder exists with ids.ts: `ls packages/common/src/utils/ids.ts`
4. Logger factory works with no args (test in REPL or simple script)
</verification>

<success_criteria>
- [ ] config folder deleted from packages/common/src/
- [ ] db folder renamed to utils in packages/common/src/
- [ ] packages/common/src/index.ts no longer exports config/env
- [ ] pnpm --filter @aesir/common build succeeds
- [ ] Importing @aesir/common in isolation does NOT exit process or validate env
</success_criteria>

<output>
After completion, create `.planning/phases/22.1-common-library-refactor/22.1-01-SUMMARY.md`
</output>

---
phase: 22.1-common-library-refactor
plan: 04
type: execute
wave: 3
depends_on: ["22.1-01", "22.1-02", "22.1-03"]
files_modified:
  - packages/platform/src/**/*.ts
  - packages/observability/src/**/*.ts
  - packages/test-utils/src/**/*.ts
  - packages/integrations/src/**/*.ts
  - docker-compose.yml
autonomous: false

must_haves:
  truths:
    - "All packages build successfully without @aesir/common config imports"
    - "All unit tests pass with updated mocking patterns"
    - "Docker containers start successfully for all services"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Docker Compose with aligned env var names"
      contains: "DB_HOST"
  key_links:
    - from: "packages/platform/src/**/*.ts"
      to: "@aesir/common"
      via: "import (no config/env)"
      pattern: 'import.*createLogger.*from.*@aesir/common'
---

<objective>
Fix remaining consumer imports, update test mocks, align Docker env vars, and verify full system works.

Purpose: Complete the migration by ensuring all consumers work without @aesir/common config, tests pass, and Docker containers start.

Output: Fully working codebase with pure library pattern. Docker Compose starts all services without env validation failures.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22.1-common-library-refactor/22.1-CONTEXT.md
@.planning/phases/22.1-common-library-refactor/22.1-RESEARCH.md
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix platform and observability package imports</name>
  <files>
    packages/platform/src/**/*.ts
    packages/observability/src/**/*.ts
  </files>
  <action>
1. Search for all imports of config/env from @aesir/common in platform package:
   ```bash
   grep -r "from.*@aesir/common" packages/platform/src/ | grep -E "config|env"
   ```

2. For each file found:
   - If it's using createLogger: Keep import, just remove config reference
   - If it's using config.database: Platform needs its own config or accept as parameter
   - If it's a script (run-cleanup.ts): May need local config

3. Platform package options:
   - Option A: Platform accepts db config as function arguments (pure library)
   - Option B: Platform has its own minimal config.ts for scripts only
   - Prefer Option A for services, Option B for CLI scripts only

4. Fix packages/platform/src/db/client.ts:
   - Currently may import config from @aesir/common
   - Change to accept connection config as parameter
   - Export createDatabaseClient(config: DbConfig) factory

5. Fix packages/platform/src/services/cleanup.ts:
   - May reference config.retention.days
   - Change to accept retentionDays as parameter

6. Fix packages/observability/src/services/execution-tracker.ts:
   - May reference config
   - Change to accept required config as parameters

7. Similar pattern for all platform and observability files:
   - Service factories accept config via options parameter
   - No global config access within these packages
  </action>
  <verify>
cd /Users/roberto.sousa/Documents/Projects/aesir && pnpm --filter @aesir/platform build && pnpm --filter @aesir/observability build && echo "PASS: Platform and Observability build"
  </verify>
  <done>
Platform and observability packages build without @aesir/common config imports.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update test mocks and fix test failures</name>
  <files>
    packages/**/*.test.ts
  </files>
  <action>
1. Find all test files mocking @aesir/common for config:
   ```bash
   grep -r "vi.mock.*@aesir/common" packages/ --include="*.test.ts"
   ```

2. For tests that were mocking to prevent env validation:
   - Many can now REMOVE the vi.mock entirely (pure library has no side effects)
   - Keep vi.mock only if test needs to stub specific behavior

3. Update mock patterns for new structure:
   - OLD: `vi.mock("@aesir/common", () => ({ config: {...}, env: {...} }))`
   - NEW: Remove mock if not needed, OR mock only the specific function used

4. For integration package tests:
   - May need to mock their own config.ts now
   - `vi.mock("../config.js", () => ({ config: {...}, env: {...} }))`

5. For agents package tests:
   - Mock packages/agents/src/config.ts if needed
   - Tests for agent-config.ts should work without mocks (it's pure Zod schema)

6. Run test suite and fix any failures:
   ```bash
   pnpm test:fast
   ```

7. Common test mock issues to fix:
   - "config is not defined" - add mock or update import
   - "createLogger is not a function" - update mock to return function
   - Type errors from removed exports - update test expectations
  </action>
  <verify>
cd /Users/roberto.sousa/Documents/Projects/aesir && pnpm test:fast && echo "PASS: All tests pass"
  </verify>
  <done>
All unit tests pass with updated mocking patterns.
  </done>
</task>

<task type="auto">
  <name>Task 3: Align Docker Compose env vars with service configs</name>
  <files>
    docker-compose.yml
  </files>
  <action>
1. Review docker-compose.yml environment sections for each service:
   - linear-integration: Uses DATABASE_HOST but schema expects DB_HOST
   - github-integration: Same mismatch
   - slack-integration: Same mismatch
   - dev-agent: Uses both DATABASE_URL and DB_* (may be OK)
   - product-agent: Same as dev-agent

2. Decision: Standardize on DB_* names (shorter, already used in many places):
   - Change docker-compose.yml from DATABASE_HOST to DB_HOST
   - Change DATABASE_PORT to DB_PORT
   - Change DATABASE_USER to DB_USER
   - Change DATABASE_PASSWORD to DB_PASSWORD
   - Change DATABASE_NAME to DB_NAME

3. Update linear-integration environment in docker-compose.yml:
   ```yaml
   environment:
     - DB_HOST=postgresql
     - DB_PORT=5432
     - DB_USER=temporal
     - DB_PASSWORD=temporal
     - DB_NAME=temporal
     # ... rest unchanged
   ```

4. Apply same change to github-integration and slack-integration

5. For dev-agent and product-agent:
   - They already have DB_* vars alongside DATABASE_URL
   - Remove DATABASE_URL if not used (agents config.ts uses individual vars)
   - Or keep both for backward compatibility

6. Verify .env.example has correct variable names documented
  </action>
  <verify>
cd /Users/roberto.sousa/Documents/Projects/aesir && grep -E "DATABASE_HOST|DATABASE_PORT|DATABASE_USER|DATABASE_PASSWORD|DATABASE_NAME" docker-compose.yml && echo "FAIL: Old DATABASE_* vars still present" || echo "PASS: Using DB_* vars"
  </verify>
  <done>
Docker Compose env vars aligned with service config schemas (DB_* pattern).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete refactor of @aesir/common to pure library pattern:
- Removed config folder and env validation from common
- Each service (integrations, agents) has its own config.ts
- All packages build successfully
- Tests pass
- Docker Compose env vars aligned
  </what-built>
  <how-to-verify>
1. Start Docker Compose and verify all containers start:
   ```bash
   docker compose build
   docker compose up -d
   docker compose ps
   ```

2. Check container logs for each service:
   ```bash
   docker compose logs linear-integration | head -20
   docker compose logs github-integration | head -20
   docker compose logs slack-integration | head -20
   docker compose logs dev-agent | head -20
   ```

3. Verify health endpoints:
   ```bash
   curl http://localhost:3001/health  # Linear
   curl http://localhost:3002/health  # GitHub
   curl http://localhost:3003/health  # Slack
   ```

4. Expected: All containers running, no "environment validation failed" errors

If issues:
- Check which env vars are missing
- Update docker-compose.yml or .env file
- Rebuild: `docker compose build <service-name>`
  </how-to-verify>
  <resume-signal>Type "approved" if all containers start and health checks pass, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. Full monorepo builds: `pnpm build`
2. All tests pass: `pnpm test:fast`
3. Docker containers start: `docker compose up -d && docker compose ps`
4. Health checks pass: `curl localhost:3001/health`, `curl localhost:3002/health`, `curl localhost:3003/health`
</verification>

<success_criteria>
- [ ] All packages build: `pnpm build` succeeds
- [ ] All tests pass: `pnpm test:fast` succeeds
- [ ] Docker containers start without env validation failures
- [ ] Health endpoints return 200 for all integration services
- [ ] No imports of config/env from @aesir/common in any consumer
</success_criteria>

<output>
After completion, create `.planning/phases/22.1-common-library-refactor/22.1-04-SUMMARY.md`
</output>
